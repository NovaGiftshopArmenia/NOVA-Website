// LOCALIZATION DICTIONARIES
const TRANSLATIONS = {
  am: {
    nav_about: "Х„ХҐЦЂ Х„ХЎХЅХ«Х¶",
    about_hook_title: "Х•Х¬Ц†ХЎХЇХїХёЦЂ Ф±ЦЂХѕХҐХЅХїХ« Ф±ЦЂХЎЦЂХёХІХ¶ХҐЦЂ",
    about_hook_desc: "Х„ХҐХ¶Ц„ Х°ХЎХѕХЎЦ„ХЎХЈЦЂХёЦ‚Хґ ХҐХ¶Ц„ Х°ХЎХ¦ХѕХЎХЈХµХёЦ‚Хї, Х¶Х«Х·ХЎХµХ«Х¶ ХўХёЦ‚ХµЦЂХҐЦЂ Ц‡ ХґХЎЦ„ХёЦ‚ЦЂ ХўХЎХІХЎХ¤ЦЂХ«Х№Х¶ХҐЦЂ ХЄХЎХґХЎХ¶ХЎХЇХЎХЇХ«ЦЃ Х§ХЅХ©ХҐХїХ¶ХҐЦЂХ« Х°ХЎХґХЎЦЂ: Х„ХҐЦЂ ХЎХјХЎЦ„ХҐХ¬ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ Х§ Х±ХҐЦЂ ХўХёЦ‚ХµЦЂХЁ ХѕХҐЦЂХЎХ®ХҐХ¬ ХЈХ¬ХёЦ‚Х­ХЈХёЦЂХ®ХёЦЃХ«:",
    about_story_title: "Х„ХҐЦЂ ХЌХЇХ«Х¦ХўХЁ",
    about_story_desc: "NOVA-Х¶ Х®Х¶ХѕХҐХ¬ Х§ ХґХҐХЇ ХґХёХ¬ХёЦ‚ЦЃЦ„Х«ЦЃХќ ХЈХїХ¶ХҐХ¬ ХЎХ¶Х¦Х«Х»ХёЦ‚Хґ, Х°ХЎХ¦ХѕХЎХЈХµХёЦ‚Хї Ц‡ Х·Х¶Х№ХЎХЇХїХёЦ‚ЦЂ ХЎХ¶ХёХІ ХўХёЦ‚ХµЦЂХҐЦЂ ХґХ« Х·ХёЦ‚ХЇХЎХµХёЦ‚Хґ, ХёЦЂХЁ ХёХІХёХІХѕХЎХ® Х§ ХЅХёХѕХёЦЂХЎХЇХЎХ¶, ХґХЎХЅХЅХЎХµХЎХЇХЎХ¶ ХїХЎЦЂХўХҐЦЂХЎХЇХ¶ХҐЦЂХёХѕ: Ф±ХµХ¶, Х«Х¶Х№ ХЅХЇХЅХѕХҐЦЃ ХёЦЂХєХҐХЅ Х°ХЎХґХЎХ·Х­ХЎЦЂХ°ХЎХµХ«Х¶ Х§Х¬Х«ХїХЎЦЂ ХўХёЦ‚ХµЦЂХҐЦЂХ« ХґХЎХЅХ¶ХЎХѕХёЦЂ Х°ХЎХѕХЎЦ„ХЎХ®ХёЦ‚, ХѕХҐЦЂХЎХ®ХѕХҐЦЃ ХЅЦЂХўХЎХѕХЎХµЦЂХ« Х¶ЦЂХЎХ¶ЦЃ Х°ХЎХґХЎЦЂ, ХёХѕЦ„ХҐЦЂ ХЈХ¶ХЎХ°ХЎХїХёЦ‚Хґ ХҐХ¶ Х¶Х«Х·ХЎХµХ«Х¶ ХєХЎЦЂЦ†ХµХёЦ‚ХґХҐЦЂХ«ХЎХµХ« Х«ХЅХЇХЎХЇХЎХ¶ ХЎЦЂХѕХҐХЅХїХЁ:",
    about_eyebrow: "Ф№Х€ХђХ„Ф±Х† Ф±ХђХЋФµХЌХЏФё вЂ” ХЂФ»Х„Х†Ф±ФґХђХЋФ±Фѕ Ф· 2026",
    about_lead: "Х•Х¬Ц†ХЎХЇХїХёЦЂ ХґХЎЦ„ХёЦ‚ЦЂ ХўХёЦ‚ХµЦЂХҐЦЂХ« ХіХЎЦЂХїХЎЦЂХЎХєХҐХїХЎХЇХЎХ¶ вЂ‹вЂ‹ХёЦ‚ХЅХёЦ‚ХґХ¶ХЎХЅХ«ЦЂХёЦ‚Х©ХµХёЦ‚Х¶Хќ Х±ХҐХјХЎХЈХёЦЂХ® ХЄХЎХґХЎХ¶ХЎХЇХЎХЇХ«ЦЃ Х§ХЅХ©ХҐХїХ«ХЇХЎХµХ« ХЅХ«ЦЂХЎХ°ХЎЦЂХ¶ХҐЦЂХ« Х°ХЎХґХЎЦЂ:",
    stat_botanical: "Х•ЦЂХ«ХЈХ«Х¶ХЎХ¬ Ц‡ ХЋХЎХѕХҐЦЂХЎХЇХЎХ¶",
    stat_batch: "ХЂХЎХґХЎХ·Х­ХЎЦЂХ°ХЎХµХ«Х¶ ФІЦЂХҐХ¶Х¤Х¶ХҐЦЂ",
    stat_batch_number: "Ф·Х¬Х«ХїХЎЦЂ",
    stat_synthetic: "Ф¶ЦЂХё Х„ХЎХЅХЅХЎХµХЎХЇХЎХ¶ ХЏХЎЦЂХўХҐЦЂХЎХЇХ¶ХҐЦЂ",
    contact_us: "ФїХЎХєХ¶ХѕХҐХ¬ Х„ХҐХ¦ Х°ХҐХї",
    about_pillars_title: "ХЋХЅХїХЎХ°ХёЦ‚Х©ХµХЎХ¶ ХЂХ«ХґЦ„ХҐЦЂХЁ",
    about_guarantee_title: "100% Ф»ХЅХЇХёЦ‚Х©ХµХЎХ¶ ФµЦЂХЎХ·Х­Х«Ц„",
    about_guarantee_desc: "Х„ХҐХ¶Ц„ ХўХёХ¬ХёЦЂ Х·Ц„ХҐХІ ХўХёЦ‚ХµЦЂХҐЦЂХЁ Х±ХҐХјЦ„ ХҐХ¶Ц„ ХўХҐЦЂХёЦ‚Хґ ХЎХ¶ХґХ«Х»ХЎХєХҐХЅ ХєХЎХ·ХїХёХ¶ХЎХЇХЎХ¶ Х¤Х«ХЅХїЦЂХ«ХўХµХёЦ‚ХїХёЦЂХ¶ХҐЦЂХ«ЦЃ Ц‡ Х°ХҐХ¶ЦЃ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х« ХїХ¶ХҐЦЂХ«ЦЃ: ФіХ¶ХҐХ¬ХёХѕ Ц…ЦЂХ«ХЈХ«Х¶ХЎХ¬ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„ ХЂХЎХµХЎХЅХїХЎХ¶ХёЦ‚Хґ ХґХҐЦЂ ХЅЦЂХЎХ°Х«ЦЃХќ Х¤ХёЦ‚Ц„ ХЅХїХЎХ¶ХёЦ‚Хґ ХҐЦ„ ХўХЎЦЃХЎЦЂХ±ХЎХЇ ХёЦЂХЎХЇХќ ХЎХјХЎХ¶ЦЃ ХЅХїХѕХҐЦЂХЎХµХ«Х¶ Х·ХёЦ‚ХЇХЎХµХ« Ц‡ Х¦Х«Х»ХёЦ‚ХґХ¶ХҐЦЂХ«:",
    about_scent_match_title: "Ф»Х¤ХҐХЎХ¬ХЎХЇХЎХ¶ ФІХёЦ‚ХµЦЂХ« ФЅХёХЅХїХёЦ‚Хґ",
    about_scent_match_desc: "Х•Х¶Х¬ХЎХµХ¶ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х« Х­ХЎХ¶ХёЦ‚Х©Х«ЦЃ ХўХёЦ‚ХµЦЂ ХЁХ¶ХїЦЂХҐХ¬ХЁ ХЇХЎЦЂХёХІ Х§ Х¤ХЄХѕХЎЦЂ Х¬Х«Х¶ХҐХ¬: Ф±ХµХ¤ Х«ХЅХЇ ХєХЎХїХіХЎХјХёХѕ ХґХҐХ¶Ц„ ХЎХјХЎХ»ХЎЦЂХЇХёЦ‚Хґ ХҐХ¶Ц„ Х¶ХёХїХЎХ¶ХҐЦЂХ« ХґХЎХ¶ЦЂХЎХґХЎХЅХ¶ Х¶ХЇХЎЦЂХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶ Ц‡ ХЎХ¶ХѕХїХЎХ¶ХЈ ХѕХҐЦЂХЎХ¤ХЎЦЂХ± Х№ХўХЎЦЃХѕХЎХ® ХїХёЦ‚ЦѓХҐЦЂХ« Х°ХЎХґХЎЦЂ, ХёЦЂХєХҐХЅХ¦Х« Х±ХҐЦЂ ХЈХ¶ХёЦ‚ХґХЁ Х¬Х«Х¶Х« ХЎХ¶Х©ХҐЦЂХ«:",
    about_storage_title: "Ф±Х¶Х©ХҐЦЂХ« ХЉХЎХ°ХєХЎХ¶ХґХЎХ¶ ХЉХЎХµХґХЎХ¶Х¶ХҐЦЂ",
    about_storage_desc: "Х…ХёЦ‚ЦЂХЎЦ„ХЎХ¶Х№ХµХёЦ‚ЦЂ Х·Х«Х· ХєХЎХ°ХѕХёЦ‚Хґ Х§ ХґХҐЦЂ Х°ХЎХїХёЦ‚ХЇ ХЇХ¬Х«ХґХЎХµХёХѕ Ц‡ Х¬ХёЦ‚ХµХЅХёХѕ ХѕХҐЦЂХЎХ°ХЅХЇХѕХёХІ ХєХЎХ°ХҐХЅХїХ¶ХҐЦЂХёЦ‚Хґ: ХЌХЎ ХєХЎХ°ХєХЎХ¶ХёЦ‚Хґ Х§ Х¶Х«Х·ХЎХµХ«Х¶ ХєХЎЦЂЦ†ХµХёЦ‚ХґХҐЦЂХ«ХЎХµХ« ХҐХ©ХҐЦЂХЎХµХёЦ‚ХІХҐЦЂХ« Х¶ХёЦ‚ЦЂХў ХЎХґХўХёХІХ»ХЎХЇХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶ХЁХќ ХЎХєХЎХ°ХёХѕХҐХ¬ХёХѕ ХўЦЂХҐХ¶Х¤ХЎХµХ«Х¶ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х« ХЇХЎХїХЎЦЂХµХЎХ¬ Х°Х¶Х№ХёХІХёЦ‚Х©ХµХёЦ‚Х¶ХЁ ФµЦЂЦ‡ХЎХ¶ХёЦ‚Хґ:",
    about_cta_title: "ХЉХЎХїЦЂХЎХћХЅХї ХҐЦ„ ХЈХїХ¶ХҐХ¬ Х±ХҐЦЂ ХўХёЦ‚ХµЦЂХЁ:",
    about_cta_btn: "ФІХЎЦЃХЎХ°ХЎХµХїХҐХ¬ ХЂХЎХѕХЎЦ„ХЎХ®ХёЦ‚Х¶",
    nav_home: "ФіХ¬Х­ХЎХѕХёЦЂ",
    nav_products: "Ф±ХєЦЂХЎХ¶Ц„Х¶ХҐЦЂ",
    nav_brands: "Ф±ХєЦЂХЎХ¶Ц„ХЎХ¶Х«Х·ХҐЦЂ",
    nav_men: "ХЏХІХЎХґХЎЦЂХ¤ХЇХЎХ¶ЦЃ",
    nav_women: "ФїХЎХ¶ХЎХ¶ЦЃ",
    nav_new: "Х†ХёЦЂХёЦ‚ХµХ©Х¶ХҐЦЂ",
    nav_bestsellers: "ФІХҐХЅХ©ХЅХҐХ¬ХҐЦЂХ¶ХҐЦЂ",
    badge_bestseller: "ФІФµХЌФ№ХЌФµФјФµХђ",
    badge_new_product: "Х†Х€ХђХ€Х’Х…Ф№",
    nav_gifts: "Х†ХѕХҐЦЂ ХЂХЎХѕХЎЦ„ХЎХ®ХёЦ‚Х¶ХҐЦЂ",
    cat_men: "ХЏХІХЎХґХЎЦЂХ¤ХЇХЎХ¶ЦЃ",
    cat_women: "ФїХЎХ¶ХЎХ¶ЦЃ",
    cat_gifts: "Х†ХѕХҐЦЂХ¶ХҐЦЂ",
    cat_bestsellers: "ФІХҐХЅХ©ХЅХҐХ¬ХҐЦЂХ¶ХҐЦЂ",
    cat_new: "Х†ХёЦЂХёЦ‚ХµХ©Х¶ХҐЦЂ",
    cat_home: "ХЏХЎХ¶ ХЂХЎХґХЎЦЂ",
    nav_admin: "WooCommerce ФїХЎХјХЎХѕХЎЦЂХёЦ‚Хґ",
    nav_order_history: "ХЉХЎХїХѕХҐЦЂХ¶ХҐЦЂХ« ХєХЎХїХґХёЦ‚Х©ХµХёЦ‚Х¶",
    nav_logout: "ФґХёЦ‚ЦЂХЅ ХЈХЎХ¬",
    auth_signin_btn: "Х„ХёЦ‚ХїЦ„",
    auth_signup_title: "ФіЦЂХЎХ¶ЦЃХѕХҐХ¬",
    auth_email_label: "Ф·Х¬. Х°ХЎХЅЦЃХҐ",
    auth_password_label: "ФіХЎХІХїХ¶ХЎХўХЎХј",
    auth_first_name: "Ф±Х¶ХёЦ‚Х¶",
    auth_last_name: "Ф±Х¦ХЈХЎХ¶ХёЦ‚Х¶",
    auth_confirm_password: "ХЂХЎХЅХїХЎХїХҐХ¬ ХЈХЎХІХїХ¶ХЎХўХЎХјХЁ",
    hello_user: "Х€ХІХ»ХёЦ‚ХµХ¶, Ф·ХґХґХЎ",
    mega_woody: "Х“ХЎХµХїХЎХµХ«Х¶",
    mega_floral: "ФѕХЎХІХЇХЎХµХ«Х¶",
    mega_citrus: "Х‘Х«ХїЦЂХёЦ‚ХЅХЎХµХ«Х¶",
    mega_amber: "Ф±ХґХўЦЂХЎХµХ«Х¶",
    search_placeholder: "Х€ЦЂХёХ¶ХҐХ¬ ХўХёЦ‚ХµЦЂХҐЦЂ...",
    newsletter_placeholder: "ХЃХҐЦЂ Х§Х¬. Х°ХЎХЅЦЃХҐХ¶",
    maison_de_parfum: "Maison de Parfum",
    hero_title: "Х”Хё ХўХёЦ‚ХµЦЂХЁ Ц„ХҐХ¦ Х§ ХЅХєХЎХЅХёЦ‚Хґ",
    hero_desc: "Х•Х®ХЎХ¶ХҐХ¬Х«Ц„, Х¶ХѕХҐЦЂХ¶ХҐЦЂ, ХЈХҐХІХҐЦЃХ«ХЇ ЦѓХЎХ©ХҐХ©ХЎХѕХёЦЂХёЦ‚Хґ",
    explore_fragrances: "ФІХЎЦЃХЎХ°ХЎХµХїХҐХ¬ ФІХёЦ‚ХµЦЂХҐЦЂХЁ",
    view_admin_demo: "ФґХҐХґХё ФїХЎХјХЎХѕХЎЦЂХёЦ‚Хґ",
    promise_title_1: "ФІХЎЦЃХЎХјХ«ХЇ Ц‡ ФёХ¶ХїЦЂХѕХЎХ® ХЂХЎХѕХЎЦ„ХЎХ®ХёЦ‚",
    promise_desc_1: "Ф±Х·Х­ХЎЦЂХ°Х« ХЎХґХҐХ¶ХЎХ°ХҐХІХ«Х¶ХЎХЇХЎХѕХёЦЂ, Х°ХЎХ¦ХѕХЎХЈХµХёЦ‚Хї Ц‡ Ц…ЦЂХ«ХЈХ«Х¶ХЎХ¬ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х¶ХҐЦЂХ« Х­Х¶ХЎХґЦ„ХёХѕ ХЁХ¶ХїЦЂХѕХЎХ® Х°ХЎХѕХЎЦ„ХЎХ®ХёЦ‚ ХЂХЎХµХЎХЅХїХЎХ¶ХёЦ‚ХґХќ ХєХЎЦЂЦ†ХµХёЦ‚ХґХҐЦЂХ«ХЎХµХ« ХЈХ«ХїХЎХЇХ¶ХҐЦЂХ« Х°ХЎХґХЎЦЂ:",
    promise_title_2: "ФµЦЂХЇХЎЦЂХЎХЇХµХЎЦЃ Ц‡ Ф±Х¶ХЇЦЂХЇХ¶ХҐХ¬Х« ФІХёЦ‚ХµЦЂХҐЦЂ",
    promise_desc_2: "ФІХЎЦЂХ±ЦЂ ХЇХЎХµХёЦ‚Х¶ХёЦ‚Х©ХµХЎХґХў Ц‡ ХµХёЦ‚ЦЂХЎХ°ХЎХїХёЦ‚ХЇ Х·Х¬ХҐХµЦ†ХёХѕ ХЁХ¶ХїЦЂХѕХЎХ® Х§Х¬Х«ХїХЎЦЂ ХєХЎЦЂЦ†ХµХёЦ‚ХґХҐЦЂХ«ХЎХќ ХЎХєХЎХ°ХёХѕХҐХ¬ХёХѕ ХҐЦЂХЇХЎЦЂХЎХЇХµХЎЦЃ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х« ХЎХјХЇХЎХµХёЦ‚Х©ХµХёЦ‚Х¶:",
    promise_title_3: "ФїХЎХµХёЦ‚Х¶ Х§ХЇХё-ЦѓХЎХ©ХҐХ©ХЎХѕХёЦЂХёЦ‚Хґ",
    promise_desc_3: "Ф·ХЇХёХ¬ХёХЈХ«ХЎХєХҐХЅ ХґХЎЦ„ХёЦ‚ЦЂ ЦѓХЎХ©ХҐХ©ХЎХѕХёЦЂХґХЎХґХў Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Хќ 100% ХЇХҐХ¶ХЅХЎЦ„ХЎХµЦ„ХЎХµХѕХёХІ FSC ХїХёЦ‚ЦѓХҐЦЂХёХѕ, ХўХЎХґХўХЎХЇХµХЎ ХєХ«ХїХЎХЇХ¶ХҐЦЂХёХѕ Ц‡ ХѕХҐЦЂХЎХґХ·ХЎХЇХѕХёХІ Х·Х·ХҐЦЂХёХѕ:",
    collections_title: "Х•ФѕФ±Х†ФµФјФ»Х”Х†ФµХђ",
    gender_unisex: "Х€Х’Х†Ф»ХЌФµХ”ХЌ",
    gender_men: "ХЏХ‚Ф±Х„Ф±ХђФґХ€Х’",
    gender_women: "ФїФ±Х†Ф±Х‘Ф»",
    featured_subtitle: "ФёХ¶ХїЦЂХѕХЎХ® ХЌХїХҐХІХ®ХЎХЈХёЦЂХ®ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂ",
    featured_title: "ФІХёЦ‚ХµЦЂХҐЦЂХ« ХЂХЎХѕХЎЦ„ХЎХ®ХёЦ‚",
    view_full_collection_btn: "ФґХ«ХїХҐХ¬ ХЎХґХўХёХІХ» Х°ХЎХѕХЎЦ„ХЎХ®ХёЦ‚Х¶",
    shop_title: "ХЂХЎХѕХЎЦ„ХЎХ®ХёЦ‚",
    shop_subtitle: "Ф¶ХїХҐЦ„ Х±ХҐЦЂ ХёЦЂХёХ¶ХёЦ‚ХґХЁ ХЁХЅХї Х¶ХёХїХЎХ¶ХҐЦЂХ« ХЇХЎХґ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х« ХЁХ¶ХїХЎХ¶Х«Ц„Х«:",
    filter_all: "ФІХёХ¬ХёЦЂХЁ",
    filter_woody: "Х“ХЎХµХїХЎХµХ«Х¶",
    filter_floral: "ФѕХЎХІХЇХЎХµХ«Х¶",
    filter_citrus: "Х‘Х«ХїЦЂХёЦ‚ХЅХЎХµХ«Х¶",
    filter_amber: "Ф±ХґХўЦЂХЎХµХ«Х¶",
    sort_default: "ФјХјХҐХ¬ХµХЎХµХ¶ ХїХҐХЅХЎХЇХЎХѕХёЦЂХёЦ‚Хґ",
    sort_price_low_high: "ФіХ«Х¶Хќ ЦЃХЎХ®ЦЂХ«ЦЃ ХўХЎЦЂХ±ЦЂ",
    sort_price_high_low: "ФіХ«Х¶Хќ ХўХЎЦЂХ±ЦЂХ«ЦЃ ЦЃХЎХ®ЦЂ",
    sort_rating: "Х„Х«Х»Х«Х¶ ХЈХ¶ХЎХ°ХЎХїХЎХЇХЎХ¶",
    cart_title: "ФіХ¶ХёЦ‚ХґХ¶ХҐЦЂХ« Ф¶ХЎХґХўХµХёЦ‚ХІ",
    cart_subtotal_label: "ФёХ¶Х¤ХЎХґХҐХ¶ХЁ",
    cart_shipping_desc: "Ф±ХјХЎЦ„ХёЦ‚ХґХЁ Ц‡ Х°ХЎЦЂХЇХҐЦЂХЁ Х°ХЎХ·ХѕХЎЦЂХЇХѕХёЦ‚Хґ ХҐХ¶ ХєХЎХїХѕХҐЦЂХЁ Х±Ц‡ХЎХЇХҐЦЂХєХҐХ¬Х«ХЅ:",
    cart_checkout_btn: "Ф±Х¶ЦЃХ¶ХҐХ¬ Х±Ц‡ХЎХЇХҐЦЂХєХґХЎХ¶",
    cart_empty_msg: "ХЃХҐЦЂ ХЈХ¶ХёЦ‚ХґХ¶ХҐЦЂХ« Х¦ХЎХґХўХµХёЦ‚ХІХЁ Х¤ХЎХїХЎЦЂХЇ Х§:",
    go_to_shop_btn: "ФіХ¶ХЎХ¬ ФЅХЎХ¶ХёЦ‚Х©",
    wishlist_title: "Ф»Хґ Х‘ХЎХ¶ХЇХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ",
    wishlist_add_all_btn: "Ф±ХѕХҐХ¬ХЎЦЃХ¶ХҐХ¬ ХўХёХ¬ХёЦЂХЁ Х¦ХЎХґХўХµХёЦ‚ХІ",
    wishlist_empty_msg: "ХЃХҐЦЂ ЦЃХЎХ¶ХЇХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХ« ЦЃХёЦ‚ЦЃХЎХЇХЁ Х¤ХЎХїХЎЦЂХЇ Х§:",
    browse_shop_btn: "ФґХ«ХїХҐХ¬ ФЅХЎХ¶ХёЦ‚Х©ХЁ",
    card_explore: "ФІХЎЦЃХЎХ°ХЎХµХїХҐХ¬",
    card_add_to_cart: "Ф±ХѕХҐХ¬ХЎЦЃХ¶ХҐХ¬",
    card_sold_out: "ХЌХєХЎХјХѕХЎХ® Х§",
    reviews_label: "ХЇХЎЦЂХ®Х«Ц„",
    in_stock_label: "Ф±ХјХЇХЎ Х§",
    out_of_stock_label: "Ф±ХјХЇХЎ Х№Х§",
    modal_fragrance_pyramid: "ФІХёЦ‚ХµЦЂХҐЦЂХ« ФІХёЦ‚ЦЂХЈ",
    modal_top: "ХЋХҐЦЂХ«Х¶ Х¶ХёХїХЎХ¶ХҐЦЂ:",
    modal_heart: "ХЌЦЂХїХ« Х¶ХёХїХЎХ¶ХҐЦЂ:",
    modal_base: "ХЂХ«ХґХ¶ХЎХµХ«Х¶ Х¶ХёХїХЎХ¶ХҐЦЂ:",
    modal_select_size: "ФёХ¶ХїЦЂХҐЦ„ Х‰ХЎЦѓХЅХЁ",
    modal_view_ingredients: "ФґХ«ХїХҐХ¬ ФІХЎХІХЎХ¤ЦЂХёЦ‚Х©ХµХёЦ‚Х¶ХЁ",
    modal_add_to_cart: "Ф±ХѕХҐХ¬ХЎЦЃХ¶ХҐХ¬ Ф¶ХЎХґХўХµХёЦ‚ХІ",
    checkout_billing_title: "ХЂХЎХ·ХѕХЎЦЂХЇХЎХµХ«Х¶ ХїХѕХµХЎХ¬Х¶ХҐЦЂ",
    checkout_first_name: "Ф±Х¶ХёЦ‚Х¶ *",
    checkout_last_name: "Ф±Х¦ХЈХЎХ¶ХёЦ‚Х¶ *",
    checkout_email: "Ф·Х¬. Х“ХёХЅХї *",
    checkout_street: "ХЂХЎХЅЦЃХҐ *",
    checkout_city: "Х”ХЎХІХЎЦ„ *",
    checkout_zip: "Х“ХёХЅХїХЎХµХ«Х¶ Х«Х¶Х¤ХҐЦ„ХЅ *",
    checkout_shipping: "Ф±ХјХЎЦ„ХёЦ‚Хґ *",
    checkout_payment_title: "ХЋХіХЎЦЂХёЦ‚Хґ",
    checkout_payment_desc: "ФґХҐХґХё ХѕХіХЎЦЂХёЦ‚Хґ. ХЌХ«ХґХёЦ‚Х¬ХµХЎЦЃХ«ХёХ¶ ХјХҐХЄХ«Хґ: Ф»ЦЂХЎХЇХЎХ¶ Ц„ХЎЦЂХїХҐЦЂХ«ЦЃ ХЈХЎХ¶Х±ХёЦ‚Хґ Х№Х« ХЇХЎХїХЎЦЂХѕХ«:",
    checkout_place_order: "ФїХЎХїХЎЦЂХҐХ¬ ХЉХЎХїХѕХҐЦЂ",
    checkout_your_order: "ХЃХҐЦЂ ХЉХЎХїХѕХҐЦЂХЁ",
    checkout_subtotal: "ФёХ¶Х¤ХЎХґХҐХ¶ХЁ:",
    checkout_shipping_row: "Ф±ХјХЎЦ„ХёЦ‚Хґ:",
    checkout_total: "ФёХ¶Х¤Х°ХЎХ¶ХёЦ‚ЦЂ:",
    order_success_title: "ХЉХЎХїХѕХҐЦЂХ¶ Х°ХЎХ»ХёХІХёЦ‚Х©ХµХЎХґХў ХЇХЎХїХЎЦЂХѕХҐХ¬ Х§",
    order_success_desc: "Х‡Х¶ХёЦЂХ°ХЎХЇХЎХ¬ХёЦ‚Х©ХµХёЦ‚Х¶ NOVA-Х¶ ХЁХ¶ХїЦЂХҐХ¬ХёЦ‚ Х°ХЎХґХЎЦЂ: ХЃХҐЦЂ ХєХЎХїХѕХҐЦЂХ« Х°ХЎХґХЎЦЂХ¶ Х§ <strong>#REF</strong>: Х„ХҐХ¶Ц„ ХёЦ‚ХІХЎЦЂХЇХҐХ¬ ХҐХ¶Ц„ Х°ХЎХЅХїХЎХїХґХЎХ¶ Х¶ХЎХґХЎХЇ EMAIL Х°ХЎХЅЦЃХҐХ«Х¶:",
    order_details: "ХЉХЎХїХѕХҐЦЂХ« ХґХЎХ¶ЦЂХЎХґХЎХЅХ¶ХҐЦЂХЁ",
    order_date: "Ф±ХґХЅХЎХ©Х«Хѕ:",
    order_ship_to: "Ф±ХјХЎЦ„ХґХЎХ¶ Х°ХЎХЅЦЃХҐ:",
    order_total_paid: "ФёХ¶Х¤Х°ХЎХ¶ХёЦ‚ЦЂ ХѕХіХЎЦЂХѕХЎХ®:",
    continue_shopping_btn: "Х‡ХЎЦЂХёЦ‚Х¶ХЎХЇХҐХ¬ ФіХ¶ХёЦ‚ХґХ¶ХҐЦЂХЁ",
    footer_about: "ФІХёЦ‚ХЅХЎХЇХЎХ¶ ХґХЎЦ„ХёЦ‚ЦЂ ХўХёЦ‚ХµЦЂХҐЦЂХ« ХіХЎЦЂХїХЎЦЂХЎХєХҐХїХЎХЇХЎХ¶ вЂ‹вЂ‹ХёЦ‚ХЅХёЦ‚ХґХ¶ХЎХЅХ«ЦЂХёЦ‚Х©ХµХёЦ‚Х¶Хќ ХєХЎХїЦЂХЎХЅХїХѕХЎХ® ХЄХЎХґХЎХ¶ХЎХЇХЎХЇХ«ЦЃ Х§ХЅХ©ХҐХїХ«ХЇХЎХµХ« ХЅХ«ЦЂХЎХ°ХЎЦЂХ¶ХҐЦЂХ« Х°ХЎХґХЎЦЂ:",
    footer_nav_title: "Х†ХЎХѕХЎЦЂХЇХёЦ‚Х©ХµХёЦ‚Х¶",
    footer_shop_link: "ФЅХЎХ¶ХёЦ‚Х©",
    footer_families_title: "ФІХёЦ‚ХµЦЂХҐЦЂХ« ФёХ¶ХїХЎХ¶Х«Ц„Х¶ХҐЦЂ",
    footer_pages_title: "Ф·Х»ХҐЦЂ",
    footer_follow_title: "ХЂХҐХїЦ‡ХҐЦ„ Х„ХҐХ¦",
    footer_newsletter_title: "ФјЦЂХЎХїХёЦ‚",
    footer_newsletter_desc: "ФІХЎХЄХЎХ¶ХёЦЂХ¤ХЎХЈЦЂХѕХҐЦ„Хќ Х¶ХёЦЂ ХЅХҐХ¦ХёХ¶ХЎХµХ«Х¶ Х©ХёЦЂХёЦ‚ХґХ¶ХҐЦЂХ« ХґХЎХЅХ«Х¶ ХїХҐХІХҐХЇХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂ ХЅХїХЎХ¶ХЎХ¬ХёЦ‚ Х°ХЎХґХЎЦЂ:",
    footer_rights: "&copy; 2026 NOVA Inc. ФІХёХ¬ХёЦЂ Х«ЦЂХЎХѕХёЦ‚Х¶Ц„Х¶ХҐЦЂХЁ ХєХЎХ·ХїХєХЎХ¶ХѕХЎХ® ХҐХ¶:",
    footer_tagline: "Ф·ФјФµФіФ±Х†ХЏХ€Х’Ф№Х…Х€Х’Х† &bull; Х„Ф»Х†Ф»Х„Ф±ФјФ»Ф¶Х„ &bull; Х„Ф±Х”ХђХ€Х’Ф№Х…Х€Х’Х†",
    home_reviews_title: "ХЂХЎХіХЎХ­ХёЦЂХ¤Х¶ХҐЦЂХ« ФїХЎЦЂХ®Х«Ц„Х¶ХҐЦЂ",
    home_instagram_title: "ХЂХҐХїЦ‡ХҐЦ„ ХґХҐХ¦ Instagram-ХёЦ‚Хґ",
    home_instagram_shortcode_title: "Ф»Х¶ХЅХїХЎХЈЦЂХЎХґ ФјЦЂХЎХ°ХёХЅ (Shortcode)",
    scroll_text: "ХЌХЎХ°ХҐЦЃХ¶ХҐХ¬ &rarr;",
    brand_nova: "NOVA",
    brand_le_labo: "Le Labo",
    brand_byredo: "Byredo",
    brand_diptyque: "Diptyque",
    brand_aesop: "Aesop",
    brand_chanel: "Chanel",
    brand_dior: "Dior",
    brand_giorgio_armani: "Giorgio Armani",
    brand_yves_saint_laurent: "Yves Saint Laurent",
    brand_gucci: "Gucci",
    brand_versace: "Versace",
    brand_calvin_klein: "Calvin Klein",
    brand_dolce_gabbana: "Dolce & Gabbana",
    brand_tom_ford: "Tom Ford",
    brand_hermes: "HermГЁs",
    brand_burberry: "Burberry",
    brand_lancome: "LancГґme",
    brand_paco_rabanne: "Paco Rabanne",
    brand_jean_paul_gaultier: "Jean Paul Gaultier",
    brand_hugo_boss: "Hugo Boss",
    brand_creed: "Creed",
    brand_jo_malone: "Jo Malone London",
    brand_maison_francis_kurkdjian: "Maison Francis Kurkdjian",
    pp_faq_title: "ХЂХЎХіХЎХ­ ХїЦЂХѕХёХІ Х°ХЎЦЂЦЃХҐЦЂ",
    pp_faq_q1: "Ф»Х¶Х№ХёХћХѕ Х§ ХµХёЦ‚ЦЂХЎХ°ХЎХїХёЦ‚ХЇ ХЎХµХЅ ХўХёЦ‚ХµЦЂХЁ:",
    pp_faq_a1: "Х„ХҐЦЂ ХўХёЦ‚ХµЦЂХҐЦЂХЁ ХўХЎХІХЇХЎЦЃХЎХ® ХҐХ¶ Х°ХЎХ¦ХѕХЎХЈХµХёЦ‚Хї ХўХёЦ‚ХЅХЎХЇХЎХ¶ Х¶ХµХёЦ‚Х©ХҐЦЂХ«ЦЃ, ХёЦЂХёХ¶Ц„ Х©ХёЦЂХѕХёЦ‚Хґ ХҐХ¶ ЦѓХёЦ„ЦЂ Х­ХґХўХЎЦ„ХЎХ¶ХЎХЇХ¶ХҐЦЂХёХѕХќ ХўХЎЦЃХЎХјХ«ХЇ ХґХЎЦ„ЦЂХёЦ‚Х©ХµХЎХ¶ Ц‡ ХҐЦЂХЇХЎЦЂХЎХЇХҐЦЃХёЦ‚Х©ХµХЎХ¶ Х°ХЎХґХЎЦЂ:",
    pp_faq_q2: "Ф±ЦЂХ¤ХµХёХћЦ„ ХЎХµХЅ ХЎХєЦЂХЎХ¶Ц„ХЁ ХѕХҐХЈХЎХ¶ Х§ Ц‡ ХЎХјХЎХ¶ЦЃ Х¤ХЎХЄХЎХ¶ХёЦ‚Х©ХµХЎХ¶:",
    pp_faq_a2: "Ф±ХµХё, ХґХҐЦЂ ХўХёХ¬ХёЦЂ ХЅХїХҐХІХ®ХЎХЈХёЦЂХ®ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ 100% ХѕХҐХЈХЎХ¶ ХҐХ¶ Ц‡ ХҐЦЂХўХҐЦ„ Х№ХҐХ¶ ЦѓХёЦЂХ±ХЎЦЂХЇХѕХҐХ¬ ХЇХҐХ¶Х¤ХЎХ¶Х«Х¶ХҐЦЂХ« ХѕЦЂХЎ: Х„ХҐХ¶Ц„ Х°ХЎХѕХЎХїХёЦ‚Хґ ХҐХ¶Ц„ ХЇХЎХµХёЦ‚Х¶, Х§Х©Х«ХЇХЎХЇХЎХ¶ Х·Ц„ХҐХІХёЦ‚Х©ХµХЎХ¶ХЁ:",
    pp_faq_q3: "Х€ЦЂЦ„ХЎХћХ¶ Х§ ХєХЎХ°ХєХЎХ¶ХѕХёЦ‚Хґ ХўХёЦ‚ХµЦЂХЁ:",
    pp_faq_a3: "Х€ЦЂХєХҐХЅ Eau de ParfumХќ Х¤ХёЦ‚Ц„ ХЇХЎЦЂХёХІ ХҐЦ„ ХЎХЇХ¶ХЇХЎХ¬ХҐХ¬ Х«Х¶ХїХҐХ¶ХЅХ«Хѕ ХўХёЦ‚ЦЂХґХёЦ‚Х¶Ц„, ХёЦЂХЁ ХїЦ‡ХёЦ‚Хґ Х§ 8-Х«ЦЃ 12 ХЄХЎХґХќ ХЇХЎХ­ХѕХЎХ® Х±ХҐЦЂ ХґХЎХ·ХЇХ« Ц„Х«ХґХ«ХЎХµХ«ЦЃ:",
    pp_faq_q4: "Х€ХћЦЂХ¶ Х§ Х±ХҐЦЂ ХѕХҐЦЂХЎХ¤ХЎЦЂХ±Х« Ц„ХЎХІХЎЦ„ХЎХЇХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶ХЁ:",
    pp_faq_a4: "Х„ХҐХ¶Ц„ ХЁХ¶Х¤ХёЦ‚Х¶ХёЦ‚Хґ ХҐХ¶Ц„ ХѕХҐЦЂХЎХ¤ХЎЦЂХ± 30 Ц…ЦЂХѕХЎ ХЁХ¶Х©ХЎЦЃЦ„ХёЦ‚ХґХќ Х№Ц…ХЈХїХЎХЈХёЦЂХ®ХѕХЎХ® ХЎХєЦЂХЎХ¶Ц„Х¶ХҐЦЂХ« Х°ХЎХґХЎЦЂ, Х¤ЦЂХЎХ¶ЦЃ Ц…ЦЂХ«ХЈХ«Х¶ХЎХ¬ ЦѓХЎХ©ХҐХ©ХЎХѕХёЦЂХґХЎХґХў:",
    admin_title: "WooCommerce ФїХЎХјХЎХѕХЎЦЂХёЦ‚Хґ",
    admin_total_revenue: "ФёХ¶Х¤Х°ХЎХ¶ХёЦ‚ЦЂ ХЂХЎХЅХёЦ‚ХµХ©",
    admin_orders_placed: "ФіЦЂХЎХ¶ЦЃХѕХЎХ® ХЉХЎХїХѕХҐЦЂХ¶ХҐЦЂ",
    admin_avg_order_value: "Х„Х«Х»Х«Х¶ ХєХЎХїХѕХҐЦЂХ« ХЎЦЂХЄХҐЦ„",
    admin_total_stock: "ФёХ¶Х¤Х°ХЎХ¶ХёЦ‚ЦЂ ХєХЎХ·ХЎЦЂ",
    admin_orders_list: "WooCommerce ХЉХЎХїХѕХҐЦЂХ¶ХҐЦЂХ« Х‘ХЎХ¶ХЇ",
    admin_th_order_id: "ХЉХЎХїХѕХҐЦЂХ« ID",
    admin_th_date: "Ф±ХґХЅХЎХ©Х«Хѕ",
    admin_th_customer: "ХЂХЎХіХЎХ­ХёЦЂХ¤",
    admin_th_items: "Ф±ХєЦЂХЎХ¶Ц„Х¶ХҐЦЂ",
    admin_th_total: "ФёХ¶Х¤ХЎХґХҐХ¶ХЁ",
    admin_th_status: "ФїХЎЦЂХЈХЎХѕХ«ХіХЎХЇ",
    admin_th_actions: "ФіХёЦЂХ®ХёХІХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂ",
    admin_inventory_title: "WooCommerce ХЉХЎХ·ХЎЦЂХ¶ХҐЦЂ Ц‡ ФіХ¶ХҐЦЂ",
    admin_inventory_desc: "Х“ХёЦѓХёХ­ХҐЦ„ ХЈХ¶ХҐЦЂХ¶ ХёЦ‚ Ц„ХЎХ¶ХЎХЇХЁЦ‰ ХЌХҐХІХґХҐЦ„ В«Ф№ХЎЦЂХґХЎЦЃХ¶ХҐХ¬В»Хќ ЦѓХёЦѓХёХ­ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХ¶ ХЎХ¶ХґХ«Х»ХЎХєХҐХЅ ХЇХ«ЦЂХЎХјХҐХ¬ХёЦ‚ Х°ХЎХґХЎЦЂЦ‰",
    admin_th_image: "Х†ХЇХЎЦЂ",
    admin_th_fragrance: "ФІХёЦ‚ХµЦЂ",
    admin_th_price: "ФіХ«Х¶ (100ml)",
    admin_th_stock_qty: "ХЉХЎХ·ХЎЦЂХ« Ц„ХЎХ¶ХЎХЇ",
    admin_th_availability: "Ф±ХјХЇХЎХµХёЦ‚Х©ХµХёЦ‚Х¶",
    admin_btn_update: "Ф№ХЎЦЂХґХЎЦЃХ¶ХҐХ¬",
    review_1_text: "\"ФєХЎХґХЎХ¶ХЎХЇХЎХЇХ«ЦЃ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х« ХўХЎЦЃХЎЦЂХ±ХЎХЇ ХЈХ¬ХёЦ‚Х­ХЈХёЦЂХ®ХёЦЃЦ‰ ХЌХЎХ¶Х¤ХЎХ¬Х« Х¶ХёХїХЎХ¶ ХЎХµХ¶Ц„ХЎХ¶ ХµХёЦ‚ХІХЎХ¬Х« Ц‡ Х§Х¬ХҐХЈХЎХ¶Хї Х§, Х«Х¶Х± ХЎХ¶ХЁХ¶Х¤Х°ХЎХї ХЇХЎХ¶ХЈХ¶ХҐЦЃХ¶ХёЦ‚Хґ Ц‡ Х°ХЎЦЂЦЃХ¶ХёЦ‚Хґ ХҐХ¶, Х©ХҐ Х«Х¶Х№ ХҐХґ ХЇЦЂХёЦ‚Хґ:\"",
    review_1_author: "Ф·Х¬ХҐХ¶ХЎ ХЋ.",
    review_2_text: "\"Х„Х«Х¶Х«ХґХЎХ¬Х«ХЅХїХЎХЇХЎХ¶ ХЇХЎХїХЎЦЂХҐХ¬ХёЦ‚Х©ХµХёЦ‚Х¶Ц‰ Х“ХЎХ©ХҐХ©ХЎХѕХёЦЂХёЦ‚ХґХЁ Х°Х«ХЎХЅЦ„ХЎХ¶Х№ Х§, Х«ХЅХЇ Х°ХҐХ¶ЦЃ ХўХёЦ‚ХµЦЂХЁХќ ХўХЎЦЂХ¤, ХёЦЂХ¶ Ц…ЦЂХѕХЎ ХЁХ¶Х©ХЎЦЃЦ„ХёЦ‚Хґ ХЈХҐХІХҐЦЃХ«ХЇ Х¦ХЎЦЂХЈХЎХ¶ХёЦ‚Хґ Х§ Х«Хґ ХґХЎХ·ХЇХ« ХѕЦЂХЎ:\"",
    review_2_author: "Х„ХЎЦЂХЇХёЦ‚ХЅ ХЏ.",
    review_3_text: "\"ХЋХҐЦЂХ»ХЎХєХҐХЅ ХЈХїХЎ Х«Хґ ХЎХµЦЃХҐЦ„ХЎЦЂХїХЎХµХ«Х¶ ХўХёЦ‚ХµЦЂХЁЦ‰ Ф±ХµХ¶ Х¶ХёЦ‚ЦЂХў Х§, ХўХЎХµЦЃ ХЎХ¶ХҐЦЂЦ‡ХЎХЇХЎХµХҐХ¬Х« ХҐЦЂХЇХЎЦЂХЎХЇХµХЎЦЃЦ‰ ФІХёЦ‚ХЅХЎХЇХЎХ¶ ХґХЎЦ„ЦЂХёЦ‚Х©ХµХёЦ‚Х¶Х¶ Х«ХЅХЇХЎХєХҐХЅ Х·ХёХІХёЦ‚Хґ Х§ ХµХёЦ‚ЦЂХЎЦ„ХЎХ¶Х№ХµХёЦ‚ЦЂ ЦЃХёХІХ« ХґХҐХ»:\"",
    review_3_author: "ХЌХЎХјХЎ Фј.",
    review_4_text: "\"ХЂХЎХіХЎХ­ХёЦЂХ¤Х¶ХҐЦЂХ« ХЅХєХЎХЅХЎЦЂХЇХёЦ‚ХґХЁ Ц‡ ЦѓХЎХ©ХҐХ©ХЎХѕХёЦЂХёЦ‚ХґХЁ ХўХЎЦЃХҐХ¬ХёЦ‚ ЦѓХёЦЂХ±ХЁ Х¶ХёЦ‚ХµХ¶Ц„ХЎХ¶ Х·Ц„ХҐХІ Х§Х«Х¶, ХёЦЂЦ„ХЎХ¶ Х«Х¶Ц„Х¶Х«Х¶ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„ХЁЦ‰ ФЅХ«ХЅХї Х­ХёЦЂХ°ХёЦ‚ЦЂХ¤ ХҐХґ ХїХЎХ¬Х«ХЅ Х­ХёЦЂХ·ХЎХµХ«Х¶ ХўХёЦ‚ХµЦЂХҐЦЂХ« ХµХёЦ‚ЦЂХЎЦ„ХЎХ¶Х№ХµХёЦ‚ЦЂ ХЅХ«ЦЂХЎХ°ХЎЦЂХ«:\"",
    review_4_author: "ФґХЎХѕХ«Х¤ Фї.",
    review_5_text: "\"ФїХЎЦЂХ®ХҐХЅ ХЇЦЂХёЦ‚Хґ ХҐХґ ХЎЦЂХѕХҐХЅХїХ« ХЈХёЦЂХ®Ц‰ ФІХёЦ‚ХµЦЂХ« ХїХЎЦЂХЎХ®ХёЦ‚ХґХЁ ХіХ«Х·Хї Х№ХЎЦѓХ« Х§Хќ Х¶ХЇХЎХїХҐХ¬Х«, ХўХЎХµЦЃ ХҐЦЂХўХҐЦ„ Х№ХЎЦѓХЎХ¦ХЎХ¶ЦЃХѕХЎХ® Х№Х§:\"",
    review_5_author: "Х„Х«ХЎ ХЊ.",
    review_6_text: "\"Ф»ХЅХЇХЎХєХҐХЅ ХµХёЦ‚ЦЂХЎХ°ХЎХїХёЦ‚ХЇ Х°ХёХїХЎХјХЎХЇХЎХ¶ ЦѓХёЦЂХ±Ц‰ Х‰ХёЦЂХЎЦЃХґХЎХ¶ ЦѓХёЦ‚Х¬ХЁ ЦѓХёХ·ХёХї Х§ Ц‡ ХўХЎЦЂХ¤Ц‰ Ф±Х¶Х°ХЎХґХўХҐЦЂ ХЅХєХЎХЅХёЦ‚Хґ ХҐХґ ЦѓХёЦЂХ±ХҐХ¬ Х°ХЎХѕХЎЦ„ХЎХ®ХёЦ‚Х« ХґХ¶ХЎЦЃХЎХ® ХґХЎХЅХЁ:\"",
    review_6_author: "Х‹ХёЦ‚Х¬Х«ХЎХ¶ Х„.",
    review_7_text: "\"ФµХЅ ХЅХ«ЦЂХЎХ°ХЎЦЂХѕХЎХ® ХҐХґ ХґХ«Х¶Х«ХґХЎХ¬Х«ХЅХїХЎХЇХЎХ¶ Х§ХЅХ©ХҐХїХ«ХЇХЎХµХ«Х¶ Ц‡ ХґХЎЦ„ХёЦ‚ЦЂ, ХёХ№ ХїХёЦ„ХЅХ«ХЇ ХўХЎХІХЎХ¤ЦЂХ«Х№Х¶ХҐЦЂХ«Х¶Ц‰ ХЋХҐЦЂХ»ХЎХєХҐХЅ Х·Ц„ХҐХІ ХЎХєЦЂХЎХ¶Ц„ХЎХ¶Х«Х·, ХёЦЂХЁ Х°ХЎХґХЎХєХЎХїХЎХЅХ­ХЎХ¶ХёЦ‚Хґ Х§ Х«Хґ ХЎЦЂХЄХҐЦ„Х¶ХҐЦЂХ«Х¶:\"",
    review_7_author: "Х”Х¬ХёХҐ ХЏ.",
    review_8_text: "\"ФіХ¶ХҐХ¬ ХҐХґ ХёЦЂХєХҐХЅ Х¶ХѕХҐЦЂ ХЇХ¶ХёХ»ХЅ Х°ХЎХґХЎЦЂ, Ц‡ Х¶ХЎ ХЎХґХўХёХІХ»ХёХѕХ«Х¶ Х°Х«ХЎЦЃХЎХ® Х§ЦЂЦ‰ ФІХёЦ‚ХµЦЂХ¶ ХЎЦЂХўХҐЦЃХ¶ХёХІ Х§ Ц‡ ХЎХ¶ХҐЦЂЦ‡ХЎХЇХЎХµХҐХ¬Х« Х¶ХёЦ‚ЦЂХў:\"",
    review_8_author: "Х‹ХҐХµХґХЅ ФІ.",
    mobile_sec_menu: "Х„ХҐХ¶ХµХёЦ‚",
    mobile_sec_actions: "ФіХ¶ХёЦ‚ХґХ¶ХҐЦЂ",
    mobile_sec_languages: "ФјХҐХ¦ХёЦ‚Х¶ХҐЦЂ",
    nav_contact: "ХЂХҐХїХЎХ¤ХЎЦЂХ± ФїХЎХє",
    contact_header_title: "ХЂФµХЏФ±ФґФ±ХђХЃ ФїФ±ХЉ",
    contact_subtitle: "ФїХЎХєХѕХҐЦ„ Х„ХҐХ¦ ХЂХҐХї",
    contact_desc: "Х€Ц‚Х¶ХҐХћЦ„ Х°ХЎЦЂЦЃХҐЦЂ ХЇХЎХґ ЦЃХЎХ¶ХЇХЎХ¶ХёЦ‚Хґ ХҐЦ„ ХєХЎХїХѕХ«ЦЂХҐХ¬ ХЎХ¶Х°ХЎХїХЎХЇХЎХ¶ ХўХёЦ‚ХµЦЂЦ‰ Х„ХҐЦЂ ХЇХёХ¶ХЅХµХҐЦЂХЄХЁ ХЅХ«ЦЂХёХѕ ХЇЦ…ХЈХ¶Х« Х±ХҐХ¦Ц‰",
    contact_address_label: "ХЂХЎХЅЦЃХҐ",
    contact_phone_label: "ХЂХҐХјХЎХ­ХёХЅ",
    contact_email_label: "Ф·Х¬. ХЂХЎХЅЦЃХҐ",
    contact_delivery_label: "Ф±ХјХЎЦ„ХёЦ‚Хґ",
    contact_delivery_value: "Ф±ХјХЎЦ„ХёЦ‚Хґ ХЂХЎХµХЎХЅХїХЎХ¶Х« ХёХІХ» ХїХЎЦЂХЎХ®Ц„ХёХѕ",
    contact_form_name_placeholder: "Ф±Х¶Х« Ф±Х¶ХёЦ‚Х¶",
    contact_form_email_placeholder: "anun@example.com",
    contact_form_subject_placeholder: "ФІХёЦ‚ХµЦЂХ« Х­ХёЦЂХ°ЦЂХ¤ХЎХїХѕХёЦ‚Х©ХµХёЦ‚Х¶",
    contact_form_message_placeholder: "ФіЦЂХҐЦ„ Х±ХҐЦЂ Х°ХЎХІХёЦЂХ¤ХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶ХЁ ХЎХµХЅХїХҐХІ...",
    contact_hours_label: "ФєХЎХґХҐЦЂ",
    contact_form_name: "Ф±Х¶ХёЦ‚Х¶ *",
    contact_form_email: "Ф·Х¬. Х“ХёХЅХї *",
    contact_form_subject: "Ф№ХҐХґХЎ *",
    contact_form_message: "ХЂХЎХІХёЦЂХ¤ХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶ *",
    contact_form_submit: "Х€Ц‚ХІХЎЦЂХЇХҐХ¬",
    footer_policy_shipping: "Ф±ХјХЎЦ„ХёЦ‚Хґ Ц‡ ХЋХҐЦЂХЎХ¤ХЎЦЂХ±",
    footer_policy_privacy: "ФіХЎХІХїХ¶Х«ХёЦ‚Х©ХµХёЦ‚Х¶ Ц‡ ХЉХЎХµХґХЎХ¶Х¶ХҐЦЂ",
    policy_shipping_header_title: "Ф±ХЊФ±Х”Х€Х’Х„",
    policy_shipping_subtitle: "Ф±Х‹Ф±ФїХ‘Х€Х’Ф№Х…Х€Х’Х†",
    policy_shipping_title: "Ф±ХјХЎЦ„ХґХЎХ¶ Ц‡ ХЋХҐЦЂХЎХ¤ХЎЦЂХ±Х« ХЉХЎХµХґХЎХ¶Х¶ХҐЦЂ",
    policy_shipping_sec1_title: "1. Ф±ХјХЎЦ„ХёЦ‚Хґ Ц‡ ХЂХЎХ¶Х±Х¶ХёЦ‚Хґ",
    policy_shipping_sec1_desc1: "NOVA-ХёЦ‚Хґ ХµХёЦ‚ЦЂХЎЦ„ХЎХ¶Х№ХµХёЦ‚ЦЂ ХўХёЦ‚ХµЦЂ Х­Х¶ХЎХґЦ„ХёХѕ ЦѓХЎХ©ХҐХ©ХЎХѕХёЦЂХѕХёЦ‚Хґ Х§Хќ ХєХЎХ°ХєХЎХ¶ХҐХ¬ХёЦ‚ Х«ЦЂ Х¶ХёЦ‚ЦЂХў ХўХёЦ‚ХЅХЎХЇХЎХ¶ ХўХЎХІХЎХ¤ЦЂХёЦ‚Х©ХµХёЦ‚Х¶ХЁЦ‰ Х„ХҐХ¶Ц„ ХЎХјХЎЦ„ХёЦ‚Хґ ХҐХ¶Ц„ Х«Х¶Х№ХєХҐХЅ ХЂХЎХµХЎХЅХїХЎХ¶Х« ХёХІХ» ХїХЎЦЂХЎХ®Ц„ХёЦ‚Хґ, ХЎХµХ¶ХєХҐХЅ Х§Х¬ ХґХ«Х»ХЎХ¦ХЈХЎХµХ«Х¶ ХґХЎХЇХЎЦЂХ¤ХЎХЇХёХѕЦ‰",
    policy_shipping_sec1_desc2: "<strong>Ф±ХјХЎЦ„ХёЦ‚Хґ ФµЦЂЦ‡ХЎХ¶ХёЦ‚Хґ:</strong> 1-2 ХЎХ·Х­ХЎХїХЎХ¶Ц„ХЎХµХ«Х¶ Ц…ЦЂ (ХЎХ¶ХѕХіХЎЦЂ 20,000 AMD-Х«ЦЃ ХўХЎЦЂХ±ЦЂ ХєХЎХїХѕХҐЦЂХ¶ХҐЦЂХ« Х¤ХҐХєЦ„ХёЦ‚Хґ):<br><strong>ХЂХЂ ХґХЎЦЂХ¦ХҐЦЂ:</strong> 2-4 ХЎХ·Х­ХЎХїХЎХ¶Ц„ХЎХµХ«Х¶ Ц…ЦЂ:<br><strong>Х„Х«Х»ХЎХ¦ХЈХЎХµХ«Х¶ ХЎХјХЎЦ„ХёЦ‚Хґ:</strong> 7-14 ХЎХ·Х­ХЎХїХЎХ¶Ц„ХЎХµХ«Х¶ Ц…ЦЂ ХЈЦЂХЎХ¶ЦЃХѕХЎХ® ЦѓХёХЅХїХёХѕ:",
    policy_shipping_sec2_title: "2. ХЋХҐЦЂХЎХ¤ХЎЦЂХ±Х« Ц‡ Х“ХёХ­ХЎХ¶ХЎХЇХґХЎХ¶ ХЉХЎХµХґХЎХ¶Х¶ХҐЦЂ",
    policy_shipping_sec2_desc1: "ХЂХЎХ·ХѕХ« ХЎХјХ¶ХҐХ¬ХёХѕ Х­ХёЦЂХ·ХЎХµХ«Х¶ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х¶ХҐЦЂХ« ХЎХ¶Х±Х¶ХЎХЇХЎХ¶ ХўХ¶ХёЦ‚ХµХ©ХЁ, ХґХҐХ¶Ц„ ХЇХЎЦЂХёХІ ХҐХ¶Ц„ ХЁХ¶Х¤ХёЦ‚Х¶ХҐХ¬ ХґХ«ХЎХµХ¶ ХЎХµХ¶ ХЎХєЦЂХЎХ¶Ц„Х¶ХҐЦЂХ« ХѕХҐЦЂХЎХ¤ХЎЦЂХ±ХЁ, ХёЦЂХёХ¶Ц„ Х¬Х«ХёХѕХ«Х¶ Х№ХўХЎЦЃХѕХЎХ® ХҐХ¶, Х№Ц…ХЈХїХЎХЈХёЦЂХ®ХѕХЎХ® Ц‡ ХЈХїХ¶ХѕХёЦ‚Хґ ХҐХ¶ Х«ЦЂХҐХ¶ЦЃ Ц…ЦЂХ«ХЈХ«Х¶ХЎХ¬ ЦѓХЎХЇ ЦѓХЎХ©ХҐХ©ХЎХѕХёЦЂХґХЎХ¶ ХґХҐХ»Хќ ХЎХјХЎЦ„ХёЦ‚ХґХ«ЦЃ Х°ХҐХїХё 14 Ц…ЦЂХѕХЎ ХЁХ¶Х©ХЎЦЃЦ„ХёЦ‚Хґ:",
    policy_shipping_sec2_desc2: "ФµХ©ХҐ ЦЃХЎХ¶ХЇХЎХ¶ХёЦ‚Хґ ХҐЦ„ ХѕХҐЦЂХЎХ¤ХЎЦЂХ± ХЇХЎХїХЎЦЂХҐХ¬, Х­Х¶Х¤ЦЂХёЦ‚Хґ ХҐХ¶Ц„ ХЇХЎХєХѕХҐХ¬ ХґХҐЦЂ ХЇХёХ¶ХЅХµХҐЦЂХЄ Х©Х«ХґХ« Х°ХҐХї concierge@novafragrances.com Х°ХЎХЅЦЃХҐХёХѕХќ Х¶ХЎХ­Ц„ХЎХ¶ Х®ЦЂХЎЦЂХЁ Х°ХҐХї ХёЦ‚ХІХЎЦЂХЇХҐХ¬ХЁ:",
    policy_shipping_sec3_title: "3. ХЋХ¶ХЎХЅХѕХЎХ® Ф±ХєЦЂХЎХ¶Ц„Х¶ХҐЦЂ",
    policy_shipping_sec3_desc1: "ФµХ©ХҐ Х±ХҐЦЂ ХЅЦЂХѕХЎХЇХЁ ХѕХ¶ХЎХЅХѕХЎХ® Х§ ХЇХЎХґ ХЎЦЂХїХЎХ°ХёХЅХёЦ‚Хґ Х§, Х­Х¶Х¤ЦЂХёЦ‚Хґ ХҐХ¶Ц„ ХЎХ¶ХґХ«Х»ХЎХєХҐХЅ Х¬ХёЦ‚ХЅХЎХ¶ХЇХЎЦЂХҐХ¬ ХЎХµХ¶ Ц‡ ХЇХЎХєХѕХҐХ¬ ХґХҐХ¦ Х°ХҐХї ХЅХїХЎХ¶ХЎХ¬ХёЦ‚ЦЃ Х°ХҐХїХё 48 ХЄХЎХґХѕХЎ ХЁХ¶Х©ХЎЦЃЦ„ХёЦ‚ХґЦ‰ Х„ХҐХ¶Ц„ ХЎХ¶ХґХ«Х»ХЎХєХҐХЅ ХЇХЇХЎХ¦ХґХЎХЇХҐЦЂХєХҐХ¶Ц„ ЦѓХёХ­ХЎЦЂХ«Х¶ХёЦ‚Хґ ХЇХЎХґ Х¬Х«ХЎЦЂХЄХҐЦ„ ХѕХҐЦЂХЎХ¤ХЎЦЂХ±:",
    policy_privacy_header_title: "Х•ХђФµХ†Х”",
    policy_privacy_subtitle: "Ф»ХђФ±ХЋФ±ФїФ±Х† ХЏФµХ‚ФµФїФ±ХЏХЋХ€Х’Ф№Х…Х€Х’Х†",
    policy_privacy_title: "ФіХЎХІХїХ¶Х«ХёЦ‚Х©ХµХЎХ¶ Х”ХЎХІХЎЦ„ХЎХЇХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶ Ц‡ Х•ХЈХїХЎХЈХёЦЂХ®ХґХЎХ¶ ХЉХЎХµХґХЎХ¶Х¶ХҐЦЂ",
    policy_privacy_sec1_title: "1. ХЂХЎХѕХЎЦ„ХЎХЈЦЂХѕХёХІ ХЏХҐХІХҐХЇХЎХїХѕХёЦ‚Х©ХµХёЦ‚Х¶",
    policy_privacy_sec1_desc: "Х„ХҐХ¶Ц„ Х°ХЎХѕХЎЦ„ХЎХЈЦЂХёЦ‚Хґ ХҐХ¶Ц„ Х°Х«ХґХ¶ХЎХЇХЎХ¶ ХЇХёХ¶ХїХЎХЇХїХЎХµХ«Х¶ Ц‡ ХЎХјХЎЦ„ХґХЎХ¶ ХїХѕХµХЎХ¬Х¶ХҐЦЂХЁ, ХҐЦЂХў ХЇХЎХїХЎЦЂХёЦ‚Хґ ХҐЦ„ ХЈХ¶ХёЦ‚ХґХ¶ХҐЦЂ ХЇХЎХґ ХўХЎХЄХЎХ¶ХёЦЂХ¤ХЎХЈЦЂХѕХёЦ‚Хґ ХҐЦ„ ХґХҐЦЂ Х¬ЦЂХЎХїХёЦ‚Х«Х¶Ц‰ Ф±ХµХЅ ХїХѕХµХЎХ¬Х¶ХҐЦЂХ¶ Ц…ХЈХїХЎХЈХёЦЂХ®ХѕХёЦ‚Хґ ХҐХ¶ ХўХЎЦЃХЎХјХЎХєХҐХЅ ХєХЎХїХѕХҐЦЂХ¶ХҐЦЂХ« Х«ЦЂХЎХЇХЎХ¶ХЎЦЃХґХЎХ¶ Х°ХЎХґХЎЦЂ:",
    policy_privacy_sec2_title: "2. ХЋХіХЎЦЂХёЦ‚ХґХ¶ХҐЦЂХ« Ф±Х¶ХѕХїХЎХ¶ХЈХёЦ‚Х©ХµХёЦ‚Х¶",
    policy_privacy_sec2_desc: "ФІХёХ¬ХёЦЂ ХѕХіХЎЦЂХёЦ‚ХґХ¶ХҐЦЂХЁ ХґХ·ХЎХЇХѕХёЦ‚Хґ ХҐХ¶ ХЎХєХЎХ°ХёХѕ Ц‡ ХЈХЎХІХїХ¶ХЎХЈЦЂХѕХЎХ® Х°ХЎХґХЎХЇХЎЦЂХЈХҐЦЂХ« ХґХ«Х»ХёЦЃХёХѕЦ‰ NOVA-Х¶ Х№Х« ХєХЎХ°ХєХЎХ¶ХёЦ‚Хґ Ц‡ Х№ХёЦ‚Х¶Х« ХёЦ‚ХІХ«ХІ Х°ХЎХЅХЎХ¶ХҐХ¬Х«ХёЦ‚Х©ХµХёЦ‚Х¶ Х±ХҐЦЂ Ц„ХЎЦЂХїХ« ХїХѕХµХЎХ¬Х¶ХҐЦЂХ«Х¶:",
    policy_privacy_sec3_title: "3. Х•ХЈХїХЎХЈХёЦЂХ®ХґХЎХ¶ ХЉХЎХµХґХЎХ¶Х¶ХҐЦЂ",
    policy_privacy_sec3_desc: "Ф±ХµХЅ ХЇХЎХµЦ„ХёЦ‚Хґ ХїХҐХІХЎХ¤ЦЂХѕХЎХ® ХёХІХ» ХўХёХѕХЎХ¶Х¤ХЎХЇХёЦ‚Х©ХµХёЦ‚Х¶ХЁ, ХєХЎХїХЇХҐЦЂХ¶ХҐЦЂХЁ Ц‡ ХїХҐЦ„ХЅХїХҐЦЂХЁ ХєХЎХ·ХїХєХЎХ¶ХѕХЎХ® ХҐХ¶ ХЎХєЦЂХЎХ¶Ц„ХЎХµХ«Х¶ Х¶Х·ХЎХ¶ХёХѕ Ц‡ Х°ХҐХІХ«Х¶ХЎХЇХЎХµХ«Х¶ Х«ЦЂХЎХѕХёЦ‚Х¶Ц„ХёХѕЦ‰ ХЉХЎХїХіХҐХ¶ХЎХ°ХЎХ¶ХёЦ‚ХґХ¶ ХЎХјХЎХ¶ЦЃ NOVA-Х« ХЈЦЂХЎХѕХёЦЂ Х©ХёЦ‚ХµХ¬ХїХѕХёЦ‚Х©ХµХЎХ¶ Х­ХЅХїХ«Хѕ ХЎЦЂХЈХҐХ¬ХѕХёЦ‚Хґ Х§:",
    wishlist_header_title: "Х‘Ф±Х†ФїХ€Х’Ф№Х…Х€Х’Х†Х†ФµХђ",
    wishlist_subtitle: "Ф»Х„ Х†Ф±ФЅФёХ†ХЏХђФµФјФ»Х†ФµХђФё",
    wishlist_view_page_btn: "ХЏХҐХЅХ¶ХҐХ¬ Х‘ХЎХ¶ХЇХёЦ‚Х©ХµХЎХ¶ Ф·Х»ХЁ",
    exp_subtitle: "Х•Х¬Ц†ХЎХЇХїХёЦЂ ФіХ«ХїХёЦ‚Х©ХµХёЦ‚Х¶",
    exp_title: "Х•Х¬Ц†ХЎХЇХїХёЦЂ ХЉХЎХїХґХёЦ‚Х©ХµХёЦ‚Х¶",
    exp_desc: "Х…ХёЦ‚ЦЂХЎЦ„ХЎХ¶Х№ХµХёЦ‚ЦЂ NOVA ХўХёЦ‚ХµЦЂ Х¤Х«Х¶ХЎХґХ«ХЇ, ХЇХҐХ¶Х¤ХЎХ¶Х« ХЇХёХґХєХёХ¦Х«ЦЃХ«ХЎ Х§, ХёЦЂХЁ Х¦ХЎЦЂХЈХЎХ¶ХёЦ‚Хґ Х§ ХґХЎХ·ХЇХ« ХѕЦЂХЎ ХґХ« Ц„ХЎХ¶Х« ХЄХЎХґХѕХЎ ХЁХ¶Х©ХЎЦЃЦ„ХёЦ‚Хґ: Х„ХҐХ¶Ц„ Х°ХҐХїЦ‡ХёЦ‚Хґ ХҐХ¶Ц„ Ц†ЦЂХЎХ¶ХЅХ«ХЎХЇХЎХ¶ ХўХЎЦЂХ±ЦЂ Ц…Х®ХЎХ¶ХҐХ¬Х«Ц„Х« ХЎХѕХЎХ¶Х¤ХЎХЇХЎХ¶ ХҐХјХЎХЅХїХ«ХіХЎХ¶ ХЇХЎХјХёЦ‚ЦЃХѕХЎХ®Ц„Х«Х¶, ХёЦЂХ¶ ХёЦ‚ХЄХҐХІХЎЦЃХѕХЎХ® Х§ ХЇХЎХµХёЦ‚Х¶ ХЎХІХўХµХёЦ‚ЦЂХ¶ХҐЦЂХ«ЦЃ Х°ХЎХѕХЎЦ„ХѕХЎХ® ХўХёЦ‚ХЅХЎХЇХЎХ¶ ХўХЎХІХЎХ¤ЦЂХ«Х№Х¶ХҐЦЂХёХѕЦ‰",
    top_desc: "Ф±ХјХЎХ»Х«Х¶ ХїХєХЎХѕХёЦЂХёЦ‚Х©ХµХёЦ‚Х¶ (0-30 ЦЂХёХєХҐ)",
    heart_desc: "ХЂХ«ХґХ¶ХЎХЇХЎХ¶ ХўХ¶ХЎХѕХёЦЂХёЦ‚Х©ХµХёЦ‚Х¶ (30 ЦЂХёХєХҐ - 4 ХЄХЎХґ)",
    base_desc: "ХЏХЎЦЂХЎХ®ХёЦ‚Хґ Ц‡ ХїЦ‡ХёХІХёЦ‚Х©ХµХёЦ‚Х¶ (4+ ХЄХЎХґ)",
    promises_title: "ФіХ«ХїХЎХЇХ«ЦЃ Х‡Ц„ХҐХІХёЦ‚Х©ХµХЎХ¶ ХЌХЇХ¦ХўХёЦ‚Х¶Ц„Х¶ХҐЦЂ",
    suggestions_title: "Ф±ХґХўХёХІХ»ХЎЦЃЦЂХҐЦ„ ФѕХ«ХЅХЎХЇХЎЦЂХЈХЁ",
    search_no_results: "ХЂХёХїХЎХјХЎХЇХЎХ¶ Х¶ХёХїХЎХ¶ХҐЦЂ Х№ХҐХ¶ ХЈХїХ¶ХѕХҐХ¬:",
    filter_scent_family: "ФІХёЦ‚ХµЦЂХҐЦЂХ« ФёХ¶ХїХЎХ¶Х«Ц„",
    filter_concentration: "ФЅХїХёЦ‚Х©ХµХёЦ‚Х¶",
    filter_gender: "ХЌХҐХј",
    filter_vibe: "ХЏЦЂХЎХґХЎХ¤ЦЂХёЦ‚Х©ХµХёЦ‚Х¶ / Ф±ХјХ«Х©",
    filter_price: "ФіХ«Х¶",
    filter_size: "Х‰ХЎЦѓХЅ",
    filter_brand: "Ф±ХєЦЂХЎХ¶Ц„ХЎХ¶Х«Х·",
    btn_apply_filters: "ФїХ«ЦЂХЎХјХҐХ¬ Ф¶ХїХ«Х№Х¶ХҐЦЂХЁ",
    btn_clear_all: "Х„ХЎЦ„ЦЂХҐХ¬ ФІХёХ¬ХёЦЂХЁ",
    btn_filters: "Ф¶ХїХ«Х№Х¶ХҐЦЂ",
    vibe_romantic: "ХЊХёХґХЎХ¶ХїХ«ХЇ / ФµЦЂХҐХЇХёХµХЎХ¶",
    vibe_daily: "Ф±ХґХҐХ¶Ц…ЦЂХµХЎ / Ф№ХЎЦЂХґ",
    vibe_professional: "ФіХёЦЂХ®Х¶ХЎХЇХЎХ¶ / Ф·Х¬ХҐХЈХЎХ¶Хї",
    vibe_cozy: "ХЏХЎЦ„ / ХЂХЎЦЂХґХЎЦЂХЎХѕХҐХї",
    vibe_mysterious: "ФЅХёЦЂХ°ЦЂХ¤ХЎХѕХёЦЂ / ХЂХЎХґХЎЦЂХ±ХЎХЇ",
    my_account_subtitle: "ХЂФ±ХѓФ±ФЅХ€ХђФґФ» ХЉХ€ХђХЏФ±Фј",
    my_account_title: "Ф»Хґ ХЂХЎХ·Х«ХѕХЁ",
    acc_nav_dashboard: "ФіХ¬Х­ХЎХѕХёЦЂ Х§Х»",
    acc_nav_wardrobe: "ФІХёЦ‚ХµЦЂХҐЦЂХ« ХЉХЎХ°ХЎЦЂХЎХ¶",
    acc_nav_discovery: "Ф±Х¶Х±Х¶ХЎХЇХЎХ¶ Ф¶ХҐХІХ№",
    acc_nav_details: "ХЂХЎХ·ХѕХ« ХЏХѕХµХЎХ¬Х¶ХҐЦЂ",
    acc_nav_orders: "ХЉХЎХїХѕХҐЦЂХ¶ХҐЦЂХ« ХЉХЎХїХґХёЦ‚Х©ХµХёЦ‚Х¶",
    acc_dash_welcome: "ФІХЎЦЂХ« ХЈХЎХ¬ХёЦ‚ХЅХї Х±ХҐЦЂ ХЎХ¶Х±Х¶ХЎХЇХЎХ¶ Х°ХёХїХЎХјХЎХЇХЎХ¶ ХєХёЦЂХїХЎХ¬: Ф±ХµХЅХїХҐХІХ«ЦЃ Х¤ХёЦ‚Ц„ ХЇХЎЦЂХёХІ ХҐЦ„ Х°ХҐХїЦ‡ХҐХ¬ Х±ХҐЦЂ ХЈХ¶ХёЦ‚ХґХ¶ХҐЦЂХ«Х¶, ХЇХЎХјХЎХѕХЎЦЂХҐХ¬ Х±ХҐЦЂ Х°ХЎХЅЦЃХҐХ¶ХҐЦЂХЁ Ц‡ ХЇХёЦ‚ХїХЎХЇХѕХЎХ® ХЇЦЂХҐХ¤Х«ХїХ¶ХҐЦЂХЁ:",
    acc_scent_profile: "Х•Х¬Ц†ХЎХЇХїХёЦЂ ХЉЦЂХёЦ†Х«Х¬",
    acc_scent_profile_desc: "ХЂХ«ХґХ¶ХѕХҐХ¬ХёХѕ Х±ХҐЦЂ ХЁХ¶ХїЦЂХЎХ® ХўХёЦ‚ХµЦЂХҐЦЂХ« ХѕЦЂХЎ, Х±ХҐЦЂ Х¶ХЎХ­ХЁХ¶ХїЦЂХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХ¶ ХҐХ¶.",
    acc_wardrobe_title: "Ф»Хґ ФІХёЦ‚ХµЦЂХҐЦЂХ« ХЉХЎХ°ХЎЦЂХЎХ¶ХЁ",
    acc_discovery_title: "Х“ХёЦЂХ±ХЎХ¶ХґХёЦ‚Х·Х¶ХҐЦЂХ« ХЂХҐХїЦ‡ХёЦ‚Хґ",
    acc_promo_tag: "Discovery ФЅХёЦ‚ХґХў",
    acc_promo_title: "ФїХ«ЦЂХЎХјХҐЦ„ Х“ХёЦЂХ±ХЎХ¶ХґХёЦ‚Х·Х« ФїЦЂХҐХ¤Х«ХїХЁ ФјХ«ХЎЦЂХЄХҐЦ„ Х‡Х·Х« ХЂХЎХґХЎЦЂ",
    acc_promo_desc: "ХЂХЎХѕХЎХ¶ХҐЦЃХ«ХћЦ„ Х¶ХґХёЦ‚Х·Х¶ХҐЦЂХЁ: ФїХ«ЦЂХЎХјХҐЦ„ Х±ХҐЦЂ 4,000 AMD Discovery ХЇЦЂХҐХ¤Х«ХїХЁ ЦЃХЎХ¶ХЇХЎЦЃХЎХ® Х¬Х«ХЎЦЂХЄХҐЦ„ (100ХґХ¬) Х·Х·Х« Х°ХЎХґХЎЦЂ:",
    acc_promo_code_lbl: "ХЃФµХђ ФµФ¶Ф±ФїФ» ФїХ€ФґФё:",
    admin_secure_portal: "Ф±ХЉФ±ХЂХ€ХЋ Х„Х€Х’ХЏХ”",
    admin_login_title: "WooCommerce Х„ХёЦ‚ХїЦ„",
    admin_pass_hint: "ХЂХёЦ‚Х·ХёЦ‚Хґ. norayrnajaryann@gmail.com / K9$v!p2WbX",
    btn_admin_login: "Х„ХёЦ‚ХїЦ„ ХЈХёЦЂХ®ХҐХ¬",
    admin_view_frontend: "ФґХ«ХїХҐХ¬ ФЅХЎХ¶ХёЦ‚Х©ХЁ",
    admin_nav_orders: "ХЉХЎХїХѕХҐЦЂХ¶ХҐЦЂХ« ФїХЎХјХЎХѕХЎЦЂХ«Х№",
    admin_nav_inventory: "ХЉХЎХ·ХЎЦЂХ¶ХҐЦЂХ« ФїХЎХјХЎХѕХЎЦЂХ«Х№",
    admin_nav_dev: "Х„Х·ХЎХЇХёХІХ« ФїХЎЦЂХЈХЎХѕХёЦЂХёЦ‚ХґХ¶ХҐЦЂ",
    admin_nav_logs: "ФіХёЦЂХ®ХёХІХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХ« Х„ХЎХїХµХЎХ¶",
    admin_nav_clients: "ХЂХЎХіХЎХ­ХёЦЂХ¤Х¶ХҐЦЂ",
    admin_clients_title: "ФіЦЂХЎХ¶ЦЃХѕХЎХ® ХЂХЎХіХЎХ­ХёЦЂХ¤Х¶ХҐЦЂ",
    admin_clients_desc: "ФїХЎХµЦ„ХёЦ‚Хґ Х°ХЎХ·Х«Хѕ ХЅХїХҐХІХ®ХЎХ® ХўХёХ¬ХёЦЂ Ц…ХЈХїХЎХЈХёЦЂХ®ХёХІХ¶ХҐЦЂХЁ:",
    admin_th_name: "Ф±Х¶ХёЦ‚Х¶",
    admin_th_registered: "ФіЦЂХЎХ¶ЦЃХґХЎХ¶ ХЎХґХЅХЎХ©Х«Хѕ",
    admin_th_actions: "ФіХёЦЂХ®ХёХІХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂ",
    acc_nav_wardrobe_sub: "ФґХ«ХїХҐХ¬ ХЈХ¶ХѕХЎХ® Х·Х·ХҐЦЂХЁ",
    acc_nav_discovery_sub: "ХЂХҐХїЦ‡ХҐХ¬ ЦѓХёЦЂХ±ХЎХ¶ХґХёЦ‚Х·Х¶ХҐЦЂХ«Х¶",
    acc_nav_orders_sub: "ХЂХҐХїЦ‡ХҐХ¬ ХЎХјХЎЦ„ХёЦ‚ХґХ¶ХҐЦЂХ«Х¶",
    acc_nav_details_sub: "ФЅХґХўХЎХЈЦЂХҐХ¬ Х°ХЎХЅЦЃХҐХ¶ХҐЦЂХЁ Ц‡ Х§Х¬. Х°ХЎХЅЦЃХҐХ¶",
    acc_th_id: "ID",
    acc_th_date: "Ф±ХґХЅХЎХ©Х«Хѕ",
    acc_th_items: "Ф±ХєЦЂХЎХ¶Ц„Х¶ХҐЦЂ",
    acc_th_total: "ФёХ¶Х¤Х°ХЎХ¶ХёЦ‚ЦЂ",
    acc_th_status: "ФїХЎЦЂХЈХЎХѕХ«ХіХЎХЇ",
    acc_th_invoice: "ХЂХЎХ·Х«Хѕ",
    acc_th_sample_kit: "Х“ХёЦЂХ±ХЎХ¶ХґХёЦ‚Х·Х« Х°ХЎХѕХЎЦ„ХЎХ®ХёЦ‚",
    acc_th_purchase_date: "ФіХ¶ХґХЎХ¶ ХЎХґХЅХЎХ©Х«Хѕ",
    acc_th_credit_value: "ФїЦЂХҐХ¤Х«ХїХ« ХЎЦЂХЄХҐЦ„",
    admin_staff_placeholder: "Ф±Х¶ХёЦ‚Х¶ (Ц…ЦЂ.Хќ Ф·Х¬ХҐХ¶ХЎ)",
    status_pending: "ФёХ¶Х©ХЎЦЃЦ„Х« ХґХҐХ»",
    status_processing: "Х„Х·ХЎХЇХѕХёЦ‚Хґ Х§",
    status_completed: "Ф±ХѕХЎЦЂХїХѕХЎХ®",
    status_failed: "ХЃХЎХ­ХёХІХѕХЎХ®",
    admin_btn_delete: "Х‹Х¶Х»ХҐХ¬",
    admin_btn_add_new: "Ф±ХѕХҐХ¬ХЎЦЃХ¶ХҐХ¬ Х¶ХёЦЂХЁ",
    plugin_pdf_label: "WooCommerce PDF ХЂХЎХ·Х«Хѕ-ХЎХєЦЂХЎХ¶Ц„ХЎХЈЦЂХҐЦЂ (v2.1)",
    plugin_subs_label: "WooCommerce ФІХЎХЄХЎХ¶ХёЦЂХ¤ХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂ (v4.0)",
    admin_insta_location_placeholder: "Ц…ЦЂ.Хќ ФіЦЂХЎХЅ, Х–ЦЂХЎХ¶ХЅХ«ХЎ",
    admin_insta_caption_placeholder: "ФІХёЦ‚ХµЦЂХҐЦЂХ« Х¶ХёХїХЎХ¶ХҐЦЂ, ХґХЎХ¶ЦЂХЎХґХЎХЅХ¶ХҐЦЂ Ц‡ ХЎХµХ¬Х¶...",
    admin_staff_locked: "Ф±ЦЂХЈХҐХ¬ХЎЦѓХЎХЇХѕХЎХ®",
    admin_logs_empty: "Х„ХЎХїХµХЎХ¶ХёЦ‚Хґ ХЈЦЂХЎХјХёЦ‚ХґХ¶ХҐЦЂ Х№ХЇХЎХ¶:",
    admin_sales_perf: "ХЋХЎХіХЎХјЦ„Х« Ф±ЦЂХ¤ХµХёЦ‚Х¶Ц„Х¶ХҐЦЂХ¶ ХЁХЅХї ФїХЎХїХҐХЈХёЦЂХ«ХЎХ¶ХҐЦЂХ«",
    admin_dev_title: "Х„Х·ХЎХЇХёХІХ« Ц‡ ХЂХЎХґХЎХЇХЎЦЂХЈХ« ФїХЎЦЂХЈХЎХѕХёЦЂХёЦ‚ХґХ¶ХҐЦЂ",
    admin_dev_desc: "ФїХЎХјХЎХѕХЎЦЂХҐЦ„ ХєХ¬ХЎХїЦ†ХёЦЂХґХ« ХЇХёХ¶Ц†Х«ХЈХёЦ‚ЦЂХЎЦЃХ«ХЎХ¶ХҐЦЂХЁ, Х°Х«ХґХ¶ХЎХЇХЎХ¶ ХЁХ¶Х¤Х¬ХЎХµХ¶ХёЦ‚ХґХ¶ХҐЦЂХЁ, ХѕХіХЎЦЂХЎХµХ«Х¶ Х¤ХЎЦЂХєХЎХЅХ¶ХҐЦЂХЁ Ц‡ ХЎХ¶Х±Х¶ХЎХЇХЎХ¦ХґХ« ХЎХ¶ХѕХїХЎХ¶ХЈХёЦ‚Х©ХµХЎХ¶ Х°ХЎХѕХЎХїХЎЦЂХґХЎХЈЦЂХҐЦЂХЁ:",
    admin_dev_plugins: "ХЂХ«ХґХ¶ХЎХЇХЎХ¶ ФёХ¶Х¤Х¬ХЎХµХ¶ХёЦ‚ХґХ¶ХҐЦЂ Ц‡ Х“Х¬ХЎХЈХ«Х¶Х¶ХҐЦЂ",
    admin_dev_gateways: "ХЋХіХЎЦЂХЎХµХ«Х¶ ФґХЎЦЂХєХЎХЅХ¶ХҐЦЂ",
    gateway_stripe_label: "Stripe Ф»Х¶ХїХҐХЈЦЂХёЦ‚Хґ",
    gateway_paypal_label: "PayPal Express Ф»Х¶ХїХҐХЈЦЂХёЦ‚Хґ",
    gateway_cod_label: "ФїХЎХ¶Х­Х«ХЇ ХЋХіХЎЦЂХёЦ‚Хґ (COD)",
    admin_dev_staff_manager: "Ф±Х¶Х±Х¶ХЎХЇХЎХ¦ХґХ« ХЉЦЂХёЦ†Х«Х¬Х¶ХҐЦЂХ« ФїХЎХјХЎХѕХЎЦЂХ«Х№",
    admin_dev_staff_desc: "ФіЦЂХЎХ¶ЦЃХҐЦ„ ХЇХЎХґ Х№ХҐХІХЎЦЂХЇХҐЦ„ ХЎХ¶Х±Х¶ХЎХЇХЎХ¦ХґХ« ХґХҐХ¶ХҐХ»ХҐЦЂХ¶ХҐЦЂХ« ХґХёЦ‚ХїЦ„Х« Х°ХЎХѕХЎХїХЎЦЂХґХЎХЈЦЂХҐЦЂХЁ:",
    admin_btn_create_profile: "ХЌХїХҐХІХ®ХҐХ¬ ХЉЦЂХёЦ†Х«Х¬",
    admin_th_staff_name: "Ф±Х¶Х±Х¶ХЎХЇХЎХ¦ХґХ« Ф±Х¶ХёЦ‚Х¶",
    admin_th_role: "ФґХҐЦЂ",
    admin_logs_title: "ФіХёЦЂХ®ХёХІХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХ« Х„ХЎХїХµХЎХ¶",
    admin_logs_desc: "ХЉХ¬ХЎХїЦ†ХёЦЂХґХ« ХЎХ¶ХѕХїХЎХ¶ХЈХёЦ‚Х©ХµХЎХ¶ ХЎХёЦ‚Х¤Х«ХїХ« ХґХЎХїХµХЎХ¶, ХёЦЂХЁ Х°ХҐХїЦ‡ХёЦ‚Хґ Х§ Х°ХЎХѕХЎХїХЎЦЂХґХЎХЈЦЂХҐЦЂХ« ХґХёЦ‚ХїЦ„Х«Х¶, ХЇХЎЦЂХЈХЎХѕХёЦЂХёЦ‚ХґХ¶ХҐЦЂХ«Х¶, ХЈХ¶ХЎХЈХёХµХЎЦЃХґХЎХ¶ ЦѓХёЦѓХёХ­ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХ«Х¶ Ц‡ Х©ХЎЦЂХґХЎЦЃХёЦ‚ХґХ¶ХҐЦЂХ«Х¶:",
    admin_logs_clear: "Х„ХЎЦ„ЦЂХҐХ¬ Х„ХЎХїХµХЎХ¶ХЁ",
    admin_logs_th_timestamp: "ФєХЎХґХЎХ¶ХЎХЇХ« Х¤ЦЂХёХ·Хґ",
    admin_logs_th_operator: "Х•ХєХҐЦЂХЎХїХёЦЂ",
    admin_logs_th_action: "ФіЦЂХЎХ¶ЦЃХѕХЎХ® ФіХёЦЂХ®ХёХІХёЦ‚Х©ХµХёЦ‚Х¶",
    admin_btn_install: "ХЏХҐХІХЎХ¤ЦЂХҐХ¬",
    acc_wardrobe_empty: "ХЃХҐЦЂ ХўХёЦ‚ХµЦЂХҐЦЂХ« ХєХЎХ°ХЎЦЂХЎХ¶ХЁ Х¤ХЎХїХЎЦЂХЇ Х§:",
    acc_discovery_empty: "ФіХ¶ХѕХЎХ® ЦѓХёЦЂХ±ХЎХ¶ХґХёЦ‚Х·Х¶ХҐЦЂ Х¤ХҐХјЦ‡ХЅ Х№ХЇХЎХ¶:",
    acc_orders_empty: "ХЉХЎХїХѕХҐЦЂХ¶ХҐЦЂ Х¤ХҐХјЦ‡ХЅ Х№ХЇХЎХ¶:",
    bg_media: "Х„ФµФґФ»Ф±",
    insta_likes: "Х°ХЎХѕХЎХ¶ХёЦ‚Хґ",
    insta_loc_1: "ФіЦЂХЎХЅ, Х–ЦЂХЎХ¶ХЅХ«ХЎ",
    insta_loc_2: "ФїХ«ХёХїХё, ХѓХЎХєХёХ¶Х«ХЎ",
    insta_loc_3: "Ф±ХґХЎХ¬Ц†Х«, Ф»ХїХЎХ¬Х«ХЎ",
    insta_loc_4: "Х“ХЎЦЂХ«Х¦, Х–ЦЂХЎХ¶ХЅХ«ХЎ",
    insta_loc_5: "ФјХёХ¶Х¤ХёХ¶, Х„ХҐХ® ФІЦЂХ«ХїХЎХ¶Х«ХЎ",
    insta_loc_6: "ХЌХЎХ¶ХїХёЦЂХ«Х¶Х«, ХЂХёЦ‚Х¶ХЎХЅХїХЎХ¶",
    insta_loc_7: "Х„Х«Х¬ХЎХ¶, Ф»ХїХЎХ¬Х«ХЎ",
    insta_loc_8: "ХЌХҐХ¶ ХЏЦЂХёХєХҐ, Х–ЦЂХЎХ¶ХЅХ«ХЎ",
    insta_caption_1: "Santal NВ°01-ХЁ ХєХЎХїХЇХҐЦЂХёЦ‚Хґ Х§ ХЎХѕХЅХїЦЂХЎХ¬Х«ХЎХЇХЎХ¶ ХЎХ¶ХЎХєХЎХїХ« Х°ХёЦ‚Хґ Х§Х¬ХҐХЈХЎХ¶ХїХёЦ‚Х©ХµХёЦ‚Х¶ХЁ: Ф№ХёЦЂХѕХЎХ® Х§ ХєХІХ¶Х±ХҐ ХЅХµХёЦ‚Х¶ХҐЦЂХёЦ‚Хґ: вњЁ",
    insta_caption_2: "ФѕХЎХІХЇХёХІ ХўХЎХ¬ХҐХ¶ХёЦ‚ Ц‡ ХґХҐХІХґ Ц„ХЎХ·ХґХ«ЦЂХҐ ХґХёЦ‚Х·ХЇХ« Х¶ХёЦ‚ЦЂХў ХєХЎЦЂ: Fleur de Cerisier-ХЁ ФїХ«ХёХїХёХ¶ Х§ ХЈХЎЦЂХ¶ХЎХ¶ХЁ: рџЊё",
    insta_caption_3: "ХЌХ«ЦЃХ«Х¬Х«ХЎХЇХЎХ¶ ХўХҐЦЂХЈХЎХґХёХїХ« Ц‡ ХЎХІХ« Х®ХёХѕХЎХµХ«Х¶ Ц…Х¤Х« ХЎЦЂЦ‡ХёХї ХєХЎХµХ©ХµХёЦ‚Х¶: Agrumes Frais-ХЁ Х±ХҐХјЦ„ХёЦ‚Хґ: рџЌ‹",
    insta_caption_4: "Х†ХѕХ«ЦЂХҐЦ„ Ц…Х¬Ц†ХЎХЇХїХёЦЂ ХєХёХҐХ¦Х«ХЎ: Х„ХҐЦЂ ХўХ¶ХёЦЂХёХ· Х°ХЎХµХїХ¶ХЎХЈХёЦЂХ®ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХ« Х°ХЎХѕХЎЦ„ХЎХ®ХёЦ‚Х¶Хќ ЦѓХЎХ©ХЎХ©ХѕХЎХ® ХЇХЎХµХёЦ‚Х¶ ХѕХёЦ‚Х·ХёХѕ: рџЋЃ",
    insta_caption_5: "Х„ХЎЦ„ХёЦ‚ЦЂ ХўХёЦ‚ХЅХЎХЇХЎХ¶ ХўХЎХІХЎХ¤ЦЂХ«Х№Х¶ХҐЦЂХќ Х°ХЎХѕХЎЦ„ХѕХЎХ® ХЎХ·Х­ХЎЦЂХ°Х« ХЇХЎХµХёЦ‚Х¶ ХЎХІХўХµХёЦ‚ЦЂХ¶ХҐЦЂХ«ЦЃ: ХЂХёЦ‚Хґ Х·Ц„ХҐХІХёЦ‚Х©ХµХёЦ‚Х¶: рџЊї",
    insta_caption_6: "Х€ХЅХЇХҐХЈХёЦ‚ХµХ¶ ХЎХґХўЦЂХЎХµХ« Ц‡ ХїХЎХєХЎХЇХЎХ® Х°ХЎХґХҐХґХёЦ‚Х¶Ц„Х¶ХҐЦЂХ« Х·Ц„ХҐХІ, Х©ХЎХѕХ·ХµХЎ Х®ХЎХ®ХЇХёЦЃ ХёХЅХЇХҐ ХЄХЎХґХ«Х¶: рџЊ…",
    insta_caption_7: "Х…ХёЦ‚ЦЂХЎЦ„ХЎХ¶Х№ХµХёЦ‚ЦЂ ЦЃХёХІ ХЇХҐХ¶Х¤ХЎХ¶Х« ХЇХёХґХєХёХ¦Х«ЦЃХ«ХЎ Х§, ХёЦЂХЁ Х¦ХЎЦЂХЈХЎХ¶ХёЦ‚Хґ Х§ ХґХЎХ·ХЇХ« ХѕЦЂХЎ ХґХ« Ц„ХЎХ¶Х« ХЄХЎХґХѕХЎ ХЁХ¶Х©ХЎЦЃЦ„ХёЦ‚Хґ: рџ’«",
    insta_caption_8: "ФїХЎХ¦Х¤ХёЦ‚ЦЂХ«Х№ Х©ХЎЦЂХґ VГ©tiver Blanc: ФїХЎХїХЎЦЂХµХЎХ¬ Х¤ХҐЦЂХ±ХЎХЇХѕХЎХ® ХЅХєХ«ХїХЎХЇ Х·ХЎХєХ«ХЇХ« Ц…Х¬Ц†ХЎХЇХїХёЦЂ Х°ХЎХґХЎЦЂХЄХҐЦ„ХЁ: рџЊЉ",
    insta_view_comments_1: "ФґХ«ХїХҐХ¬ ХўХёХ¬ХёЦЂ 12 ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ",
    insta_view_comments_2: "ФґХ«ХїХҐХ¬ ХўХёХ¬ХёЦЂ 8 ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ",
    insta_view_comments_3: "ФґХ«ХїХҐХ¬ ХўХёХ¬ХёЦЂ 15 ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ",
    insta_view_comments_4: "ФґХ«ХїХҐХ¬ ХўХёХ¬ХёЦЂ 9 ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ",
    insta_view_comments_5: "ФґХ«ХїХҐХ¬ ХўХёХ¬ХёЦЂ 11 ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ",
    insta_view_comments_6: "ФґХ«ХїХҐХ¬ ХўХёХ¬ХёЦЂ 14 ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ХҐЦЂХЁ",
    insta_date_1: "2 Х•Хђ Ф±ХЊФ±Х‹",
    insta_date_2: "3 Х•Хђ Ф±ХЊФ±Х‹",
    insta_date_3: "4 Х•Хђ Ф±ХЊФ±Х‹",
    insta_date_4: "5 Х•Хђ Ф±ХЊФ±Х‹",
    insta_date_5: "1 Х‡Ф±ФІФ±Ф№ Ф±ХЊФ±Х‹",
    insta_date_6: "1 Х‡Ф±ФІФ±Ф№ Ф±ХЊФ±Х‹",
    admin_nav_instagram: "Instagram ФїХЎХјХЎХѕХЎЦЂХ«Х№",
    admin_insta_title: "Instagram ФјЦЂХЎХ°ХёХЅХ« ФїХЎХјХЎХѕХЎЦЂХёЦ‚Хґ",
    admin_insta_desc: "Ф±ХѕХҐХ¬ХЎЦЃЦЂХҐЦ„ Х¶ХёЦЂ ХЈЦЂХЎХјХёЦ‚ХґХ¶ХҐЦЂ Х«Х¶ХЅХїХЎХЈЦЂХЎХґХ« Х¬ЦЂХЎХ°ХёХЅХёЦ‚Хґ, Х­ХґХўХЎХЈЦЂХҐЦ„ ХїХѕХµХЎХ¬Х¶ХҐЦЂХЁ ХЇХЎХґ Х»Х¶Х»ХҐЦ„ ХЈХёХµХёЦ‚Х©ХµХёЦ‚Х¶ ХёЦ‚Х¶ХҐЦЃХёХІ ХЈЦЂХЎХјХёЦ‚ХґХ¶ХҐЦЂХЁ:",
    admin_insta_add_post: "Ф±ХѕХҐХ¬ХЎЦЃХ¶ХҐХ¬ Х†ХёЦЂ ФіЦЂХЎХјХёЦ‚Хґ",
    admin_insta_location: "ХЋХЎХµЦЂ / ХЏХҐХІХЎХ¶Ц„ *",
    admin_insta_caption: "Х†ХЇХЎЦЂХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶ *",
    admin_insta_date: "Ф±ХґХЅХЎХ©Х«Хѕ *",
    admin_insta_images: "Х†ХЇХЎЦЂХ¶ХҐЦЂ (Х©ХёЦ‚ХµХ¬ХЎХїЦЂХѕХёЦ‚Хґ Х§ ХґХ« Ц„ХЎХ¶Х«ХЅХЁ) *",
    admin_insta_active_posts: "Ф±ХЇХїХ«Хѕ ФіЦЂХЎХјХёЦ‚ХґХ¶ХҐЦЂ",
    admin_th_thumbnail: "Х†ХЇХЎЦЂ",
    admin_th_location: "ХЋХЎХµЦЂ",
    admin_th_caption: "Х†ХЇХЎЦЂХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶",
    admin_th_date: "Ф±ХґХЅХЎХ©Х«Хѕ",
    admin_btn_add_post: "Ф±ХѕХҐХ¬ХЎЦЃХ¶ХҐХ¬ ФіЦЂХЎХјХёЦ‚Хґ",
    insta_feed_empty: "ФіЦЂХЎХјХёЦ‚ХґХ¶ХҐЦЂ Х¤ХҐХј Х№ХЇХЎХ¶: Ф±ХѕХҐХ¬ХЎЦЃЦЂХҐЦ„ ХЈЦЂХЎХјХёЦ‚ХґХ¶ХҐЦЂ WooCommerce ХЇХЎХјХЎХѕХЎЦЂХґХЎХ¶ ХѕХЎХ°ХЎХ¶ХЎХЇХ«ЦЃ:",
    modal_product_edit_title: "ФЅХґХўХЎХЈЦЂХҐХ¬ Ф±ХєЦЂХЎХ¶Ц„Х« ХЏХѕХµХЎХ¬Х¶ХҐЦЂХЁ",
    modal_lbl_name: "Ф±ХєЦЂХЎХ¶Ц„Х« Ф±Х¶ХёЦ‚Х¶ХЁ *",
    modal_lbl_brand: "Ф±ХєЦЂХЎХ¶Ц„ХЎХ¶Х«Х· *",
    modal_lbl_tagline: "ФїХЎЦЂХі Х†ХЇХЎЦЂХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶ *",
    modal_lbl_desc: "Х†ХЇХЎЦЂХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶ *",
    modal_lbl_ingredients: "ФІХЎХІХЎХ¤ЦЂХёЦ‚Х©ХµХёЦ‚Х¶",
    modal_lbl_image_url: "Х†ХЇХЎЦЂ (Image URL) *",
    modal_lbl_or_upload: "ФїФ±Х„ ХѕХҐЦЂХўХҐХјХ¶ХҐХ¬ Ц†ХЎХµХ¬Хќ",
    modal_lbl_family: "ФёХ¶ХїХЎХ¶Х«Ц„ *",
    modal_lbl_concentration: "ХЏХҐХЅХЎХЇ *",
    modal_lbl_gender: "ХЌХҐХј *",
    modal_lbl_stock: "ХЉХЎХ·ХЎЦЂХ« Х”ХЎХ¶ХЎХЇ *",
    modal_lbl_notes_top: "ХЋХҐЦЂХ«Х¶ Х†ХёХїХЎХ¶ХҐЦЂ *",
    modal_lbl_notes_heart: "ХЌЦЂХїХ« Х†ХёХїХЎХ¶ХҐЦЂ *",
    modal_lbl_notes_base: "ХЂХ«ХґХ¶ХЎХµХ«Х¶ Х†ХёХїХЎХ¶ХҐЦЂ *",
    modal_btn_cancel: "Х‰ХҐХІХЎЦЂХЇХҐХ¬",
    modal_btn_save: "ХЉХЎХ°ХєХЎХ¶ХҐХ¬",
    home_video_title: "ФІХЎЦЃХЎХјХ«ХЇ ФІХёЦ‚ХµЦЂХҐЦЂХ« Ф±ЦЂХѕХҐХЅХїХЁ",
    home_video_paragraph: "NOVA-Х« ХµХёЦ‚ЦЂХЎЦ„ХЎХ¶Х№ХµХёЦ‚ЦЂ ХЇХЎХ©Х«Х¬ХЁ ХЇХЎХїХЎЦЂХҐХ¬ХёЦ‚Х©ХµХЎХ¶ Ц‡ Х·Ц„ХҐХІХёЦ‚Х©ХµХЎХ¶ ХЎЦЂХїХЎЦЃХёХ¬ХёЦ‚ХґХ¶ Х§Ц‰ Х„ХҐХ¶Ц„ ХЁХ¶ХїЦЂХёЦ‚Хґ ХҐХ¶Ц„ ХЎХ·Х­ХЎЦЂХ°Х« Х¬ХЎХѕХЎХЈХёЦ‚ХµХ¶ ХёЦ‚ Х°ХЎХ¦ХѕХЎХЈХµХёЦ‚Хї ХўХЎХІХЎХ¤ЦЂХ«Х№Х¶ХҐЦЂХЁХќ ХЅХїХҐХІХ®ХҐХ¬ХёХѕ Х¦ХЎЦЂХЈХЎЦЃХёХІ Х¦ХЈХЎХµХЎХЇХЎХ¶ ХіХЎХ¶ХЎХєХЎЦЂХ°ХёЦЂХ¤ХёЦ‚Х©ХµХёЦ‚Х¶ Х±ХҐЦЂ ХґХЎХ·ХЇХ« ХѕЦЂХЎЦ‰ ФјХ«Х¶ХҐХ¬ХёХѕ Х§Х¬Х«ХїХЎЦЂ ХєХЎЦЂЦ†ХµХёЦ‚ХґХҐЦЂХ«ХЎХµХ« ХЎХјХЎХ»ХЎХїХЎЦЂХЁ ХЂХЎХµХЎХЅХїХЎХ¶ХёЦ‚ХґХќ NOVA-Х¶ ХЎХјХЎХ»ХЎЦЂХЇХёЦ‚Хґ Х§ ХёХ№ Х©ХҐ ХєХЎЦЂХ¦ХЎХєХҐХЅ ХўХёЦ‚ХµЦЂ, ХЎХµХ¬ ХЎХ¶ХґХёХјХЎХ¶ХЎХ¬Х« Х·Х¬ХҐХµЦ†, ХёЦЂХ¶ ХЁХ¶Х¤ХЈХ®ХёЦ‚Хґ Х§ Х±ХҐЦЂ ХЎХ¶Х°ХЎХїХЎХЇХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶ХЁ:",
    home_video_cta: "Ф»ХґХЎХ¶ХЎХ¬ Ф±ХѕХҐХ¬Х«Х¶"
  },
  ru: {
    nav_about: "Рћ РЅР°СЃ",
    about_hook_title: "РЎРѕР·РґР°С‚РµР»Рё РћР»СЊС„Р°РєС‚РѕСЂРЅРѕРіРѕ РСЃРєСѓСЃСЃС‚РІР°",
    about_hook_desc: "РњС‹ РѕС‚Р±РёСЂР°РµРј СЂРµРґРєРёРµ РЅРёС€РµРІС‹Рµ Р°СЂРѕРјР°С‚С‹ Рё С‡РёСЃС‚С‹Рµ РёРЅРіСЂРµРґРёРµРЅС‚С‹ РґР»СЏ СЃРѕРІСЂРµРјРµРЅРЅС‹С… СЌСЃС‚РµС‚РѕРІ. РќР°С€Р° РјРёСЃСЃРёСЏ вЂ” РїСЂРµРІСЂР°С‚РёС‚СЊ РІР°С€ С„РёСЂРјРµРЅРЅС‹Р№ Р°СЂРѕРјР°С‚ РёР· РІС‚РѕСЂРѕСЃС‚РµРїРµРЅРЅРѕРіРѕ РІ С€РµРґРµРІСЂ.",
    about_story_title: "РќР°С€Рµ РќР°С‡Р°Р»Рѕ",
    about_story_desc: "NOVA СЂРѕРґРёР»Р°СЃСЊ РёР· РѕРґРЅРѕР№ СЃС‚СЂР°СЃС‚Рё: РЅР°С…РѕРґРёС‚СЊ Р±РµСЃРєРѕРјРїСЂРѕРјРёСЃСЃРЅС‹Рµ, СЂРµРґРєРёРµ Рё Р·Р°С…РІР°С‚С‹РІР°СЋС‰РёРµ РґСѓС… Р°СЂРѕРјР°С‚С‹ РЅР° СЂС‹РЅРєРµ, РїРµСЂРµРїРѕР»РЅРµРЅРЅРѕРј РјР°СЃСЃ-РјР°СЂРєРµС‚РѕРј. РўРѕ, С‡С‚Рѕ РЅР°С‡РёРЅР°Р»РѕСЃСЊ РєР°Рє С‡Р°СЃС‚РЅР°СЏ РєРѕР»Р»РµРєС†РёСЏ Р»СѓС‡С€РёС… РјРёСЂРѕРІС‹С… С€РµРґРµРІСЂРѕРІ, РїСЂРµРІСЂР°С‚РёР»РѕСЃСЊ РІ РїСЂРµРјРёР°Р»СЊРЅС‹Р№ Р±СѓС‚РёРє РґР»СЏ С‚РµС…, РєС‚Рѕ С†РµРЅРёС‚ РёСЃС‚РёРЅРЅРѕРµ РёСЃРєСѓСЃСЃС‚РІРѕ РЅРёС€РµРІРѕР№ РїР°СЂС„СЋРјРµСЂРёРё.",
    about_eyebrow: "РРЎРљРЈРЎРЎРўР’Рћ Р”РРЎРўРР›Р›РЇР¦РР вЂ” РћРЎРќ. Р’ 2026",
    about_lead: "РђСЂС…РёС‚РµРєС‚СѓСЂРЅРѕРµ РёСЃСЃР»РµРґРѕРІР°РЅРёРµ С‡РёСЃС‚С‹С… Р±РѕС‚Р°РЅРёС‡РµСЃРєРёС… Р°СЂРѕРјР°С‚РѕРІ, СЃРѕР·РґР°РЅРЅС‹С… РІСЂСѓС‡РЅСѓСЋ РґР»СЏ С†РµРЅРёС‚РµР»РµР№ СЃРѕРІСЂРµРјРµРЅРЅРѕР№ СЌСЃС‚РµС‚РёРєРё.",
    stat_botanical: "РџРѕРґР»РёРЅРЅРѕСЃС‚СЊ Рё РљР°С‡РµСЃС‚РІРѕ",
    stat_batch: "Р­Р»РёС‚РЅС‹Рµ РњРёСЂРѕРІС‹Рµ Р‘СЂРµРЅРґС‹",
    stat_batch_number: "Р­Р»РёС‚РЅС‹Рµ",
    stat_synthetic: "РќРѕР»СЊ Р”РµС€РµРІС‹С… РђРЅР°Р»РѕРіРѕРІ",
    contact_us: "РЎРІСЏР·Р°С‚СЊСЃСЏ СЃ РЅР°РјРё",
    about_pillars_title: "РћСЃРЅРѕРІС‹ Р”РѕРІРµСЂРёСЏ",
    about_guarantee_title: "100% Р“Р°СЂР°РЅС‚РёСЏ РџРѕРґР»РёРЅРЅРѕСЃС‚Рё",
    about_guarantee_desc: "РњС‹ Р·Р°РєСѓРїР°РµРј РІСЃРµ С‚РѕРІР°СЂС‹ РЅР°РїСЂСЏРјСѓСЋ Сѓ РѕС„РёС†РёР°Р»СЊРЅС‹С… РґРёСЃС‚СЂРёР±СЊСЋС‚РѕСЂРѕРІ Рё РјРёСЂРѕРІС‹С… РїР°СЂС„СЋРјРµСЂРЅС‹С… РґРѕРјРѕРІ. Р–РµР»Р°РЅРёРµ РєСѓРїРёС‚СЊ РѕСЂРёРіРёРЅР°Р»СЊРЅС‹Рµ РґСѓС…Рё РІ РђСЂРјРµРЅРёРё Сѓ РЅР°СЃ вЂ” СЌС‚Рѕ РіР°СЂР°РЅС‚РёСЏ С‡РёСЃС‚РµР№С€РµРіРѕ РєР°С‡РµСЃС‚РІР°: РЅРёРєР°РєРѕРіРѕ СЃРµСЂРѕРіРѕ РёРјРїРѕСЂС‚Р° Рё РЅРёРєР°РєРёС… РєРѕРјРїСЂРѕРјРёСЃСЃРѕРІ.",
    about_scent_match_title: "РћР±РµС‰Р°РЅРёРµ РРґРµР°Р»СЊРЅРѕРіРѕ РђСЂРѕРјР°С‚Р°",
    about_scent_match_desc: "Р’С‹Р±СЂР°С‚СЊ РЅРёС€РµРІС‹Рµ Р°СЂРѕРјР°С‚С‹ РІ РёРЅС‚РµСЂРЅРµС‚-РјР°РіР°Р·РёРЅРµ РїР°СЂС„СЋРјРµСЂРёРё РІ Р•СЂРµРІР°РЅРµ Р±С‹РІР°РµС‚ РЅРµРїСЂРѕСЃС‚Рѕ. РџРѕСЌС‚РѕРјСѓ РјС‹ РїСЂРµРґР»Р°РіР°РµРј РґРµС‚Р°Р»СЊРЅРѕРµ РѕРїРёСЃР°РЅРёРµ РѕР»СЊС„Р°РєС‚РѕСЂРЅС‹С… РЅРѕС‚ Рё Р»РµРіРєРёР№ РІРѕР·РІСЂР°С‚ РЅРµСЂР°СЃРїРµС‡Р°С‚Р°РЅРЅС‹С… С„Р»Р°РєРѕРЅРѕРІ РґР»СЏ РІР°С€РµРіРѕ РїРѕР»РЅРѕРіРѕ СЃРїРѕРєРѕР№СЃС‚РІРёСЏ.",
    about_storage_title: "Р‘РµР·СѓРїСЂРµС‡РЅРѕРµ РҐСЂР°РЅРµРЅРёРµ",
    about_storage_desc: "РљР°Р¶РґС‹Р№ С„Р»Р°РєРѕРЅ С…СЂР°РЅРёС‚СЃСЏ РІ РЅР°С€РµР№ СЃРїРµС†РёР°Р»РёР·РёСЂРѕРІР°РЅРЅРѕР№ РєР»РёРјР°С‚РёС‡РµСЃРєРѕР№ РіР°Р»РµСЂРµРµ СЃ РєРѕРЅС‚СЂРѕР»РµРј СЃРІРµС‚Р° Рё С‚РµРјРїРµСЂР°С‚СѓСЂС‹. Р­С‚Рѕ Р·Р°С‰РёС‰Р°РµС‚ СЌС„РёСЂРЅС‹Рµ РјР°СЃР»Р° РѕС‚ СЂР°Р·СЂСѓС€РµРЅРёСЏ, СЃРѕС…СЂР°РЅСЏСЏ СЌР»РёС‚РЅС‹Р№ РїР°СЂС„СЋРј РІ РµРіРѕ РїРµСЂРІРѕР·РґР°РЅРЅРѕРј, РіР»СѓР±РѕРєРѕРј Р·РІСѓС‡Р°РЅРёРё.",
    about_cta_title: "Р“РѕС‚РѕРІС‹ РЅР°Р№С‚Рё СЃРІРѕР№ РёРґРµР°Р»СЊРЅС‹Р№ Р°СЂРѕРјР°С‚?",
    about_cta_btn: "РСЃСЃР»РµРґРѕРІР°С‚СЊ РљРѕР»Р»РµРєС†РёСЋ",
    nav_home: "Р“Р»Р°РІРЅР°СЏ",
    nav_products: "РўРѕРІР°СЂС‹",
    nav_brands: "Р‘СЂРµРЅРґС‹",
    nav_men: "РњСѓР¶С‡РёРЅР°Рј",
    nav_women: "Р–РµРЅС‰РёРЅР°Рј",
    nav_new: "РќРѕРІРёРЅРєРё",
    nav_bestsellers: "Р‘РµСЃС‚СЃРµР»Р»РµСЂС‹",
    badge_bestseller: "Р‘Р•РЎРўРЎР•Р›Р›Р•Р ",
    badge_new_product: "РќРћР’РРќРљРђ",
    nav_gifts: "РџРѕРґР°СЂРѕС‡РЅС‹Рµ РЅР°Р±РѕСЂС‹",
    cat_men: "РњСѓР¶С‡РёРЅР°Рј",
    cat_women: "Р–РµРЅС‰РёРЅР°Рј",
    cat_gifts: "РџРѕРґР°СЂРєРё",
    cat_bestsellers: "Р‘РµСЃС‚СЃРµР»Р»РµСЂС‹",
    cat_new: "РќРѕРІРёРЅРєРё",
    cat_home: "Р”Р»СЏ Р”РѕРјР°",
    nav_admin: "WooCommerce РђРґРјРёРЅ",
    nav_order_history: "РСЃС‚РѕСЂРёСЏ Р·Р°РєР°Р·РѕРІ",
    nav_logout: "Р’С‹Р№С‚Рё",
    auth_signin_btn: "Р’РѕР№С‚Рё",
    auth_signup_title: "РЎРѕР·РґР°С‚СЊ Р°РєРєР°СѓРЅС‚",
    auth_email_label: "Р­Р». РїРѕС‡С‚Р°",
    auth_password_label: "РџР°СЂРѕР»СЊ",
    auth_first_name: "РРјСЏ",
    auth_last_name: "Р¤Р°РјРёР»РёСЏ",
    auth_confirm_password: "РџРѕРґС‚РІРµСЂРґРёС‚Рµ РїР°СЂРѕР»СЊ",
    hello_user: "РџСЂРёРІРµС‚, Р­РјРјР°",
    mega_woody: "Р”СЂРµРІРµСЃРЅС‹Рµ",
    mega_floral: "Р¦РІРµС‚РѕС‡РЅС‹Рµ",
    mega_citrus: "Р¦РёС‚СЂСѓСЃРѕРІС‹Рµ",
    mega_amber: "РђРјР±СЂРѕРІС‹Рµ",
    search_placeholder: "РџРѕРёСЃРє Р°СЂРѕРјР°С‚РѕРІ...",
    newsletter_placeholder: "Р’Р°С€ СЌР». Р°РґСЂРµСЃ",
    maison_de_parfum: "Maison de Parfum",
    hero_title: "РўРІРѕР№ Р°СЂРѕРјР°С‚ Р¶РґРµС‚ С‚РµР±СЏ",
    hero_desc: "РџР°СЂС„СЋРјРµСЂРёСЏ, РїРѕРґР°СЂРєРё, РєСЂР°СЃРёРІР°СЏ СѓРїР°РєРѕРІРєР°",
    explore_fragrances: "РСЃСЃР»РµРґРѕРІР°С‚СЊ РђСЂРѕРјР°С‚С‹",
    view_admin_demo: "Р”РµРјРѕ РЈРїСЂР°РІР»РµРЅРёРµ",
    promise_title_1: "Р­РєСЃРєР»СЋР·РёРІРЅС‹Рµ РљРѕР»Р»РµРєС†РёРё РђСЂРѕРјР°С‚РѕРІ",
    promise_desc_1: "РўС‰Р°С‚РµР»СЊРЅРѕ РѕС‚РѕР±СЂР°РЅРЅР°СЏ РєРѕР»Р»РµРєС†РёСЏ СЂРµРґРєРёС… Рё РѕСЂРёРіРёРЅР°Р»СЊРЅС‹С… РґСѓС…РѕРІ РІ РђСЂРјРµРЅРёРё РѕС‚ Р»СѓС‡С€РёС… РјРёСЂРѕРІС‹С… Р±СЂРµРЅРґРѕРІ.",
    promise_title_2: "РЎС‚РѕР№РєРёРµ Рё РЁР»РµР№С„РѕРІС‹Рµ Р”СѓС…Рё",
    promise_desc_2: "Р­Р»РёС‚РЅР°СЏ Р±СЂРµРЅРґРѕРІР°СЏ РїР°СЂС„СЋРјРµСЂРёСЏ СЃ РІС‹СЃРѕРєРѕР№ РєРѕРЅС†РµРЅС‚СЂР°С†РёРµР№ РјР°СЃРµР», РіР°СЂР°РЅС‚РёСЂСѓСЋС‰Р°СЏ РёСЃРєР»СЋС‡РёС‚РµР»СЊРЅСѓСЋ СЃС‚РѕР№РєРѕСЃС‚СЊ С€Р»РµР№С„РѕРІС‹С… РґСѓС…РѕРІ.",
    promise_title_3: "Р­РєРѕР»РѕРіРёС‡РЅР°СЏ РЈРїР°РєРѕРІРєР°",
    promise_desc_3: "Р­РєРѕР»РѕРіРёС‡РЅР°СЏ СѓРїР°РєРѕРІРєР° РґСѓС…РѕРІ: 100% Р±РёРѕСЂР°Р·Р»Р°РіР°РµРјС‹Рµ РєРѕСЂРѕР±РєРё FSC, РЅР°С‚СѓСЂР°Р»СЊРЅС‹Рµ С…Р»РѕРїРєРѕРІС‹Рµ СЌС‚РёРєРµС‚РєРё Рё РїРµСЂРµСЂР°Р±Р°С‚С‹РІР°РµРјС‹Рµ С„Р»Р°РєРѕРЅС‹.",
    collections_title: "РџРђР Р¤Р®РњР•Р РРЇ",
    gender_unisex: "РЈРќРРЎР•РљРЎ",
    gender_men: "РњРЈР–РЎРљРћР™",
    gender_women: "Р–Р•РќРЎРљРР™",
    featured_subtitle: "РР·Р±СЂР°РЅРЅС‹Рµ РўРІРѕСЂРµРЅРёСЏ",
    featured_title: "РљРѕР»Р»РµРєС†РёСЏ РђСЂРѕРјР°С‚РѕРІ",
    view_full_collection_btn: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ РІСЃСЋ РєРѕР»Р»РµРєС†РёСЋ",
    shop_title: "РљРѕР»Р»РµРєС†РёСЏ",
    shop_subtitle: "РЈС‚РѕС‡РЅРёС‚Рµ РїРѕРёСЃРє РїРѕ РїСЂРѕС„РёР»СЋ РЅРѕС‚ РёР»Рё СЃРµРјРµР№СЃС‚РІСѓ Р°СЂРѕРјР°С‚РѕРІ.",
    filter_all: "Р’СЃРµ",
    filter_woody: "Р”СЂРµРІРµСЃРЅС‹Рµ",
    filter_floral: "Р¦РІРµС‚РѕС‡РЅС‹Рµ",
    filter_citrus: "Р¦РёС‚СЂСѓСЃРѕРІС‹Рµ",
    filter_amber: "РђРјР±СЂРѕРІС‹Рµ",
    sort_default: "РЎРѕСЂС‚РёСЂРѕРІРєР° РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ",
    sort_price_low_high: "Р¦РµРЅР°: РѕС‚ РЅРёР·РєРѕР№ Рє РІС‹СЃРѕРєРѕР№",
    sort_price_high_low: "Р¦РµРЅР°: РѕС‚ РІС‹СЃРѕРєРѕР№ Рє РЅРёР·РєРѕР№",
    sort_rating: "РЎСЂРµРґРЅРёР№ СЂРµР№С‚РёРЅРі",
    cart_title: "РљРѕСЂР·РёРЅР° РїРѕРєСѓРїРѕРє",
    cart_subtotal_label: "РџРѕРґС‹С‚РѕРі",
    cart_shipping_desc: "Р”РѕСЃС‚Р°РІРєР° Рё РЅР°Р»РѕРіРё СЂР°СЃСЃС‡РёС‚С‹РІР°СЋС‚СЃСЏ РїСЂРё РѕС„РѕСЂРјР»РµРЅРёРё Р·Р°РєР°Р·Р°.",
    cart_checkout_btn: "РџРµСЂРµР№С‚Рё Рє РѕС„РѕСЂРјР»РµРЅРёСЋ",
    cart_empty_msg: "Р’Р°С€Р° РєРѕСЂР·РёРЅР° РІ РґР°РЅРЅС‹Р№ РјРѕРјРµРЅС‚ РїСѓСЃС‚Р°.",
    go_to_shop_btn: "Р’ РјР°РіР°Р·РёРЅ",
    wishlist_title: "РњРѕР№ РІРёС€Р»РёСЃС‚",
    wishlist_add_all_btn: "Р”РѕР±Р°РІРёС‚СЊ РІСЃРµ РІ РєРѕСЂР·РёРЅСѓ",
    wishlist_empty_msg: "Р’Р°С€ СЃРїРёСЃРѕРє Р¶РµР»Р°РЅРёР№ РїСѓСЃС‚.",
    browse_shop_btn: "Р’ РєР°С‚Р°Р»РѕРі",
    card_explore: "РџРѕРґСЂРѕР±РЅРµРµ",
    card_add_to_cart: "Р’ РєРѕСЂР·РёРЅСѓ",
    card_sold_out: "РџСЂРѕРґР°РЅРѕ",
    reviews_label: "РѕС‚Р·С‹РІРѕРІ",
    in_stock_label: "Р’ РЅР°Р»РёС‡РёРё",
    out_of_stock_label: "РќРµС‚ РІ РЅР°Р»РёС‡РёРё",
    modal_fragrance_pyramid: "РџРёСЂР°РјРёРґР° РђСЂРѕРјР°С‚Р°",
    modal_top: "Р’РµСЂС…РЅРёРµ РЅРѕС‚С‹:",
    modal_heart: "РќРѕС‚С‹ СЃРµСЂРґС†Р°:",
    modal_base: "Р‘Р°Р·РѕРІС‹Рµ РЅРѕС‚С‹:",
    modal_select_size: "Р’С‹Р±РµСЂРёС‚Рµ РѕР±СЉРµРј",
    modal_view_ingredients: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ СЃРѕСЃС‚Р°РІ",
    modal_add_to_cart: "Р”РѕР±Р°РІРёС‚СЊ РІ РєРѕСЂР·РёРЅСѓ",
    checkout_billing_title: "Р”РµС‚Р°Р»Рё РѕРїР»Р°С‚С‹",
    checkout_first_name: "РРјСЏ *",
    checkout_last_name: "Р¤Р°РјРёР»РёСЏ *",
    checkout_email: "Р­Р»РµРєС‚СЂРѕРЅРЅР°СЏ РїРѕС‡С‚Р° *",
    checkout_street: "РђРґСЂРµСЃ *",
    checkout_city: "Р“РѕСЂРѕРґ *",
    checkout_zip: "РџРѕС‡С‚РѕРІС‹Р№ РёРЅРґРµРєСЃ *",
    checkout_shipping: "Р”РѕСЃС‚Р°РІРєР° *",
    checkout_payment_title: "РћРїР»Р°С‚Р°",
    checkout_payment_desc: "Р”РµРјРѕ-РїР»Р°С‚РµР¶: СЂРµР¶РёРј СЃРёРјСѓР»СЏС‚РѕСЂР°. Р РµР°Р»СЊРЅС‹Рµ РєР°СЂС‚С‹ РЅРµ Р±СѓРґСѓС‚ РёСЃРїРѕР»СЊР·РѕРІР°С‚СЊСЃСЏ.",
    checkout_place_order: "Р Р°Р·РјРµСЃС‚РёС‚СЊ Р·Р°РєР°Р·",
    checkout_your_order: "Р’Р°С€ Р—Р°РєР°Р·",
    checkout_subtotal: "РџРѕРґС‹С‚РѕРі:",
    checkout_shipping_row: "Р”РѕСЃС‚Р°РІРєР°:",
    checkout_total: "РС‚РѕРіРѕ:",
    order_success_title: "Р—Р°РєР°Р· СѓСЃРїРµС€РЅРѕ РѕС„РѕСЂРјР»РµРЅ",
    order_success_desc: "РЎРїР°СЃРёР±Рѕ Р·Р° РІС‹Р±РѕСЂ NOVA. РќРѕРјРµСЂ РІР°С€РµРіРѕ Р·Р°РєР°Р·Р° <strong>#REF</strong>. РњС‹ РѕС‚РїСЂР°РІРёР»Рё РїРѕРґС‚РІРµСЂР¶РґРµРЅРёРµ РЅР° Р°РґСЂРµСЃ EMAIL.",
    order_details: "Р”РµС‚Р°Р»Рё Р·Р°РєР°Р·Р°",
    order_date: "Р”Р°С‚Р°:",
    order_ship_to: "Р”РѕСЃС‚Р°РІРєР° РІ:",
    order_total_paid: "РС‚РѕРіРѕ РѕРїР»Р°С‡РµРЅРѕ:",
    continue_shopping_btn: "РџСЂРѕРґРѕР»Р¶РёС‚СЊ РїРѕРєСѓРїРєРё",
    footer_about: "РђСЂС…РёС‚РµРєС‚СѓСЂРЅРѕРµ РёСЃСЃР»РµРґРѕРІР°РЅРёРµ С‡РёСЃС‚С‹С… СЂР°СЃС‚РёС‚РµР»СЊРЅС‹С… Р°СЂРѕРјР°С‚РѕРІ, СЃРѕР·РґР°РЅРЅРѕРµ РґР»СЏ С†РµРЅРёС‚РµР»РµР№ СЃРѕРІСЂРµРјРµРЅРЅРѕР№ СЌСЃС‚РµС‚РёРєРё.",
    footer_nav_title: "РќР°РІРёРіР°С†РёСЏ",
    footer_shop_link: "РљР°С‚Р°Р»РѕРі",
    footer_families_title: "РЎРµРјРµР№СЃС‚РІР° РђСЂРѕРјР°С‚РѕРІ",
    footer_pages_title: "РЎС‚СЂР°РЅРёС†С‹",
    footer_follow_title: "РџРѕРґРїРёСЃС‹РІР°Р№С‚РµСЃСЊ",
    footer_newsletter_title: "РџРѕРґРїРёСЃРєР°",
    footer_newsletter_desc: "РџРѕРґРїРёС€РёС‚РµСЃСЊ, С‡С‚РѕР±С‹ РїРѕР»СѓС‡Р°С‚СЊ СѓРІРµРґРѕРјР»РµРЅРёСЏ Рѕ РЅРѕРІС‹С… СЃРµР·РѕРЅРЅС‹С… РІС‹РїСѓСЃРєР°С….",
    footer_rights: "&copy; 2026 NOVA Inc. Р’СЃРµ РїСЂР°РІР° Р·Р°С‰РёС‰РµРЅС‹.",
    footer_tagline: "Р­Р›Р•Р“РђРќРўРќРћРЎРўР¬ &bull; РњРРќРРњРђР›РР—Рњ &bull; Р§РРЎРўРћРўРђ",
    home_reviews_title: "РћС‚Р·С‹РІС‹ РљР»РёРµРЅС‚РѕРІ",
    home_instagram_title: "РЎР»РµРґРёС‚Рµ Р·Р° РЅР°РјРё РІ Instagram",
    home_instagram_shortcode_title: "Р›РµРЅС‚Р° Instagram (РЁРѕСЂС‚РєРѕРґ)",
    scroll_text: "РџР РћРљР РЈРўРљРђ &rarr;",
    brand_nova: "NOVA",
    brand_le_labo: "Le Labo",
    brand_byredo: "Byredo",
    brand_diptyque: "Diptyque",
    brand_aesop: "Aesop",
    brand_chanel: "Chanel",
    brand_dior: "Dior",
    brand_giorgio_armani: "Giorgio Armani",
    brand_yves_saint_laurent: "Yves Saint Laurent",
    brand_gucci: "Gucci",
    brand_versace: "Versace",
    brand_calvin_klein: "Calvin Klein",
    brand_dolce_gabbana: "Dolce & Gabbana",
    brand_tom_ford: "Tom Ford",
    brand_hermes: "HermГЁs",
    brand_burberry: "Burberry",
    brand_lancome: "LancГґme",
    brand_paco_rabanne: "Paco Rabanne",
    brand_jean_paul_gaultier: "Jean Paul Gaultier",
    brand_hugo_boss: "Hugo Boss",
    brand_creed: "Creed",
    brand_jo_malone: "Jo Malone London",
    brand_maison_francis_kurkdjian: "Maison Francis Kurkdjian",
    pp_faq_title: "Р§Р°СЃС‚Рѕ Р·Р°РґР°РІР°РµРјС‹Рµ РІРѕРїСЂРѕСЃС‹",
    pp_faq_q1: "Р§С‚Рѕ РґРµР»Р°РµС‚ СЌС‚РѕС‚ Р°СЂРѕРјР°С‚ СѓРЅРёРєР°Р»СЊРЅС‹Рј?",
    pp_faq_a1: "РќР°С€Рё Р°СЂРѕРјР°С‚С‹ СЃРѕСЃС‚РѕСЏС‚ РёР· СЂРµРґС‡Р°Р№С€РёС… СЂР°СЃС‚РёС‚РµР»СЊРЅС‹С… РєРѕРјРїРѕРЅРµРЅС‚РѕРІ, РґРёСЃС‚РёР»Р»РёСЂРѕРІР°РЅРЅС‹С… РЅРµР±РѕР»СЊС€РёРјРё РїР°СЂС‚РёСЏРјРё РґР»СЏ РёСЃРєР»СЋС‡РёС‚РµР»СЊРЅРѕР№ С‡РёСЃС‚РѕС‚С‹ Рё СЃС‚РѕР№РєРѕСЃС‚Рё.",
    pp_faq_q2: "РЇРІР»СЏРµС‚СЃСЏ Р»Рё СЌС‚РѕС‚ РїСЂРѕРґСѓРєС‚ РІРµРіР°РЅСЃРєРёРј Рё Р±РµР· Р¶РµСЃС‚РѕРєРѕСЃС‚Рё?",
    pp_faq_a2: "Р”Р°, РІСЃРµ РЅР°С€Рё С‚РІРѕСЂРµРЅРёСЏ РЅР° 100% РІРµРіР°РЅСЃРєРёРµ Рё РЅРёРєРѕРіРґР° РЅРµ С‚РµСЃС‚РёСЂРѕРІР°Р»РёСЃСЊ РЅР° Р¶РёРІРѕС‚РЅС‹С…. РњС‹ РІРµСЂРёРј РІ СѓСЃС‚РѕР№С‡РёРІСѓСЋ, СЌС‚РёС‡РЅСѓСЋ СЂРѕСЃРєРѕС€СЊ.",
    pp_faq_q3: "РљР°Рє РґРѕР»РіРѕ РґРµСЂР¶РёС‚СЃСЏ Р°СЂРѕРјР°С‚?",
    pp_faq_a3: "РљР°Рє Eau de Parfum, РѕРЅ РґРµСЂР¶РёС‚СЃСЏ РѕС‚ 8 РґРѕ 12 С‡Р°СЃРѕРІ РІ Р·Р°РІРёСЃРёРјРѕСЃС‚Рё РѕС‚ С…РёРјРёРё РІР°С€РµР№ РєРѕР¶Рё.",
    pp_faq_q4: "РљР°РєРѕРІР° РІР°С€Р° РїРѕР»РёС‚РёРєР° РІРѕР·РІСЂР°С‚Р°?",
    pp_faq_a4: "РњС‹ РїСЂРёРЅРёРјР°РµРј РІРѕР·РІСЂР°С‚С‹ РІ С‚РµС‡РµРЅРёРµ 30 РґРЅРµР№ РґР»СЏ РЅРµРёСЃРїРѕР»СЊР·РѕРІР°РЅРЅС‹С… РїСЂРѕРґСѓРєС‚РѕРІ РІ РѕСЂРёРіРёРЅР°Р»СЊРЅРѕР№ СѓРїР°РєРѕРІРєРµ.",
    admin_title: "WooCommerce РђРґРјРёРЅ",
    admin_total_revenue: "РћР±С‰РёР№ Р”РѕС…РѕРґ",
    admin_orders_placed: "РћС„РѕСЂРјР»РµРЅРЅС‹Рµ Р—Р°РєР°Р·С‹",
    admin_avg_order_value: "РЎСЂРµРґРЅРёР№ Р§РµРє",
    admin_total_stock: "РћР±С‰РёР№ Р—Р°РїР°СЃ",
    admin_orders_list: "РЎРїРёСЃРѕРє Р—Р°РєР°Р·РѕРІ WooCommerce",
    admin_th_order_id: "ID Р—Р°РєР°Р·Р°",
    admin_th_date: "Р”Р°С‚Р°",
    admin_th_customer: "РљР»РёРµРЅС‚",
    admin_th_items: "РўРѕРІР°СЂС‹",
    admin_th_total: "РС‚РѕРіРѕ",
    admin_th_status: "РЎС‚Р°С‚СѓСЃ",
    admin_th_actions: "Р”РµР№СЃС‚РІРёСЏ",
    admin_inventory_title: "WooCommerce Р—Р°РїР°СЃС‹ Рё Р¦РµРЅС‹",
    admin_inventory_desc: "РР·РјРµРЅРёС‚Рµ С†РµРЅС‹ Рё РєРѕР»РёС‡РµСЃС‚РІРѕ. РќР°Р¶РјРёС‚Рµ В«РћР±РЅРѕРІРёС‚СЊВ», С‡С‚РѕР±С‹ РїСЂРёРјРµРЅРёС‚СЊ РЅРµРјРµРґР»РµРЅРЅРѕ.",
    admin_th_image: "РР·РѕР±СЂР°Р¶РµРЅРёРµ",
    admin_th_fragrance: "РђСЂРѕРјР°С‚",
    admin_th_price: "Р¦РµРЅР° (100ml)",
    admin_th_stock_qty: "РљРѕР»-РІРѕ РЅР° СЃРєР»Р°РґРµ",
    admin_th_availability: "РќР°Р»РёС‡РёРµ",
    admin_btn_update: "РћР±РЅРѕРІРёС‚СЊ",
    review_1_text: "\"РђР±СЃРѕР»СЋС‚РЅС‹Р№ С€РµРґРµРІСЂ СЃРѕРІСЂРµРјРµРЅРЅРѕР№ РїР°СЂС„СЋРјРµСЂРёРё. РќРѕС‚Р° СЃР°РЅРґР°Р»Р° С‚Р°РєР°СЏ СЃР»РёРІРѕС‡РЅР°СЏ Рё СЌР»РµРіР°РЅС‚РЅР°СЏ, РјРµРЅСЏ РїРѕСЃС‚РѕСЏРЅРЅРѕ РѕСЃС‚Р°РЅР°РІР»РёРІР°СЋС‚ Рё СЃРїСЂР°С€РёРІР°СЋС‚, С‡С‚Рѕ РЅР° РјРЅРµ РЅР°РґРµС‚Рѕ.\"",
    review_1_author: "Р•Р»РµРЅР° Р’.",
    review_2_text: "\"РњРёРЅРёРјР°Р»РёСЃС‚РёС‡РµСЃРєРѕРµ СЃРѕРІРµСЂС€РµРЅСЃС‚РІРѕ. РЈРїР°РєРѕРІРєР° РІРµР»РёРєРѕР»РµРїРЅР°, РЅРѕ СЃР°Рј Р°СЂРѕРјР°С‚ СЃР»РѕР¶РµРЅ, РєСЂР°СЃРёРІРѕ СЂР°СЃРєСЂС‹РІР°СЏСЃСЊ РІ С‚РµС‡РµРЅРёРµ РґРЅСЏ РЅР° РјРѕРµР№ РєРѕР¶Рµ.\"",
    review_2_author: "РњР°СЂРєСѓСЃ Рў.",
    review_3_text: "\"РЇ РЅР°РєРѕРЅРµС† РЅР°С€Р»Р° СЃРІРѕР№ Р°СЂРѕРјР°С‚. РћРЅ С‚РѕРЅРєРёР№, РЅРѕ РЅРµРІРµСЂРѕСЏС‚РЅРѕ СЃС‚РѕР№РєРёР№. Р‘РѕС‚Р°РЅРёС‡РµСЃРєР°СЏ С‡РёСЃС‚РѕС‚Р° РґРµР№СЃС‚РІРёС‚РµР»СЊРЅРѕ СЃРёСЏРµС‚ РІ РєР°Р¶РґРѕРј СЂР°СЃРїС‹Р»РµРЅРёРё.\"",
    review_3_author: "РЎР°СЂР° Р›.",
    review_4_text: "\"РћР±СЃР»СѓР¶РёРІР°РЅРёРµ РєР»РёРµРЅС‚РѕРІ Рё СЂР°СЃРїР°РєРѕРІРєР° Р±С‹Р»Рё С‚Р°РєРёРјРё Р¶Рµ СЂРѕСЃРєРѕС€РЅС‹РјРё, РєР°Рє Рё СЃР°Рј РїР°СЂС„СЋРј. РћС‡РµРЅСЊ СЂРµРєРѕРјРµРЅРґСѓСЋ Р»СЋР±РѕРјСѓ Р»СЋР±РёС‚РµР»СЋ РЅРёС€РµРІРѕР№ РїР°СЂС„СЋРјРµСЂРёРё.\"",
    review_4_author: "Р”Р°РІРёРґ Рљ.",
    review_5_text: "\"РћС‰СѓС‰РµРЅРёРµ, Р±СѓРґС‚Рѕ РЅРѕСЃРёС€СЊ РёРЅРґРёРІРёРґСѓР°Р»СЊРЅРѕРµ РїСЂРѕРёР·РІРµРґРµРЅРёРµ РёСЃРєСѓСЃСЃС‚РІР°. РЁР»РµР№С„ РєР°Рє СЂР°Р· С‚Рѕ, С‡С‚Рѕ РЅСѓР¶РЅРѕ вЂ” Р·Р°РјРµС‚РЅС‹Р№, РЅРѕ РЅРёРєРѕРіРґР° РЅРµ РїРѕРґР°РІР»СЏСЋС‰РёР№.\"",
    review_5_author: "РњРёСЏ Р .",
    review_6_text: "\"РџРѕРёСЃС‚РёРЅРµ СѓРЅРёРєР°Р»СЊРЅС‹Р№ РѕР»СЊС„Р°РєС‚РѕСЂРЅС‹Р№ РѕРїС‹С‚. Р‘Р°Р·Р° РїСѓРґСЂРѕРІР°СЏ Рё СѓС‚РѕРЅС‡РµРЅРЅР°СЏ. РЇ РЅРµ РјРѕРіСѓ РґРѕР¶РґР°С‚СЊСЃСЏ, С‡С‚РѕР±С‹ РїРѕРїСЂРѕР±РѕРІР°С‚СЊ РѕСЃС‚Р°Р»СЊРЅСѓСЋ С‡Р°СЃС‚СЊ РєРѕР»Р»РµРєС†РёРё.\"",
    review_6_author: "Р”Р¶СѓР»РёР°РЅ Рњ.",
    review_7_text: "\"РЇ РІР»СЋР±Р»РµРЅР° РІ РјРёРЅРёРјР°Р»РёСЃС‚РёС‡РЅСѓСЋ СЌСЃС‚РµС‚РёРєСѓ Рё С‡РёСЃС‚С‹Рµ, РЅРµС‚РѕРєСЃРёС‡РЅС‹Рµ РёРЅРіСЂРµРґРёРµРЅС‚С‹. РќР°РєРѕРЅРµС†, СЂРѕСЃРєРѕС€РЅС‹Р№ Р±СЂРµРЅРґ, РєРѕС‚РѕСЂС‹Р№ СЃРѕРѕС‚РІРµС‚СЃС‚РІСѓРµС‚ РјРѕРёРј С†РµРЅРЅРѕСЃС‚СЏРј.\"",
    review_7_author: "РҐР»РѕСЏ Рў.",
    review_8_text: "\"РљСѓРїРёР» РІ РїРѕРґР°СЂРѕРє Р¶РµРЅРµ, Рё РѕРЅР° Р±С‹Р»Р° РІ РїРѕР»РЅРѕРј РІРѕСЃС‚РѕСЂРіРµ. РђСЂРѕРјР°С‚ РїСЊСЏРЅСЏС‰РёР№ Рё РЅРµРІРµСЂРѕСЏС‚РЅРѕ РёР·С‹СЃРєР°РЅРЅС‹Р№.\"",
    review_8_author: "Р”Р¶РµР№РјСЃ Р‘.",
    mobile_sec_menu: "РњРµРЅСЋ",
    mobile_sec_actions: "РџРѕРєСѓРїРєРё",
    mobile_sec_languages: "РЇР·С‹РєРё",
    nav_contact: "РљРѕРЅС‚Р°РєС‚С‹",
    contact_header_title: "РљРћРќРўРђРљРўР«",
    contact_subtitle: "РЎРІСЏР·Р°С‚СЊСЃСЏ СЃ РЅР°РјРё",
    contact_desc: "Р•СЃС‚СЊ РІРѕРїСЂРѕСЃС‹ РёР»Рё С…РѕС‚РёС‚Рµ Р·Р°РєР°Р·Р°С‚СЊ РёРЅРґРёРІРёРґСѓР°Р»СЊРЅС‹Р№ Р°СЂРѕРјР°С‚? РќР°С€ РєРѕРЅСЃСЊРµСЂР¶ Р±СѓРґРµС‚ СЂР°Рґ РїРѕРјРѕС‡СЊ РІР°Рј.",
    contact_address_label: "РђРґСЂРµСЃ",
    contact_phone_label: "РўРµР»РµС„РѕРЅ",
    contact_email_label: "Р­Р». РїРѕС‡С‚Р°",
    contact_delivery_label: "Р”РѕСЃС‚Р°РІРєР°",
    contact_delivery_value: "Р”РѕСЃС‚Р°РІРєР° РїРѕ РІСЃРµР№ РђСЂРјРµРЅРёРё",
    contact_form_name_placeholder: "РђРЅРЅР° РРІР°РЅРѕРІР°",
    contact_form_email_placeholder: "anna@example.com",
    contact_form_subject_placeholder: "РљРѕРЅСЃСѓР»СЊС‚Р°С†РёСЏ РїРѕ Р°СЂРѕРјР°С‚Р°Рј",
    contact_form_message_placeholder: "РќР°РїРёС€РёС‚Рµ РІР°С€Рµ СЃРѕРѕР±С‰РµРЅРёРµ Р·РґРµСЃСЊ...",
    contact_hours_label: "Р§Р°СЃС‹ СЂР°Р±РѕС‚С‹",
    contact_form_name: "РРјСЏ *",
    contact_form_email: "Р­Р». РїРѕС‡С‚Р° *",
    contact_form_subject: "РўРµРјР° *",
    contact_form_message: "РЎРѕРѕР±С‰РµРЅРёРµ *",
    contact_form_submit: "РћС‚РїСЂР°РІРёС‚СЊ",
    footer_policy_shipping: "Р”РѕСЃС‚Р°РІРєР° Рё РІРѕР·РІСЂР°С‚",
    footer_policy_privacy: "РљРѕРЅС„РёРґРµРЅС†РёР°Р»СЊРЅРѕСЃС‚СЊ Рё СѓСЃР»РѕРІРёСЏ",
    policy_shipping_header_title: "Р”РћРЎРўРђР’РљРђ",
    policy_shipping_subtitle: "РџРћР”Р”Р•Р Р–РљРђ РљР›РР•РќРўРћР’",
    policy_shipping_title: "РџСЂР°РІРёР»Р° РґРѕСЃС‚Р°РІРєРё Рё РІРѕР·РІСЂР°С‚Р°",
    policy_shipping_sec1_title: "1. Р”РѕСЃС‚Р°РІРєР° Рё РїРѕР»СѓС‡РµРЅРёРµ",
    policy_shipping_sec1_desc1: "Р’ NOVA РєР°Р¶РґС‹Р№ Р°СЂРѕРјР°С‚ С‚С‰Р°С‚РµР»СЊРЅРѕ СѓРїР°РєРѕРІС‹РІР°РµС‚СЃСЏ РґР»СЏ СЃРѕС…СЂР°РЅРµРЅРёСЏ РµРіРѕ С‚РѕРЅРєРѕР№ СЂР°СЃС‚РёС‚РµР»СЊРЅРѕР№ РєРѕРјРїРѕР·РёС†РёРё. РњС‹ РѕСЃСѓС‰РµСЃС‚РІР»СЏРµРј РґРѕСЃС‚Р°РІРєСѓ РєР°Рє РїРѕ РђСЂРјРµРЅРёРё, С‚Р°Рє Рё РїРѕ РІСЃРµРјСѓ РјРёСЂСѓ.",
    policy_shipping_sec1_desc2: "<strong>Р”РѕСЃС‚Р°РІРєР° РїРѕ Р•СЂРµРІР°РЅСѓ:</strong> 1-2 СЂР°Р±РѕС‡РёС… РґРЅСЏ (Р±РµСЃРїР»Р°С‚РЅРѕ РїСЂРё Р·Р°РєР°Р·Рµ РЅР° СЃСѓРјРјСѓ Р±РѕР»РµРµ 20 000 AMD).<br><strong>Р РµРіРёРѕРЅС‹ РђСЂРјРµРЅРёРё:</strong> 2-4 СЂР°Р±РѕС‡РёС… РґРЅСЏ.<br><strong>РњРµР¶РґСѓРЅР°СЂРѕРґРЅР°СЏ РґРѕСЃС‚Р°РІРєР°:</strong> 7-14 СЂР°Р±РѕС‡РёС… РґРЅРµР№ Р·Р°РєР°Р·РЅРѕР№ РїРѕС‡С‚РѕР№.",
    policy_shipping_sec2_title: "2. РџСЂР°РІРёР»Р° РІРѕР·РІСЂР°С‚Р° Рё РѕР±РјРµРЅР°",
    policy_shipping_sec2_desc1: "Р’ СЃРІСЏР·Рё СЃ РёРЅРґРёРІРёРґСѓР°Р»СЊРЅС‹Рј Рё РіРёРіРёРµРЅРёС‡РµСЃРєРёРј С…Р°СЂР°РєС‚РµСЂРѕРј РЅРёС€РµРІРѕР№ РїР°СЂС„СЋРјРµСЂРёРё, РјС‹ РјРѕР¶РµРј РїСЂРёРЅСЏС‚СЊ РІРѕР·РІСЂР°С‚ С‚РѕР»СЊРєРѕ РїРѕР»РЅРѕСЃС‚СЊСЋ Р·Р°РїРµС‡Р°С‚Р°РЅРЅРѕР№, РЅРµРёСЃРїРѕР»СЊР·РѕРІР°РЅРЅРѕР№ РїСЂРѕРґСѓРєС†РёРё РІ РѕСЂРёРіРёРЅР°Р»СЊРЅРѕР№ СѓРїР°РєРѕРІРєРµ РІ С‚РµС‡РµРЅРёРµ 14 РґРЅРµР№ СЃ РјРѕРјРµРЅС‚Р° РґРѕСЃС‚Р°РІРєРё.",
    policy_shipping_sec2_desc2: "Р•СЃР»Рё РІС‹ С…РѕС‚РёС‚Рµ РѕС„РѕСЂРјРёС‚СЊ РІРѕР·РІСЂР°С‚, РїРѕР¶Р°Р»СѓР№СЃС‚Р°, СЃРІСЏР¶РёС‚РµСЃСЊ СЃ РЅР°С€РµР№ СЃР»СѓР¶Р±РѕР№ РїРѕРґРґРµСЂР¶РєРё РїРѕ Р°РґСЂРµСЃСѓ concierge@novafragrances.com РїРµСЂРµРґ РѕС‚РїСЂР°РІРєРѕР№ С‚РѕРІР°СЂР° РЅР°Р·Р°Рґ.",
    policy_shipping_sec3_title: "3. РџРѕРІСЂРµР¶РґРµРЅРЅС‹Рµ С‚РѕРІР°СЂС‹",
    policy_shipping_sec3_desc1: "Р•СЃР»Рё С„Р»Р°РєРѕРЅ РїСЂРёС€РµР» РїРѕРІСЂРµР¶РґРµРЅРЅС‹Рј РёР»Рё РїСЂРѕС‚РµРєР°РµС‚, РЅРµРјРµРґР»РµРЅРЅРѕ СЃРґРµР»Р°Р№С‚Рµ С„РѕС‚РѕРіСЂР°С„РёСЋ Рё СЃРІСЏР¶РёС‚РµСЃСЊ СЃ РЅР°РјРё РІ С‚РµС‡РµРЅРёРµ 48 С‡Р°СЃРѕРІ СЃ РјРѕРјРµРЅС‚Р° РїРѕР»СѓС‡РµРЅРёСЏ. РњС‹ РѕСЂРіР°РЅРёР·СѓРµРј Р·Р°РјРµРЅСѓ РёР»Рё РїРѕР»РЅС‹Р№ РІРѕР·РІСЂР°С‚ СЃСЂРµРґСЃС‚РІ.",
    policy_privacy_header_title: "Р—РђРљРћРќ",
    policy_privacy_subtitle: "Р®Р РР”РР§Р•РЎРљРђРЇ РРќР¤РћР РњРђР¦РРЇ",
    policy_privacy_title: "РљРѕРЅС„РёРґРµРЅС†РёР°Р»СЊРЅРѕСЃС‚СЊ Рё РЈСЃР»РѕРІРёСЏ РёСЃРїРѕР»СЊР·РѕРІР°РЅРёСЏ",
    policy_privacy_sec1_title: "1. РЎР±РѕСЂ РёРЅС„РѕСЂРјР°С†РёРё",
    policy_privacy_sec1_desc: "РњС‹ СЃРѕР±РёСЂР°РµРј Р±Р°Р·РѕРІСѓСЋ РєРѕРЅС‚Р°РєС‚РЅСѓСЋ РёРЅС„РѕСЂРјР°С†РёСЋ Рё Р°РґСЂРµСЃ РґРѕСЃС‚Р°РІРєРё РїСЂРё РѕС„РѕСЂРјР»РµРЅРёРё Р·Р°РєР°Р·Р° РёР»Рё РїРѕРґРїРёСЃРєРµ РЅР° РЅР°С€Сѓ СЂР°СЃСЃС‹Р»РєСѓ. Р­С‚Рё РґР°РЅРЅС‹Рµ РёСЃРїРѕР»СЊР·СѓСЋС‚СЃСЏ РёСЃРєР»СЋС‡РёС‚РµР»СЊРЅРѕ РґР»СЏ РІС‹РїРѕР»РЅРµРЅРёСЏ РІР°С€РёС… Р·Р°РєР°Р·РѕРІ.",
    policy_privacy_sec2_title: "2. Р‘РµР·РѕРїР°СЃРЅРѕСЃС‚СЊ РїР»Р°С‚РµР¶РµР№",
    policy_privacy_sec2_desc: "Р’СЃРµ РїР»Р°С‚РµР¶Рё РѕР±СЂР°Р±Р°С‚С‹РІР°СЋС‚СЃСЏ С‡РµСЂРµР· Р·Р°С‰РёС‰РµРЅРЅС‹Рµ Р·Р°С€РёС„СЂРѕРІР°РЅРЅС‹Рµ С€Р»СЋР·С‹. NOVA РЅРµ СЃРѕС…СЂР°РЅСЏРµС‚ Рё РЅРµ РёРјРµРµС‚ РґРѕСЃС‚СѓРїР° Рє РІР°С€РµР№ РїР»Р°С‚РµР¶РЅРѕР№ РєР°СЂС‚Рµ.",
    policy_privacy_sec3_title: "3. РЈСЃР»РѕРІРёСЏ РёСЃРїРѕР»СЊР·РѕРІР°РЅРёСЏ",
    policy_privacy_sec3_desc: "Р’РµСЃСЊ РєРѕРЅС‚РµРЅС‚, РёР·РѕР±СЂР°Р¶РµРЅРёСЏ Рё С‚РµРєСЃС‚С‹ РЅР° СЌС‚РѕРј СЃР°Р№С‚Рµ Р·Р°С‰РёС‰РµРЅС‹ С‚РѕРІР°СЂРЅС‹Рј Р·РЅР°РєРѕРј Рё Р°РІС‚РѕСЂСЃРєРёРј РїСЂР°РІРѕРј. РљРѕРїРёСЂРѕРІР°РЅРёРµ Р±РµР· РїРёСЃСЊРјРµРЅРЅРѕРіРѕ СЂР°Р·СЂРµС€РµРЅРёСЏ NOVA СЃС‚СЂРѕРіРѕ Р·Р°РїСЂРµС‰РµРЅРѕ.",
    wishlist_header_title: "РЎРџРРЎРћРљ Р–Р•Р›РђРќРР™",
    wishlist_subtitle: "РњРћР Р—РђРљР›РђР”РљР",
    wishlist_view_page_btn: "РћС‚РєСЂС‹С‚СЊ СЃРїРёСЃРѕРє Р¶РµР»Р°РЅРёР№",
    exp_subtitle: "РћР»СЊС„Р°РєС‚РѕСЂРЅР°СЏ РќР°СѓРєР°",
    exp_title: "РћР»СЊС„Р°РєС‚РѕСЂРЅР°СЏ РСЃС‚РѕСЂРёСЏ",
    exp_desc: "РљР°Р¶РґС‹Р№ Р°СЂРѕРјР°С‚ NOVA РїСЂРµРґСЃС‚Р°РІР»СЏРµС‚ СЃРѕР±РѕР№ РґРёРЅР°РјРёС‡РЅСѓСЋ, Р¶РёРІСѓСЋ РєРѕРјРїРѕР·РёС†РёСЋ, РєРѕС‚РѕСЂР°СЏ СЂР°Р·РІРёРІР°РµС‚СЃСЏ РЅР° РєРѕР¶Рµ РІ С‚РµС‡РµРЅРёРµ РЅРµСЃРєРѕР»СЊРєРёС… С‡Р°СЃРѕРІ. РњС‹ СЃР»РµРґСѓРµРј С‚СЂР°РґРёС†РёРѕРЅРЅРѕР№ С‚СЂРµС…СЉСЏСЂСѓСЃРЅРѕР№ СЃС‚СЂСѓРєС‚СѓСЂРµ С„СЂР°РЅС†СѓР·СЃРєРѕР№ РІС‹СЃРѕРєРѕР№ РїР°СЂС„СЋРјРµСЂРёРё, СѓСЃРёР»РµРЅРЅРѕР№ Р±РѕС‚Р°РЅРёС‡РµСЃРєРёРјРё РёРЅРіСЂРµРґРёРµРЅС‚Р°РјРё, СЃРѕР±СЂР°РЅРЅС‹РјРё РёР· СѓСЃС‚РѕР№С‡РёРІС‹С… РёСЃС‚РѕС‡РЅРёРєРѕРІ РїРѕ РІСЃРµРјСѓ РјРёСЂСѓ.",
    top_desc: "РџРµСЂРІРѕРµ РІРїРµС‡Р°С‚Р»РµРЅРёРµ (0-30 РјРёРЅСѓС‚)",
    heart_desc: "РћСЃРЅРѕРІРЅРѕР№ С…Р°СЂР°РєС‚РµСЂ (30 РјРёРЅСѓС‚ - 4 С‡Р°СЃР°)",
    base_desc: "РЁР»РµР№С„ Рё СЃС‚РѕР№РєРѕСЃС‚СЊ (4+ С‡Р°СЃРѕРІ)",
    promises_title: "РџСЂРёРЅС†РёРїС‹ РћСЃРѕР·РЅР°РЅРЅРѕР№ Р РѕСЃРєРѕС€Рё",
    suggestions_title: "Р—Р°РІРµСЂС€РёС‚Рµ Р РёС‚СѓР°Р»",
    search_no_results: "РћР»СЊС„Р°РєС‚РѕСЂРЅС‹Рµ РЅРѕС‚С‹ РЅРµ РЅР°Р№РґРµРЅС‹.",
    filter_scent_family: "РЎРµРјРµР№СЃС‚РІРѕ Р°СЂРѕРјР°С‚РѕРІ",
    filter_concentration: "РљРѕРЅС†РµРЅС‚СЂР°С†РёСЏ",
    filter_gender: "РџРѕР»",
    filter_vibe: "РђС‚РјРѕСЃС„РµСЂР° / РџРѕРІРѕРґ",
    filter_price: "Р¦РµРЅР°",
    filter_size: "РћР±СЉРµРј",
    filter_brand: "Р‘СЂРµРЅРґ",
    btn_apply_filters: "РџСЂРёРјРµРЅРёС‚СЊ С„РёР»СЊС‚СЂС‹",
    btn_clear_all: "РЎР±СЂРѕСЃРёС‚СЊ РІСЃРµ",
    btn_filters: "Р¤РёР»СЊС‚СЂС‹",
    vibe_romantic: "Р РѕРјР°РЅС‚РёС‡РµСЃРєРёР№ / Р’РµС‡РµСЂРЅРёР№",
    vibe_daily: "РџРѕРІСЃРµРґРЅРµРІРЅС‹Р№ / РЎРІРµР¶РёР№",
    vibe_professional: "Р”РµР»РѕРІРѕР№ / Р­Р»РµРіР°РЅС‚РЅС‹Р№",
    vibe_cozy: "РўРµРїР»С‹Р№ / РЈСЋС‚РЅС‹Р№",
    vibe_mysterious: "Р—Р°РіР°РґРѕС‡РЅС‹Р№ / РЎРјРµР»С‹Р№",
    my_account_subtitle: "РљР›РР•РќРўРЎРљРР™ РџРћР РўРђР›",
    my_account_title: "Р›РёС‡РЅС‹Р№ РљР°Р±РёРЅРµС‚",
    acc_nav_dashboard: "РџР°РЅРµР»СЊ СѓРїСЂР°РІР»РµРЅРёСЏ",
    acc_nav_wardrobe: "Р“Р°СЂРґРµСЂРѕР± Р°СЂРѕРјР°С‚РѕРІ",
    acc_nav_discovery: "РџРµСЂСЃРѕРЅР°Р»СЊРЅР°СЏ РЎРєРёРґРєР°",
    acc_nav_details: "РќР°СЃС‚СЂРѕР№РєРё Р°РєРєР°СѓРЅС‚Р°",
    acc_nav_orders: "РСЃС‚РѕСЂРёСЏ Р·Р°РєР°Р·РѕРІ",
    acc_dash_welcome: "Р”РѕР±СЂРѕ РїРѕР¶Р°Р»РѕРІР°С‚СЊ РІ РІР°С€ Р»РёС‡РЅС‹Р№ РѕР»СЊС„Р°РєС‚РѕСЂРЅС‹Р№ РїРѕСЂС‚Р°Р». РћС‚СЃСЋРґР° РІС‹ РјРѕР¶РµС‚Рµ РѕС‚СЃР»РµР¶РёРІР°С‚СЊ СЃРІРѕРё РїРѕРєСѓРїРєРё РїР°СЂС„СЋРјРµСЂРёРё, СѓРїСЂР°РІР»СЏС‚СЊ Р°РґСЂРµСЃР°РјРё Рё СЃРєРёРґРєР°РјРё.",
    acc_scent_profile: "РџСЂРѕС„РёР»СЊ Р°СЂРѕРјР°С‚РѕРІ",
    acc_scent_profile_desc: "РќР° РѕСЃРЅРѕРІР°РЅРёРё РІР°С€РёС… РїРѕРєСѓРїРѕРє, РІР°С€Рё РѕР»СЊС„Р°РєС‚РѕСЂРЅС‹Рµ РїСЂРµРґРїРѕС‡С‚РµРЅРёСЏ:",
    acc_wardrobe_title: "РњРѕР№ РіР°СЂРґРµСЂРѕР± Р°СЂРѕРјР°С‚РѕРІ",
    acc_discovery_title: "РџРµСЂСЃРѕРЅР°Р»СЊРЅР°СЏ РЎРєРёРґРєР°",
    acc_promo_tag: "РљР›РЈР‘ DISCOVERY",
    acc_promo_title: "РћР±РјРµРЅСЏР№С‚Рµ РїСЂРѕР±РЅРёРє РЅР° С„Р»Р°РєРѕРЅ",
    acc_promo_desc: "РџРѕРЅСЂР°РІРёР»РёСЃСЊ РїСЂРѕР±РЅРёРєРё? РџСЂРёРјРµРЅРёС‚Рµ СЃРєРёРґРєСѓ 4,000 AMD РїСЂРё РїРѕРєСѓРїРєРµ Р»СЋР±РѕРіРѕ РїРѕР»РЅРѕСЂР°Р·РјРµСЂРЅРѕРіРѕ (100РјР») С„Р»Р°РєРѕРЅР°!",
    acc_promo_code_lbl: "Р’РђРЁ РЈРќРРљРђР›Р¬РќР«Р™ РљРћР” РЎРљРР”РљР:",
    admin_secure_portal: "Р‘Р•Р—РћРџРђРЎРќР«Р™ РџРћР РўРђР›",
    admin_login_title: "Р’С…РѕРґ РІ WooCommerce",
    admin_pass_hint: "РџРѕРґСЃРєР°Р·РєР°: norayrnajaryann@gmail.com / K9$v!p2WbX",
    btn_admin_login: "Р’РѕР№С‚Рё",
    admin_view_frontend: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ СЃР°Р№С‚",
    admin_nav_orders: "РњРµРЅРµРґР¶РµСЂ Р·Р°РєР°Р·РѕРІ",
    admin_nav_inventory: "РЈРїСЂР°РІР»РµРЅРёРµ Р·Р°РїР°СЃР°РјРё",
    admin_nav_dev: "РќР°СЃС‚СЂРѕР№РєРё СЂР°Р·СЂР°Р±РѕС‚С‡РёРєР°",
    admin_nav_logs: "Р–СѓСЂРЅР°Р» Р°РєС‚РёРІРЅРѕСЃС‚Рё",
    admin_nav_clients: "РљР»РёРµРЅС‚С‹",
    admin_clients_title: "Р—Р°СЂРµРіРёСЃС‚СЂРёСЂРѕРІР°РЅРЅС‹Рµ РєР»РёРµРЅС‚С‹",
    admin_clients_desc: "Р’СЃРµ РїРѕР»СЊР·РѕРІР°С‚РµР»Рё, СЃРѕР·РґР°РІС€РёРµ Р°РєРєР°СѓРЅС‚ РЅР° СЃР°Р№С‚Рµ.",
    admin_th_name: "РРјСЏ",
    admin_th_registered: "Р—Р°СЂРµРіРёСЃС‚СЂРёСЂРѕРІР°РЅ",
    admin_th_actions: "Р”РµР№СЃС‚РІРёСЏ",
    acc_nav_wardrobe_sub: "РџСЂРѕСЃРјРѕС‚СЂ РєСѓРїР»РµРЅРЅС‹С… С„Р»Р°РєРѕРЅРѕРІ",
    acc_nav_discovery_sub: "Р’Р°С€ СЌРєСЃРєР»СЋР·РёРІРЅС‹Р№ РєРѕРґ СЃРєРёРґРєРё",
    acc_nav_orders_sub: "РћС‚СЃР»РµР¶РёРІР°РЅРёРµ РѕС‚РїСЂР°РІР»РµРЅРёР№",
    acc_nav_details_sub: "Р РµРґР°РєС‚РёСЂРѕРІР°С‚СЊ Р°РґСЂРµСЃР° Рё СЌР». РїРѕС‡С‚Сѓ",
    acc_th_id: "ID",
    acc_th_date: "Р”Р°С‚Р°",
    acc_th_items: "РўРѕРІР°СЂС‹",
    acc_th_total: "РС‚РѕРіРѕ",
    acc_th_status: "РЎС‚Р°С‚СѓСЃ",
    acc_th_invoice: "РЎС‡РµС‚",
    acc_th_sample_kit: "РќР°Р±РѕСЂ РїСЂРѕР±РЅРёРєРѕРІ",
    acc_th_purchase_date: "Р”Р°С‚Р° РїРѕРєСѓРїРєРё",
    acc_th_credit_value: "РЎСѓРјРјР° СЃРєРёРґРєРё",
    admin_staff_placeholder: "РРјСЏ (РЅР°РїСЂРёРјРµСЂ, Р•Р»РµРЅР°)",
    status_pending: "Р’ РѕР¶РёРґР°РЅРёРё",
    status_processing: "РћР±СЂР°Р±Р°С‚С‹РІР°РµС‚СЃСЏ",
    status_completed: "Р’С‹РїРѕР»РЅРµРЅ",
    status_failed: "РћС€РёР±РєР°",
    admin_btn_delete: "РЈРґР°Р»РёС‚СЊ",
    admin_btn_add_new: "Р”РѕР±Р°РІРёС‚СЊ РЅРѕРІС‹Р№",
    plugin_pdf_label: "WooCommerce PDF РРЅРІРѕР№СЃС‹ (v2.1)",
    plugin_subs_label: "WooCommerce РџРѕРґРїРёСЃРєРё (v4.0)",
    admin_insta_location_placeholder: "РЅР°РїСЂРёРјРµСЂ, Р“СЂР°СЃ, Р¤СЂР°РЅС†РёСЏ",
    admin_insta_caption_placeholder: "РќРѕС‚С‹ Р°СЂРѕРјР°С‚Р°, РґРµС‚Р°Р»Рё Рё С‚.Рґ.",
    admin_staff_locked: "Р—Р°Р±Р»РѕРєРёСЂРѕРІР°РЅРѕ",
    admin_logs_empty: "Р—Р°РїРёСЃРё РІ Р¶СѓСЂРЅР°Р»Рµ РѕС‚СЃСѓС‚СЃС‚РІСѓСЋС‚.",
    admin_sales_perf: "РџРѕРєР°Р·Р°С‚РµР»Рё РїСЂРѕРґР°Р¶ РїРѕ РєР°С‚РµРіРѕСЂРёСЏРј",
    admin_dev_title: "РќР°СЃС‚СЂРѕР№РєРё СЂР°Р·СЂР°Р±РѕС‚С‡РёРєР° Рё СЃРёСЃС‚РµРјС‹",
    admin_dev_desc: "РЈРїСЂР°РІР»РµРЅРёРµ РєРѕРЅС„РёРіСѓСЂР°С†РёСЏРјРё РїР»Р°С‚С„РѕСЂРјС‹, РѕСЃРЅРѕРІРЅС‹РјРё СЂР°СЃС€РёСЂРµРЅРёСЏРјРё, РїР»Р°С‚РµР¶РЅС‹РјРё С€Р»СЋР·Р°РјРё Рё СѓС‡РµС‚РЅС‹РјРё РґР°РЅРЅС‹РјРё Р±РµР·РѕРїР°СЃРЅРѕСЃС‚Рё РїРµСЂСЃРѕРЅР°Р»Р°.",
    admin_dev_plugins: "РћСЃРЅРѕРІРЅС‹Рµ СЂР°СЃС€РёСЂРµРЅРёСЏ Рё РїР»Р°РіРёРЅС‹",
    admin_dev_gateways: "РџР»Р°С‚РµР¶РЅС‹Рµ С€Р»СЋР·С‹",
    gateway_stripe_label: "РРЅС‚РµРіСЂР°С†РёСЏ Stripe Checkout",
    gateway_paypal_label: "PayPal Express Standard",
    gateway_cod_label: "РћРїР»Р°С‚Р° РїСЂРё РґРѕСЃС‚Р°РІРєРµ (COD)",
    admin_dev_staff_manager: "РњРµРЅРµРґР¶РµСЂ РїСЂРѕС„РёР»РµР№ РїРµСЂСЃРѕРЅР°Р»Р°",
    admin_dev_staff_desc: "Р РµРіРёСЃС‚СЂР°С†РёСЏ РёР»Рё РѕС‚Р·С‹РІ СѓС‡РµС‚РЅС‹С… РґР°РЅРЅС‹С… РґРѕСЃС‚СѓРїР° РґР»СЏ РјРµРЅРµРґР¶РµСЂРѕРІ РїРµСЂСЃРѕРЅР°Р»Р°.",
    admin_btn_create_profile: "РЎРѕР·РґР°С‚СЊ РїСЂРѕС„РёР»СЊ",
    admin_th_staff_name: "РРјСЏ СЃРѕС‚СЂСѓРґРЅРёРєР°",
    admin_th_role: "Р РѕР»СЊ",
    admin_logs_title: "РЎРёСЃС‚РµРјР° Р»РѕРіРёСЂРѕРІР°РЅРёСЏ Р°РєС‚РёРІРЅРѕСЃС‚Рё",
    admin_logs_desc: "Р–СѓСЂРЅР°Р» Р°СѓРґРёС‚Р° Р±РµР·РѕРїР°СЃРЅРѕСЃС‚Рё РїР»Р°С‚С„РѕСЂРјС‹, РѕС‚СЃР»РµР¶РёРІР°СЋС‰РёР№ РґРѕСЃС‚СѓРї РїРѕ СѓС‡РµС‚РЅС‹Рј РґР°РЅРЅС‹Рј, РёР·РјРµРЅРµРЅРёСЏ РЅР°СЃС‚СЂРѕРµРє, С†РµРЅ Рё РѕР±РЅРѕРІР»РµРЅРёР№.",
    admin_logs_clear: "РћС‡РёСЃС‚РёС‚СЊ Р»РѕРіРё",
    admin_logs_th_timestamp: "Р’СЂРµРјРµРЅРЅР°СЏ РјРµС‚РєР°",
    admin_logs_th_operator: "РћРїРµСЂР°С‚РѕСЂ",
    admin_logs_th_action: "Р—Р°РїРёСЃР°РЅРЅРѕРµ РґРµР№СЃС‚РІРёРµ",
    admin_btn_install: "РЈСЃС‚Р°РЅРѕРІРёС‚СЊ",
    acc_wardrobe_empty: "Р’Р°С€ РіР°СЂРґРµСЂРѕР± Р°СЂРѕРјР°С‚РѕРІ РїСѓСЃС‚.",
    acc_discovery_empty: "РќР°Р±РѕСЂС‹ РїСЂРѕР±РЅРёРєРѕРІ РµС‰Рµ РЅРµ РїСЂРёРѕР±СЂРµС‚РµРЅС‹.",
    acc_orders_empty: "Р—Р°РєР°Р·С‹ РµС‰Рµ РЅРµ РѕС„РѕСЂРјР»РµРЅС‹.",
    bg_media: "РњР•Р”РA",
    insta_likes: "РѕС‚РјРµС‚РѕРє В«РќСЂР°РІРёС‚СЃСЏВ»",
    insta_loc_1: "Р“СЂР°СЃ, Р¤СЂР°РЅС†РёСЏ",
    insta_loc_2: "РљРёРѕС‚Рѕ, РЇРїРѕРЅРёСЏ",
    insta_loc_3: "РђРјР°Р»СЊС„Рё, РС‚Р°Р»РёСЏ",
    insta_loc_4: "РџР°СЂРёР¶, Р¤СЂР°РЅС†РёСЏ",
    insta_loc_5: "Р›РѕРЅРґРѕРЅ, Р’РµР»РёРєРѕР±СЂРёС‚Р°РЅРёСЏ",
    insta_loc_6: "РЎР°РЅС‚РѕСЂРёРЅРё, Р“СЂРµС†РёСЏ",
    insta_loc_7: "РњРёР»Р°РЅ, РС‚Р°Р»РёСЏ",
    insta_loc_8: "РЎРµРЅ-РўСЂРѕРїРµ, Р¤СЂР°РЅС†РёСЏ",
    insta_caption_1: "Santal NВ°01 РѕС‚СЂР°Р¶Р°РµС‚ РїРµСЂРІРѕР·РґР°РЅРЅСѓСЋ СЌР»РµРіР°РЅС‚РЅРѕСЃС‚СЊ Р°РІСЃС‚СЂР°Р»РёР№СЃРєРѕР№ РїСѓСЃС‚С‹РЅРё. Р”РёСЃС‚РёР»Р»РёСЂРѕРІР°РЅ РІ РјРµРґРЅС‹С… РєРѕР»РѕРЅРЅР°С…. вњЁ",
    insta_caption_2: "РќРµР¶РЅС‹Р№ С‚Р°РЅРµС† С†РІРµС‚СѓС‰РµР№ РІРёС€РЅРё Рё РјСЏРіРєРѕРіРѕ РєР°С€РµРјРёСЂРѕРІРѕРіРѕ РјСѓСЃРєСѓСЃР°. Fleur de Cerisier вЂ” СЌС‚Рѕ РљРёРѕС‚Рѕ РІРµСЃРЅРѕР№. рџЊё",
    insta_caption_3: "РџСЂРѕРїРёС‚Р°РЅРЅС‹Р№ СЃРѕР»РЅС†РµРј РІР·СЂС‹РІ СЃРёС†РёР»РёР№СЃРєРѕРіРѕ Р±РµСЂРіР°РјРѕС‚Р° Рё СЃРѕР»РµРЅРѕРіРѕ РјРѕСЂСЃРєРѕРіРѕ РІРѕР·РґСѓС…Р°. Agrumes Frais РІ СЂСѓРєРµ. рџЌ‹",
    insta_caption_4: "РџРѕРґР°СЂРёС‚Рµ РѕР»СЊС„Р°РєС‚РѕСЂРЅСѓСЋ РїРѕСЌР·РёСЋ. РќР°С€ С„РёСЂРјРµРЅРЅС‹Р№ РЅР°Р±РѕСЂ РѕС‚РєСЂС‹С‚РёР№, Р·Р°РІРµСЂРЅСѓС‚С‹Р№ РІ СЌРєРѕР»РѕРіРёС‡РЅС‹Р№ Р»РµРЅ. рџЋЃ",
    insta_caption_5: "Р§РёСЃС‚С‹Рµ СЂР°СЃС‚РёС‚РµР»СЊРЅС‹Рµ РёРЅРіСЂРµРґРёРµРЅС‚С‹, СЃРѕР±СЂР°РЅРЅС‹Рµ РёР· СЌРєРѕР»РѕРіРёС‡РµСЃРєРё С‡РёСЃС‚С‹С… РёСЃС‚РѕС‡РЅРёРєРѕРІ РїРѕ РІСЃРµРјСѓ РјРёСЂСѓ. РџРµСЂРІРѕР·РґР°РЅРЅР°СЏ СЂРѕСЃРєРѕС€СЊ. рџЊї",
    insta_caption_6: "Р РѕСЃРєРѕС€РЅРѕРµ Р±Р°СЂС…Р°С‚РЅРѕРµ РѕРґРµСЏР»Рѕ РёР· Р·РѕР»РѕС‚РѕР№ Р°РјР±СЂС‹ Рё РїРѕРґР¶Р°СЂРµРЅРЅС‹С… СЃРїРµС†РёР№ РІ Р·РѕР»РѕС‚РѕР№ С‡Р°СЃ. рџЊ…",
    insta_caption_7: "РљР°Р¶РґРѕРµ СЂР°СЃРїС‹Р»РµРЅРёРµ вЂ” СЌС‚Рѕ Р¶РёРІР°СЏ РєРѕРјРїРѕР·РёС†РёСЏ, РєРѕС‚РѕСЂР°СЏ СЂР°СЃРєСЂС‹РІР°РµС‚СЃСЏ РЅР° РєРѕР¶Рµ РІ С‚РµС‡РµРЅРёРµ РЅРµСЃРєРѕР»СЊРєРёС… С‡Р°СЃРѕРІ. рџ’«",
    insta_caption_8: "Р‘РѕРґСЂСЏС‰Рµ СЃРІРµР¶РёР№ VГ©tiver Blanc. РћР»СЊС„Р°РєС‚РѕСЂРЅС‹Р№ СЌРєРІРёРІР°Р»РµРЅС‚ СЃС€РёС‚РѕР№ РЅР° Р·Р°РєР°Р· Р±РµР»РѕР№ СЂСѓР±Р°С€РєРё. рџЊЉ",
    insta_view_comments_1: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ РІСЃРµ 12 РєРѕРјРјРµРЅС‚Р°СЂРёРµРІ",
    insta_view_comments_2: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ РІСЃРµ 8 РєРѕРјРјРµРЅС‚Р°СЂРёРµРІ",
    insta_view_comments_3: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ РІСЃРµ 15 РєРѕРјРјРµРЅС‚Р°СЂРёРµРІ",
    insta_view_comments_4: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ РІСЃРµ 9 РєРѕРјРјРµРЅС‚Р°СЂРёРµРІ",
    insta_view_comments_5: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ РІСЃРµ 11 РєРѕРјРјРµРЅС‚Р°СЂРёРµРІ",
    insta_view_comments_6: "РџРѕСЃРјРѕС‚СЂРµС‚СЊ РІСЃРµ 14 РєРѕРјРјРµРЅС‚Р°СЂРёРµРІ",
    insta_date_1: "2 Р”РќРЇ РќРђР—РђР”",
    insta_date_2: "3 Р”РќРЇ РќРђР—РђР”",
    insta_date_3: "4 Р”РќРЇ РќРђР—РђР”",
    insta_date_4: "5 Р”РќР•Р™ РќРђР—РђР”",
    insta_date_5: "1 РќР•Р”Р•Р›Р® РќРђР—РђР”",
    insta_date_6: "1 РќР•Р”Р•Р›Р® РќРђР—РђР”",
    admin_nav_instagram: "Instagram РњРµРЅРµРґР¶РµСЂ",
    admin_insta_title: "РЈРїСЂР°РІР»РµРЅРёРµ Р»РµРЅС‚РѕР№ Instagram",
    admin_insta_desc: "Р”РѕР±Р°РІР»СЏР№С‚Рµ РЅРѕРІС‹Рµ РїСѓР±Р»РёРєР°С†РёРё РІ Р»РµРЅС‚Сѓ Instagram, СЂРµРґР°РєС‚РёСЂСѓР№С‚Рµ РёРЅС„РѕСЂРјР°С†РёСЋ РёР»Рё СѓРґР°Р»СЏР№С‚Рµ СЃСѓС‰РµСЃС‚РІСѓСЋС‰РёРµ Р·Р°РїРёСЃРё.",
    admin_insta_add_post: "Р”РѕР±Р°РІРёС‚СЊ РЅРѕРІСѓСЋ Р·Р°РїРёСЃСЊ",
    admin_insta_location: "РњРµСЃС‚РѕРїРѕР»РѕР¶РµРЅРёРµ / РњРµСЃС‚Рѕ *",
    admin_insta_caption: "РћРїРёСЃР°РЅРёРµ / РџРѕРґРїРёСЃСЊ *",
    admin_insta_date: "Р”Р°С‚Р° РїСѓР±Р»РёРєР°С†РёРё *",
    admin_insta_images: "РР·РѕР±СЂР°Р¶РµРЅРёСЏ (РјРѕР¶РЅРѕ РІС‹Р±СЂР°С‚СЊ РЅРµСЃРєРѕР»СЊРєРѕ) *",
    admin_insta_active_posts: "РђРєС‚РёРІРЅС‹Рµ РїСѓР±Р»РёРєР°С†РёРё",
    admin_th_thumbnail: "РњРёРЅРёР°С‚СЋСЂР°",
    admin_th_location: "РњРµСЃС‚РѕРїРѕР»РѕР¶РµРЅРёРµ",
    admin_th_caption: "РџРѕРґРїРёСЃСЊ",
    admin_th_date: "Р”Р°С‚Р°",
    admin_btn_add_post: "Р”РѕР±Р°РІРёС‚СЊ РїСѓР±Р»РёРєР°С†РёСЋ",
    insta_feed_empty: "РџСѓР±Р»РёРєР°С†РёР№ РїРѕРєР° РЅРµС‚. Р”РѕР±Р°РІСЊС‚Рµ РЅРѕРІС‹Рµ РїСѓР±Р»РёРєР°С†РёРё РёР· РїР°РЅРµР»Рё СѓРїСЂР°РІР»РµРЅРёСЏ WooCommerce.",
    modal_product_edit_title: "Р РµРґР°РєС‚РёСЂРѕРІР°С‚СЊ Р”РµС‚Р°Р»Рё РўРѕРІР°СЂР°",
    modal_lbl_name: "РќР°Р·РІР°РЅРёРµ РўРѕРІР°СЂР° *",
    modal_lbl_brand: "Р‘СЂРµРЅРґ *",
    modal_lbl_tagline: "РљСЂР°С‚РєРѕРµ РћРїРёСЃР°РЅРёРµ *",
    modal_lbl_desc: "РћРїРёСЃР°РЅРёРµ *",
    modal_lbl_ingredients: "РЎРѕСЃС‚Р°РІ (РРЅРіСЂРµРґРёРµРЅС‚С‹)",
    modal_lbl_image_url: "Р¤РѕС‚Рѕ (РР·РѕР±СЂР°Р¶РµРЅРёРµ URL) *",
    modal_lbl_or_upload: "РР›Р Р·Р°РіСЂСѓР·РёС‚СЊ С„Р°Р№Р»:",
    modal_lbl_family: "РЎРµРјРµР№СЃС‚РІРѕ *",
    modal_lbl_concentration: "РўРёРї *",
    modal_lbl_gender: "РџРѕР» *",
    modal_lbl_stock: "РљРѕР»РёС‡РµСЃС‚РІРѕ РЅР° СЃРєР»Р°РґРµ *",
    modal_lbl_notes_top: "Р’РµСЂС…РЅРёРµ РќРѕС‚С‹ *",
    modal_lbl_notes_heart: "РќРѕС‚С‹ РЎРµСЂРґС†Р° *",
    modal_lbl_notes_base: "Р‘Р°Р·РѕРІС‹Рµ РќРѕС‚С‹ *",
    modal_btn_cancel: "РћС‚РјРµРЅР°",
    modal_btn_save: "РЎРѕС…СЂР°РЅРёС‚СЊ РР·РјРµРЅРµРЅРёСЏ",
    home_video_title: "РСЃРєСѓСЃСЃС‚РІРѕ РР·С‹СЃРєР°РЅРЅРѕРіРѕ Р’С‹Р±РѕСЂР°",
    home_video_paragraph: "РљР°Р¶РґР°СЏ РєР°РїР»СЏ РїР°СЂС„СЋРјР° Nova вЂ” СЌС‚Рѕ РјР°РЅРёС„РµСЃС‚ СЂРѕСЃРєРѕС€Рё Рё Р±РµР·СѓРїСЂРµС‡РЅРѕРіРѕ РІРєСѓСЃР°. РњС‹ РѕС‚Р±РёСЂР°РµРј СЃР°РјС‹Рµ СЂРµРґРєРёРµ Рё С†РµРЅРЅС‹Рµ РїР°СЂС„СЋРјРµСЂРЅС‹Рµ РєРѕРјРїРѕРЅРµРЅС‚С‹ СЃРѕ РІСЃРµРіРѕ РјРёСЂР°, СЃРѕР·РґР°РІР°СЏ РјРЅРѕРіРѕРіСЂР°РЅРЅСѓСЋ, СЂР°СЃРєСЂС‹РІР°СЋС‰СѓСЋСЃСЏ РёСЃС‚РѕСЂРёСЋ РЅР° РІР°С€РµР№ РєРѕР¶Рµ. Nova вЂ” СЌС‚Рѕ РІРµРґСѓС‰РёР№ Р±СЂРµРЅРґ РЅРёС€РµРІРѕР№ РїР°СЂС„СЋРјРµСЂРёРё РІ РђСЂРјРµРЅРёРё, СЃРѕР·РґР°СЋС‰РёР№ СЃС‚РѕР№РєРёРµ С€Р»РµР№С„РѕРІС‹Рµ Р°СЂРѕРјР°С‚С‹, РєРѕС‚РѕСЂС‹Рµ Р·Р°СЏРІР»СЏСЋС‚ Рѕ РІР°С€РµРј РїСЂРёСЃСѓС‚СЃС‚РІРёРё Р±РµР· РµРґРёРЅРѕРіРѕ СЃР»РѕРІР°.",
    home_video_cta: "РџРѕРґСЂРѕР±РЅРµРµ"
  },
  en: {
    nav_about: "About Us",
    about_hook_title: "Purveyors of Olfactive Art",
    about_hook_desc: "We curate rare, niche fragrances and raw, clean ingredients for the modern aesthete. Our mission is to elevate your signature scent from an afterthought to a masterpiece.",
    about_story_title: "Our Beginning",
    about_story_desc: "NOVA was born from a singular obsession: to discover uncompromising, rare, and breathtaking scents in a market flooded with mass-produced options. What started as a private curation of the worldвЂ™s finest global fragrances has evolved into a premier sanctuary for those who appreciate the true artistry of niche perfumery.",
    about_eyebrow: "THE ART OF DISTILLATION вЂ” EST. 2026",
    about_lead: "An architectural study of pure botanical scents, handcrafted for the modern aesthetic enthusiast.",
    stat_botanical: "Authentic Curation",
    stat_batch: "Global Brands",
    stat_batch_number: "Elite",
    stat_synthetic: "Mass-Produced Fluff",
    contact_us: "Get In Touch",
    about_pillars_title: "The Pillars of Trust",
    about_guarantee_title: "100% Authenticity Guaranteed",
    about_guarantee_desc: "We source every luxury fragrance directly from authorized distributors and the original perfume houses themselves. When you buy original perfume in Armenia from us, you get absolute purityвЂ”no grey markets, no compromises.",
    about_scent_match_title: "The Perfect Scent Promise",
    about_scent_match_desc: "Finding a fragrance at an online perfume shop in Yerevan can be challenging. That is why we provide deep scent profiles and worry-free returns on all unopened boxes, making it easy to buy perfume online with total confidence.",
    about_storage_title: "Impeccable Storage Conditions",
    about_storage_desc: "Every bottle is kept in our climate- and light-controlled vaults, protecting the delicate molecular integrity of the precious essential oils and ensuring your premium perfume boutique purchase retains its full, long-lasting power.",
    about_cta_title: "Ready to find your signature scent?",
    about_cta_btn: "Explore the Collection",
    nav_home: "Home",
    nav_products: "Products",
    nav_brands: "Brands",
    nav_men: "Men",
    nav_women: "Women",
    nav_new: "New Arrivals",
    nav_bestsellers: "Best Sellers",
    badge_bestseller: "BEST SELLER",
    badge_new_product: "NEW PRODUCT",
    nav_gifts: "Gift Sets",
    cat_men: "Men",
    cat_women: "Women",
    cat_gifts: "Gifts",
    cat_bestsellers: "Best Sellers",
    cat_new: "New Arrivals",
    cat_home: "For Home",
    nav_admin: "WooCommerce Admin",
    nav_order_history: "Order History",
    nav_logout: "Log Out",
    auth_signin_btn: "Sign In",
    auth_signup_title: "Create Account",
    auth_email_label: "Email Address",
    auth_password_label: "Password",
    auth_first_name: "First Name",
    auth_last_name: "Last Name",
    auth_confirm_password: "Confirm Password",
    hello_user: "Hello, Emma",
    mega_woody: "Woody Notes",
    mega_floral: "Floral Notes",
    mega_citrus: "Citrus Notes",
    mega_amber: "Amber Notes",
    search_placeholder: "Search olfactory note...",
    newsletter_placeholder: "Your email address",
    maison_de_parfum: "Maison de Parfum",
    hero_title: "Your scent is waiting for you",
    hero_desc: "Perfumes, gifts, beautiful packaging",
    explore_fragrances: "Explore Fragrances",
    view_admin_demo: "View Admin Demo",
    promise_title_1: "Exclusively Curated Collections",
    promise_desc_1: "Expertly curated selection of the world's most prestigious, rare, and authentic designer perfumes in Armenia.",
    promise_title_2: "Long-Lasting Signature Scents",
    promise_desc_2: "Hand-selected niche fragrance collections with masterful oil concentrations, ensuring a breathtaking, long-lasting signature scent.",
    promise_title_3: "Sustainable Packaging",
    promise_desc_3: "Eco-friendly perfume packaging featuring 100% biodegradable FSC boxes, cotton labels, and recyclable glass bottles.",
    collections_title: "PERFUMES",
    gender_unisex: "UNISEX",
    gender_men: "FOR MEN",
    gender_women: "FOR WOMEN",
    featured_subtitle: "Selected Creations",
    featured_title: "The Fragrance Collective",
    view_full_collection_btn: "View Full Collection",
    shop_title: "The Collection",
    shop_subtitle: "Refine your search by notes profile or olfactory family.",
    filter_all: "All",
    filter_woody: "Woody",
    filter_floral: "Floral",
    filter_citrus: "Citrus",
    filter_amber: "Amber",
    sort_default: "Default sorting",
    sort_price_low_high: "Price: Low to High",
    sort_price_high_low: "Price: High to Low",
    sort_rating: "Average Rating",
    cart_title: "Shopping Bag",
    cart_subtotal_label: "Subtotal",
    cart_shipping_desc: "Shipping and taxes calculated at checkout.",
    cart_checkout_btn: "Proceed to Checkout",
    cart_empty_msg: "Your shopping bag is currently empty.",
    go_to_shop_btn: "Go to Shop",
    wishlist_title: "My Wishlist",
    wishlist_add_all_btn: "Add All to Bag",
    wishlist_empty_msg: "Your wishlist is currently empty.",
    browse_shop_btn: "Browse Shop",
    card_explore: "Explore",
    card_add_to_cart: "Add to Cart",
    card_sold_out: "Sold Out",
    reviews_label: "reviews",
    in_stock_label: "In Stock",
    out_of_stock_label: "Out of Stock",
    modal_fragrance_pyramid: "Fragrance Pyramid",
    modal_top: "Top Notes:",
    modal_heart: "Heart Notes:",
    modal_base: "Base Notes:",
    modal_select_size: "Select Size",
    modal_view_ingredients: "View Ingredients",
    modal_add_to_cart: "Add to Cart",
    checkout_billing_title: "Billing Details",
    checkout_first_name: "First Name *",
    checkout_last_name: "Last Name *",
    checkout_email: "Email Address *",
    checkout_street: "Street Address *",
    checkout_city: "City *",
    checkout_zip: "ZIP / Postal Code *",
    checkout_shipping: "Shipping *",
    checkout_payment_title: "Payment",
    checkout_payment_desc: "Demo payment: Simulator mode. No real cards will be charged.",
    checkout_place_order: "Place Order",
    checkout_your_order: "Your Order",
    checkout_subtotal: "Subtotal:",
    checkout_shipping_row: "Shipping:",
    checkout_total: "Total:",
    order_success_title: "Order Placed Successfully",
    order_success_desc: "Thank you for choosing NOVA. Your order number is <strong>#REF</strong>. We have sent a confirmation email to EMAIL.",
    order_details: "Order Details",
    order_date: "Date:",
    order_ship_to: "Ship To:",
    order_total_paid: "Total Paid:",
    continue_shopping_btn: "Continue Shopping",
    footer_about: "An architectural study of pure botanical scents, handcrafted for the modern aesthetic enthusiast.",
    footer_nav_title: "Navigation",
    footer_shop_link: "Shop Collection",
    footer_families_title: "Olfactory Families",
    footer_pages_title: "Pages",
    footer_follow_title: "Follow Us",
    footer_newsletter_title: "Newsletter",
    footer_newsletter_desc: "Subscribe to receive previews of new seasonal distillations.",
    footer_rights: "&copy; 2026 NOVA Inc. All rights reserved.",
    footer_tagline: "ELEGANCE &bull; MINIMALISM &bull; PURITY",
    home_reviews_title: "What Our Clients Say",
    home_instagram_title: "Follow Us @NovaFragrances",
    home_instagram_shortcode_title: "Instagram Feed (Shortcode)",
    scroll_text: "SCROLL &rarr;",
    brand_nova: "NOVA",
    brand_le_labo: "Le Labo",
    brand_byredo: "Byredo",
    brand_diptyque: "Diptyque",
    brand_aesop: "Aesop",
    brand_chanel: "Chanel",
    brand_dior: "Dior",
    brand_giorgio_armani: "Giorgio Armani",
    brand_yves_saint_laurent: "Yves Saint Laurent",
    brand_gucci: "Gucci",
    brand_versace: "Versace",
    brand_calvin_klein: "Calvin Klein",
    brand_dolce_gabbana: "Dolce & Gabbana",
    brand_tom_ford: "Tom Ford",
    brand_hermes: "HermГЁs",
    brand_burberry: "Burberry",
    brand_lancome: "LancГґme",
    brand_paco_rabanne: "Paco Rabanne",
    brand_jean_paul_gaultier: "Jean Paul Gaultier",
    brand_hugo_boss: "Hugo Boss",
    brand_creed: "Creed",
    brand_jo_malone: "Jo Malone London",
    brand_maison_francis_kurkdjian: "Maison Francis Kurkdjian",
    pp_faq_title: "Frequently Asked Questions",
    pp_faq_q1: "What makes this fragrance unique?",
    pp_faq_a1: "Our fragrances are composed of the rarest botanicals, distilled in small batches for exceptional purity and longevity.",
    pp_faq_q2: "Is this product cruelty-free and vegan?",
    pp_faq_a2: "Yes, all our creations are 100% vegan and never tested on animals. We believe in sustainable, ethical luxury.",
    pp_faq_q3: "How long does the scent last?",
    pp_faq_a3: "Formulated as an Eau de Parfum, you can expect an intoxicating sillage that lasts between 8 to 12 hours depending on your skin chemistry.",
    pp_faq_q4: "What is your return policy?",
    pp_faq_a4: "We accept returns within 30 days of purchase for unused products in their original packaging.",
    admin_title: "WooCommerce Control Panel",
    admin_total_revenue: "Total Revenue",
    admin_orders_placed: "Orders Placed",
    admin_avg_order_value: "Avg. Order Value",
    admin_total_stock: "Total Stock Items",
    admin_orders_list: "WooCommerce Orders List",
    admin_th_order_id: "Order ID",
    admin_th_date: "Date",
    admin_th_customer: "Customer",
    admin_th_items: "Purchased Items",
    admin_th_total: "Total",
    admin_th_status: "Status",
    admin_th_actions: "Actions",
    admin_inventory_title: "WooCommerce Inventory & Price Control",
    admin_inventory_desc: "Edit prices and quantity stock levels. Click \"Update\" to apply to storefront immediately.",
    admin_th_image: "Image",
    admin_th_fragrance: "Fragrance",
    admin_th_price: "Price (100ml)",
    admin_th_stock_qty: "Stock Quantity",
    admin_th_availability: "Availability",
    admin_btn_update: "Update",
    review_1_text: "\"An absolute masterpiece of modern perfumery. The sandalwood note is so creamy and elegant, I constantly get stopped and asked what I'm wearing.\"",
    review_1_author: "Elena V.",
    review_2_text: "\"Minimalist perfection. The packaging is gorgeous, but the scent itself is complex, evolving beautifully throughout the day on my skin.\"",
    review_2_author: "Marcus T.",
    review_3_text: "\"I have finally found my signature scent. It's subtle yet incredibly long-lasting. The botanical purity really shines through in every spray.\"",
    review_3_author: "Sarah L.",
    review_4_text: "\"The customer service and unboxing experience were as luxurious as the perfume itself. Highly recommend to any niche fragrance lover.\"",
    review_4_author: "David K.",
    review_5_text: "\"It feels like wearing a customized piece of art. The sillage is just rightвЂ”noticeable but never overwhelming.\"",
    review_5_author: "Mia R.",
    review_6_text: "\"A truly unique olfactive experience. The dry down is powdery and sophisticated. I cannot wait to try the rest of the collection.\"",
    review_6_author: "Julian M.",
    review_7_text: "\"I am in love with the minimalist aesthetic and the pure, non-toxic ingredients. Finally a luxury brand that aligns with my values.\"",
    review_7_author: "Chloe T.",
    review_8_text: "\"Bought this as a gift for my wife and she was completely blown away. The scent is intoxicating and incredibly refined.\"",
    review_8_author: "James B.",
    mobile_sec_menu: "Menu",
    mobile_sec_actions: "Shopping",
    mobile_sec_languages: "Languages",
    nav_contact: "Contact",
    contact_header_title: "CONTACT US",
    contact_subtitle: "Get In Touch",
    contact_desc: "Have questions or want to request a bespoke fragrance consultation? Our concierge is here to assist you.",
    contact_address_label: "Address",
    contact_address_value: "C. Vedi, Armenia",
    contact_phone_label: "Phone",
    contact_email_label: "Email",
    contact_delivery_label: "Delivery",
    contact_delivery_value: "Delivery across all of Armenia",
    contact_form_name: "Name *",
    contact_form_email: "Email *",
    contact_form_subject: "Subject *",
    contact_form_message: "Message *",
    contact_form_submit: "Send Message",
    contact_form_name_placeholder: "Emma Watson",
    contact_form_email_placeholder: "emma@example.com",
    contact_form_subject_placeholder: "Fragrance consultation request",
    contact_form_message_placeholder: "Write your message here...",
    footer_policy_shipping: "Shipping & Returns",
    footer_policy_privacy: "Privacy & Terms",
    policy_shipping_header_title: "SHIPPING",
    policy_shipping_subtitle: "CUSTOMER SUPPORT",
    policy_shipping_title: "Shipping & Returns Policy",
    policy_shipping_sec1_title: "1. Shipping & Delivery",
    policy_shipping_sec1_desc1: "At NOVA, each fragrance is carefully packed to preserve its delicate botanical composition. We ship both locally across Armenia and internationally.",
    policy_shipping_sec1_desc2: "<strong>Yerevan Delivery:</strong> 1-2 business days (Free for orders over 20,000 AMD).<br><strong>Regional Armenia:</strong> 2-4 business days.<br><strong>International Delivery:</strong> 7-14 business days via registered postal service.",
    policy_shipping_sec2_title: "2. Return & Exchange Policy",
    policy_shipping_sec2_desc1: "Due to the personal and cosmetic nature of niche perfumery, we are only able to accept returns on products that are completely unopened, unused, and in their original sealed cellophane packaging within 14 days of delivery.",
    policy_shipping_sec2_desc2: "If you wish to initiate a return, please contact our concierge team at concierge@novafragrances.com before sending the package back.",
    policy_shipping_sec3_title: "3. Damaged or Faulty Items",
    policy_shipping_sec3_desc1: "If your bottle arrives damaged or leaking, please take a photograph immediately and contact us within 48 hours of receipt. We will arrange a replacement or full refund immediately.",
    policy_privacy_header_title: "PRIVACY",
    policy_privacy_subtitle: "LEGAL STATEMENT",
    policy_privacy_title: "Privacy Policy & Terms of Service",
    policy_privacy_sec1_title: "1. Information We Collect",
    policy_privacy_sec1_desc: "We collect basic billing, contact, and shipping information when you purchase from our collection or subscribe to our newsletter. This data is exclusively used to fulfill your orders and enhance your experience.",
    policy_privacy_sec2_title: "2. Payment Security",
    policy_privacy_sec2_desc: "All payments are processed securely through encrypted merchant gateways. NOVA does not store or have direct access to your credit card numbers or private financial details.",
    policy_privacy_sec3_title: "3. Terms of Use",
    policy_privacy_sec3_desc: "All content, imagery, and text on this platform are protected under trademark and intellectual property rights. Copying or redistribution is strictly prohibited without explicit written permission from NOVA.",
    wishlist_header_title: "WISHLIST",
    wishlist_subtitle: "MY FAVORITES",
    wishlist_view_page_btn: "View Wishlist Page",
    exp_subtitle: "Olfactive Science",
    exp_title: "The Olfactory Narrative",
    exp_desc: "Every NOVA fragrance is a dynamic, living composition that evolves on the skin over several hours. We follow the traditional three-tiered structure of French high perfumery, enhanced with botanical ingredients gathered from sustainable sources worldwide.",
    top_desc: "First impression (0-30 mins)",
    heart_desc: "Core personality (30 mins - 4 hours)",
    base_desc: "Sillage & longevity (4+ hours)",
    promises_title: "Conscious Luxury Principles",
    suggestions_title: "Complete the Ritual",
    search_no_results: "No olfactory notes found.",
    filter_scent_family: "Scent Family",
    filter_concentration: "Concentration",
    filter_gender: "Gender",
    filter_vibe: "Vibe / Occasion",
    filter_price: "Price",
    filter_size: "Size",
    filter_brand: "Brand",
    btn_apply_filters: "Apply Filters",
    btn_clear_all: "Clear All",
    btn_filters: "Filters",
    vibe_romantic: "Romantic / Evening",
    vibe_daily: "Daily / Fresh",
    vibe_professional: "Business / Elegant",
    vibe_cozy: "Warm / Cozy",
    vibe_mysterious: "Mysterious / Bold",
    my_account_subtitle: "CLIENT PORTAL",
    my_account_title: "My Account",
    acc_nav_dashboard: "Dashboard",
    acc_nav_wardrobe: "Scent Wardrobe",
    acc_nav_discovery: "Personal Discount",
    acc_nav_details: "Account Details",
    acc_nav_orders: "Order History",
    acc_dash_welcome: "Welcome to your private olfactory portal. From here, you can track your seasonal scent purchases, configure your delivery preferences, and manage your discovery credit.",
    acc_scent_profile: "Scent Profile Summary",
    acc_scent_profile_desc: "Based on your olfactory choices, your main preferences are:",
    acc_wardrobe_title: "My Scent Wardrobe",
    acc_discovery_title: "Personal Discount",
    acc_promo_tag: "DISCOVERY CLUB",
    acc_promo_title: "Convert Sample to Full-Bottle",
    acc_promo_desc: "Loved your samples? Apply your 4,000 AMD discovery credit toward any full-sized (100ml) bottle!",
    acc_promo_code_lbl: "YOUR UNIQUE CREDIT CODE:",
    admin_secure_portal: "SECURE PORTAL",
    admin_login_title: "WooCommerce Login",
    admin_pass_hint: "Hint: norayrnajaryann@gmail.com / K9$v!p2WbX",
    btn_admin_login: "Login",
    admin_view_frontend: "View Front End",
    admin_nav_orders: "Orders Manager",
    admin_nav_inventory: "Inventory Tracker",
    admin_nav_dev: "Developer Settings",
    admin_nav_logs: "Activity Logs",
    admin_nav_clients: "Clients",
    admin_clients_title: "Registered Clients",
    admin_clients_desc: "All users who have created an account on the website.",
    admin_th_name: "Name",
    admin_th_registered: "Registered",
    admin_th_actions: "Actions",
    acc_nav_wardrobe_sub: "View purchased bottles",
    acc_nav_discovery_sub: "Your exclusive brand code",
    acc_nav_orders_sub: "Track shipments",
    acc_nav_details_sub: "Edit addresses & email",
    acc_th_id: "ID",
    acc_th_date: "Date",
    acc_th_items: "Items",
    acc_th_total: "Total",
    acc_th_status: "Status",
    acc_th_invoice: "Invoice",
    acc_th_sample_kit: "Sample Kit",
    acc_th_purchase_date: "Purchase Date",
    acc_th_credit_value: "Credit Value",
    admin_staff_placeholder: "Name (e.g. Elena)",
    status_pending: "Pending",
    status_processing: "Processing",
    status_completed: "Completed",
    status_failed: "Failed",
    admin_btn_delete: "Delete",
    admin_btn_add_new: "Add New",
    plugin_pdf_label: "WooCommerce PDF Invoices (v2.1)",
    plugin_subs_label: "WooCommerce Subscriptions (v4.0)",
    admin_insta_location_placeholder: "e.g. Grasse, France",
    admin_insta_caption_placeholder: "Scent notes, details, etc.",
    admin_staff_locked: "Locked",
    admin_logs_empty: "No logs recorded.",
    admin_sales_perf: "Sales Performance by Fragrance Category",
    admin_dev_title: "Developer & System Settings",
    admin_dev_desc: "Manage platform configurations, core extensions, payment gateways, and staff security credentials.",
    admin_dev_plugins: "Core Extensions & Plugins",
    admin_dev_gateways: "Payment Gateways",
    gateway_stripe_label: "Stripe Checkout Integration",
    gateway_paypal_label: "PayPal Standard Express",
    gateway_cod_label: "Cash on Delivery (COD)",
    admin_dev_staff_manager: "Staff Profiles Manager",
    admin_dev_staff_desc: "Register or revoke backend access credentials for staff managers.",
    admin_btn_create_profile: "Create Profile",
    admin_th_staff_name: "Staff Name",
    admin_th_role: "Role",
    admin_logs_title: "Activity Logging System",
    admin_logs_desc: "Platform security audit log tracking credentials access, settings adjustments, pricing edits, and updates.",
    admin_logs_clear: "Clear Logs",
    admin_logs_th_timestamp: "Timestamp",
    admin_logs_th_operator: "Operator",
    admin_logs_th_action: "Recorded Action",
    admin_btn_install: "Install",
    acc_wardrobe_empty: "Your scent wardrobe is empty.",
    acc_discovery_empty: "No discovery samples purchased yet.",
    acc_orders_empty: "No orders placed yet.",
    bg_media: "MEDIA",
    insta_likes: "likes",
    insta_loc_1: "Grasse, France",
    insta_loc_2: "Kyoto, Japan",
    insta_loc_3: "Amalfi, Italy",
    insta_loc_4: "Paris, France",
    insta_loc_5: "London, United Kingdom",
    insta_loc_6: "Santorini, Greece",
    insta_loc_7: "Milan, Italy",
    insta_loc_8: "St. Tropez, France",
    insta_caption_1: "Santal NВ°01 captures the raw elegance of the Australian outback. Distilled in copper columns. вњЁ",
    insta_caption_2: "A delicate dance of cherry blossom and soft cashmere musk. Fleur de Cerisier is Kyoto in springtime. рџЊё",
    insta_caption_3: "A sun-drenched explosion of Sicilian bergamot and salted sea air. Agrumes Frais in hand. рџЌ‹",
    insta_caption_4: "Give the gift of olfactory poetry. Our signature discovery set wrapped in sustainable linen. рџЋЃ",
    insta_caption_5: "Pure botanical ingredients gathered from sustainable sources worldwide. Raw luxury. рџЊї",
    insta_caption_6: "An opulent, velvet blanket of golden amber and toasted spices at the golden hour. рџЊ…",
    insta_caption_7: "Every spray is a living composition that evolves on the skin over several hours. рџ’«",
    insta_caption_8: "Bracingly fresh VГ©tiver Blanc. The olfactive equivalent of a tailored white shirt. рџЊЉ",
    insta_view_comments_1: "View all 12 comments",
    insta_view_comments_2: "View all 8 comments",
    insta_view_comments_3: "View all 15 comments",
    insta_view_comments_4: "View all 9 comments",
    insta_view_comments_5: "View all 11 comments",
    insta_view_comments_6: "View all 14 comments",
    insta_date_1: "2 DAYS AGO",
    insta_date_2: "3 DAYS AGO",
    insta_date_3: "4 DAYS AGO",
    insta_date_4: "5 DAYS AGO",
    insta_date_5: "1 WEEK AGO",
    insta_date_6: "1 WEEK AGO",
    admin_nav_instagram: "Instagram Manager",
    admin_insta_title: "Instagram Feed Manager",
    admin_insta_desc: "Add new posts to the front page Instagram feed, edit details, or delete existing posts.",
    admin_insta_add_post: "Add New Post",
    admin_insta_location: "Location / Place *",
    admin_insta_caption: "Caption / Description *",
    admin_insta_date: "Post Date *",
    admin_insta_images: "Images (multiple allowed) *",
    admin_insta_active_posts: "Active Posts Feed",
    admin_th_thumbnail: "Thumbnail",
    admin_th_location: "Location",
    admin_th_caption: "Caption",
    admin_th_date: "Date",
    admin_btn_add_post: "Add Post",
    insta_feed_empty: "No posts yet. Add posts from the WooCommerce Control Panel.",
    modal_product_edit_title: "Edit Product Details",
    modal_lbl_name: "Product Name *",
    modal_lbl_brand: "Brand *",
    modal_lbl_tagline: "Tagline / Short Desc *",
    modal_lbl_desc: "Description *",
    modal_lbl_ingredients: "Ingredients",
    modal_lbl_image_url: "Photo (Image URL) *",
    modal_lbl_or_upload: "OR upload a file:",
    modal_lbl_family: "Family *",
    modal_lbl_concentration: "Type *",
    modal_lbl_gender: "Gender *",
    modal_lbl_stock: "Stock Quantity *",
    modal_lbl_notes_top: "Top Notes *",
    modal_lbl_notes_heart: "Heart Notes *",
    modal_lbl_notes_base: "Base Notes *",
    modal_btn_cancel: "Cancel",
    modal_btn_save: "Save Changes",
    home_video_title: "The Art of Rare Curation",
    home_video_paragraph: "Every drop of Nova is a masterpiece of precision and luxury. We source the worldвЂ™s finest premium ingredients, seamlessly blending rare elements to create an evolving sensory journey on your skin. As a premier niche fragrance brand in Armenia, Nova doesnвЂ™t just offer a scentвЂ”it crafts an unforgettable identity that lingers beautifully.",
    home_video_cta: "Learn More"
  }
};

const PRODUCT_TRANSLATIONS = {
  am: {
    "santal-01": {
      tagline: "Ф±ХѕХЅХїЦЂХЎХ¬Х«ХЎХЇХЎХ¶ ХЅХЎХ¶Х¤ХЎХ¬Х«, Х°Х«Х¬Х« Ц‡ ХЇХЎХ·ХѕХ« Х®Х­ХЎХЈХёЦ‚ХµХ¶, ХґХЎХЈХ¶Х«ХЅХЎХЇХЎХ¶ ХЅХїХёЦЂХЎХЈЦЂХёЦ‚Х©ХµХёЦ‚Х¶:",
      description: "Santal NВ°01-ХЁ ХґХҐЦЂ ХЅХїХёЦЂХЎХЈЦЂХёЦ‚Х©ХµХЎХ¶ ХЅХїХҐХІХ®ХЎХЈХёЦЂХ®ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ Х§: Ф±ХµХ¶ ХёЦЂХЅХёЦ‚Хґ Х§ ХЎХѕХЅХїЦЂХЎХ¬Х«ХЎХЇХЎХ¶ ХЎХ¶ХЎХєХЎХїХ« Х°ХёЦ‚Хґ Х§Х¬ХҐХЈХЎХ¶ХїХёЦ‚Х©ХµХёЦ‚Х¶ХЁ ХЇХҐХЅХЈХ«Х·ХҐЦЂХЎХµХ«Х¶ ХҐЦЂХЇХ¶Ц„Х« ХїХЎХЇ: ХЏХЎЦ„, Х№ХёЦЂ Ц‡ Х¦ХЈХЎХµХЎХЇХЎХ¶, ХЎХµХ¶ ЦѓХЎХ©ХЎХ©ХёЦ‚Хґ Х§ ХЇЦЂХёХІХ«Х¶ ХЇХЎХ·ХѕХ«, ХЎХґХўЦЂХЎХµХ« Ц‡ Х°ХЎХґХҐХґХёЦ‚Х¶Ц„Х¶ХҐЦЂХ« Х°ХЎЦЂХґХЎЦЂХЎХѕХҐХї, ХўХЎХµЦЃ Х­ХёЦЂХ°ЦЂХ¤ХЎХѕХёЦЂ ХЎХёЦ‚ЦЂХЎХµХёХѕХќ Х№ХёЦЂХЎХ¶ХЎХ¬ХёХѕ ХґХ«Х¶Х№Ц‡ ХЅХҐЦЂХёЦ‚ЦЃЦ„ХЎХµХ«Х¶, ХЇХЎХ©Х¶ХЎХµХ«Х¶ ЦѓХЎХµХїХЎХµХ¶ХёЦ‚Х©ХµХёЦ‚Х¶:",
      top: ["ХЂХ«Х¬", "Ф»ЦЂХ«ХЅ", "Х„ХЎХ¶ХёЦ‚Х·ХЎХЇ"],
      heart: ["ХЌХЎХ¶Х¤ХЎХ¬ХҐ ЦѓХЎХµХї", "ХЋХ«ЦЂХ»Х«Х¶ХµХЎХ¶ ХґХЎХµЦЂХ«", "ХЉХЎХєХ«ЦЂХёЦ‚ХЅ"],
      base: ["ФїХЎХ·Х«", "Ф±ХґХўЦЂХЎ", "Х„ХёЦ‚Х·ХЇ"]
    },
    "fleur-cerisier": {
      tagline: "ФІХЎХ¬ХҐХ¶ХёЦ‚ Х®ХЎХІХЇХ«, ХЅХєХ«ХїХЎХЇ Х©ХҐХµХ« Ц‡ Х¶ХёЦ‚ЦЂХў ХЇХЎХ·ХґХ«ЦЂХҐ ХґХёЦ‚Х·ХЇХ« Х¶ХёЦ‚ЦЂХў, ХҐХ©ХҐЦЂХЎХµХ«Х¶ ХєХЎЦЂ:",
      description: "Fleur de Cerisier-ХЁ ФїХ«ХёХїХёХµХёЦ‚Хґ ХЈХЎЦЂХ¶ХЎХ¶ХЎХµХ«Х¶ ХЄХЎХґХЎХ¶ХЎХЇХ« ХєХёХҐХїХ«ХЇ ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶Х¶ Х§: Х€ЦЂХЅХЎХ¬ХёХѕ ХїХЎЦ„ Ц„ХЎХґХёЦ‚ЦЃ ХєХїХїХѕХёХІ ХўХЎХ¬ХҐХ¶ХёЦ‚ Х®ХЎХІХ«ХЇХ¶ХҐЦЂХ« ХЎХ¶ЦЃХёХІХ«ХЇ ХЈХҐХІХҐЦЃХЇХёЦ‚Х©ХµХёЦ‚Х¶ХЁХќ ХЎХµХЅ ХўХёЦ‚ХµЦЂХЁ ХўХЎЦЃХѕХёЦ‚Хґ Х§ ХѕХЎХј, Х»ЦЂХЎХµХ«Х¶ Х¶ХёХїХЎХ¶ХҐЦЂХёХѕ, Х¶ХЎХ­Ц„ХЎХ¶ ЦѓХёХ·ХёХї Х®ХЎХІХЇХЎХµХ«Х¶ ХЅЦЂХїХ« ХґХҐХ» ХїХҐХІХЎХѕХёЦЂХѕХҐХ¬ХЁ, ХёЦЂХ¶ ХЎХґЦЂХЎЦЃХѕХЎХ® Х§ ХЇХЎХ·ХґХ«ЦЂХ« Ц‡ ХЅХєХ«ХїХЎХЇ ХґХёЦ‚Х·ХЇХ« Х°Х«ХґЦ„ХёХѕ:",
      top: ["ФІХҐЦЂХЈХЎХґХёХї", "ХЌХєХ«ХїХЎХЇ Х©ХҐХµ", "ХЏХЎХ¶Х±"],
      heart: ["ФІХЎХ¬ХҐХ¶ХёЦ‚ Х®ХЎХІХ«ХЇ", "Х–ЦЂХЎХ¶ХЅХ«ХЎХЇХЎХ¶ ХѕХЎЦЂХ¤", "ХЂХЎХЅХґХ«ХЇ"],
      base: ["ФїХЎХ·ХґХ«ЦЂХҐ ХґХёЦ‚Х·ХЇ", "ХЌХЎХ¶Х¤ХЎХ¬ХҐ ЦѓХЎХµХї", "ХЌХєХ«ХїХЎХЇ ХЎХґХўЦЂХЎ"]
    },
    "agrumes-frais": {
      tagline: "ХЌХ«ЦЃХ«Х¬Х«ХЎХЇХЎХ¶ ХўХҐЦЂХЈХЎХґХёХїХ«, ХЇХЎХ¶ХЎХ№ ХґХЎХ¶Х¤ХЎЦЂХ«Х¶Х« Ц‡ ХЎХІХ« Х®ХёХѕХЎХµХ«Х¶ Ц…Х¤Х« ХЎЦЂЦ‡ХёХѕ ХёХІХёХІХѕХЎХ® ХєХЎХµХ©ХµХёЦ‚Х¶:",
      description: "Agrumes Frais-ХЁ Х°ХЎЦЂХЈХЎХ¶Ц„Х« ХїХёЦ‚ЦЂЦ„ Х§ Ф±ХґХЎХ¬Ц†Х«Х« ХЎЦѓХ« ХЎЦЂЦ‡ХёХѕ ХёХІХёХІХѕХЎХ® ХЄХЎХµХјХҐЦЂХ«Х¶: ФїХҐХ¶ХЅХёЦ‚Х¶ХЎХЇ, ХЇХЎХµХ®ХЇХ¬ХїХёХІ Ц‡ Х©ХЎЦЂХґХЎЦЃХ¶ХёХІ, ХЎХµХ¶ ХЎЦЂХ©Х¶ХЎЦЃХ¶ХёЦ‚Хґ Х§ ЦЃХ«ХїЦЂХёЦ‚ХЅХЎХµХ«Х¶ ХЎХµХЈХ«Х¶ХҐЦЂХёХѕ ХЎХ¶ЦЃХ¶ХёХІ ХЎХјХЎХѕХёХїХµХЎХ¶ Х¦ХҐЦѓХµХёЦ‚ХјХ« Х©ХЎЦЂХґ Х¦ХЈХЎЦЃХёЦ‚ХґХЁ: Ф±ХІХ« Х®ХёХѕХЎХµХ«Х¶ ХЎХЇХёЦЂХ¤Х¶ХҐЦЂХЁ Х°ХЎХѕХЎХЅХЎЦЂХЎХЇХ·ХјХёЦ‚Хґ ХҐХ¶ ХѕХЎХј Х¶ХёХїХЎХ¶ХҐЦЂХЁ, Х«ХЅХЇ ХѕХҐХїХ«ХѕХҐЦЂХ¶ ХёЦ‚ ХЅХєХ«ХїХЎХЇ ХЎХґХўЦЂХЎХ¶ ХїХЎХ¬Х«ХЅ ХҐХ¶ ХЇХЎХµХёЦ‚Х¶ ХЇХЎХјХёЦ‚ЦЃХѕХЎХ®Ц„:",
      top: ["ХЌХ«ЦЃХ«Х¬Х«ХЎХЇХЎХ¶ ХўХҐЦЂХЈХЎХґХёХї", "ФїХЎХ¶ХЎХ№ ХґХЎХ¶Х¤ХЎЦЂХ«Х¶", "ФїХ«ХїЦЂХёХ¶Х« ХЇХҐХІЦ‡"],
      heart: ["ФѕХёХѕХ« ХЎХІХ« ХЎХЇХёЦЂХ¤", "Х†ХҐЦЂХёХ¬Х«", "ХЊХҐХ°ХЎХ¶"],
      base: ["ХЂХЎХµХ«Х©ХµХЎХ¶ ХѕХҐХїХ«ХѕХҐЦЂ", "ХЌХєХ«ХїХЎХЇ ХЎХґХўЦЂХЎ", "Х„ХЎХµЦЂХ«"]
    },
    "ambre-chaud": {
      tagline: "Х€ХЅХЇХҐ ХЎХґХўЦЂХЎХµХ«, ХїХЎЦ„ ХѕХЎХ¶Х«Х¬Х« Ц‡ ХїХЎХєХЎХЇХЎХ® Х°ХЎХґХҐХґХёЦ‚Х¶Ц„Х¶ХҐЦЂХ« Х·Ц„ХҐХІ, Х©ХЎХѕХ·ХµХЎ Х®ХЎХ®ХЇХёЦЃ:",
      description: "Ambre Chaud-ХЁ Х«Х¶ХїХҐХ¶ХЅХ«Хѕ, Х·Ц„ХҐХІ ХўХёЦ‚ХµЦЂ Х§, ХёЦЂХЁ Х¶ХЎХ­ХЎХїХҐХЅХѕХЎХ® Х§ ЦЃХёЦ‚ЦЂХї ХЎХґХ«ХЅХ¶ХҐЦЂХ« Ц‡ ХґХїХҐЦЂХґХ«ХЇ ХҐЦЂХҐХЇХёХ¶ХҐЦЂХ« Х°ХЎХґХЎЦЂ: Ф±ХµХ¶ ЦѓХЎХ©ХЎХ©ХёЦ‚Хґ Х§ ХґХЎХ·ХЇХЁ ХЎХґХўЦЂХЎХµХ« Ц‡ Ц„ХЎХІЦЃЦЂ, ХґХёЦ‚Х© ХѕХЎХ¶Х«Х¬Х« ХїХЎЦ„ ХЈЦЂХЇХёЦ‚Хґ, ХёЦЂХ¶ ХёЦ‚ХЄХҐХІХЎЦЃХѕХЎХ® Х§ ХЇХЎЦЂХ¤ХЎХґХёХ¶Х« Ц‡ ХґХ·ХЇХЁХ¶ХЇХёЦ‚ХµХ¦Х« Х­ХЎХјХ¶ХёЦ‚ЦЂХ¤ХёХѕ Ц‡ Х°ХҐХ¶ХѕХёЦ‚Хґ Х§ Х®Х­ХЎХ­ХёХїХ« ХїХҐЦЂЦ‡Х« Ц‡ Х¬ХЎХўХ¤ХЎХ¶ХёЦ‚ХґХ« ХҐЦЂХЇХЎЦЂХЎХїЦ‡ Х°Х«ХґЦ„Х« ХѕЦЂХЎ:",
      top: ["Х„Х·ХЇХЁХ¶ХЇХёЦ‚ХµХ¦", "ФїХёЦЂХ«ХЎХ¶Х¤ЦЂ", "ФґХЎЦЂХ№Х«Х¶"],
      heart: ["Х€ХЅХЇХҐ ХЎХґХўЦЂХЎ", "ФјХЎХўХ¤ХЎХ¶ХёЦ‚Хґ", "ХЉХЎХ№ХёЦ‚Х¬Х«"],
      base: ["ХЋХЎХ¶Х«Х¬", "ФѕХ­ХЎХ­ХёХїХ« ХїХҐЦЂЦ‡", "ХЌХ«ХЎХґХЎХЇХЎХ¶ ХўХҐХ¶Х¦ХёХ«Х¶"]
    },
    "figue-noire": {
      tagline: "Х„Х«Х»ХҐЦЂХЇЦЂХЎХЇХЎХ¶ ХЎЦЂЦ‡Х« ХїХЎХЇ Х°ХЎХЅХёЦ‚Х¶ Х©Х¦Х« ХґХёЦ‚ХЈ, ХЇХЎХ¶ХЎХ№ ХґХҐХЇХ¶ХЎХўХЎХ¶ХёЦ‚Х©ХµХёЦ‚Х¶:",
      description: "Figue Noire-ХЁ ХўХЎЦЃХѕХёЦ‚Хґ Х§ Х©Х¦Х« Х­ЦЂХ©Х­ЦЂХ©ХЎХ¶ ХїХҐЦЂЦ‡Х¶ХҐЦЂХ« ХєХЎХµХ©ХµХёЦ‚Х¶ХёХѕХќ ХЎЦЂХЎХЈХёЦЂХҐХ¶ ХўХЎЦЃХЎХ°ХЎХµХїХҐХ¬ХёХѕ ХўХёЦ‚Х¶ ХґЦЂХЈХ« ХґХёЦ‚ХЈ, Х»ХҐХґХЎХµХ«Х¶ Ц„ХЎХІЦЃЦЂХёЦ‚Х©ХµХёЦ‚Х¶ХЁ: ХЂХЎЦЂХґХЎЦЂХѕХҐХ¬ХёХѕХќ ХїХЎЦ„ ХґХЎХµЦЂХ« Ц‡ Х°ХёХІХҐХІХҐХ¶ ХѕХҐХїХ«ХѕХҐЦЂХЁ ХЎХґЦЂХЎЦЃХ¶ХёЦ‚Хґ ХҐХ¶ ХўХёЦ‚ХµЦЂХЁХќ ХЅХїХҐХІХ®ХҐХ¬ХёХѕ ХўХЎЦЂХ¤ ХЇХЎХ¶ХЎХ№ ХўХёЦ‚ХµЦЂ, ХёЦЂХЁ Ц‡Х› Х©ХЎЦЂХґ Х§, Ц‡Х› Х№ХЎЦѓХЎХ¦ХЎХ¶ЦЃ Х­ХёЦЂХЁ:",
      top: ["Ф№Х¦Х« ХїХҐЦЂЦ‡", "ФїХЎХ¶ХЎХ№ Х°ХµХёЦ‚Х©", "ФІХҐЦЂХЈХЎХґХёХї"],
      heart: ["ХЌЦ‡ Х©ХёЦ‚Х¦", "ФїХёХЇХёХЅХ« ХЇХЎХ©", "Ф»ЦЂХ«ХЅ"],
      base: ["Х„ХЎХµЦЂХ«", "ХЋХҐХїХ«ХѕХҐЦЂ", "ХЏХёХ¶ХЇХЎ Х°ХЎХїХ«ХЇ"]
    },
    "vetiver-blanc": {
      tagline: "ФґХЎХЅХЎХЇХЎХ¶ ХЂХЎХµХ«Х©ХµХЎХ¶ ХѕХҐХїХ«ХѕХҐЦЂХ« Х©ХЎЦЂХґ, ХґХЎЦ„ХёЦ‚ЦЂ Ц‡ ХЎХјХёЦ‚ХµХЈХЎЦЃХ¶ХёХІ ХґХёХїХҐЦЃХёЦ‚Хґ:",
      description: "VГ©tiver Blanc-ХЁ ХґХЎЦ„ЦЂХёЦ‚Хґ Х§ ХѕХҐХїХ«ХѕХҐЦЂХ« ХЎЦЂХґХЎХїХ¶ Х«ЦЂ ХЅХёХѕХёЦЂХЎХЇХЎХ¶ Х®Х­ХёХїХёЦ‚Х©ХµХёЦ‚Х¶Х«ЦЃХќ ЦѓХёХ­ХЎЦЂХҐХ¶ХЁ ХЇХҐХ¶ХїЦЂХёХ¶ХЎХ¶ХЎХ¬ХёХѕ Х¤ЦЂХЎ ХѕХЎХј, Х©ХёЦ‚ЦЂХ«Х¶Х»ХЎХ¶ХґХЎХ¶ ХЎХЅХєХҐХЇХїХ¶ХҐЦЂХ« ХѕЦЂХЎ: ХЂХЎХґХЎХ¤ЦЂХѕХЎХ® Х¶ХҐЦЂХёХ¬Х«Х« Ц‡ ХЅХєХ«ХїХЎХЇ ХґХёЦ‚Х·ХЇХ« Х°ХєХґХЎХ¶ Х°ХҐХїХќ ХЅХЎ ХЇХЎХїХЎЦЂХµХЎХ¬ Х°ХЎЦЂХґХЎЦЂХҐЦЃХѕХЎХ® Х­ЦЂХ©Х­ЦЂХ©ХЎХ¶ ХЅХєХ«ХїХЎХЇ ХѕХҐЦЂХ¶ХЎХ·ХЎХєХ«ХЇХ« Х°ХёХїХЎХјХЎХЇХЎХ¶ Х°ХЎХґХЎЦЂХЄХҐЦ„Х¶ Х§:",
      top: ["Ф№ХёЦ‚ЦЂХ«Х¶Х»", "Х†ХҐЦЂХёХ¬Х«", "ФІХҐЦЂХЈХЎХґХёХї"],
      heart: ["ХЂХЎХµХ«Х©ХµХЎХ¶ ХѕХҐХїХ«ХѕХҐЦЂ", "Х†ХЎЦЂХ¶Х»Х« Х®ХЎХІХ«ХЇ", "ХЋХЎЦЂХ¤ХЎХЈХёЦ‚ХµХ¶ ХєХІХєХҐХІ"],
      base: ["ХЌХєХ«ХїХЎХЇ ХґХёЦ‚Х·ХЇ", "Х„ХЎХµЦЂХ«", "Ф±ХґХўЦЂХёЦ„ХЅХЎХ¶"]
    },
    "rose-epicee": {
      tagline: "ФїЦЂХЎХЇХёХї, ХЄХЎХґХЎХ¶ХЎХЇХЎХЇХ«ЦЃ ХѕХЎЦЂХ¤Хќ ЦѓХЎХ©ХЎХ©ХѕХЎХ® Х¦ХЎЦ†ЦЂХЎХ¶ХёХѕ Ц‡ ХґХЎХ¶ЦЂХЎЦЃХѕХЎХ® ХЅЦ‡ ХєХІХєХҐХІХёХѕ:",
      description: "Rose Г‰picГ©e-Х¶ ХѕХҐЦЂЦЃХ¶ХёЦ‚Хґ Х§ Х¤ХЎХЅХЎХЇХЎХ¶ Х¤ХЎХґХЎХЅХЇХёХЅХµХЎХ¶ ХѕХЎЦЂХ¤ХЁ Ц‡ ХЇЦЂХЎХЇХ« Х§ ХїХЎХ¬Х«ХЅ ХЎХµХ¶: Ф¶ХЎЦ†ЦЂХЎХ¶ХёХѕ Ц‡ ХЅЦ‡ ХєХІХєХҐХІХёХѕ Х°ХЎХґХҐХґХѕХЎХ®Хќ ХЎХµХ¶ Х·ЦЂХ»ХЎХ¶ЦЃХёЦ‚Хґ Х§ ХЎХѕХЎХ¶Х¤ХЎХЇХЎХ¶ Х®ХЎХІХ«ХЇХ¶ХҐЦЂХ« ЦѓХёХ·ХёХї ХјХёХґХЎХ¶ХїХ«ХЇХЎХ¶Хќ ЦѓХёХ­ХЎЦЂХҐХ¶ХЁ ХґХЎХїХёЦ‚ЦЃХҐХ¬ХёХѕ ХЇХ®ХёЦ‚, ХѕХЅХїХЎХ° Ц‡ ХґХ«ХЎХ¶ХЈХЎХґХЎХµХ¶ ХЄХЎХґХЎХ¶ХЎХЇХЎХЇХ«ЦЃ ХЎЦЂХїХЎХ°ХЎХµХїХёЦ‚Х©ХµХёЦ‚Х¶:",
      top: ["Ф¶ХЎЦ†ЦЂХЎХ¶", "ХЌЦ‡ ХєХІХєХҐХІ", "ХЌХЎХ¬ХёЦЂ"],
      heart: ["ФґХЎХґХЎХЅХЇХёХЅХµХЎХ¶ ХѕХЎЦЂХ¤", "ХЉХЎХ№ХёЦ‚Х¬Х«", "Х„ХҐХ­ХЎХЇ"],
      base: ["Х€Ц‚Х¤", "ФїХЎХ·Х«", "ХЋХЎХ¶Х«Х¬"]
    },
    "musc-invisible": {
      tagline: "ХЋХҐЦЂХ»Х¶ХЎХЇХЎХ¶ В«Ц„Хё ХґХЎХ·ХЇХЁ, ХўХЎХµЦЃ ХЎХѕХҐХ¬Х« Х¬ХЎХѕВ» ХўХёЦ‚ХµЦЂ, ЦѓХЎЦѓХёЦ‚ХЇ Ц‡ ХґХїХҐЦЂХґХ«ХЇ:",
      description: "Musc Invisible-ХЁ Х¶ХЎХ­ХЎХЈХ®ХѕХЎХ® Х§ ХґХЎЦ„ХёЦ‚ЦЂ, ХїХЎЦ„ ХґХЎХ·ХЇХ« ХўХёЦ‚ХµЦЂХЁ ХЁХ¶Х¤Ц…ЦЂХ«Х¶ХЎХЇХҐХ¬ХёЦ‚ Х°ХЎХґХЎЦЂ: Ф±ХµХ¶ ХЎХ¶Х°ХЎХѕХЎХ¶ХЎХЇХЎХ¶ Х¶ХёЦ‚ЦЂХў Х§Хќ Х°Х«ХґХ¶ХѕХҐХ¬ХёХѕ ХЅХ«Х¶Х©ХҐХїХ«ХЇ ХґХёЦ‚Х·ХЇХҐЦЂХ«, ХЎХґХўЦЂХҐХїХїХЎХµХ« ХЅХҐЦЂХґХ« Ц‡ ХїХЎХ¶Х±Х« Х·Х·ХёЦ‚ХЇХ« ХўХЎЦЂХ¤ ХЎХЇХёЦЂХ¤Х« ХѕЦЂХЎХќ ХЅХїХҐХІХ®ХҐХ¬ХёХѕ ХўХёЦ‚ХµЦЂ, ХёЦЂХЁ Х°ХЎХґХЁХ¶Х¤Х°ХЎХ¶ХёЦ‚ЦЂ Х·ХёХµХёХІ Ц‡ ХЎХ¶ХѕХҐЦЂХ» Х°ХЎЦЂХґХЎЦЂХЎХѕХҐХї Х§:",
      top: ["Ф±ХґХўЦЂХҐХїХїХЎХµХ« ХЅХҐЦЂХґ", "ХЏХЎХ¶Х±", "Ф±Х¬Х¤ХҐХ°Х«Х¤Х¶ХҐЦЂ"],
      heart: ["ХЌХєХ«ХїХЎХЇ ХґХёЦ‚Х·ХЇ", "ХЂХЎХЅХґХ«ХЇХ« Х©ХҐЦЂХ©Х«ХЇХ¶ХҐЦЂ", "ФІХЎХґХўХЎХЇ"],
      base: ["ФїХЎХ·ХґХҐЦЂХЎХ¶", "ХЌХєХ«ХїХЎХЇ ЦѓХЎХµХїХҐЦЂ", "Iso E Super"]
    },
    "oud-absolu": {
      tagline: "ХЂХЎХ¦ХѕХЎХЈХµХёЦ‚Хї ХЎХЈХЎЦЂХ« ЦѓХЎХµХїХ« Ц‡ ХґХёЦ‚Х© Х­Х¶ХЇХ« Х°Х«ХєХ¶ХёХїХ«ХЇ, Х«Х¶ХїХҐХ¶ХЅХ«Хѕ Х©ХёЦЂХёЦ‚Хґ:",
      description: "Oud Absolu-Х¶ Х©ХёЦ‚ХµХ¬ХҐЦЂХ« Х°ХЎХґХЎЦЂ Х№Х§: ФґХЎ ХґХёЦ‚Х© ХЎХ¶ХїХЎХјХ¶ХҐЦЂХ« Х°Х¦ХёЦЂ, ХЎХ¶Х¦Х«Х»ХёЦ‚Хґ ХёЦ‚ХЅХёЦ‚ХґХ¶ХЎХЅХ«ЦЂХёЦ‚Х©ХµХёЦ‚Х¶ Х§: Х†ХҐЦЂХЎХјХҐХ¬ХёХѕ ХЇХЎХµХёЦ‚Х¶ ХЎХІХўХµХёЦ‚ЦЂХ¶ХҐЦЂХ«ЦЃ ХЅХїХЎЦЃХѕХЎХ® ХЇХЎХґХўХёХ»ХЎХЇХЎХ¶ ХЎХЈХЎЦЂХ« ЦѓХЎХµХїХќ ХЎХµХ¶ Х°ХЎЦЂХёЦ‚ХЅХї Х§, Х®Х­ХЎХЈХёЦ‚ХµХ¶ Ц‡ ЦѓХёЦ„ЦЂ-Х«Х¶Х№ ХўХЄХ·ХЇХЎХЇХЎХ¶Хќ 24 ХЄХЎХґХѕХЎ ХЁХ¶Х©ХЎЦЃЦ„ХёЦ‚Хґ ЦѓХЎЦѓХЇХҐХ¬ХёХѕ Х©ХЎХѕХ·ХµХЎ ХЎХґХўЦЂХЎХµХ« Х°Х«ХґЦ„ХёЦ‚Хґ:",
      top: ["ФЅХёЦ‚Х¶ХЇ", "ХЌЦ‡ ХєХІХєХҐХІ", "Ф·Х¬ХҐХґХ«"],
      heart: ["ФїХЎХґХўХёХ»ХЎХЇХЎХ¶ ХёЦ‚Х¤", "Х‘Х«ХєЦЂХ«ХёХ¬", "ФјХЎХўХ¤ХЎХ¶ХёЦ‚Хґ"],
      base: ["Ф±ХґХўЦЂХЎ", "ХЉХЎХ№ХёЦ‚Х¬Х«", "Х„ХёЦ‚Х© ХґХёЦ‚Х·ХЇ"]
    },
    "neroli-portofino": {
      tagline: "Ф±ЦЂЦ‡ХёХѕ ХёХІХёХІХѕХЎХ® ЦЃХ«ХїЦЂХёЦ‚ХЅ Ц‡ ХЅХєХ«ХїХЎХЇ Х®ХЎХІХ«ХЇХ¶ХҐЦЂХќ ХёЦЂХЅХЎХ¬ХёХѕ Х«ХїХЎХ¬ХЎХЇХЎХ¶ ХЎЦѓХЁ:",
      description: "NГ©roli Riviera-Х¶ Х·Х·ХЎХ¬ЦЃХѕХЎХ® ХЎЦЂЦ‡Х« Х¬ХёЦ‚ХµХЅ Х§: Ф±ХµХ¶ ХЇХЎХїХЎЦЂХҐХ¬ХЎХєХҐХЅ Х°ХЎХѕХЎХЅХЎЦЂХЎХЇХ·ХјХёЦ‚Хґ Х§ Х«ХїХЎХ¬ХЎХЇХЎХ¶ ХЇХ«ХїЦЂХёХ¶Х« Ц‡ Х¤ХЎХјХЁ Х¶ХЎЦЂХ¶Х»Х« ХїХїХ«Хє Х­ХЎХµХ©ХёЦЃХЁ Х¶ХҐЦЂХёХ¬Х«Х« Ц…ХіХЎХјХЎХµХ«Х¶, ХЇХЎХ¶ХЎХ№ Ц„ХЎХІЦЃЦЂХёЦ‚Х©ХµХЎХ¶ Х°ХҐХї: ХЂХ«ХґЦ„ХёЦ‚Хґ ХЎХґХўЦЂХЎХµХ« Х°ХєХёЦ‚ХґХ¶ ХЎХєХЎХ°ХёХѕХёЦ‚Хґ Х§, ХёЦЂ ХЎХµХЅ ХЎХґХЎХјХЎХµХ«Х¶ ХўХёЦ‚ХµЦЂХЁ ХҐЦЂХЇХЎЦЂ ХєХЎХ°ХєХЎХ¶ХѕХ« ХґХЎХµЦЂХЎХґХёЦ‚ХїХ«ЦЃ Х°ХҐХїХё:",
      top: ["Ф»ХїХЎХ¬ХЎХЇХЎХ¶ ХЇХ«ХїЦЂХёХ¶", "ФґХЎХјХЁ Х¶ХЎЦЂХ«Х¶Х»", "ФІХҐЦЂХЈХЎХґХёХї"],
      heart: ["Х†ХҐЦЂХёХ¬Х«", "Х†ХЎЦЂХ¶Х»Х« Х®ХЎХІХ«ХЇ", "ФјХЎХѕХЎХ¶Х¤ХЎ"],
      base: ["Ф±ХґХўЦЂХЎ", "ХЊХёХ¦ХґХЎЦЂХ«Х¶", "Х„ХёЦ‚Х·ХЇ"]
    }
  },
  ru: {
    "santal-01": {
      tagline: "Р”С‹РјРЅР°СЏ, РїСЂРёС‚СЏРіР°С‚РµР»СЊРЅР°СЏ РєРѕРјРїРѕР·РёС†РёСЏ РёР· Р°РІСЃС‚СЂР°Р»РёР№СЃРєРѕРіРѕ СЃР°РЅРґР°Р»Р°, РєР°СЂРґР°РјРѕРЅР° Рё РєРѕР¶Рё.",
      description: "Santal NВ°01 вЂ” С„РёСЂРјРµРЅРЅРѕРµ С‚РІРѕСЂРµРЅРёРµ. РџРµСЂРµРґР°РµС‚ РїРµСЂРІРѕР·РґР°РЅРЅСѓСЋ СЌР»РµРіР°РЅС‚РЅРѕСЃС‚СЊ Р°РІСЃС‚СЂР°Р»РёР№СЃРєРѕР№ РїСѓСЃС‚С‹РЅРё РїРѕРґ РїРѕР»СѓРЅРѕС‡РЅС‹Рј РЅРµР±РѕРј. РўРµРїР»С‹Р№, СЃСѓС…РѕР№ Рё С‡СѓРІСЃС‚РІРµРЅРЅС‹Р№, РѕРєСѓС‚С‹РІР°РµС‚ СѓСЋС‚РЅРѕР№ Р°СѓСЂРѕР№ РєРѕР¶Рё, Р°РјР±СЂС‹ Рё СЃРїРµС†РёР№.",
      top: ["РљР°СЂРґР°РјРѕРЅ", "РСЂРёСЃ", "Р¤РёР°Р»РєР°"],
      heart: ["РЎР°РЅРґР°Р»", "Р’РёСЂРґР¶РёРЅСЃРєРёР№ РєРµРґСЂ", "РџР°РїРёСЂСѓСЃ"],
      base: ["РљРѕР¶Р°", "РђРјР±СЂР°", "РњСѓСЃРєСѓСЃ"]
    },
    "fleur-cerisier": {
      tagline: "РќРµР¶РЅС‹Р№, РІРѕР·РґСѓС€РЅС‹Р№ С‚Р°РЅРµС† С†РІРµС‚СѓС‰РµР№ РІРёС€РЅРё, Р±РµР»РѕРіРѕ С‡Р°СЏ Рё РјСЏРіРєРѕРіРѕ РєР°С€РµРјРёСЂРѕРІРѕРіРѕ РјСѓСЃРєСѓСЃР°.",
      description: "Fleur de Cerisier вЂ” РїРѕСЌС‚РёС‡РµСЃРєР°СЏ РёРЅС‚РµСЂРїСЂРµС‚Р°С†РёСЏ РІРµСЃРЅС‹ РІ РљРёРѕС‚Рѕ. Р—Р°РїРµС‡Р°С‚Р»РµРІ РјРёРјРѕР»РµС‚РЅСѓСЋ РєСЂР°СЃРѕС‚Сѓ С†РІРµС‚РѕРІ РІРёС€РЅРё, РєСЂСѓР¶Р°С‰РёС…СЃСЏ РЅР° С‚РµРїР»РѕРј РІРµС‚СЂСѓ, СЌС‚РѕС‚ Р°СЂРѕРјР°С‚ РѕС‚РєСЂС‹РІР°РµС‚СЃСЏ РІРѕРґСЏРЅС‹РјРё РЅРѕС‚Р°РјРё.",
      top: ["Р‘РµСЂРіР°РјРѕС‚", "Р‘РµР»С‹Р№ С‡Р°Р№", "Р“СЂСѓС€Р°"],
      heart: ["Р¦РІРµС‚СѓС‰Р°СЏ РІРёС€РЅСЏ", "Р¤СЂР°РЅС†СѓР·СЃРєР°СЏ СЂРѕР·Р°", "Р–Р°СЃРјРёРЅ"],
      base: ["РљР°С€РµРјРёСЂРѕРІС‹Р№ РјСѓСЃРєСѓСЃ", "РЎР°РЅРґР°Р»", "Р‘РµР»Р°СЏ Р°РјР±СЂР°"]
    },
    "agrumes-frais": {
      tagline: "РќР°РїРѕР»РЅРµРЅРЅС‹Р№ СЃРѕР»РЅС†РµРј РІР·СЂС‹РІ СЃРёС†РёР»РёР№СЃРєРѕРіРѕ Р±РµСЂРіР°РјРѕС‚Р°, Р·РµР»РµРЅРѕРіРѕ РјР°РЅРґР°СЂРёРЅР° Рё СЃРѕР»РµРЅРѕРіРѕ РјРѕСЂСЃРєРѕРіРѕ РІРѕР·РґСѓС…Р°.",
      description: "Agrumes Frais вЂ” РґР°РЅСЊ СѓРІР°Р¶РµРЅРёСЏ Р·Р°Р»РёС‚С‹Рј СЃРѕР»РЅС†РµРј СѓС‚РµСЃР°Рј РїРѕР±РµСЂРµР¶СЊСЏ РђРјР°Р»СЊС„Рё. РЇСЂРєРёР№, РёСЃРєСЂСЏС‰РёР№СЃСЏ Рё РѕСЃРІРµР¶Р°СЋС‰РёР№, РѕРЅ РІРѕСЃСЃРѕР·РґР°РµС‚ РѕС‰СѓС‰РµРЅРёРµ СѓС‚СЂРµРЅРЅРµРіРѕ Р±СЂРёР·Р° РІ С†РёС‚СЂСѓСЃРѕРІРѕРј СЃР°РґСѓ.",
      top: ["РЎРёС†РёР»РёР№СЃРєРёР№ Р±РµСЂРіР°РјРѕС‚", "Р—РµР»РµРЅС‹Р№ РјР°РЅРґР°СЂРёРЅ", "Р›РёРјРѕРЅРЅР°СЏ С†РµРґСЂР°"],
      heart: ["РђРєРєРѕСЂРґ РјРѕСЂСЃРєРѕР№ СЃРѕР»Рё", "РќРµСЂРѕР»Рё", "Р‘Р°Р·РёР»РёРє"],
      base: ["Р“Р°РёС‚СЏРЅСЃРєРёР№ РІРµС‚РёРІРµСЂ", "Р‘РµР»Р°СЏ Р°РјР±СЂР°", "РљРµРґСЂ"]
    },
    "ambre-chaud": {
      tagline: "Р РѕСЃРєРѕС€РЅРѕРµ Р±Р°СЂС…Р°С‚РЅРѕРµ РѕРґРµСЏР»Рѕ РёР· Р·РѕР»РѕС‚РѕР№ Р°РјР±СЂС‹, С‚РµРїР»РѕР№ РІР°РЅРёР»Рё Рё РїСЂСЏРЅРѕСЃС‚РµР№.",
      description: "Ambre Chaud вЂ” РёРЅС‚РµРЅСЃРёРІРЅС‹Р№, СЂРѕСЃРєРѕС€РЅС‹Р№ Р°СЂРѕРјР°С‚, СЃРѕР·РґР°РЅРЅС‹Р№ РґР»СЏ С…РѕР»РѕРґРЅС‹С… РјРµСЃСЏС†РµРІ Рё РёРЅС‚РёРјРЅС‹С… РІРµС‡РµСЂРѕРІ. РћРЅ РѕРєСѓС‚С‹РІР°РµС‚ РєРѕР¶Сѓ С‚РµРїР»С‹Рј РѕР±СЉСЏС‚РёРµРј Р°РјР±СЂС‹ Рё СЃР»Р°РґРєРѕР№ С‚РµРјРЅРѕР№ РІР°РЅРёР»Рё.",
      top: ["РњСѓСЃРєР°С‚РЅС‹Р№ РѕСЂРµС…", "РљРѕСЂРёР°РЅРґСЂ", "РљРѕСЂРёС†Р°"],
      heart: ["Р—РѕР»РѕС‚Р°СЏ Р°РјР±СЂР°", "Р›Р°Р±РґР°РЅСѓРј", "РџР°С‡СѓР»Рё"],
      base: ["РЎС‚СЂСѓС‡РѕРє РІР°РЅРёР»Рё", "РўР°Р±Р°С‡РЅС‹Р№ Р»РёСЃС‚", "РЎРёР°РјСЃРєРёР№ Р±РµРЅР·РѕРёРЅ"]
    },
    "figue-noire": {
      tagline: "РўРµРјРЅР°СЏ, Р·РµР»РµРЅР°СЏ РёРЅС‚РµСЂРїСЂРµС‚Р°С†РёСЏ СЃРїРµР»РѕРіРѕ РёРЅР¶РёСЂР° РїРѕРґ СЃСЂРµРґРёР·РµРјРЅРѕРјРѕСЂСЃРєРёРј СЃРѕР»РЅС†РµРј.",
      description: "Figue Noire РѕС‚РєСЂС‹РІР°РµС‚СЃСЏ РІСЃРїР»РµСЃРєРѕРј С…СЂСѓСЃС‚СЏС‰РёС… С„РёРіРѕРІС‹С… Р»РёСЃС‚СЊРµРІ, Р±С‹СЃС‚СЂРѕ СЂР°СЃРєСЂС‹РІР°СЏ С‚РµРјРЅСѓСЋ РґР¶РµРјРѕРІСѓСЋ СЃР»Р°РґРѕСЃС‚СЊ СЃР°РјРѕРіРѕ С„СЂСѓРєС‚Р°. РЈСЃР°Р¶РёРІР°СЏСЃСЊ, С‚РµРїР»С‹Р№ РєРµРґСЂ Рё Р·РµРјР»РёСЃС‚С‹Р№ РІРµС‚РёРІРµСЂ Р·Р°РєСЂРµРїР»СЏСЋС‚ Р°СЂРѕРјР°С‚, СЃРѕР·РґР°РІР°СЏ СЃР»РѕР¶РЅС‹Р№ Р·РµР»РµРЅС‹Р№ РїР°СЂС„СЋРј, РѕРґРЅРѕРІСЂРµРјРµРЅРЅРѕ СЃРІРµР¶РёР№ Рё РіР»СѓР±РѕРєРёР№.",
      top: ["Р¤РёРіРѕРІС‹Р№ Р»РёСЃС‚", "Р—РµР»РµРЅС‹Р№ СЃРѕРє", "Р‘РµСЂРіР°РјРѕС‚"],
      heart: ["Р§РµСЂРЅС‹Р№ РёРЅР¶РёСЂ", "РљРѕРєРѕСЃРѕРІРѕРµ РјРѕР»РѕРєРѕ", "РСЂРёСЃ"],
      base: ["РљРµРґСЂ", "Р’РµС‚РёРІРµСЂ", "Р‘РѕР±С‹ С‚РѕРЅРєР°"]
    },
    "vetiver-blanc": {
      tagline: "РҐСЂСѓСЃС‚СЏС‰РёР№, С‡РёСЃС‚С‹Р№ Рё Р±РѕРґСЂСЏС‰Рµ СЃРІРµР¶РёР№ РІР·РіР»СЏРґ РЅР° РєР»Р°СЃСЃРёС‡РµСЃРєРёР№ РіР°РёС‚СЏРЅСЃРєРёР№ РІРµС‚РёРІРµСЂ.",
      description: "VГ©tiver Blanc Р»РёС€Р°РµС‚ РєРѕСЂРµРЅСЊ РІРµС‚РёРІРµСЂР° РµРіРѕ РѕР±С‹С‡РЅРѕР№ РґС‹РјРЅРѕСЃС‚Рё, СЃРѕСЃСЂРµРґРѕС‚Р°С‡РёРІР°СЏСЃСЊ РІРјРµСЃС‚Рѕ СЌС‚РѕРіРѕ РЅР° РµРіРѕ СЏСЂРєРёС…, РіСЂРµР№РїС„СЂСѓС‚РѕРІС‹С… РіСЂР°РЅСЏС…. Р’ СЃРѕС‡РµС‚Р°РЅРёРё СЃ РЅРµСЂРѕР»Рё Рё Р»РµРіРєРёРј РїСЂРёРєРѕСЃРЅРѕРІРµРЅРёРµРј Р±РµР»РѕРіРѕ РјСѓСЃРєСѓСЃР°, СЌС‚Рѕ РѕР»СЊС„Р°РєС‚РѕСЂРЅС‹Р№ СЌРєРІРёРІР°Р»РµРЅС‚ РёРґРµР°Р»СЊРЅРѕ СЃС€РёС‚РѕР№ Р±РµР»РѕР№ СЂСѓР±Р°С€РєРё.",
      top: ["Р“СЂРµР№РїС„СЂСѓС‚", "РќРµСЂРѕР»Рё", "Р‘РµСЂРіР°РјРѕС‚"],
      heart: ["Р“Р°РёС‚СЏРЅСЃРєРёР№ РІРµС‚РёРІРµСЂ", "Р¦РІРµС‚РѕРє Р°РїРµР»СЊСЃРёРЅР°", "Р РѕР·РѕРІС‹Р№ РїРµСЂРµС†"],
      base: ["Р‘РµР»С‹Р№ РјСѓСЃРєСѓСЃ", "РљРµРґСЂ", "РђРјР±СЂРѕРєСЃР°РЅ"]
    },
    "rose-epicee": {
      tagline: "РћРіРЅРµРЅРЅР°СЏ, СЃРѕРІСЂРµРјРµРЅРЅР°СЏ СЂРѕР·Р°, РѕРєСѓС‚Р°РЅРЅР°СЏ С€Р°С„СЂР°РЅРѕРј Рё С‚РѕР»С‡РµРЅС‹Рј С‡РµСЂРЅС‹Рј РїРµСЂС†РµРј.",
      description: "Rose Г‰picГ©e Р±РµСЂРµС‚ РєР»Р°СЃСЃРёС‡РµСЃРєСѓСЋ РґР°РјР°СЃСЃРєСѓСЋ СЂРѕР·Сѓ Рё РїРѕРґР¶РёРіР°РµС‚ РµРµ. РџСЂРёРїСЂР°РІР»РµРЅРЅС‹Р№ С€Р°С„СЂР°РЅРѕРј Рё С‡РµСЂРЅС‹Рј РїРµСЂС†РµРј, РѕРЅ РѕР±С…РѕРґРёС‚ РїСѓРґСЂРѕРІСѓСЋ СЂРѕРјР°РЅС‚РёРєСѓ С‚СЂР°РґРёС†РёРѕРЅРЅС‹С… С†РІРµС‚РѕС‡РЅС‹С… Р°СЂРѕРјР°С‚РѕРІ, С‡С‚РѕР±С‹ РІРјРµСЃС‚Рѕ СЌС‚РѕРіРѕ СЃРґРµР»Р°С‚СЊ РїСЂСЏРЅРѕРµ, СѓРІРµСЂРµРЅРЅРѕРµ Рё Р°Р±СЃРѕР»СЋС‚РЅРѕ СЃРѕРІСЂРµРјРµРЅРЅРѕРµ Р·Р°СЏРІР»РµРЅРёРµ.",
      top: ["РЁР°С„СЂР°РЅ", "Р§РµСЂРЅС‹Р№ РїРµСЂРµС†", "РЎР»РёРІР°"],
      heart: ["Р”Р°РјР°СЃСЃРєР°СЏ СЂРѕР·Р°", "РџР°С‡СѓР»Рё", "Р“РІРѕР·РґРёРєР°"],
      base: ["РЈРґ", "РљРѕР¶Р°", "Р’Р°РЅРёР»СЊ"]
    },
    "musc-invisible": {
      tagline: "РРґРµР°Р»СЊРЅС‹Р№ Р°СЂРѕРјР°С‚ В«РІР°С€Р° РєРѕР¶Р°, С‚РѕР»СЊРєРѕ Р»СѓС‡С€РµВ», РјСЏРіРєРёР№ Рё РёРЅС‚РёРјРЅС‹Р№.",
      description: "Musc Invisible СЃРѕР·РґР°РЅ, С‡С‚РѕР±С‹ РёРјРёС‚РёСЂРѕРІР°С‚СЊ Р·Р°РїР°С… С‡РёСЃС‚РѕР№, С‚РµРїР»РѕР№ РєРѕР¶Рё. РћРЅ РЅРµРІРµСЂРѕСЏС‚РЅРѕ РґРµР»РёРєР°С‚РµРЅ, РїРѕР»Р°РіР°СЏСЃСЊ РЅР° СЃР»РѕР¶РЅС‹Р№ Р°РєРєРѕСЂРґ СЃРёРЅС‚РµС‚РёС‡РµСЃРєРёС… РјСѓСЃРєСѓСЃРѕРІ, СЃРµРјСЏРЅ Р°РјР±СЂРµС‚С‚С‹ Рё С€РµРїРѕС‚ РіСЂСѓС€Рё, СЃРѕР·РґР°РІР°СЏ Р°СЂРѕРјР°С‚, РєРѕС‚РѕСЂС‹Р№ СѓРЅРёРІРµСЂСЃР°Р»СЊРЅРѕ РїСЂРёСЏС‚РµРЅ Рё Р±РµСЃРєРѕРЅРµС‡РЅРѕ СѓСЋС‚РµРЅ.",
      top: ["РЎРµРјРµРЅР° Р°РјР±СЂРµС‚С‚С‹", "Р“СЂСѓС€Р°", "РђР»СЊРґРµРіРёРґС‹"],
      heart: ["Р‘РµР»С‹Р№ РјСѓСЃРєСѓСЃ", "Р›РµРїРµСЃС‚РєРё Р¶Р°СЃРјРёРЅР°", "РҐР»РѕРїРѕРє"],
      base: ["РљР°С€РјРµСЂР°РЅ", "Р‘РµР»РѕРµ РґРµСЂРµРІРѕ", "Iso E Super"]
    },
    "oud-absolu": {
      tagline: "Р“РёРїРЅРѕС‚РёС‡РµСЃРєР°СЏ, РёРЅС‚РµРЅСЃРёРІРЅР°СЏ РґРёСЃС‚РёР»Р»СЏС†РёСЏ СЂРµРґРєРѕРіРѕ Р°РіР°СЂРѕРІРѕРіРѕ РґРµСЂРµРІР° Рё С‚РµРјРЅРѕРіРѕ Р»Р°РґР°РЅР°.",
      description: "Oud Absolu РЅРµ РґР»СЏ СЃР»Р°Р±РѕРЅРµСЂРІРЅС‹С…. Р­С‚Рѕ РјРѕС‰РЅРѕРµ, Р±РµСЃРєРѕРјРїСЂРѕРјРёСЃСЃРЅРѕРµ РёСЃСЃР»РµРґРѕРІР°РЅРёРµ СЃР°РјС‹С… С‚РµРјРЅС‹С… РґРµСЂРµРІСЊРµРІ. РЎ РёСЃРїРѕР»СЊР·РѕРІР°РЅРёРµРј РєР°РјР±РѕРґР¶РёР№СЃРєРѕРіРѕ СѓРґР°, РїРѕР»СѓС‡РµРЅРЅРѕРіРѕ РёР· СѓСЃС‚РѕР№С‡РёРІС‹С… РёСЃС‚РѕС‡РЅРёРєРѕРІ, РѕРЅ Р±РѕРіР°С‚, РґС‹РјРµРЅ Рё СЃР»РµРіРєР° Р»РµС‡РµР±РµРЅ, СЃРјСЏРіС‡Р°СЏСЃСЊ РІ Р±Р°СЂС…Р°С‚РЅСѓСЋ Р°РјР±СЂРѕРІСѓСЋ Р±Р°Р·Сѓ РІ С‚РµС‡РµРЅРёРµ 24 С‡Р°СЃРѕРІ.",
      top: ["Р›Р°РґР°РЅ", "Р§РµСЂРЅС‹Р№ РїРµСЂРµС†", "Р­Р»РµРјРё"],
      heart: ["РљР°РјР±РѕРґР¶РёР№СЃРєРёР№ СѓРґ", "Р¦РёРїСЂРёРѕР»", "Р›Р°Р±РґР°РЅСѓРј"],
      base: ["РђРјР±СЂР°", "РџР°С‡СѓР»Рё", "РўРµРјРЅС‹Р№ РјСѓСЃРєСѓСЃ"]
    },
    "neroli-portofino": {
      tagline: "Р—Р°Р»РёС‚С‹Рµ СЃРѕР»РЅС†РµРј С†РёС‚СЂСѓСЃРѕРІС‹Рµ Рё Р±РµР»С‹Рµ С†РІРµС‚С‹, РїРµСЂРµРґР°СЋС‰РёРµ РґСѓС… РёС‚Р°Р»СЊСЏРЅСЃРєРѕРіРѕ РїРѕР±РµСЂРµР¶СЊСЏ.",
      description: "NГ©roli Riviera вЂ” СЌС‚Рѕ СЃРѕР»РЅС†Рµ РІРѕ С„Р»Р°РєРѕРЅРµ. РћРЅ РёРґРµР°Р»СЊРЅРѕ СѓСЂР°РІРЅРѕРІРµС€РёРІР°РµС‚ С‚РµСЂРїРєСѓСЋ РіРѕСЂРµС‡СЊ РёС‚Р°Р»СЊСЏРЅСЃРєРѕРіРѕ Р»РёРјРѕРЅР° Рё РіРѕСЂСЊРєРѕРіРѕ Р°РїРµР»СЊСЃРёРЅР° СЃ РјС‹Р»СЊРЅРѕР№, Р·РµР»РµРЅРѕР№ СЃР»Р°РґРѕСЃС‚СЊСЋ РЅРµСЂРѕР»Рё. РџСЂРёРєРѕСЃРЅРѕРІРµРЅРёРµ Р°РјР±СЂС‹ РІ Р±Р°Р·Рµ РіР°СЂР°РЅС‚РёСЂСѓРµС‚, С‡С‚Рѕ СЌС‚РѕС‚ Р»РµС‚РЅРёР№ Р°СЂРѕРјР°С‚ СЃРѕС…СЂР°РЅРёС‚СЃСЏ РЅР°РґРѕР»РіРѕ РїРѕСЃР»Рµ Р·Р°РєР°С‚Р°.",
      top: ["РС‚Р°Р»СЊСЏРЅСЃРєРёР№ Р»РёРјРѕРЅ", "Р“РѕСЂСЊРєРёР№ Р°РїРµР»СЊСЃРёРЅ", "Р‘РµСЂРіР°РјРѕС‚"],
      heart: ["РќРµСЂРѕР»Рё", "Р¦РІРµС‚РѕРє Р°РїРµР»СЊСЃРёРЅР°", "Р›Р°РІР°РЅРґР°"],
      base: ["РђРјР±СЂР°", "Р РѕР·РјР°СЂРёРЅ", "РњСѓСЃРєСѓСЃ"]
    }
  },
  en: {
    "santal-01": {
      tagline: "A smoky, magnetic signature of Australian sandalwood, cardamom, and leather.",
      description: "Santal NВ°01 is our signature creation. It captures the raw elegance of the Australian outback under a midnight sky. Warm, dry, and sensuous, it envelopes the wearer in a comforting yet mysterious aura of leather, amber, and spice, drying down to an creamy, milky woodiness.",
      top: ["Cardamom", "Iris", "Violet"],
      heart: ["Sandalwood", "Virginia Cedar", "Papyrus"],
      base: ["Leather", "Amber", "Musk"]
    },
    "fleur-cerisier": {
      tagline: "A delicate, airy dance of cherry blossom, white tea, and soft cashmere musk.",
      description: "Fleur de Cerisier is a poetic interpretation of springtime in Kyoto. Capturing the fleeting beauty of cherry blossoms drifting on a warm breeze, this fragrance opens with bright, watery notes before settling into a soft, powdery floral heart, anchored by a comforting base of clean cashmere wood and white musk.",
      top: ["Bergamot", "White Tea", "Pear"],
      heart: ["Cherry Blossom", "French Rose", "Jasmine"],
      base: ["Cashmere Musk", "Sandalwood", "White Amber"]
    },
    "agrumes-frais": {
      tagline: "A sun-drenched explosion of Sicilian bergamot, green mandarin, and salted sea air.",
      description: "Agrumes Frais is a tribute to the sun-soaked cliffs of the Amalfi coast. Vibrant, sparkling, and refreshing, it evokes the crisp feeling of a morning breeze rustling through citrus groves. Salty marine accords balance the bright, zesty top notes, while clean vetiver and white amber give it a sophisticated, lasting structure.",
      top: ["Sicilian Bergamot", "Green Mandarin", "Lemon Zest"],
      heart: ["Sea Salt Accord", "Neroli", "Basil"],
      base: ["Haitian Vetiver", "White Amber", "Cedarwood"]
    },
    "ambre-chaud": {
      tagline: "An opulent, velvet blanket of golden amber, warm vanilla bean, and toasted spices.",
      description: "Ambre Chaud is an intense, luxurious fragrance designed for the colder months and intimate evenings. It wraps the skin in a warm, resinous embrace of amber and sweet, dark vanilla bean, elevated by a sophisticated blend of cardamon and nutmeg, and resting on a long-lasting base of tobacco leaf and rich labdanum.",
      top: ["Nutmeg", "Coriander", "Cinnamon"],
      heart: ["Golden Amber", "Labdanum", "Patchouli"],
      base: ["Vanilla Bean", "Tobacco Leaf", "Siam Benzoin"]
    },
    "figue-noire": {
      tagline: "A dark, green interpretation of ripe figs under the Mediterranean sun.",
      description: "Figue Noire opens with a burst of crisp fig leaves, quickly revealing the dark, jammy sweetness of the fruit itself. As it settles, warm cedarwood and earthy vetiver anchor the scent, creating a sophisticated green fragrance that feels both fresh and profoundly deep.",
      top: ["Fig Leaf", "Green Sap", "Bergamot"],
      heart: ["Black Fig", "Coconut Milk", "Iris"],
      base: ["Cedarwood", "Vetiver", "Tonka Bean"]
    },
    "vetiver-blanc": {
      tagline: "A crisp, clean, and bracingly fresh take on classic Haitian vetiver.",
      description: "VГ©tiver Blanc strips the vetiver root of its usual smokiness, focusing instead on its bright, grapefruit-like facets. Paired with neroli and a touch of white musk, it's the olfactive equivalent of a perfectly tailored crisp white shirt.",
      top: ["Grapefruit", "Neroli", "Bergamot"],
      heart: ["Haitian Vetiver", "Orange Blossom", "Pink Pepper"],
      base: ["White Musk", "Cedar", "Ambroxan"]
    },
    "rose-epicee": {
      tagline: "A fiery, modern rose wrapped in saffron and crushed black pepper.",
      description: "Rose Г‰picГ©e takes the classic Damask rose and sets it alight. Spiked with saffron and black pepper, it bypasses the powdery romance of traditional florals to deliver a spicy, confident, and utterly contemporary statement.",
      top: ["Saffron", "Black Pepper", "Plum"],
      heart: ["Damask Rose", "Patchouli", "Clove"],
      base: ["Oud", "Leather", "Vanilla"]
    },
    "musc-invisible": {
      tagline: "The ultimate 'your skin but better' fragrance, soft and intimate.",
      description: "Musc Invisible is designed to mimic the scent of clean, warm skin. It is incredibly delicate, relying on a complex accord of synthetic musks, ambrette seed, and a whisper of pear to create a fragrance that is universally flattering and endlessly comforting.",
      top: ["Ambrette Seed", "Pear", "Aldehydes"],
      heart: ["White Musk", "Jasmine Petals", "Cotton"],
      base: ["Cashmeran", "White Woods", "Iso E Super"]
    },
    "oud-absolu": {
      tagline: "A hypnotic, intense distillation of rare agarwood and dark incense.",
      description: "Oud Absolu is not for the faint of heart. It is a powerful, uncompromising exploration of the darkest woods. Featuring sustainably sourced Cambodian agarwood, it is rich, smoky, and slightly medicinal, softening into a velvety amber base over 24 hours.",
      top: ["Incense", "Black Pepper", "Elemi"],
      heart: ["Cambodian Oud", "Cypriol", "Labdanum"],
      base: ["Amber", "Patchouli", "Dark Musk"]
    },
    "neroli-portofino": {
      tagline: "Sun-drenched citrus and white florals capturing the Italian coast.",
      description: "NГ©roli Riviera is bottled sunshine. It perfectly balances the tart bite of Italian lemon and bitter orange with the soapy, green sweetness of neroli. A touch of amber in the base ensures this summery scent lingers long after sunset.",
      top: ["Italian Lemon", "Bitter Orange", "Bergamot"],
      heart: ["Neroli", "Orange Blossom", "Lavender"],
      base: ["Amber", "Rosemary", "Musk"]
    }
  }
};

// EmailJS Global Init
if (typeof emailjs !== 'undefined') {
  emailjs.init('bYE8lSm0rcSBnWLKb');
}

// Contact form submission via EmailJS
  const contactForm = document.getElementById('contact-us-form');
  if (contactForm) {
    const EMAILJS_SERVICE_ID = 'service_58z9fml';
    const EMAILJS_TEMPLATE_ID = 'template_0ej17sd';

    contactForm.addEventListener('submit', async (e) => {
      e.preventDefault();

      const submitBtn = contactForm.querySelector('button[type="submit"]');
      const originalText = submitBtn.innerText;

      // Get form values
      const name = document.getElementById('contact-name').value.trim();
      const email = document.getElementById('contact-email').value.trim();
      const subject = document.getElementById('contact-subject').value.trim();
      const message = document.getElementById('contact-message').value.trim();

      // Show loading state
      submitBtn.disabled = true;
      submitBtn.innerText = AppState.language === 'am' ? '\u0548\u0552\u0542\u0531\u054C\u053F\u054E\u0548\u0552\u0544 \u0537...' :
                            AppState.language === 'ru' ? '\u041E\u0422\u041F\u0420\u0410\u0412\u041A\u0410...' : 'SENDING...';

      try {
        if (typeof emailjs === 'undefined') {
          throw new Error('EmailJS not loaded');
        }

        // Send contact email to NOVA
        await emailjs.send(EMAILJS_SERVICE_ID, EMAILJS_TEMPLATE_ID, {
          from_name: name,
          from_email: email,
          subject: subject,
          message: message,
          to_email: 'novagiftshoparmenia@gmail.com'
        });

        // Success
        let successMsg = 'THANK YOU FOR CONTACTING NOVA. OUR CONCIERGE WILL REPLY WITHIN 24 HOURS.';
        if (AppState.language === 'am') {
          successMsg = '\u0547\u0546\u0548\u054c\u0540\u0531\u053f\u0531\u053c\u0548\u0552\u0539\u0545\u0548\u0552\u0546 NOVA-\u053b\u0546 \u0534\u053b\u0544\u0535\u053c\u0548\u0552 \u0540\u0531\u0544\u0531\u0550\u0589 \u0544\u0535\u054c \u053f\u0548\u0546\u054d\u0545\u0535\u054c\u053e\u0538 \u053f\u054a\u0531\u054f\u0531\u054d\u053d\u0531\u0546\u053b 24 \u053a\u0531\u0544\u054e\u0531 \u0538\u0546\u0539\u0531\u0551\u0554\u0548\u0552\u0544\u0589';
        } else if (AppState.language === 'ru') {
          successMsg = '\u0421\u041f\u0410\u0421\u0418\u0411\u041e \u0417\u0410 \u041e\u0411\u0420\u0410\u0429\u0415\u041d\u0418\u0415 \u0412 NOVA. \u041d\u0410\u0428 \u041a\u041e\u041d\u0421\u042c\u0415\u0420\u0416 \u041e\u0422\u0412\u0415\u0422\u0418\u0422 \u0412 \u0422\u0415\u0427\u0415\u041d\u0418\u0415 24 \u0427\u0410\u0421\u041e\u0412.';
        }
        showToast(successMsg);
        contactForm.reset();
      } catch (error) {
        console.error('EmailJS Error:', error);
        let errorMsg = 'SORRY, SOMETHING WENT WRONG. PLEASE TRY AGAIN OR EMAIL US DIRECTLY.';
        if (AppState.language === 'am') {
          errorMsg = '\u0546\u0535\u054c\u0548\u0542\u0548\u0552\u0539\u0545\u0548\u0552\u0546 \u0540\u0531\u0545\u0531\u054c\u0589 \u053d\u0546\u0534\u054c\u0565\u056c \u0553\u0578\u0580\u0571\u0565\u056c \u056f\u0580\u056f\u056b\u0576\u0589';
        } else if (AppState.language === 'ru') {
          errorMsg = '\u0418\u0417\u0412\u0418\u041d\u0418\u0422\u0415, \u041f\u0420\u041e\u0418\u0417\u041e\u0428\u041b\u0410 \u041e\u0428\u0418\u0411\u041a\u0410. \u041f\u041e\u041f\u0420\u041e\u0411\u0423\u0419\u0422\u0415 \u0415\u0429\u0401 \u0420\u0410\u0417.';
        }
        showToast(errorMsg);
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerText = originalText;
      }
    });
  }

// DEFAULT MOCK INSTAGRAM FEED SEED DATA
const DEFAULT_INSTAGRAM_POSTS = [
  {
    id: "insta-post-1",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "Grasse, France",
    caption: "Santal NВ°01 captures the raw elegance of the Australian outback. Distilled in copper columns. вњЁ",
    date: new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_1.webp"],
    likes: 420
  },
  {
    id: "insta-post-2",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "Kyoto, Japan",
    caption: "A delicate dance of cherry blossom and soft cashmere musk. Fleur de Cerisier is Kyoto in springtime. рџЊё",
    date: new Date(Date.now() - 3 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_2.webp"],
    likes: 315
  },
  {
    id: "insta-post-3",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "Amalfi, Italy",
    caption: "A sun-drenched explosion of Sicilian bergamot and salted sea air. Agrumes Frais in hand. рџЌ‹",
    date: new Date(Date.now() - 4 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_3.webp"],
    likes: 512
  },
  {
    id: "insta-post-4",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "Paris, France",
    caption: "Give the gift of olfactory poetry. Our signature discovery set wrapped in sustainable linen. рџЋЃ",
    date: new Date(Date.now() - 5 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_4.webp"],
    likes: 289
  },
  {
    id: "insta-post-5",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "London, United Kingdom",
    caption: "Pure botanical ingredients gathered from sustainable sources worldwide. Raw luxury. рџЊї",
    date: new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_5.webp"],
    likes: 340
  },
  {
    id: "insta-post-6",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "Santorini, Greece",
    caption: "An opulent, velvet blanket of golden amber and toasted spices at the golden hour. рџЊ…",
    date: new Date(Date.now() - 8 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_6.webp"],
    likes: 476
  },
  {
    id: "insta-post-7",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "Milan, Italy",
    caption: "Every spray is a living composition that evolves on the skin over several hours. рџ’«",
    date: new Date(Date.now() - 10 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_7.webp"],
    likes: 612
  },
  {
    id: "insta-post-8",
    username: "nova_giftshop",
    avatar: "assets/Favicon.svg",
    location: "St. Tropez, France",
    caption: "Bracingly fresh VГ©tiver Blanc. The olfactive equivalent of a tailored white shirt. рџЊЉ",
    date: new Date(Date.now() - 12 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    images: ["assets/insta_8.webp"],
    likes: 395
  }
];

// Helper: add computed getters to product objects
function addProductGetters(productsArray) {
  productsArray.forEach(p => {
    // Only add if not already a getter
    const catDesc = Object.getOwnPropertyDescriptor(p, 'category');
    if (!catDesc || typeof catDesc.get !== 'function') {
      Object.defineProperty(p, 'category', {
        get() {
          const fam = window.GLOBAL_ATTRIBUTES.scent_families[p.scent_family];
          return fam ? fam.label.en : "";
        },
        configurable: true,
        enumerable: true
      });
    }
    const genDesc = Object.getOwnPropertyDescriptor(p, 'gender');
    if (!genDesc || typeof genDesc.get !== 'function') {
      Object.defineProperty(p, 'gender', {
        get() {
          const g = window.GLOBAL_ATTRIBUTES.genders[p.gender_id];
          return g ? g.label.en : "";
        },
        configurable: true,
        enumerable: true
      });
    }
  });
}

// APP STATE
const AppState = {
  products: [...INITIAL_PRODUCTS], // Will be overwritten by Firestore data on init
  cart: [],
  wishlist: [], // Wishlist product IDs
  instagramPosts: [...DEFAULT_INSTAGRAM_POSTS], // Will be overwritten by Firestore data on init
  currentRoute: 'home', // 'home', 'shop', 'checkout', 'admin', 'my-account'
  language: localStorage.getItem('nova_lang_choice') || 'am', // Default language is Armenian ('am')
  filters: {
    category: 'all',
    search: '',
    sort: 'default',
    brand: '',
    gender: '',
    tag: '',
    scent_families: [],
    concentrations: [],
    genders: [],
    vibes: [],
    sizes: [],
    brands: [],
    priceRange: { min: 0, max: 400 }
  },
  selectedProduct: null,
  selectedSize: '100ml', // default size selection
  customer: {
    firstName: 'Norayr',
    lastName: 'Najaryan',
    email: 'norayrnajaryann@gmail.com',
    billing: {
      street: '221B Baker St',
      city: 'London',
      zip: 'NW1 6XE'
    },
    pastPurchases: [],
    discoverySamples: [],
    orders: []
  },
  outOfStockNotifications: []
};

window.saveProductsToStorage = async function() {
  console.log('[NOVA] saveProductsToStorage called with', AppState.products.length, 'products');
  await NovaDB.saveProducts(AppState.products);
};

let TempMobileFilters = {};

function toggleFilterValue(arr, val) {
  const idx = arr.indexOf(val);
  if (idx > -1) {
    arr.splice(idx, 1);
  } else {
    arr.push(val);
  }
}

// HELPERS FOR LOCALIZED STRINGS
function getTranslatedCategory(category) {
  const catMap = {
    Woody: { am: "Х“ХЎХµХїХЎХµХ«Х¶", ru: "Р”СЂРµРІРµСЃРЅС‹Рµ", en: "Woody" },
    Floral: { am: "ФѕХЎХІХЇХЎХµХ«Х¶", ru: "Р¦РІРµС‚РѕС‡РЅС‹Рµ", en: "Floral" },
    Citrus: { am: "Х‘Х«ХїЦЂХёЦ‚ХЅХЎХµХ«Х¶", ru: "Р¦РёС‚СЂСѓСЃРѕРІС‹Рµ", en: "Citrus" },
    Amber: { am: "Ф±ХґХўЦЂХЎХµХ«Х¶", ru: "РђРјР±СЂРѕРІС‹Рµ", en: "Amber" }
  };
  const key = category.trim();
  if (catMap[key]) {
    return catMap[key][AppState.language] || category;
  }
  return category;
}

function getTranslatedProduct(productId) {
  if (PRODUCT_TRANSLATIONS[AppState.language]) {
    return PRODUCT_TRANSLATIONS[AppState.language][productId];
  }
  return null;
}

// DOM CACHE
const DOM = {
  // Navigation
  navLinks: document.querySelectorAll('[data-route]'),
  routeViews: {
    home: document.getElementById('view-home'),
    shop: document.getElementById('view-shop'),
    checkout: document.getElementById('view-checkout'),
    admin: document.getElementById('view-admin'),
    about: document.getElementById('view-about'),
    contact: document.getElementById('view-contact'),
    'policy-shipping': document.getElementById('view-policy-shipping'),
    'policy-privacy': document.getElementById('view-policy-privacy'),
    wishlist: document.getElementById('view-wishlist'),
    'my-account': document.getElementById('view-my-account')
  },

  // Shop Elements
  shopProductsGrid: document.getElementById('shop-products-grid'),
  featuredProductsGrid: document.getElementById('featured-products-grid'),
  filterButtons: document.querySelectorAll('.filter-btn'),
  sortSelect: document.getElementById('sort-select'),
  searchInput: document.getElementById('search-input'),
  searchContainer: document.querySelector('.search-container'),

  // Cart Drawer Elements
  cartDrawerOverlay: document.getElementById('cart-drawer-overlay'),
  cartItemsContainer: document.getElementById('cart-items-container'),
  cartSubtotal: document.getElementById('cart-subtotal'),
  cartCountBadge: document.getElementById('cart-count-badge'),

  // Wishlist Drawer Elements
  wishlistDrawerOverlay: document.getElementById('wishlist-drawer-overlay'),
  wishlistItemsContainer: document.getElementById('wishlist-items-container'),
  wishlistCountBadge: document.getElementById('wishlist-count-badge'),
  headerWishlistIcon: document.getElementById('header-wishlist-icon'),
  wishlistHeaderBtn: document.getElementById('wishlist-toggle-btn'),

  // Product Detail Modal
  modalOverlay: document.getElementById('modal-overlay'),
  modalCloseBtn: document.getElementById('modal-close-btn'),
  modalWishlistBtn: document.getElementById('modal-wishlist-btn'),

  // Toast
  toast: document.getElementById('toast-notification')
};

// INITIALIZATION
window.addEventListener('DOMContentLoaded', async () => {
  // --- FIREBASE: Load all shared data from Firestore before rendering ---
  try {
    await NovaDB.init();
    console.log('[NOVA] Firestore data loaded successfully');

    // Load products from Firestore (or seed if first run)
    const firestoreProducts = NovaDB.getProducts();
    console.log('[NOVA] Firestore products:', firestoreProducts ? firestoreProducts.length : 'null');
    if (firestoreProducts !== null) {
      // Document exists in Firestore вЂ” use it (even if empty, that means admin deleted all)
      AppState.products = firestoreProducts;
      addProductGetters(AppState.products);
      console.log('[NOVA] Loaded', AppState.products.length, 'products from Firestore');
    } else {
      // First run only: no products doc in Firestore yet вЂ” seed with defaults
      addProductGetters(INITIAL_PRODUCTS);
      NovaDB.saveProducts(INITIAL_PRODUCTS);
      console.log('[NOVA] Seeded Firestore with', INITIAL_PRODUCTS.length, 'initial products');
    }

    // Load orders from Firestore
    const firestoreOrders = NovaDB.getOrders();
    if (firestoreOrders) {
      WooCommerceAdmin.orders = firestoreOrders;
    }

    // Load Instagram posts from Firestore
    const firestoreInsta = NovaDB.getInstagramPosts();
    if (firestoreInsta && firestoreInsta.length > 0) {
      AppState.instagramPosts = firestoreInsta;
    } else {
      NovaDB.saveInstagramPosts(DEFAULT_INSTAGRAM_POSTS);
    }
  } catch (e) {
    console.warn('[NOVA] Firestore init failed, using defaults:', e);
  }

  const isProductPage = window.location.pathname.endsWith('/product') || window.location.pathname.endsWith('/product.html');
  if (isProductPage) {
    initProductPage();
  } else {
    initRouter();
  }
  initScrollHeader();
  initMegaMenu();
  initBrandsMegaMenu();
  initEventListeners();
  loadCartFromStorage();
  loadWishlistFromStorage();

  // Initialize default language
  changeLanguage(AppState.language);
});

// LANGUAGE CHANGE ACTION
window.changeLanguage = function (lang) {
  AppState.language = lang;
  localStorage.setItem('nova_lang_choice', lang);

  // Update active state on language buttons
  document.querySelectorAll('.lang-btn').forEach(btn => {
    if (btn.getAttribute('data-lang') === lang) {
      btn.classList.add('active');
    } else {
      btn.classList.remove('active');
    }
  });

  // Loop through all data-trans elements and translate them
  document.querySelectorAll('[data-trans]').forEach(el => {
    const key = el.getAttribute('data-trans');
    if (TRANSLATIONS[lang] && TRANSLATIONS[lang][key]) {
      el.innerHTML = TRANSLATIONS[lang][key];
    }
  });

  // Loop through all data-trans-placeholder elements and translate them
  document.querySelectorAll('[data-trans-placeholder]').forEach(el => {
    const key = el.getAttribute('data-trans-placeholder');
    if (TRANSLATIONS[lang] && TRANSLATIONS[lang][key]) {
      el.placeholder = TRANSLATIONS[lang][key];
    }
  });

  // Translate placeholders and other dynamic inputs
  const searchInput = document.getElementById('search-input');
  if (searchInput) {
    searchInput.placeholder = TRANSLATIONS[lang]['search_placeholder'] || 'Search olfactory note...';
  }

  const newsletterInput = document.querySelector('.newsletter-input');
  if (newsletterInput) {
    newsletterInput.placeholder = TRANSLATIONS[lang]['newsletter_placeholder'] || 'Your email address';
  }

  const staffInput = document.getElementById('new-staff-name');
  if (staffInput) {
    staffInput.placeholder = TRANSLATIONS[lang]['admin_staff_placeholder'] || 'Name (e.g. Elena)';
  }

  // Re-render components that are dynamically built
  renderFeaturedProducts();
  if (typeof renderBrandSlider === 'function') renderBrandSlider();
  if (typeof renderFilterWidgets === 'function') {
    renderFilterWidgets('desktop-sidebar-filters', false);
  }
  renderShop();
  updateCartUI();
  updateWishlistUI();
  initMegaMenu(); // Re-seed mega menu elements so their product names/taglines translate!
  initBrandsMegaMenu();

  // Update header dropdown greeting with actual customer name
  document.querySelectorAll('[data-trans="hello_user"]').forEach(el => {
    el.innerText = lang === 'am' ? `Х€ХІХ»ХёЦ‚ХµХ¶, ${AppState.customer.firstName}` :
                   lang === 'ru' ? `РџСЂРёРІРµС‚, ${AppState.customer.firstName}` :
                   `Hello, ${AppState.customer.firstName}`;
  });

  // Translate plugin buttons dynamically based on their state (class)
  const pdfBtn = document.getElementById('plugin-btn-pdf');
  if (pdfBtn) {
    if (pdfBtn.classList.contains('btn-secondary')) {
      pdfBtn.innerText = lang === 'am' ? "Ф±ХєХЎХїХҐХІХЎХ¤ЦЂХҐХ¬" : lang === 'ru' ? "РЈРґР°Р»РёС‚СЊ" : "Uninstall";
    } else {
      pdfBtn.innerText = lang === 'am' ? "ХЏХҐХІХЎХ¤ЦЂХҐХ¬" : lang === 'ru' ? "РЈСЃС‚Р°РЅРѕРІРёС‚СЊ" : "Install";
    }
  }
  const subsBtn = document.getElementById('plugin-btn-subs');
  if (subsBtn) {
    if (subsBtn.classList.contains('btn-secondary')) {
      subsBtn.innerText = lang === 'am' ? "Ф±ХєХЎХїХҐХІХЎХ¤ЦЂХҐХ¬" : lang === 'ru' ? "РЈРґР°Р»РёС‚СЊ" : "Uninstall";
    } else {
      subsBtn.innerText = lang === 'am' ? "ХЏХҐХІХЎХ¤ЦЂХҐХ¬" : lang === 'ru' ? "РЈСЃС‚Р°РЅРѕРІРёС‚СЊ" : "Install";
    }
  }

  // If we are on checkout, refresh order summary translation
  if (AppState.currentRoute === 'checkout') {
    renderCheckoutPage();
  }

  // If we are on customer account page, refresh it to translate empty states and welcome text
  if (AppState.currentRoute === 'my-account') {
    renderMyAccount();
  }

  // If we are on admin dashboard, refresh it to translate stats grids, tables, logs, etc.
  if (AppState.currentRoute === 'admin') {
    refreshAdminDashboard();
  }

  // If we are on wishlist, refresh wishlist page translation
  if (AppState.currentRoute === 'wishlist') {
    renderWishlistPage();
  }

  // If we are on product page, refresh product details translation dynamically
  const isProductPage = window.location.pathname.endsWith('/product') || window.location.pathname.endsWith('/product.html');
  if (isProductPage) {
    const params = new URLSearchParams(window.location.search);
    const productId = params.get('id');
    if (productId) {
      renderProductPage(productId);
    }
  }

  // Render Instagram dynamic feed on language change
  renderInstagramFeed();
};


// ROUTING SYSTEM (Hash-based)
function initRouter() {
  const handleRouteChange = () => {
    const hash = window.location.hash || '#/home';
    let route = hash.replace('#/', '');

    // Fallback if route does not exist
    if (!DOM.routeViews[route]) {
      route = 'home';
    }

    AppState.currentRoute = route;

    // Toggle View Sections
    Object.keys(DOM.routeViews).forEach(key => {
      if (key === route) {
        DOM.routeViews[key].classList.remove('hidden');
      } else {
        DOM.routeViews[key].classList.add('hidden');
      }
    });

    // Update Nav Active States
    DOM.navLinks.forEach(link => {
      if (link.getAttribute('href') === `#/${route}`) {
        link.classList.add('active');
      } else {
        link.classList.remove('active');
      }
    });

    // Scroll to top on page change
    window.scrollTo(0, 0);

    // If entering admin dashboard, verify session and redirect if needed
    if (route === 'admin') {
      checkAdminSession();
    }

    // If entering wishlist page, populate it
    if (route === 'wishlist') {
      renderWishlistPage();
    }

    // If entering customer account dashboard, refresh it
    if (route === 'my-account') {
      renderMyAccount();
    }

    // If entering shop page, populate sidebar filters
    if (route === 'shop') {
      if (typeof renderFilterWidgets === 'function') {
        renderFilterWidgets('desktop-sidebar-filters', false);
      }
      renderShop();
    }
  };

  window.addEventListener('hashchange', handleRouteChange);
  handleRouteChange(); // Trigger on initial load
}

// SCROLL HEADER TRANSITION SYSTEM
function initScrollHeader() {
  const header = document.querySelector('header');
  if (!header) return;

  window.addEventListener('scroll', () => {
    const currentScroll = window.scrollY;

    // Trigger after leaving the hero section (around 400px down)
    if (currentScroll > 400) {
      if (!header.classList.contains('header-scrolled')) {
        // Hide instantly
        header.classList.add('header-out');
        // Force reflow
        header.offsetHeight;
        // Slide down styled header
        header.classList.add('header-scrolled');
        header.classList.remove('header-out');
      }
    }
    // Only remove sticky header when scrolling back up near the top (below 100px)
    else if (currentScroll < 100) {
      if (header.classList.contains('header-scrolled')) {
        header.classList.remove('header-scrolled');
        header.classList.remove('header-out');
      }
    }
  });
}

// PRODUCTS MEGA MENU HOVER SYSTEM
function initMegaMenu() {
  const catItems = document.querySelectorAll('.mega-cat-item');
  const panel = document.getElementById('mega-products-panel');
  if (!panel) return;

  const renderMegaCategory = (categoryName) => {
    panel.innerHTML = '';

    let filtered = [];
    if (categoryName === 'New' || categoryName === 'Gift') {
      filtered = AppState.products.filter(p => p.tags && p.tags.includes(categoryName));
    } else if (categoryName === 'Men' || categoryName === 'Women') {
      filtered = AppState.products.filter(p => p.gender === categoryName || p.gender === 'Unisex');
    } else {
      filtered = AppState.products.filter(p => p.category.toLowerCase() === categoryName.toLowerCase());
    }

    if (filtered.length === 0) {
      panel.innerHTML = `<div style="grid-column: 1/-1; color: var(--color-medium-gray); font-size: 0.8rem;">No products.</div>`;
      return;
    }

    filtered.forEach(prod => {
      const item = document.createElement('div');
      item.className = 'mega-product-item';
      item.onclick = () => {
        openProductModal(prod.id);
      };
      item.innerHTML = `
        <img src="${prod.image}" alt="${prod.name}" class="mega-product-thumb">
        <div class="mega-product-info">
          <span class="mega-product-name">${prod.name}</span>
          <span class="mega-product-tagline">${prod.tagline}</span>
        </div>
      `;
      panel.appendChild(item);
    });
  };

  catItems.forEach(item => {
    item.addEventListener('mouseenter', () => {
      catItems.forEach(i => i.classList.remove('active'));
      item.classList.add('active');
      const cat = item.getAttribute('data-mega-cat');
      renderMegaCategory(cat);
    });

    item.addEventListener('click', () => {
      const cat = item.getAttribute('data-mega-cat');
      if (cat === 'New' || cat === 'Gift') {
        applyPresetFilter('tag', cat);
      } else if (cat === 'Men' || cat === 'Women') {
        applyPresetFilter('gender', cat);
      } else {
        applyPresetFilter('category', cat);
      }
    });
  });

  // Initial populate with Woody notes
  renderMegaCategory('Woody');
}

// BRANDS MEGA MENU HOVER SYSTEM
function initBrandsMegaMenu() {
  const brandItems = document.querySelectorAll('#brands-mega-categories .mega-cat-item');
  const panel = document.getElementById('brands-mega-products-panel');
  if (!panel) return;

  const renderBrandsMegaCategory = (brandName) => {
    panel.innerHTML = '';

    const filtered = AppState.products.filter(p => p.brand.toLowerCase() === brandName.toLowerCase());

    if (filtered.length === 0) {
      panel.innerHTML = `<div style="grid-column: 1/-1; color: var(--color-medium-gray); font-size: 0.8rem;">No products.</div>`;
      return;
    }

    filtered.forEach(prod => {
      const item = document.createElement('div');
      item.className = 'mega-product-item';
      item.onclick = () => {
        openProductModal(prod.id);
      };
      item.innerHTML = `
        <img src="${prod.image}" alt="${prod.name}" class="mega-product-thumb">
        <div class="mega-product-info">
          <span class="mega-product-name">${prod.name}</span>
          <span class="mega-product-tagline">${prod.tagline}</span>
        </div>
      `;
      panel.appendChild(item);
    });
  };

  brandItems.forEach(item => {
    item.addEventListener('mouseenter', () => {
      brandItems.forEach(i => i.classList.remove('active'));
      item.classList.add('active');
      const brand = item.getAttribute('data-mega-brand');
      renderBrandsMegaCategory(brand);
    });

    item.addEventListener('click', () => {
      const brand = item.getAttribute('data-mega-brand');
      applyPresetFilter('brand', brand);
    });
  });

  // Initial populate with NOVA
  renderBrandsMegaCategory('NOVA');
}

// EVENT LISTENERS SETUP
function initEventListeners() {
  // Mobile Filter Drawer Toggle triggers
  const mobileToggle = document.getElementById('mobile-filter-toggle-btn');
  const drawerOverlay = document.getElementById('filter-drawer-overlay');
  const drawerClose = document.getElementById('filter-drawer-close-btn');
  const applyBtnMobile = document.getElementById('filter-apply-btn-mobile');
  const clearBtnMobile = document.getElementById('filter-clear-btn-mobile');

  if (mobileToggle) {
    mobileToggle.addEventListener('click', () => {
      // Deep copy filters
      TempMobileFilters = JSON.parse(JSON.stringify(AppState.filters));
      renderFilterWidgets('mobile-drawer-filters', true);
      drawerOverlay.classList.add('active');
      document.body.style.overflow = 'hidden'; // prevent page scroll
    });
  }

  if (drawerClose) {
    drawerClose.addEventListener('click', () => {
      drawerOverlay.classList.remove('active');
      document.body.style.overflow = '';
    });
  }

  if (drawerOverlay) {
    drawerOverlay.addEventListener('click', (e) => {
      if (e.target === drawerOverlay) {
        drawerOverlay.classList.remove('active');
        document.body.style.overflow = '';
      }
    });
  }

  if (applyBtnMobile) {
    applyBtnMobile.addEventListener('click', () => {
      AppState.filters = JSON.parse(JSON.stringify(TempMobileFilters));
      drawerOverlay.classList.remove('active');
      document.body.style.overflow = '';
      renderFilterWidgets('desktop-sidebar-filters', false);
      renderShop();
    });
  }

  if (clearBtnMobile) {
    clearBtnMobile.addEventListener('click', () => {
      TempMobileFilters.scent_families = [];
      TempMobileFilters.concentrations = [];
      TempMobileFilters.genders = [];
      TempMobileFilters.vibes = [];
      TempMobileFilters.sizes = [];
      TempMobileFilters.brands = [];
      TempMobileFilters.priceRange = { min: 0, max: 400 };
      TempMobileFilters.search = '';
      renderFilterWidgets('mobile-drawer-filters', true);
    });
  }

  // Sort dropdown change
  const sortDropdown = document.getElementById('custom-sort-dropdown');
  if (sortDropdown) {
    const trigger = sortDropdown.querySelector('#sort-dropdown-trigger');
    const valueSpan = sortDropdown.querySelector('#sort-dropdown-value');
    const items = sortDropdown.querySelectorAll('.dropdown-item');

    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      sortDropdown.classList.toggle('active');
    });

    document.addEventListener('click', () => {
      sortDropdown.classList.remove('active');
    });

    items.forEach(item => {
      item.addEventListener('click', (e) => {
        e.stopPropagation();
        const value = item.getAttribute('data-value');
        const transKey = item.getAttribute('data-trans');

        AppState.filters.sort = value;

        items.forEach(i => i.classList.remove('active'));
        item.classList.add('active');

        valueSpan.setAttribute('data-trans', transKey);
        valueSpan.innerText = TRANSLATIONS[AppState.language][transKey] || item.innerText;

        sortDropdown.classList.remove('active');
        renderShop();
      });
    });
  }

  // Init search popup system
  initSearch();

  // Close Modal trigger
  if (DOM.modalOverlay) {
    DOM.modalOverlay.addEventListener('click', (e) => {
      if (e.target === DOM.modalOverlay) {
        closeProductModal();
      }
    });
  }

  if (DOM.modalCloseBtn) {
    DOM.modalCloseBtn.addEventListener('click', closeProductModal);
  }

  // Cart Drawer open/close triggers
  document.getElementById('cart-toggle-btn').addEventListener('click', openCartDrawer);
  document.getElementById('cart-drawer-close').addEventListener('click', closeCartDrawer);
  DOM.cartDrawerOverlay.addEventListener('click', (e) => {
    if (e.target === DOM.cartDrawerOverlay) {
      closeCartDrawer();
    }
  });

  // Wishlist Drawer open/close triggers
  if (DOM.wishlistHeaderBtn) {
    DOM.wishlistHeaderBtn.addEventListener('click', openWishlistDrawer);
  }
  const wishlistCloseBtn = document.getElementById('wishlist-drawer-close');
  if (wishlistCloseBtn) {
    wishlistCloseBtn.addEventListener('click', closeWishlistDrawer);
  }
  if (DOM.wishlistDrawerOverlay) {
    DOM.wishlistDrawerOverlay.addEventListener('click', (e) => {
      if (e.target === DOM.wishlistDrawerOverlay) {
        closeWishlistDrawer();
      }
    });
  }

  // Newsletter form submission
  const newsletterForm = document.querySelector('.newsletter-form');
  if (newsletterForm) {
    newsletterForm.addEventListener('submit', (e) => {
      e.preventDefault();
      const email = newsletterForm.querySelector('input').value;
      if (email) {
        showToast("THANK YOU FOR SUBSCRIBING TO NOVA JOURNEY.");
        newsletterForm.reset();
      }
    });
  }

  // Checkout shipping change
  const shippingSelect = document.getElementById('shipping-method');
  if (shippingSelect) {
    shippingSelect.addEventListener('change', () => {
      updateCheckoutTotals();
    });
  }

  // Custom Checkout Shipping Dropdown Events
  const checkoutShipping = document.getElementById('checkout-shipping-dropdown');
  if (checkoutShipping) {
    const trigger = checkoutShipping.querySelector('#checkout-shipping-trigger');
    const valueSpan = checkoutShipping.querySelector('#checkout-shipping-value');
    const items = checkoutShipping.querySelectorAll('.dropdown-item');

    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      checkoutShipping.classList.toggle('active');
    });

    document.addEventListener('click', () => {
      checkoutShipping.classList.remove('active');
    });

    items.forEach(item => {
      item.addEventListener('click', (e) => {
        e.stopPropagation();
        const value = item.getAttribute('data-value');

        if (shippingSelect) {
          shippingSelect.value = value;
          shippingSelect.dispatchEvent(new Event('change'));
        }

        items.forEach(i => i.classList.remove('active'));
        item.classList.add('active');

        valueSpan.innerText = item.innerText;
        valueSpan.setAttribute('data-value', value);

        checkoutShipping.classList.remove('active');
      });
    });
  }

  // Checkout form submit
  const checkoutForm = document.getElementById('checkout-billing-form');
  if (checkoutForm) {
    checkoutForm.addEventListener('submit', (e) => {
      e.preventDefault();
      processCheckout();
    });
  }



  // Language selectors click event
  document.querySelectorAll('.lang-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      const selectedLang = e.target.getAttribute('data-lang');
      changeLanguage(selectedLang);
    });
  });

  // Mobile Menu Drawer triggers
  const mobileToggleBtn = document.getElementById('mobile-menu-toggle-btn');
  if (mobileToggleBtn) {
    mobileToggleBtn.addEventListener('click', openMobileMenu);
  }
  const mobileCloseBtn = document.getElementById('mobile-menu-drawer-close');
  if (mobileCloseBtn) {
    mobileCloseBtn.addEventListener('click', closeMobileMenu);
  }
  const mobileOverlay = document.getElementById('mobile-menu-drawer-overlay');
  if (mobileOverlay) {
    mobileOverlay.addEventListener('click', (e) => {
      if (e.target === mobileOverlay) {
        closeMobileMenu();
      }
    });
  }

}

// RENDER FEATURED PRODUCTS ON HOME PAGE
function renderFeaturedProducts() {
  renderSliders();
  if (!DOM.featuredProductsGrid) return;
  DOM.featuredProductsGrid.innerHTML = '';

  const featured = AppState.products.filter(p => p.featured);

  featured.forEach(product => {
    DOM.featuredProductsGrid.appendChild(createProductCard(product));
  });
}

// HORIZONTAL SLIDERS RENDER
function renderSliders() {
  const bestSellersTrack = document.getElementById('best-sellers-track');
  const newArrivalsTrack = document.getElementById('new-arrivals-track');

  if (bestSellersTrack) {
    bestSellersTrack.innerHTML = '';
    const bestSellers = AppState.products.filter(p => p.tags && p.tags.includes('Best Seller'));
    const bsBadge = TRANSLATIONS[AppState.language]['badge_bestseller'] || 'BEST SELLER';
    bestSellers.forEach(product => {
      bestSellersTrack.appendChild(createSliderProductCard(product, bsBadge));
    });
  }

  if (newArrivalsTrack) {
    newArrivalsTrack.innerHTML = '';
    const newArrivals = AppState.products.filter(p => p.tags && p.tags.includes('New'));
    const npBadge = TRANSLATIONS[AppState.language]['badge_new_product'] || 'NEW PRODUCT';
    newArrivals.forEach(product => {
      newArrivalsTrack.appendChild(createSliderProductCard(product, npBadge));
    });
  }
}

function createSliderProductCard(product, badgeText) {
  const card = document.createElement('div');
  card.className = 'product-card slider-product-card';
  card.onclick = () => openProductModal(product.id);

  const isWishlisted = AppState.wishlist.includes(product.id);
  const wishlistClass = isWishlisted ? 'active' : '';

  const transProd = getTranslatedProduct(product.id);
  const tagline = transProd ? transProd.tagline : product.tagline;
  const translatedCategory = getTranslatedCategory(product.category);

  const exploreLabel = TRANSLATIONS[AppState.language]['card_explore'] || 'Explore';
  const cartLabel = product.stock > 0
    ? (TRANSLATIONS[AppState.language]['card_add_to_cart'] || 'Add to Cart')
    : (TRANSLATIONS[AppState.language]['card_sold_out'] || 'Sold Out');

  const bsBadge = TRANSLATIONS[AppState.language]['badge_bestseller'] || 'BEST SELLER';
  const badgeClass = badgeText === bsBadge ? 'tag-bestseller' : 'tag-new';

  card.innerHTML = `
    <div class="product-card-image-wrap">
      ${badgeText ? `<div class="card-badge ${badgeClass}">${badgeText}</div>` : ''}
      <img src="${product.image}" alt="${product.name}" class="product-card-image" loading="lazy">
      
      <button class="card-wishlist ${wishlistClass}" onclick="event.stopPropagation(); toggleWishlist('${product.id}'); this.classList.toggle('active');" aria-label="Add to Wishlist">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
      </button>

      <div class="product-card-actions">
        <button class="btn-secondary product-card-action-btn" onclick="event.stopPropagation(); quickAddToCart('${product.id}')" ${product.stock <= 0 ? 'disabled' : ''}>
          ${cartLabel}
        </button>
      </div>
    </div>
    <div class="product-card-category">${translatedCategory} &bull; ${product.brand}</div>
    <h3 class="product-card-name serif-title">${product.name}</h3>
    <p class="product-card-tagline">${tagline}</p>
    <div class="product-card-footer">
      <span class="product-card-price">ЦЏ${product.price}</span>
      <div class="product-card-rating">
        <span class="rating-star">в…</span>
        <span>${product.rating}</span>
      </div>
    </div>
  `;

  card.onclick = (e) => {
    if (!e.target.closest('button')) {
      openProductModal(product.id);
    }
  };
  return card;
}

window.scrollSlider = function (trackId, amount) {
  const track = document.getElementById(trackId);
  if (track) {
    track.scrollBy({ left: amount, behavior: 'smooth' });
  }
};

// RENDER CATALOG SHOP GRID
// RENDER CATALOG SHOP GRID
function renderShop() {
  if (!DOM.shopProductsGrid) return;

  // Fade out product grid to simulate AJAX load
  DOM.shopProductsGrid.style.transition = 'opacity 0.2s ease';
  DOM.shopProductsGrid.style.opacity = '0.3';

  setTimeout(() => {
    DOM.shopProductsGrid.innerHTML = '';

    let filtered = AppState.products;

    // Filter Scent Family (Multi-Select)
    if (AppState.filters.scent_families && AppState.filters.scent_families.length > 0) {
      filtered = filtered.filter(p => AppState.filters.scent_families.includes(p.scent_family));
    }

    // Filter Concentration (Multi-Select)
    if (AppState.filters.concentrations && AppState.filters.concentrations.length > 0) {
      filtered = filtered.filter(p => AppState.filters.concentrations.includes(p.concentration));
    }

    // Filter Gender (Multi-Select)
    if (AppState.filters.genders && AppState.filters.genders.length > 0) {
      filtered = filtered.filter(p => AppState.filters.genders.includes(p.gender_id));
    }

    // Filter Vibe (Multi-Select)
    if (AppState.filters.vibes && AppState.filters.vibes.length > 0) {
      filtered = filtered.filter(p => p.vibes && p.vibes.some(v => AppState.filters.vibes.includes(v)));
    }

    // Filter Size (Multi-Select)
    if (AppState.filters.sizes && AppState.filters.sizes.length > 0) {
      filtered = filtered.filter(p => p.sizes && p.sizes.some(s => AppState.filters.sizes.includes(s.size)));
    }

    // Filter Brand (Multi-Select)
    if (AppState.filters.brands && AppState.filters.brands.length > 0) {
      filtered = filtered.filter(p => AppState.filters.brands.some(b => b.toLowerCase() === p.brand.toLowerCase()));
    }

    // Filter Tags Preset
    if (AppState.filters.tag) {
      const requestedTag = AppState.filters.tag.toLowerCase();
      filtered = filtered.filter(p => p.tags.some(t => t.toLowerCase() === requestedTag));
    }

    // Filter Search Query
    if (AppState.filters.search) {
      const query = AppState.filters.search.toLowerCase();
      filtered = filtered.filter(p =>
        p.name.toLowerCase().includes(query) ||
        p.tagline.toLowerCase().includes(query) ||
        (GLOBAL_ATTRIBUTES.scent_families[p.scent_family]?.label[AppState.language] || "").toLowerCase().includes(query) ||
        p.brand.toLowerCase().includes(query)
      );
    }

    // Filter Price Range
    if (AppState.filters.priceRange) {
      filtered = filtered.filter(p => {
        return p.price >= AppState.filters.priceRange.min && p.price <= AppState.filters.priceRange.max;
      });
    }

    // Sort Logic
    if (AppState.filters.sort === 'price-low-high') {
      filtered.sort((a, b) => a.price - b.price);
    } else if (AppState.filters.sort === 'price-high-low') {
      filtered.sort((a, b) => b.price - a.price);
    } else if (AppState.filters.sort === 'rating') {
      filtered.sort((a, b) => b.rating - a.rating);
    } else {
      // Default restore array sorting by id
      filtered.sort((a, b) => a.id.localeCompare(b.id));
    }

    // Visual header tags on Shop view to clarify active presets
    renderActivePresetIndicators();

    if (filtered.length === 0) {
      const searchNoResultsText = TRANSLATIONS[AppState.language]['search_no_results'] || 'No fragrances found matching your filters.';
      DOM.shopProductsGrid.innerHTML = `
        <div style="grid-column: 1/-1; text-align: center; padding: 40px 0; color: var(--color-medium-gray);">
          <p>${searchNoResultsText}</p>
        </div>
      `;
    } else {
      filtered.forEach(product => {
        DOM.shopProductsGrid.appendChild(createProductCard(product));
      });
    }

    // Fade back in
    DOM.shopProductsGrid.style.opacity = '1';
  }, 200); // 200ms delay for visual AJAX reload skeleton/fade simulation
}

// RENDER ACTIVE FILTER LABEL BAR ON SHOP
function renderActivePresetIndicators() {
  let subheader = document.getElementById('shop-presets-indicator');
  if (!subheader) {
    const headerSec = document.querySelector('.shop-header');
    if (!headerSec) return;
    subheader = document.createElement('div');
    subheader.id = 'shop-presets-indicator';
    subheader.className = 'shop-presets-indicator';
    subheader.style.cssText = 'display:flex; gap:8px; margin-bottom: 24px; flex-wrap:wrap;';
    headerSec.parentNode.insertBefore(subheader, headerSec.nextSibling);
  }

  subheader.innerHTML = '';

  const addIndicator = (label, onClear) => {
    const span = document.createElement('span');
    span.style.cssText = 'font-size:0.75rem; background:var(--color-sage-light); color:var(--color-black); padding:4px 12px; border-radius:12px; display:inline-flex; align-items:center; gap:8px;';
    span.innerHTML = `
      <span>${label}</span>
      <button style="font-weight:600; font-size:0.85rem; background:none; border:none; cursor:pointer;" onclick="${onClear}">Г—</button>
    `;
    subheader.appendChild(span);
  };

  // Indicators for brand
  if (AppState.filters.brands && AppState.filters.brands.length > 0) {
    AppState.filters.brands.forEach(brand => {
      addIndicator(`Brand: ${brand}`, `clearFilterItem('brands', '${brand}')`);
    });
  }
  // Indicators for scent family
  if (AppState.filters.scent_families && AppState.filters.scent_families.length > 0) {
    AppState.filters.scent_families.forEach(sf => {
      const label = GLOBAL_ATTRIBUTES.scent_families[sf]?.label[AppState.language] || sf;
      addIndicator(`Family: ${label}`, `clearFilterItem('scent_families', '${sf}')`);
    });
  }
  // Indicators for gender
  if (AppState.filters.genders && AppState.filters.genders.length > 0) {
    AppState.filters.genders.forEach(g => {
      const label = GLOBAL_ATTRIBUTES.genders[g]?.label[AppState.language] || g;
      addIndicator(`Gender: ${label}`, `clearFilterItem('genders', '${g}')`);
    });
  }
  // Indicators for vibe
  if (AppState.filters.vibes && AppState.filters.vibes.length > 0) {
    AppState.filters.vibes.forEach(v => {
      const label = GLOBAL_ATTRIBUTES.vibes[v]?.label[AppState.language] || v;
      addIndicator(`Vibe: ${label}`, `clearFilterItem('vibes', '${v}')`);
    });
  }
  // Indicators for size
  if (AppState.filters.sizes && AppState.filters.sizes.length > 0) {
    AppState.filters.sizes.forEach(s => {
      addIndicator(`Size: ${s}`, `clearFilterItem('sizes', '${s}')`);
    });
  }
  // Tag indicator (New, Best Seller, Gift)
  if (AppState.filters.tag) {
    addIndicator(`Tag: ${AppState.filters.tag}`, "clearPresetFilter('tag')");
  }

  if (subheader.children.length === 0) {
    subheader.style.display = 'none';
  } else {
    subheader.style.display = 'flex';
  }
}

// FILTER ITEMS ACTIONS
window.clearFilterItem = function (arrayName, value) {
  if (AppState.filters[arrayName]) {
    const idx = AppState.filters[arrayName].indexOf(value);
    if (idx > -1) {
      AppState.filters[arrayName].splice(idx, 1);
    }
  }
  renderFilterWidgets('desktop-sidebar-filters', false);
  renderShop();
};

window.applyPresetFilter = function (type, value) {
  AppState.filters.scent_families = [];
  AppState.filters.concentrations = [];
  AppState.filters.genders = [];
  AppState.filters.vibes = [];
  AppState.filters.sizes = [];
  AppState.filters.brands = [];
  AppState.filters.priceRange = { min: 0, max: 400 };
  AppState.filters.search = '';
  AppState.filters.tag = '';

  if (type === 'brand') {
    AppState.filters.brands = [value];
  } else if (type === 'gender') {
    AppState.filters.genders = [value.toLowerCase()];
  } else if (type === 'category') {
    AppState.filters.scent_families = [value.toLowerCase()];
  } else if (type === 'tag') {
    AppState.filters.tag = value;
  }

  window.location.hash = '#/shop';

  if (AppState.currentRoute === 'shop') {
    renderFilterWidgets('desktop-sidebar-filters', false);
    renderShop();
  }
};

window.clearPresetFilter = function (type) {
  if (type === 'tag') {
    AppState.filters.tag = '';
  }
  renderShop();
};

// DYNAMIC FILTER WIDGETS RENDERING
window.renderFilterWidgets = function (containerId, isMobile) {
  const container = document.getElementById(containerId);
  if (!container) return;

  const filtersState = isMobile ? TempMobileFilters : AppState.filters;

  container.innerHTML = '';

  const lang = AppState.language;

  // Desktop sidebar filters header with Clear All link
  if (!isMobile) {
    const sidebarHeader = document.createElement('div');
    sidebarHeader.className = 'sidebar-filters-header';
    sidebarHeader.innerHTML = `
      <h3 class="serif-title" style="font-size: 1.25rem; letter-spacing:0.05em; text-transform:uppercase;" data-trans="btn_filters">${TRANSLATIONS[lang]['btn_filters'] || 'Filters'}</h3>
      <button class="desktop-clear-all-btn" data-trans="btn_clear_all" style="font-size:0.75rem; text-decoration:underline; cursor:pointer; background:none; border:none; color: var(--color-medium-gray);">${TRANSLATIONS[lang]['btn_clear_all'] || 'Clear All'}</button>
    `;
    sidebarHeader.querySelector('.desktop-clear-all-btn').addEventListener('click', () => {
      AppState.filters.scent_families = [];
      AppState.filters.concentrations = [];
      AppState.filters.genders = [];
      AppState.filters.vibes = [];
      AppState.filters.sizes = [];
      AppState.filters.brands = [];
      AppState.filters.priceRange = { min: 0, max: 400 };
      AppState.filters.search = '';
      AppState.filters.tag = '';

      renderFilterWidgets(containerId, false);
      renderShop();
    });
    container.appendChild(sidebarHeader);
  }

  // 1. Search Box Group
  const searchGroup = document.createElement('div');
  searchGroup.className = 'filter-group search-filter-group';
  searchGroup.innerHTML = `
    <div class="search-input-wrapper">
      <input type="text" class="filter-search-input" placeholder="${TRANSLATIONS[lang]['search_placeholder'] || 'Search...'}" value="${filtersState.search || ''}">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" class="filter-search-icon">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
    </div>
  `;
  const searchInput = searchGroup.querySelector('.filter-search-input');
  searchInput.addEventListener('input', (e) => {
    filtersState.search = e.target.value.toLowerCase();
    if (!isMobile) {
      AppState.filters.search = filtersState.search;
      renderShop();
    }
  });
  container.appendChild(searchGroup);

  const createFilterHeader = (transKey, defaultText) => {
    const h4 = document.createElement('h4');
    h4.className = 'filter-group-title';
    h4.setAttribute('data-trans', transKey);
    h4.innerText = TRANSLATIONS[lang][transKey] || defaultText;
    return h4;
  };

  // 2. Scent Family swatches (Visual color circles)
  const scentGroup = document.createElement('div');
  scentGroup.className = 'filter-group';
  scentGroup.appendChild(createFilterHeader('filter_scent_family', 'Scent Family'));
  const scentList = document.createElement('div');
  scentList.className = 'scent-swatches-grid';

  Object.keys(GLOBAL_ATTRIBUTES.scent_families).forEach(key => {
    const item = GLOBAL_ATTRIBUTES.scent_families[key];
    const isActive = filtersState.scent_families.includes(key);
    const swatch = document.createElement('button');
    swatch.className = `scent-swatch ${isActive ? 'active' : ''}`;
    swatch.style.setProperty('--swatch-color', item.color);
    swatch.innerHTML = `
      <span class="swatch-circle" style="background-color: ${item.color}"></span>
      <span class="swatch-label">${item.label[lang] || item.label.en}</span>
    `;
    swatch.addEventListener('click', () => {
      toggleFilterValue(filtersState.scent_families, key);
      if (!isMobile) {
        swatch.classList.toggle('active');
        renderShop();
      } else {
        renderFilterWidgets(containerId, isMobile);
      }
    });
    scentList.appendChild(swatch);
  });
  scentGroup.appendChild(scentList);
  container.appendChild(scentGroup);

  // 3. Concentration swatches (Pills)
  const concGroup = document.createElement('div');
  concGroup.className = 'filter-group';
  concGroup.appendChild(createFilterHeader('filter_concentration', 'Concentration'));
  const concList = document.createElement('div');
  concList.className = 'swatches-wrap';

  Object.keys(GLOBAL_ATTRIBUTES.concentrations).forEach(key => {
    const item = GLOBAL_ATTRIBUTES.concentrations[key];
    const isActive = filtersState.concentrations.includes(key);
    const button = document.createElement('button');
    button.className = `swatch-pill ${isActive ? 'active' : ''}`;
    button.innerText = item.short;
    button.title = item.label[lang] || item.label.en;
    button.addEventListener('click', () => {
      toggleFilterValue(filtersState.concentrations, key);
      if (!isMobile) {
        button.classList.toggle('active');
        renderShop();
      } else {
        renderFilterWidgets(containerId, isMobile);
      }
    });
    concList.appendChild(button);
  });
  concGroup.appendChild(concList);
  container.appendChild(concGroup);

  // 4. Gender swatches (Pills)
  const genGroup = document.createElement('div');
  genGroup.className = 'filter-group';
  genGroup.appendChild(createFilterHeader('filter_gender', 'Gender'));
  const genList = document.createElement('div');
  genList.className = 'swatches-wrap';

  Object.keys(GLOBAL_ATTRIBUTES.genders).forEach(key => {
    const item = GLOBAL_ATTRIBUTES.genders[key];
    const isActive = filtersState.genders.includes(key);
    const button = document.createElement('button');
    button.className = `swatch-pill ${isActive ? 'active' : ''}`;
    button.innerText = item.label[lang] || item.label.en;
    button.addEventListener('click', () => {
      toggleFilterValue(filtersState.genders, key);
      if (!isMobile) {
        button.classList.toggle('active');
        renderShop();
      } else {
        renderFilterWidgets(containerId, isMobile);
      }
    });
    genList.appendChild(button);
  });
  genGroup.appendChild(genList);
  container.appendChild(genGroup);

  // 5. Vibe / Occasion swatches (Pills)
  const vibeGroup = document.createElement('div');
  vibeGroup.className = 'filter-group';
  vibeGroup.appendChild(createFilterHeader('filter_vibe', 'Vibe / Occasion'));
  const vibeList = document.createElement('div');
  vibeList.className = 'swatches-wrap';

  Object.keys(GLOBAL_ATTRIBUTES.vibes).forEach(key => {
    const item = GLOBAL_ATTRIBUTES.vibes[key];
    const isActive = filtersState.vibes.includes(key);
    const button = document.createElement('button');
    button.className = `swatch-pill ${isActive ? 'active' : ''}`;
    button.innerText = item.label[lang] || item.label.en;
    button.addEventListener('click', () => {
      toggleFilterValue(filtersState.vibes, key);
      if (!isMobile) {
        button.classList.toggle('active');
        renderShop();
      } else {
        renderFilterWidgets(containerId, isMobile);
      }
    });
    vibeList.appendChild(button);
  });
  vibeGroup.appendChild(vibeList);
  container.appendChild(vibeGroup);

  // 6. Size swatches (Pills)
  const sizeGroup = document.createElement('div');
  sizeGroup.className = 'filter-group';
  sizeGroup.appendChild(createFilterHeader('filter_size', 'Size'));
  const sizeList = document.createElement('div');
  sizeList.className = 'swatches-wrap';

  const allSizes = ["50ml", "100ml", "200ml"];
  allSizes.forEach(size => {
    const isActive = filtersState.sizes.includes(size);
    const button = document.createElement('button');
    button.className = `swatch-pill ${isActive ? 'active' : ''}`;
    button.innerText = size;
    button.addEventListener('click', () => {
      toggleFilterValue(filtersState.sizes, size);
      if (!isMobile) {
        button.classList.toggle('active');
        renderShop();
      } else {
        renderFilterWidgets(containerId, isMobile);
      }
    });
    sizeList.appendChild(button);
  });
  sizeGroup.appendChild(sizeList);
  container.appendChild(sizeGroup);

  // 7. Brand swatches (Pills)
  const brandGroup = document.createElement('div');
  brandGroup.className = 'filter-group';
  brandGroup.appendChild(createFilterHeader('filter_brand', 'Brand'));
  const brandList = document.createElement('div');
  brandList.className = 'swatches-wrap';

  const allBrands = ["NOVA", "Byredo", "Le Labo"];
  allBrands.forEach(brand => {
    const isActive = filtersState.brands.some(b => b.toLowerCase() === brand.toLowerCase());
    const button = document.createElement('button');
    button.className = `swatch-pill ${isActive ? 'active' : ''}`;
    button.innerText = brand;
    button.addEventListener('click', () => {
      const existingIdx = filtersState.brands.findIndex(b => b.toLowerCase() === brand.toLowerCase());
      if (existingIdx > -1) {
        filtersState.brands.splice(existingIdx, 1);
      } else {
        filtersState.brands.push(brand);
      }
      if (!isMobile) {
        button.classList.toggle('active');
        renderShop();
      } else {
        renderFilterWidgets(containerId, isMobile);
      }
    });
    brandList.appendChild(button);
  });
  brandGroup.appendChild(brandList);
  container.appendChild(brandGroup);

  // 8. Price Range Slider
  const priceGroup = document.createElement('div');
  priceGroup.className = 'filter-group price-filter-group';
  priceGroup.appendChild(createFilterHeader('filter_price', 'Price'));

  const sliderContainer = document.createElement('div');
  sliderContainer.className = 'price-slider-container';

  const minVal = filtersState.priceRange.min;
  const maxVal = filtersState.priceRange.max;

  sliderContainer.innerHTML = `
    <div class="price-slider-track-wrap">
      <div class="price-slider-track" id="${isMobile ? 'm' : 'd'}-slider-track"></div>
      <input type="range" min="0" max="400" value="${minVal}" class="min-price-slider" id="${isMobile ? 'm' : 'd'}-min-price-input" aria-label="Min price">
      <input type="range" min="0" max="400" value="${maxVal}" class="max-price-slider" id="${isMobile ? 'm' : 'd'}-max-price-input" aria-label="Max price">
    </div>
    <div class="price-range-values">
      <span>$<span id="${isMobile ? 'm' : 'd'}-min-val">${minVal}</span></span>
      <span>$<span id="${isMobile ? 'm' : 'd'}-max-val">${maxVal}</span></span>
    </div>
  `;

  const minInput = sliderContainer.querySelector('.min-price-slider');
  const maxInput = sliderContainer.querySelector('.max-price-slider');
  const minText = sliderContainer.querySelector(`#${isMobile ? 'm' : 'd'}-min-val`);
  const maxText = sliderContainer.querySelector(`#${isMobile ? 'm' : 'd'}-max-val`);
  const track = sliderContainer.querySelector('.price-slider-track');

  function updateTrack() {
    const val1 = parseInt(minInput.value);
    const val2 = parseInt(maxInput.value);

    if (val1 > val2) {
      if (this === minInput) {
        minInput.value = val2;
      } else {
        maxInput.value = val1;
      }
    }

    const finalMin = Math.min(parseInt(minInput.value), parseInt(maxInput.value));
    const finalMax = Math.max(parseInt(minInput.value), parseInt(maxInput.value));

    minText.innerText = finalMin;
    maxText.innerText = finalMax;

    filtersState.priceRange.min = finalMin;
    filtersState.priceRange.max = finalMax;

    const pctMin = (finalMin / 400) * 100;
    const pctMax = (finalMax / 400) * 100;

    track.style.left = `${pctMin}%`;
    track.style.width = `${pctMax - pctMin}%`;
  }

  minInput.addEventListener('input', updateTrack);
  maxInput.addEventListener('input', updateTrack);

  const handlePointerMove = (e) => {
    const rect = minInput.getBoundingClientRect();
    if (rect.width === 0) return;
    const clientX = (e.touches && e.touches.length > 0) ? e.touches[0].clientX : e.clientX;
    const pct = (clientX - rect.left) / rect.width;
    const hoverVal = pct * 400;

    const val1 = parseInt(minInput.value);
    const val2 = parseInt(maxInput.value);

    const distMin = Math.abs(hoverVal - val1);
    const distMax = Math.abs(hoverVal - val2);

    if (distMin === distMax) {
      if (hoverVal < val1) {
        minInput.style.zIndex = '3';
        maxInput.style.zIndex = '1';
      } else {
        minInput.style.zIndex = '1';
        maxInput.style.zIndex = '3';
      }
    } else if (distMin < distMax) {
      minInput.style.zIndex = '3';
      maxInput.style.zIndex = '1';
    } else {
      minInput.style.zIndex = '1';
      maxInput.style.zIndex = '3';
    }
  };

  sliderContainer.addEventListener('mousemove', handlePointerMove);
  sliderContainer.addEventListener('touchmove', handlePointerMove);
  sliderContainer.addEventListener('mousedown', handlePointerMove);
  sliderContainer.addEventListener('touchstart', handlePointerMove);

  if (!isMobile) {
    minInput.addEventListener('change', () => {
      AppState.filters.priceRange.min = filtersState.priceRange.min;
      renderShop();
    });
    maxInput.addEventListener('change', () => {
      AppState.filters.priceRange.max = filtersState.priceRange.max;
      renderShop();
    });
  }

  priceGroup.appendChild(sliderContainer);
  container.appendChild(priceGroup);

  // Wait short delay to initialize CSS variables positions
  setTimeout(updateTrack, 0);
};

// CREATE COMPONENT FOR PRODUCT CARD
function createProductCard(product) {
  const card = document.createElement('div');
  card.className = 'product-card';
  card.onclick = () => openProductModal(product.id);
  const isWishlisted = AppState.wishlist.includes(product.id);
  const wishlistClass = isWishlisted ? 'wishlist-filled' : '';

  // Get translations
  const transProd = getTranslatedProduct(product.id);
  const tagline = transProd ? transProd.tagline : product.tagline;
  const translatedCategory = getTranslatedCategory(product.category);

  const exploreLabel = TRANSLATIONS[AppState.language]['card_explore'] || 'Explore';
  const cartLabel = product.stock > 0
    ? (TRANSLATIONS[AppState.language]['card_add_to_cart'] || 'Add to Cart')
    : (TRANSLATIONS[AppState.language]['card_sold_out'] || 'Sold Out');

  let badgeText = product.tags && product.tags.length > 0 ? product.tags[0] : '';
  let badgeClass = '';
  if (badgeText) {
    if (badgeText.toLowerCase().includes('best seller') || badgeText.toLowerCase().includes('bestseller')) {
      badgeText = TRANSLATIONS[AppState.language]['badge_bestseller'] || 'BEST SELLER';
      badgeClass = 'tag-bestseller';
    } else if (badgeText.toLowerCase().includes('new')) {
      badgeText = TRANSLATIONS[AppState.language]['badge_new_product'] || 'NEW';
      badgeClass = 'tag-new';
    }
  }

  card.innerHTML = `
    <div class="product-card-image-wrap">
      ${badgeText ? `<div class="card-badge ${badgeClass}">${badgeText}</div>` : ''}
      <img src="${product.image}" alt="${product.name}" class="product-card-image" loading="lazy">
      
      <!-- Floating Card Wishlist Toggle -->
      <button class="product-card-wishlist-btn ${wishlistClass}" onclick="event.stopPropagation(); toggleWishlist('${product.id}')" aria-label="Add to Wishlist">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
      </button>

      <div class="product-card-actions">
        <button class="btn-secondary product-card-action-btn" onclick="event.stopPropagation(); quickAddToCart('${product.id}')" ${product.stock <= 0 ? 'disabled' : ''}>
          ${cartLabel}
        </button>
      </div>
    </div>
    <div class="product-card-category">${translatedCategory} &bull; ${product.brand}</div>
    <h3 class="product-card-name serif-title">${product.name}</h3>
    <p class="product-card-tagline">${tagline}</p>
    <div class="product-card-footer">
      <span class="product-card-price">ЦЏ${product.price}</span>
      <div class="product-card-rating">
        <span class="rating-star">в…</span>
        <span>${product.rating}</span>
      </div>
    </div>
  `;
  return card;
}

// TOAST NOTIFICATIONS
function showToast(message) {
  const lang = AppState.language;
  let translatedMessage = message;
  const normMsg = message.trim().toUpperCase();

  if (normMsg.startsWith("UPDATED ") && normMsg.endsWith(" STOCK AND PRICE.")) {
    const prodName = message.substring(8, message.length - 16);
    translatedMessage = lang === 'am' ? `Ф№ХЎЦЂХґХЎЦЃХѕХҐХ¬ Х§ ${prodName}-Х« ХєХЎХ·ХЎЦЂХЁ Ц‡ ХЈХ«Х¶ХЁ:` :
                        lang === 'ru' ? `РћР±РЅРѕРІР»РµРЅС‹ Р·Р°РїР°СЃ Рё С†РµРЅР° РґР»СЏ ${prodName}.` :
                        `Updated ${prodName} stock and price.`;
  } else if (normMsg.startsWith("ORDER #") && normMsg.includes(" SET TO ")) {
    const match = message.match(/ORDER #(.+?) SET TO (.+?)\./i);
    if (match) {
      const orderId = match[1];
      const status = match[2].toLowerCase();
      const transStatus = status === "pending" ? (lang === 'am' ? "ФёХ¶Х©ХЎЦЃЦ„Х« ХґХҐХ»" : lang === 'ru' ? "Р’ РѕР¶РёРґР°РЅРёРё" : "Pending") :
                          status === "processing" ? (lang === 'am' ? "Х„Х·ХЎХЇХѕХёЦ‚Хґ Х§" : lang === 'ru' ? "РћР±СЂР°Р±Р°С‚С‹РІР°РµС‚СЃСЏ" : "Processing") :
                          status === "completed" ? (lang === 'am' ? "Ф±ХѕХЎЦЂХїХѕХЎХ®" : lang === 'ru' ? "Р’С‹РїРѕР»РЅРµРЅ" : "Completed") :
                          status === "failed" ? (lang === 'am' ? "ХЃХЎХ­ХёХІХѕХЎХ®" : lang === 'ru' ? "РћС€РёР±РєР°" : "Failed") : status;
      translatedMessage = lang === 'am' ? `ХЉХЎХїХѕХҐЦЂ #${orderId}-Х« ХЇХЎЦЂХЈХЎХѕХ«ХіХЎХЇХЁ ХЅХЎХ°ХґХЎХ¶ХѕХҐЦЃХќ ${transStatus.toUpperCase()}:` :
                          lang === 'ru' ? `Р—Р°РєР°Р· #${orderId} РїРµСЂРµРІРµРґРµРЅ РІ СЃС‚Р°С‚СѓСЃ ${transStatus.toUpperCase()}.` :
                          `Order #${orderId} set to ${transStatus.toUpperCase()}.`;
    }
  } else if (normMsg.startsWith("WELCOME BACK, ")) {
    const name = message.substring(14, message.length - 1);
    translatedMessage = lang === 'am' ? `ФІХЎЦЂХ« ХѕХҐЦЂХЎХ¤ХЎЦЂХ±, ${name}:` :
                        lang === 'ru' ? `РЎ РІРѕР·РІСЂР°С‰РµРЅРёРµРј, ${name}.` :
                        `Welcome back, ${name}.`;
  } else if (normMsg.startsWith("PLUGIN ") && normMsg.endsWith(" INSTALLED.")) {
    const pluginId = message.substring(7, message.length - 11);
    translatedMessage = lang === 'am' ? `Х“ФјФ±ФіФ»Х† ${pluginId} ХЏФµХ‚Ф±ФґХђХЋФ±Фѕ Ф·:` :
                        lang === 'ru' ? `РџР›РђР“РРќ ${pluginId} РЈРЎРўРђРќРћР’Р›Р•Рќ.` :
                        `Plugin ${pluginId} installed.`;
  } else if (normMsg.startsWith("PLUGIN ") && normMsg.endsWith(" UNINSTALLED.")) {
    const pluginId = message.substring(7, message.length - 13);
    translatedMessage = lang === 'am' ? `Х“ФјФ±ФіФ»Х† ${pluginId} Ф±ХЉФ±ХЏФµХ‚Ф±ФґХђХЋФ±Фѕ Ф·:` :
                        lang === 'ru' ? `РџР›РђР“РРќ ${pluginId} РЈР”РђР›Р•Рќ.` :
                        `Plugin ${pluginId} uninstalled.`;
  } else if (normMsg.startsWith("PAYMENT GATEWAY ") && (normMsg.endsWith(" ENABLED.") || normMsg.endsWith(" DISABLED."))) {
    const isEnabled = normMsg.endsWith(" ENABLED.");
    const gw = message.substring(16, message.length - (isEnabled ? 9 : 10));
    const statusText = isEnabled ? (lang === 'am' ? "Ф±ФїХЏФ»ХЋФ±Х‘ХЋФ±Фѕ Ф·" : lang === 'ru' ? "Р’РљР›Р®Р§Р•Рќ" : "ENABLED") : (lang === 'am' ? "Ф±ХЉФ±Ф±ФїХЏФ»ХЋФ±Х‘ХЋФ±Фѕ Ф·" : lang === 'ru' ? "РћРўРљР›Р®Р§Р•Рќ" : "DISABLED");
    translatedMessage = lang === 'am' ? `ХЋХѓФ±ХђФ±Х…Ф»Х† ФґФ±ХђХЉФ±ХЌ ${gw} ${statusText}:` :
                        lang === 'ru' ? `РџР›РђРўР•Р–РќР«Р™ РЁР›Р®Р— ${gw} ${statusText}.` :
                        `Payment gateway ${gw} ${isEnabled ? 'enabled' : 'disabled'}.`;
  } else if (normMsg.startsWith("STAFF PROFILE FOR ") && normMsg.endsWith(" CREATED.")) {
    const name = message.substring(18, message.length - 9);
    translatedMessage = lang === 'am' ? `Ф±Х†ХЃХ†Ф±ФїФ±Ф¶Х„Ф» ХЉХђХ€Х–Ф»ФјФё ${name}-Ф» ХЂФ±Х„Ф±Хђ ХЌХЏФµХ‚ФѕХЋФ±Фѕ Ф·:` :
                        lang === 'ru' ? `РџР РћР¤РР›Р¬ РЎРћРўР РЈР”РќРРљРђ Р”Р›РЇ ${name} РЎРћР—Р”РђРќ.` :
                        `Staff profile for ${name} created.`;
  } else if (normMsg.startsWith("STAFF PROFILE FOR ") && normMsg.endsWith(" DELETED.")) {
    const name = message.substring(18, message.length - 9);
    translatedMessage = lang === 'am' ? `Ф±Х†ХЃХ†Ф±ФїФ±Ф¶Х„Ф» ХЉХђХ€Х–Ф»ФјФё ${name}-Ф» ХЂФ±Х„Ф±Хђ Х‹Х†Х‹ХЋФ±Фѕ Ф·:` :
                        lang === 'ru' ? `РџР РћР¤РР›Р¬ РЎРћРўР РЈР”РќРРљРђ Р”Р›РЇ ${name} РЈР”РђР›Р•Рќ.` :
                        `Staff profile for ${name} deleted.`;
  } else if (normMsg.startsWith("CANNOT ADD. ONLY ") && normMsg.endsWith(" ITEMS AVAILABLE IN STOCK.")) {
    const qty = message.match(/\d+/)[0];
    translatedMessage = lang === 'am' ? `ХЂХ†Ф±ХђФ±ХЋХ€Хђ Х‰Ф· Ф±ХЋФµФјФ±Х‘Х†ФµФј: ХЉФ±Х‡Ф±ХђХ€Х’Х„ Ф±ХЊФїФ± Ф· ФёХ†ФґФ±Х„ФµХ†Фё ${qty} Ф±ХЉХђФ±Х†Х”:` :
                        lang === 'ru' ? `РќР•Р’РћР—РњРћР–РќРћ Р”РћР‘РђР’РРўР¬. РќРђ РЎРљР›РђР”Р• РћРЎРўРђР›РћРЎР¬ Р’РЎР•Р“Рћ ${qty} РЁРў.` :
                        `Cannot add. Only ${qty} items available in stock.`;
  } else if (normMsg.startsWith("ONLY ") && normMsg.endsWith(" ITEMS AVAILABLE IN STOCK.")) {
    const qty = message.match(/\d+/)[0];
    translatedMessage = lang === 'am' ? `ХЉФ±Х‡Ф±ХђХ€Х’Х„ Ф±ХЊФїФ± Ф· ФёХ†ФґФ±Х„ФµХ†Фё ${qty} Ф±ХЉХђФ±Х†Х”:` :
                        lang === 'ru' ? `РќР° СЃРєР»Р°РґРµ РѕСЃС‚Р°Р»РѕСЃСЊ РІСЃРµРіРѕ ${qty} С€С‚.` :
                        `Only ${qty} items available in stock.`;
  } else if (normMsg.endsWith(" ADDED TO CART.")) {
    const details = message.substring(0, message.length - 15);
    translatedMessage = lang === 'am' ? `${details} Ф±ХЋФµФјФ±Х‘ХЋФµФј Ф· Ф¶Ф±Х„ФІХ…Х€Х’Х‚:` :
                        lang === 'ru' ? `${details} РґРѕР±Р°РІР»РµРЅ РІ РєРѕСЂР·РёРЅСѓ.` :
                        `${details} added to cart.`;
  } else if (normMsg.endsWith(" REMOVED FROM CART.")) {
    const name = message.substring(0, message.length - 19);
    translatedMessage = lang === 'am' ? `${name} ХЂФµХЊФ±Х‘ХЋФµФј Ф· Ф¶Ф±Х„ФІХ…Х€Х’Х‚Ф»Х‘:` :
                        lang === 'ru' ? `${name} СѓРґР°Р»РµРЅ РёР· РєРѕСЂР·РёРЅС‹.` :
                        `${name} removed from cart.`;
  } else if (normMsg.endsWith(" REMOVED FROM WISHLIST.")) {
    const name = message.substring(0, message.length - 23);
    translatedMessage = lang === 'am' ? `${name} ХЂФµХЊФ±Х‘ХЋФµФј Ф· Х‘Ф±Х†ФїХ€Х’Ф№Х…Х€Х’Х†Х†ФµХђФ»Х‘:` :
                        lang === 'ru' ? `${name} СѓРґР°Р»РµРЅ РёР· СЃРїРёСЃРєР° Р¶РµР»Р°РЅРёР№.` :
                        `${name} removed from wishlist.`;
  } else if (normMsg.endsWith(" SAVED TO WISHLIST.")) {
    const name = message.substring(0, message.length - 19);
    translatedMessage = lang === 'am' ? `${name} Ф±ХЋФµФјФ±Х‘ХЋФµФј Ф· Х‘Ф±Х†ФїХ€Х’Ф№Х…Х€Х’Х†Х†ФµХђФ» Х‘Х€Х’Х‘Ф±ФїХ€Х’Х„:` :
                        lang === 'ru' ? `${name} РґРѕР±Р°РІР»РµРЅ РІ СЃРїРёСЃРѕРє Р¶РµР»Р°РЅРёР№.` :
                        `${name} saved to wishlist.`;
  } else if (normMsg.startsWith("ADDED ") && normMsg.endsWith(" FRAGRANCES TO BAG.")) {
    const qty = message.match(/\d+/)[0];
    translatedMessage = lang === 'am' ? `Ф±ХЋФµФјФ±Х‘ХЋФµФј Ф· ${qty} ФІХ€Х’Х…Хђ Ф¶Ф±Х„ФІХ…Х€Х’Х‚Х€Х’Х„:` :
                        lang === 'ru' ? `Р”РѕР±Р°РІР»РµРЅРѕ ${qty} Р°СЂРѕРјР°С‚РѕРІ РІ РєРѕСЂР·РёРЅСѓ.` :
                        `Added ${qty} fragrances to bag.`;
  } else {
    const toastKeys = {
      "PASSWORD UPDATED SECURELY.": {
        am: "ФіФ±Х‚ХЏХ†Ф±ФІФ±ХЊФё Ф±ХЉФ±ХЂХ€ХЋ Ф№Ф±ХђХ„Ф±Х‘ХЋФµФј Ф·:",
        ru: "РџРђР РћР›Р¬ РќРђР”Р•Р–РќРћ РћР‘РќРћР’Р›Р•Рќ.",
        en: "PASSWORD UPDATED SECURELY."
      },
      "ACCOUNT DETAILS SAVED.": {
        am: "ХЂФ±Х‡ХЋФ» ХЏХЋХ…Ф±ФјХ†ФµХђФё ХЉФ±ХЂХЉФ±Х†ХЋФµФј ФµХ†:",
        ru: "Р”РђРќРќР«Р• РђРљРљРђРЈРќРўРђ РЎРћРҐР РђРќР•РќР«.",
        en: "ACCOUNT DETAILS SAVED."
      },
      "RESTOCK NOTIFICATION REMOVED.": {
        am: "ХЉФ±Х‡Ф±ХђФ» ХЂФ±Х„Ф±ФјХђХ„Ф±Х† ФѕФ±Х†Х€Х’Х‘Х€Х’Х„Фё ХЂФµХЊФ±Х‘ХЋФ±Фѕ Ф·:",
        ru: "РЈР’Р•Р”РћРњР›Р•РќРР• Рћ РџРћРЎРўРЈРџР›Р•РќРР РЈР”РђР›Р•РќРћ.",
        en: "RESTOCK NOTIFICATION REMOVED."
      },
      "RESTOCK NOTIFICATION ENABLED.": {
        am: "ХЉФ±Х‡Ф±ХђФ» ХЂФ±Х„Ф±ФјХђХ„Ф±Х† ФѕФ±Х†Х€Х’Х‘Х€Х’Х„Фё Ф±ФїХЏФ»ХЋФ±Х‘ХЋФ±Фѕ Ф·:",
        ru: "РЈР’Р•Р”РћРњР›Р•РќРР• Рћ РџРћРЎРўРЈРџР›Р•РќРР Р’РљР›Р®Р§Р•РќРћ.",
        en: "RESTOCK NOTIFICATION ENABLED."
      },
      "PLEASE FILL IN ALL FIELDS.": {
        am: "ФЅХ†ФґХђХ€Х’Х„ ФµХ†Х” ФјХђФ±Х‘Х†ФµФј ФІХ€ФјХ€Хђ ФґФ±Х‡ХЏФµХђФё:",
        ru: "РџРћР–РђР›РЈР™РЎРўРђ, Р—РђРџРћР›РќРРўР• Р’РЎР• РџРћР›РЇ.",
        en: "PLEASE FILL IN ALL FIELDS."
      },
      "INVALID EMAIL OR PASSWORD.": {
        am: "ХЌФЅФ±Фј Ф·Фј. ХЂФ±ХЌХ‘Фµ ФїФ±Х„ ФіФ±Х‚ХЏХ†Ф±ФІФ±ХЊ:",
        ru: "РќР•Р’Р•Р РќР«Р™ EMAIL РР›Р РџРђР РћР›Р¬.",
        en: "INVALID EMAIL OR PASSWORD."
      },
      "LOGGED OUT OF WOOCOMMERCE ADMIN PANEL.": {
        am: "ФґХ€Х’ХђХЌ ФµХ” ФіХђХЋФµФј WOOCOMMERCE ФїФ±ХЊФ±ХЋФ±ХђХ„Ф±Х† ХЋФ±ХЂФ±Х†Ф±ФїФ»Х‘:",
        ru: "Р’Р« Р’Р«РЁР›Р РР— РџРђРќР•Р›Р РЈРџР РђР’Р›Р•РќРРЇ WOOCOMMERCE.",
        en: "LOGGED OUT OF WOOCOMMERCE ADMIN PANEL."
      },
      "ACCESS DENIED. DEVELOPER SETTINGS RESTRICTED TO SUPER ADMIN.": {
        am: "Х„Х€Х’ХЏХ”Фё Х„ФµХђФєХЋФ±Фѕ Ф·: Х„Х‡Ф±ФїХ€Х‚Ф» ФїФ±ХђФіФ±ХЋХ€ХђХ€Х’Х„Х†ФµХђФё ХЂФ±ХЌФ±Х†ФµФјФ» ФµХ† Х„Ф»Ф±Х…Х† ХЌХ€Х’ХЉФµХђ Ф±ФґХ„Ф»Х†Ф»Х†:",
        ru: "Р”РћРЎРўРЈРџ Р—РђРџР Р•Р©Р•Рќ. РќРђРЎРўР РћР™РљР Р РђР—Р РђР‘РћРўР§РРљРђ Р”РћРЎРўРЈРџРќР« РўРћР›Р¬РљРћ Р”Р›РЇ РЎРЈРџР•Р  РђР”РњРРќРРЎРўР РђРўРћР Рђ.",
        en: "ACCESS DENIED. DEVELOPER SETTINGS RESTRICTED TO SUPER ADMIN."
      },
      "PROFILE ALREADY EXISTS.": {
        am: "ХЉХђХ€Х–Ф»ФјХ† Ф±ХђФґФµХ† ФіХ€Х…Х€Х’Ф№Х…Х€Х’Х† Х€Х’Х†Ф»:",
        ru: "РџР РћР¤РР›Р¬ РЈР–Р• РЎРЈР©Р•РЎРўР’РЈР•Рў.",
        en: "PROFILE ALREADY EXISTS."
      },
      "CANNOT DELETE SUPER ADMIN ACCOUNT.": {
        am: "ХЂХ†Ф±ХђФ±ХЋХ€Хђ Х‰Ф· Х‹Х†Х‹ФµФј ХЌХ€Х’ХЉФµХђ Ф±ФґХ„Ф»Х†Ф» ХЉХђХ€Х–Ф»ФјФё:",
        ru: "РќР•Р’РћР—РњРћР–РќРћ РЈР”РђР›РРўР¬ РЈР§Р•РўРќРЈР® Р—РђРџРРЎР¬ РЎРЈРџР•Р  РђР”РњРРќРРЎРўР РђРўРћР Рђ.",
        en: "CANNOT DELETE SUPER ADMIN ACCOUNT."
      },
      "ACCESS DENIED. ONLY SUPER ADMIN CAN CLEAR LOGS.": {
        am: "Х„Х€Х’ХЏХ”Фё Х„ФµХђФєХЋФ±Фѕ Ф·: Х„Ф»Ф±Х…Х† ХЌХ€Х’ХЉФµХђ Ф±ФґХ„Ф»Х†Фё ФїФ±ХђХ€Х‚ Ф· Х„Ф±Х”ХђФµФј Х„Ф±ХЏХ…Ф±Х†Фё:",
        ru: "Р”РћРЎРўРЈРџ Р—РђРџР Р•Р©Р•Рќ. РўРћР›Р¬РљРћ РЎРЈРџР•Р  РђР”РњРРќРРЎРўР РђРўРћР  РњРћР–Р•Рў РћР§РРЎРўРРўР¬ Р›РћР“Р.",
        en: "ACCESS DENIED. ONLY SUPER ADMIN CAN CLEAR LOGS."
      },
      "AUDIT LOGS HISTORY CLEARED.": {
        am: "Ф±Х€Х’ФґФ»ХЏФ» Х„Ф±ХЏХ…Ф±Х†Х†ФµХђФ» ХЉФ±ХЏХ„Х€Х’Ф№Х…Х€Х’Х†Фё Х„Ф±Х”ХђХЋФµФј Ф·:",
        ru: "РРЎРўРћР РРЇ Р–РЈР РќРђР›Рђ РђРЈР”РРўРђ РћР§РР©Р•РќРђ.",
        en: "AUDIT LOGS HISTORY CLEARED."
      },
      "INVALID PRICE VALUE.": {
        am: "Ф±Х†ХЋФ±ХЋФµХђ ФіХ†Ф» Ф±ХђФєФµХ”:",
        ru: "РќР•Р’Р•Р РќРћР• Р—РќРђР§Р•РќРР• Р¦Р•РќР«.",
        en: "INVALID PRICE VALUE."
      },
      "INVALID STOCK VALUE.": {
        am: "Ф±Х†ХЋФ±ХЋФµХђ ХЉФ±Х‡Ф±ХђФ» Ф±ХђФєФµХ”:",
        ru: "РќР•Р’Р•Р РќРћР• Р—РќРђР§Р•РќРР• Р—РђРџРђРЎРђ.",
        en: "INVALID STOCK VALUE."
      },
      "INVOICE DOWNLOAD SIMULATED": {
        am: "ХЂФ±Х‡ХЋФ» Х†ФµХђФІФµХЊХ†Х€Х’Х„Фё ХЌФ»Х„Х€Х’ФјФ±Х‘ХЋФ±Фѕ Ф·:",
        ru: "РЎРљРђР§РР’РђРќРР• РЎР§Р•РўРђ РЎРРњРЈР›РР РћР’РђРќРћ",
        en: "Invoice download simulated"
      },
      "WISHLIST ITEMS ARE ALREADY IN BAG OR SOLD OUT.": {
        am: "Х‘Ф±Х†ФїХ€Х’Ф№Х…Ф±Х† ФІХ€ФјХ€Хђ Ф±ХЉХђФ±Х†Х”Х†ФµХђХ† Ф±ХђФґФµХ† Ф¶Ф±Х„ФІХ…Х€Х’Х‚Х€Х’Х„ ФµХ† ФїФ±Х„ ХЌХЉФ±ХЊХЋФ±Фѕ ФµХ†:",
        ru: "Р’РЎР• РўРћР’РђР Р« РР— РЎРџРРЎРљРђ Р–Р•Р›РђРќРР™ РЈР–Р• Р’ РљРћР Р—РРќР• РР›Р Р РђРЎРџР РћР”РђРќР«.",
        en: "Wishlist items are already in bag or sold out."
      },
      "YOUR CART IS EMPTY.": {
        am: "ХЃФµХђ Ф¶Ф±Х„ФІХ…Х€Х’Х‚Фё ФґФ±ХЏФ±ХђФї Ф·:",
        ru: "Р’РђРЁРђ РљРћР Р—РРќРђ РџРЈРЎРўРђ.",
        en: "Your cart is empty."
      },
      "THANK YOU FOR SUBSCRIBING TO NOVA JOURNEY.": {
        am: "Х‡Х†Х€ХђХЂФ±ФїФ±ФјХ€Х’Ф№Х…Х€Х’Х† NOVA-Ф»Х† ФІФ±ФєФ±Х†Х€ХђФґФ±ФіХђХЋФµФјХ€Х’ ХЂФ±Х„Ф±Хђ:",
        ru: "Р‘Р›РђР“РћР”РђР РРњ Р—Рђ РџРћР”РџРРЎРљРЈ РќРђ РќРћР’РћРЎРўР NOVA.",
        en: "Thank you for subscribing to NOVA journey."
      },
      "SORRY, THIS FRAGRANCE IS TEMPORARILY OUT OF STOCK.": {
        am: "Х‘Ф±ХЋХ€Х’Х„ ФµХ†Х”, Ф±Х…ХЌ ФІХ€Х’Х…ХђФё ФєФ±Х„Ф±Х†Ф±ФїФ±ХЋХ€ХђФ±ХЉФµХЌ ХЌХЉФ±ХЊХЋФ±Фѕ Ф·:",
        ru: "РР—Р’РРќРРўР•, Р­РўРћРў РђР РћРњРђРў Р’Р Р•РњР•РќРќРћ РћРўРЎРЈРўРЎРўР’РЈР•Рў РќРђ РЎРљР›РђР”Р•.",
        en: "Sorry, this fragrance is temporarily out of stock."
      }
    };

    if (toastKeys[normMsg] || toastKeys[message]) {
      const entry = toastKeys[normMsg] || toastKeys[message];
      translatedMessage = entry[lang] || entry['en'] || message;
    }
  }

  DOM.toast.innerText = translatedMessage;
  DOM.toast.classList.add('active');
  setTimeout(() => {
    DOM.toast.classList.remove('active');
  }, 3000);
}

// ADD TO CART SYSTEM (Quick Add)
window.quickAddToCart = function (productId) {
  const product = AppState.products.find(p => p.id === productId);
  if (!product) return;

  if (product.stock <= 0) {
    showToast("SORRY, THIS FRAGRANCE IS TEMPORARILY OUT OF STOCK.");
    return;
  }

  addToCart(product, '100ml', 1);
};

// DETAILED ADD TO CART WITH SIZE & QTY
function addToCart(product, size, qty) {
  // Find current price matching this size
  const sizeObj = product.sizes.find(s => s.size === size);
  const itemPrice = sizeObj ? sizeObj.price : product.price;

  // Check if item already exists in cart
  const cartIndex = AppState.cart.findIndex(item => item.product.id === product.id && item.size === size);

  if (cartIndex > -1) {
    // Check stock limit
    const futureQty = AppState.cart[cartIndex].quantity + qty;
    if (futureQty > product.stock) {
      showToast(`CANNOT ADD. ONLY ${product.stock} ITEMS AVAILABLE IN STOCK.`);
      return;
    }
    AppState.cart[cartIndex].quantity = futureQty;
  } else {
    if (qty > product.stock) {
      showToast(`CANNOT ADD. ONLY ${product.stock} ITEMS AVAILABLE IN STOCK.`);
      return;
    }
    AppState.cart.push({
      product: product,
      size: size,
      quantity: qty,
      price: itemPrice
    });
  }

  saveCartToStorage();
  updateCartUI();

  // Bounce cart bubble count
  DOM.cartCountBadge.classList.add('bounce');
  setTimeout(() => DOM.cartCountBadge.classList.remove('bounce'), 400);

  showToast(`${product.name.toUpperCase()} (${size}) ADDED TO CART.`);
  openCartDrawer();
}

// CART SYSTEM UI UPDATES
function updateCartUI() {
  // Update badge number
  const totalCount = AppState.cart.reduce((sum, item) => sum + item.quantity, 0);
  DOM.cartCountBadge.innerText = totalCount;

  const mobileCartBadge = document.getElementById('mobile-cart-count-badge');
  if (mobileCartBadge) {
    mobileCartBadge.innerText = totalCount;
  }

  // Fill Cart Items
  if (!DOM.cartItemsContainer) return;
  DOM.cartItemsContainer.innerHTML = '';

  if (AppState.cart.length === 0) {
    const cartEmptyMsg = TRANSLATIONS[AppState.language]['cart_empty_msg'] || 'Your shopping cart is currently empty.';
    const goShopBtnText = TRANSLATIONS[AppState.language]['go_to_shop_btn'] || 'Go to Shop';
    DOM.cartItemsContainer.innerHTML = `
      <div class="cart-empty-state">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
        </svg>
        <p style="font-weight: 300; font-size: 0.9rem; letter-spacing: 0.05em;">${cartEmptyMsg}</p>
        <button class="btn-primary" onclick="closeCartDrawer(); window.location.hash='#/shop';">${goShopBtnText}</button>
      </div>
    `;
    DOM.cartSubtotal.innerText = 'ЦЏ0';
    return;
  }

  let subtotal = 0;

  let sizePrefix = "Size: ";
  if (AppState.language === 'am') sizePrefix = "Х‰ХЎЦѓХЅХЁХќ ";
  else if (AppState.language === 'ru') sizePrefix = "РћР±СЉРµРј: ";

  AppState.cart.forEach((item, index) => {
    const itemTotal = item.price * item.quantity;
    subtotal += itemTotal;

    const itemDiv = document.createElement('div');
    itemDiv.className = 'cart-item';
    itemDiv.innerHTML = `
      <img src="${item.product.image}" alt="${item.product.name}" class="cart-item-image">
      <div class="cart-item-details">
        <h4 class="cart-item-name">${item.product.name}</h4>
        <div class="cart-item-meta">${sizePrefix}${item.size}</div>
        <div class="cart-item-row">
          <div class="cart-item-qty">
            <button class="cart-item-qty-btn" onclick="updateCartQty(${index}, -1)">в€’</button>
            <input type="text" class="cart-item-qty-input" value="${item.quantity}" readonly>
            <button class="cart-item-qty-btn" onclick="updateCartQty(${index}, 1)">+</button>
          </div>
          <span class="cart-item-price">ЦЏ${itemTotal}</span>
        </div>
      </div>
      <button class="cart-item-remove" onclick="removeCartItem(${index})">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
        </svg>
      </button>
    `;
    DOM.cartItemsContainer.appendChild(itemDiv);
  });

  DOM.cartSubtotal.innerText = `ЦЏ${subtotal}`;
}

window.updateCartQty = function (index, change) {
  const item = AppState.cart[index];
  const newQty = item.quantity + change;

  if (newQty <= 0) {
    removeCartItem(index);
    return;
  }

  // Stock safety check
  if (newQty > item.product.stock) {
    showToast(`SORRY, ONLY ${item.product.stock} ITEMS AVAILABLE IN STOCK.`);
    return;
  }

  item.quantity = newQty;
  saveCartToStorage();
  updateCartUI();

  // If we are on checkout, update checkout summaries too
  if (AppState.currentRoute === 'checkout') {
    updateCheckoutTotals();
  }
};

window.removeCartItem = function (index) {
  const name = AppState.cart[index].product.name;
  AppState.cart.splice(index, 1);
  saveCartToStorage();
  updateCartUI();
  showToast(`${name.toUpperCase()} REMOVED FROM CART.`);

  if (AppState.currentRoute === 'checkout') {
    renderCheckoutPage();
  }
};

// CART STORAGE
function saveCartToStorage() {
  localStorage.setItem('nova_cart_storage', JSON.stringify(AppState.cart));
}

function loadCartFromStorage() {
  const saved = localStorage.getItem('nova_cart_storage');
  if (saved) {
    try {
      AppState.cart = JSON.parse(saved);
      // Re-map product references to our runtime state
      AppState.cart.forEach(item => {
        const productRef = AppState.products.find(p => p.id === item.product.id);
        if (productRef) {
          item.product = productRef;
        }
      });
    } catch (e) {
      AppState.cart = [];
    }
  }
}

// CART DRAWER NAV ACTIONS
function openCartDrawer() {
  DOM.cartDrawerOverlay.classList.add('active');
}

function closeCartDrawer() {
  DOM.cartDrawerOverlay.classList.remove('active');
}

window.openMobileMenu = function () {
  const overlay = document.getElementById('mobile-menu-drawer-overlay');
  if (overlay) overlay.classList.add('active');
};

window.closeMobileMenu = function () {
  const overlay = document.getElementById('mobile-menu-drawer-overlay');
  if (overlay) overlay.classList.remove('active');
};

// WISHLIST FUNCTIONAL SYSTEM
window.toggleWishlist = function (productId) {
  const index = AppState.wishlist.indexOf(productId);
  const product = AppState.products.find(p => p.id === productId);
  if (!product) return;

  if (index > -1) {
    // Remove from wishlist
    AppState.wishlist.splice(index, 1);
    showToast(`${product.name.toUpperCase()} REMOVED FROM WISHLIST.`);
  } else {
    // Add to wishlist
    AppState.wishlist.push(productId);
    showToast(`${product.name.toUpperCase()} SAVED TO WISHLIST.`);
  }

  saveWishlistToStorage();
  updateWishlistUI();

  // Re-render shop and featured grids to reflect card heart state
  renderFeaturedProducts();
  renderShop();

  // If details modal is open on this product, toggle button class too
  if (AppState.selectedProduct && AppState.selectedProduct.id === productId) {
    const isWishlisted = AppState.wishlist.includes(productId);
    if (DOM.modalWishlistBtn) {
      if (isWishlisted) {
        DOM.modalWishlistBtn.classList.add('wishlist-filled');
      } else {
        DOM.modalWishlistBtn.classList.remove('wishlist-filled');
      }
    }
  }
};

function updateWishlistUI() {
  const count = AppState.wishlist.length;
  DOM.wishlistCountBadge.innerText = count;

  const mobileWishlistBadge = document.getElementById('mobile-wishlist-count-badge');
  if (mobileWishlistBadge) {
    mobileWishlistBadge.innerText = count;
  }

  // Toggle header heart filled styling
  if (count > 0) {
    DOM.wishlistHeaderBtn.classList.add('wishlist-filled');
    DOM.wishlistCountBadge.classList.remove('hidden');
  } else {
    DOM.wishlistHeaderBtn.classList.remove('wishlist-filled');
    DOM.wishlistCountBadge.classList.add('hidden');
  }

  // Populate Wishlist Drawer items
  if (!DOM.wishlistItemsContainer) return;
  DOM.wishlistItemsContainer.innerHTML = '';

  if (count === 0) {
    const wishlistEmptyMsg = TRANSLATIONS[AppState.language]['wishlist_empty_msg'] || 'Your wishlist is currently empty.';
    const browseShopBtnText = TRANSLATIONS[AppState.language]['browse_shop_btn'] || 'Browse Shop';
    DOM.wishlistItemsContainer.innerHTML = `
      <div class="cart-empty-state">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
        <p style="font-weight: 300; font-size: 0.9rem; letter-spacing: 0.05em;">${wishlistEmptyMsg}</p>
        <button class="btn-primary" onclick="closeWishlistDrawer(); window.location.hash='#/shop';">${browseShopBtnText}</button>
      </div>
    `;
    return;
  }

  AppState.wishlist.forEach(prodId => {
    const prod = AppState.products.find(p => p.id === prodId);
    if (!prod) return;

    const translatedCategory = getTranslatedCategory(prod.category);
    const addBtnLabel = prod.stock > 0
      ? (TRANSLATIONS[AppState.language]['card_add_to_cart'] || 'Add to Bag')
      : (TRANSLATIONS[AppState.language]['card_sold_out'] || 'Sold Out');

    const itemDiv = document.createElement('div');
    itemDiv.className = 'cart-item';
    itemDiv.innerHTML = `
      <img src="${prod.image}" alt="${prod.name}" class="cart-item-image">
      <div class="cart-item-details">
        <h4 class="cart-item-name">${prod.name}</h4>
        <div class="cart-item-meta">${translatedCategory} &bull; ${prod.brand}</div>
        <div class="cart-item-row" style="margin-top: 10px;">
          <span class="cart-item-price">ЦЏ${prod.price}</span>
          <button class="btn-primary" style="font-size:0.7rem; padding: 6px 12px;" onclick="closeWishlistDrawer(); quickAddToCart('${prod.id}')" ${prod.stock <= 0 ? 'disabled' : ''}>
            ${addBtnLabel}
          </button>
        </div>
      </div>
      <button class="cart-item-remove" onclick="toggleWishlist('${prod.id}')">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
        </svg>
      </button>
    `;
    DOM.wishlistItemsContainer.appendChild(itemDiv);
  });

  if (AppState.currentRoute === 'wishlist') {
    renderWishlistPage();
  }
}

window.addAllWishlistToCart = function () {
  if (AppState.wishlist.length === 0) return;

  let addedCount = 0;
  AppState.wishlist.forEach(prodId => {
    const prod = AppState.products.find(p => p.id === prodId);
    if (prod && prod.stock > 0) {
      // Add standard 100ml size
      const cartIndex = AppState.cart.findIndex(item => item.product.id === prod.id && item.size === '100ml');
      if (cartIndex === -1) {
        AppState.cart.push({
          product: prod,
          size: '100ml',
          quantity: 1,
          price: prod.price
        });
        addedCount++;
      }
    }
  });

  if (addedCount > 0) {
    saveCartToStorage();
    updateCartUI();
    showToast(`ADDED ${addedCount} FRAGRANCES TO BAG.`);
  } else {
    showToast("WISHLIST ITEMS ARE ALREADY IN BAG OR SOLD OUT.");
  }

  closeWishlistDrawer();
  openCartDrawer();
};

function saveWishlistToStorage() {
  localStorage.setItem('nova_wishlist_storage', JSON.stringify(AppState.wishlist));
}

function loadWishlistFromStorage() {
  const saved = localStorage.getItem('nova_wishlist_storage');
  if (saved) {
    try {
      AppState.wishlist = JSON.parse(saved);
    } catch (e) {
      AppState.wishlist = [];
    }
  }
}

window.openWishlistDrawer = function () {
  DOM.wishlistDrawerOverlay.classList.add('active');
};

window.closeWishlistDrawer = function () {
  DOM.wishlistDrawerOverlay.classList.remove('active');
};

window.renderWishlistPage = function () {
  const container = document.getElementById('wishlist-page-container');
  if (!container) return;
  container.innerHTML = '';

  const count = AppState.wishlist.length;
  if (count === 0) {
    const wishlistEmptyMsg = TRANSLATIONS[AppState.language]['wishlist_empty_msg'] || 'Your wishlist is currently empty.';
    const browseShopBtnText = TRANSLATIONS[AppState.language]['browse_shop_btn'] || 'Browse Shop';
    container.innerHTML = `
      <div class="wishlist-page-empty">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
        <p style="font-weight: 300; font-size: 1.1rem; letter-spacing: 0.05em;">${wishlistEmptyMsg}</p>
        <button class="btn-primary" onclick="window.location.hash='#/shop';" style="padding: 12px 30px; border-radius: 8px;">${browseShopBtnText}</button>
      </div>
    `;
    return;
  }

  const grid = document.createElement('div');
  grid.className = 'wishlist-page-grid';

  AppState.wishlist.forEach(prodId => {
    const prod = AppState.products.find(p => p.id === prodId);
    if (!prod) return;

    const translatedCategory = getTranslatedCategory(prod.category);
    const addBtnLabel = prod.stock > 0
      ? (TRANSLATIONS[AppState.language]['card_add_to_cart'] || 'Add to Bag')
      : (TRANSLATIONS[AppState.language]['card_sold_out'] || 'Sold Out');

    const itemDiv = document.createElement('div');
    itemDiv.className = 'wishlist-page-item';
    itemDiv.innerHTML = `
      <div class="wishlist-page-image-wrap" style="cursor: pointer;" onclick="openProductModal('${prod.id}')">
        <img src="${prod.image}" alt="${prod.name}" class="wishlist-page-image">
      </div>
      <div class="wishlist-page-details">
        <div class="wishlist-page-meta">${translatedCategory} &bull; ${prod.brand}</div>
        <h4 class="wishlist-page-name" style="cursor: pointer;" onclick="openProductModal('${prod.id}')">${prod.name}</h4>
        <div class="wishlist-page-price">ЦЏ${prod.price}</div>
        <div class="wishlist-page-actions">
          <button class="btn-primary wishlist-page-add-btn" onclick="quickAddToCart('${prod.id}')" ${prod.stock <= 0 ? 'disabled' : ''}>
            ${addBtnLabel}
          </button>
          <button class="wishlist-page-remove-btn" onclick="toggleWishlist('${prod.id}')" aria-label="Remove from Wishlist">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
            </svg>
          </button>
        </div>
      </div>
    `;
    grid.appendChild(itemDiv);
  });

  container.appendChild(grid);
};

// PRODUCT DETAILS PAGE VIEW
window.openProductModal = function (productId) {
  window.location.href = 'product.html?id=' + productId;
};

window.initProductPage = function () {
  const params = new URLSearchParams(window.location.search);
  const productId = params.get('id');
  if (!productId) {
    window.location.href = 'index.html';
    return;
  }
  renderProductPage(productId);
  initProductInteractiveFeatures();
};

function initProductInteractiveFeatures() {
  // 1. Hover Inspect Zoom Magnifier
  const zoomContainer = document.getElementById('pp-zoom-container');
  const zoomImg = document.getElementById('pp-img');
  if (zoomContainer && zoomImg) {
    zoomContainer.addEventListener('mousemove', (e) => {
      const rect = zoomContainer.getBoundingClientRect();
      const x = ((e.clientX - rect.left) / rect.width) * 100;
      const y = ((e.clientY - rect.top) / rect.height) * 100;
      zoomImg.style.transformOrigin = `${x}% ${y}%`;
    });
    zoomContainer.addEventListener('mouseenter', () => {
      zoomContainer.classList.add('is-zoomed');
    });
    zoomContainer.addEventListener('mouseleave', () => {
      zoomContainer.classList.remove('is-zoomed');
      zoomImg.style.transformOrigin = 'center center';
    });
  }

  // 2. Smooth FAQ Accordion Transitions
  const faqItems = document.querySelectorAll('.faq-item');
  faqItems.forEach(item => {
    const summary = item.querySelector('summary');
    const content = item.querySelector('.faq-item-content');
    if (summary && content) {
      summary.addEventListener('click', (e) => {
        e.preventDefault();
        if (item.hasAttribute('open')) {
          content.style.gridTemplateRows = '0fr';
          content.addEventListener('transitionend', function handler() {
            item.removeAttribute('open');
            content.removeEventListener('transitionend', handler);
          }, { once: true });
        } else {
          item.setAttribute('open', '');
          // trigger reflow
          content.offsetHeight;
          content.style.gridTemplateRows = '1fr';
        }
      });
    }
  });
}

function renderProductPage(productId) {
  const product = AppState.products.find(p => p.id === productId);
  if (!product) return;

  // Determine available sizes and select first available
  const availableSizes = (product.sizes || []).filter(s => s.price && s.price > 0);
  AppState.selectedProduct = product;
  AppState.selectedSize = availableSizes.length > 0 ? availableSizes[0].size : '100ml';

  // Get translations
  const transProd = getTranslatedProduct(productId);
  const tagline = transProd ? transProd.tagline : product.tagline;
  const description = transProd ? transProd.description : product.description;
  const translatedCategory = getTranslatedCategory(product.category);
  const topNotes = transProd ? transProd.top.join(', ') : product.notes.top.join(', ');
  const heartNotes = transProd ? transProd.heart.join(', ') : product.notes.heart.join(', ');
  const baseNotes = transProd ? transProd.base.join(', ') : product.notes.base.join(', ');
  const reviewsText = TRANSLATIONS[AppState.language]['reviews_label'] || 'reviews';

  // Populate Product Page Fields
  document.getElementById('pp-img').src = product.image;
  document.getElementById('pp-category').innerText = `${translatedCategory} вЂў ${product.brand}`;
  document.getElementById('pp-name').innerText = product.name;
  document.getElementById('pp-tagline').innerText = tagline;
  document.getElementById('pp-desc').innerText = description;
  document.getElementById('pp-rating-val').innerText = `${product.rating} (${product.reviewsCount} ${reviewsText})`;

  // Show "Edit This Product" button for admins вЂ” inline with rating
  let editBtn = document.getElementById('pp-admin-edit-btn');
  if (editBtn) editBtn.remove();
  const adminSession = sessionStorage.getItem('nova_admin_session');
  if (adminSession) {
    editBtn = document.createElement('button');
    editBtn.id = 'pp-admin-edit-btn';
    editBtn.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" style="width:12px; height:12px;"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg> Edit`;
    editBtn.style.cssText = 'display:inline-flex; align-items:center; gap:4px; padding:3px 10px; font-size:0.65rem; font-weight:600; text-transform:uppercase; letter-spacing:0.04em; border:1px solid var(--color-sage); background:none; color:var(--color-sage); border-radius:3px; cursor:pointer; margin-left:12px; transition:all 0.2s; vertical-align:middle;';
    editBtn.addEventListener('mouseenter', () => { editBtn.style.background = 'var(--color-sage)'; editBtn.style.color = '#fff'; });
    editBtn.addEventListener('mouseleave', () => { editBtn.style.background = 'none'; editBtn.style.color = 'var(--color-sage)'; });
    editBtn.addEventListener('click', () => {
      window.openDetailedProductModal(product.id);
    });
    // Insert inside the rating row, after the rating text
    const ratingRow = document.querySelector('.pp-rating-row');
    if (ratingRow) {
      ratingRow.appendChild(editBtn);
    }
  }

  // Render thumbnails from product.images array only
  const allImages = [];
  if (product.image) allImages.push(product.image);
  if (product.images && Array.isArray(product.images)) {
    product.images.forEach(img => {
      if (!allImages.includes(img)) allImages.push(img);
    });
  }

  const thumbGrid = document.getElementById('pp-thumbnails');
  if (thumbGrid) {
    thumbGrid.innerHTML = '';
    if (allImages.length > 1) {
      allImages.forEach((src, idx) => {
        const thumbItem = document.createElement('div');
        thumbItem.className = `pp-thumbnail-item ${idx === 0 ? 'active' : ''}`;
        thumbItem.innerHTML = `<img src="${src}" alt="Thumbnail ${idx + 1}">`;
        thumbItem.addEventListener('click', () => {
          document.getElementById('pp-img').src = src;
          thumbGrid.querySelectorAll('.pp-thumbnail-item').forEach(btn => btn.classList.remove('active'));
          thumbItem.classList.add('active');
        });
        thumbGrid.appendChild(thumbItem);
      });
    }
  }

  // Populate Olfactory narrative notes
  const expTop = document.getElementById('exp-top-notes');
  const expHeart = document.getElementById('exp-heart-notes');
  const expBase = document.getElementById('exp-base-notes');
  if (expTop && expHeart && expBase) {
    expTop.innerText = topNotes;
    expHeart.innerText = heartNotes;
    expBase.innerText = baseNotes;
  }

  // Populate Suggested Products ("Complete the Ritual")
  const suggestionsGrid = document.getElementById('pp-suggestions-grid');
  if (suggestionsGrid) {
    suggestionsGrid.innerHTML = '';
    // filter out current product
    let recommendations = AppState.products.filter(p => p.id !== product.id);
    // prioritize same category
    let sameCat = recommendations.filter(p => p.category === product.category);
    let others = recommendations.filter(p => p.category !== product.category);
    let finalRecs = [...sameCat, ...others].slice(0, 3);

    finalRecs.forEach(rec => {
      suggestionsGrid.appendChild(createProductCard(rec));
    });
  }

  // Reset Quantity Input
  const qtyInput = document.getElementById('pp-qty-input');
  qtyInput.value = 1;

  // Manage Stock indicator
  const addBtn = document.getElementById('pp-add-btn');
  if (product.stock > 0) {
    addBtn.disabled = false;
    addBtn.innerText = TRANSLATIONS[AppState.language]['modal_add_to_cart'] || 'Add to Cart';
  } else {
    addBtn.disabled = true;
    addBtn.innerText = TRANSLATIONS[AppState.language]['card_sold_out'] || 'Sold Out';
  }

  // Populate Fragrance Pyramid
  document.getElementById('pp-top').innerText = topNotes;
  document.getElementById('pp-heart').innerText = heartNotes;
  document.getElementById('pp-base').innerText = baseNotes;

  // Size Selector options
  renderSizeSelectors(product);
  updateModalPrice(product);

  // Bind Add to Cart action inside modal
  addBtn.onclick = () => {
    const qty = parseInt(qtyInput.value) || 1;
    addToCart(product, AppState.selectedSize, qty);
    openCartDrawer();
  };

  // Bind Qty buttons inside modal
  const minusBtn = document.getElementById('pp-qty-minus');
  const plusBtn = document.getElementById('pp-qty-plus');

  minusBtn.onclick = () => {
    let q = parseInt(qtyInput.value) || 1;
    if (q > 1) {
      qtyInput.value = q - 1;
    }
  };

  plusBtn.onclick = () => {
    let q = parseInt(qtyInput.value) || 1;
    if (q < product.stock) {
      qtyInput.value = q + 1;
    } else {
      showToast(`ONLY ${product.stock} ITEMS AVAILABLE IN STOCK.`);
    }
  };
}

function closeProductModal() { }

function renderSizeSelectors(product) {
  const container = document.getElementById('pp-sizes');
  container.innerHTML = '';

  // Only show sizes that have a valid price
  const validSizes = (product.sizes || []).filter(s => s.price && s.price > 0);
  
  validSizes.forEach(sizeObj => {
    const btn = document.createElement('button');
    btn.className = `size-btn ${sizeObj.size === AppState.selectedSize ? 'active' : ''}`;
    btn.innerText = sizeObj.size;
    btn.addEventListener('click', () => {
      // Toggle select size
      container.querySelectorAll('.size-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      AppState.selectedSize = sizeObj.size;
      updateModalPrice(product);
    });
    container.appendChild(btn);
  });
}

function updateModalPrice(product) {
  const sizeObj = product.sizes.find(s => s.size === AppState.selectedSize);
  const price = sizeObj ? sizeObj.price : product.price;
  document.getElementById('pp-price').innerText = `ЦЏ${price}`;
}

// CHECKOUT LOGIC
window.goToCheckout = function () {
  if (AppState.cart.length === 0) {
    showToast("YOUR CART IS EMPTY.");
    return;
  }
  closeCartDrawer();
  // If on product page, redirect to main page checkout
  const isProductPage = window.location.pathname.endsWith('/product') || window.location.pathname.endsWith('/product.html');
  if (isProductPage) {
    window.location.href = 'index.html#/checkout';
    return;
  }
  window.location.hash = '#/checkout';
  renderCheckoutPage();
};

function renderCheckoutPage() {
  const summaryContainer = document.getElementById('checkout-items-list');
  if (!summaryContainer) return;
  summaryContainer.innerHTML = '';

  AppState.cart.forEach(item => {
    const div = document.createElement('div');
    div.className = 'order-summary-item';
    div.innerHTML = `
      <span>${item.product.name} (x${item.quantity}) - ${item.size}</span>
      <strong>ЦЏ${item.price * item.quantity}</strong>
    `;
    summaryContainer.appendChild(div);
  });

  updateCheckoutTotals();
  prefillCheckoutForm();
}

// Pre-fill checkout form from saved billing info or logged-in user
function prefillCheckoutForm() {
  const saved = JSON.parse(localStorage.getItem('nova_billing_info') || 'null');
  const firstNameEl = document.getElementById('billing-first-name');
  const lastNameEl = document.getElementById('billing-last-name');
  const emailEl = document.getElementById('billing-email');
  const addressEl = document.getElementById('billing-address');
  const cityEl = document.getElementById('billing-city');
  const zipEl = document.getElementById('billing-zip');

  if (saved) {
    if (firstNameEl) firstNameEl.value = saved.firstName || '';
    if (lastNameEl) lastNameEl.value = saved.lastName || '';
    if (emailEl) emailEl.value = saved.email || '';
    if (addressEl) addressEl.value = saved.address || '';
    if (cityEl) cityEl.value = saved.city || '';
    if (zipEl) zipEl.value = saved.zip || '';
  } else if (AppState.customer && AppState.customer.firstName) {
    if (firstNameEl) firstNameEl.value = AppState.customer.firstName || '';
    if (lastNameEl) lastNameEl.value = AppState.customer.lastName || '';
    if (emailEl) emailEl.value = AppState.customer.email || '';
  }
}

function updateCheckoutTotals() {
  const subtotal = AppState.cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);

  const shippingSelect = document.getElementById('shipping-method');
  const shippingCost = shippingSelect ? parseInt(shippingSelect.value) : 0;
  const total = subtotal + shippingCost;

  document.getElementById('checkout-subtotal-val').innerText = `ЦЏ${subtotal}`;
  document.getElementById('checkout-shipping-val').innerText = shippingCost === 0 ? 'Free' : `ЦЏ${shippingCost}`;
  document.getElementById('checkout-total-val').innerText = `ЦЏ${total}`;
}

function processCheckout() {
  const shippingSelect = document.getElementById('shipping-method');
  const subtotal = AppState.cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const shippingCost = shippingSelect ? parseInt(shippingSelect.value) : 0;
  const total = subtotal + shippingCost;

  const customerData = {
    firstName: document.getElementById('billing-first-name').value,
    lastName: document.getElementById('billing-last-name').value,
    email: document.getElementById('billing-email').value,
    address: document.getElementById('billing-address').value,
    city: document.getElementById('billing-city').value,
    zip: document.getElementById('billing-zip').value
  };

  // Save billing info to localStorage for next time
  localStorage.setItem('nova_billing_info', JSON.stringify(customerData));

  // Deduct products stocks
  AppState.cart.forEach(item => {
    const prod = AppState.products.find(p => p.id === item.product.id);
    if (prod) {
      prod.stock = Math.max(0, prod.stock - item.quantity);
    }
  });
  saveProductsToStorage();

  // Record order in WooCommerce database
  const order = WooCommerceAdmin.addOrder(customerData, AppState.cart, total);

  // Clear Cart
  AppState.cart = [];
  saveCartToStorage();
  updateCartUI();

  // Show Success Receipt view
  const checkoutContainer = document.getElementById('view-checkout');
  const successTitle = TRANSLATIONS[AppState.language]['order_success_title'] || 'Order Placed Successfully';
  const successDesc = (TRANSLATIONS[AppState.language]['order_success_desc'] || 'Thank you for choosing NOVA. Your order reference is <strong>#REF</strong>. We have sent a confirmation email to EMAIL.')
    .replace('#REF', `#${order.id}`)
    .replace('EMAIL', order.email);
  const orderDetailsLabel = TRANSLATIONS[AppState.language]['order_details'] || 'Order details';
  const orderDateLabel = TRANSLATIONS[AppState.language]['order_date'] || 'Date:';
  const orderShipToLabel = TRANSLATIONS[AppState.language]['order_ship_to'] || 'Ship to:';
  const orderTotalPaidLabel = TRANSLATIONS[AppState.language]['order_total_paid'] || 'Total paid:';
  const continueShoppingBtnText = TRANSLATIONS[AppState.language]['continue_shopping_btn'] || 'Continue Shopping';

  checkoutContainer.innerHTML = `
    <div class="container">
      <div class="checkout-success-view text-center">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" style="margin: 0 auto 16px auto;">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <h2 class="serif-title" style="font-size: 2.2rem; margin-bottom: 12px;">${successTitle}</h2>
        <p style="font-size: 0.9rem; color: var(--color-medium-gray); margin-bottom: 24px;">
          ${successDesc}
        </p>
        <div style="border-top: 1px solid var(--color-border); padding-top: 24px; text-align: left; margin-bottom: 24px;">
          <h4 style="font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 12px;">${orderDetailsLabel}</h4>
          <div style="font-size: 0.85rem; display: flex; flex-direction: column; gap: 8px;">
            <div style="display:flex; justify-content:space-between;"><span>${orderDateLabel}</span><span>${order.date}</span></div>
            <div style="display:flex; justify-content:space-between;"><span>${orderShipToLabel}</span><span>${customerData.address}, ${customerData.city}</span></div>
            <div style="display:flex; justify-content:space-between; font-weight: 600;"><span>${orderTotalPaidLabel}</span><span>ЦЏ${order.total}</span></div>
          </div>
        </div>
        <button class="btn-primary" onclick="restoreCheckoutPage(); window.location.hash='#/shop';">${continueShoppingBtnText}</button>
      </div>
    </div>
  `;

  // Send purchase confirmation email to customer
  if (typeof emailjs !== 'undefined') {
    const shippingSelect = document.getElementById('shipping-method');
    const shippingVal = shippingSelect ? parseInt(shippingSelect.value) : 0;
    const ordersList = order.items.map(i => {
      const prod = AppState.products.find(p => p.name === i.name);
      const itemPrice = prod ? prod.price : 0;
      return {
        name: i.name,
        price: `ЦЏ${itemPrice}`,
        qty: i.qty,
        bunch: i.qty
      };
    });

    emailjs.send('service_58z9fml', 'template_zpv3fet', {
      email: customerData.email,
      order_id: order.id,
      orders: ordersList,
      cost: {
        shipping: shippingVal === 0 ? 'Free' : `ЦЏ${shippingVal}`,
        tax: 'ЦЏ0',
        total: `ЦЏ${order.total}`
      }
    }).catch(err => console.error('Order confirmation email error:', err));
  }
}

// Restore default checkout page format after success screen
window.restoreCheckoutPage = function () {
  const checkoutContainer = document.getElementById('view-checkout');
  checkoutContainer.innerHTML = `
    <div class="container">
      <div class="checkout-grid">
        <!-- Billing Details Form -->
        <div class="admin-card">
          <h2 class="checkout-section-title serif-title">Billing Details</h2>
          <form id="checkout-billing-form">
            <div class="form-row">
              <div class="form-group">
                <label for="billing-first-name">First Name *</label>
                <input type="text" id="billing-first-name" required placeholder="John">
              </div>
              <div class="form-group">
                <label for="billing-last-name">Last Name *</label>
                <input type="text" id="billing-last-name" required placeholder="Doe">
              </div>
            </div>
            <div class="form-group">
              <label for="billing-email">Email Address *</label>
              <input type="email" id="billing-email" required placeholder="john@example.com">
            </div>
            <div class="form-group">
              <label for="billing-address">Street Address *</label>
              <input type="text" id="billing-address" placeholder="House number and street name" required value="221B Baker St">
            </div>
            <div class="form-row">
              <div class="form-group">
                <label for="billing-city">City *</label>
                <input type="text" id="billing-city" required placeholder="Yerevan">
              </div>
              <div class="form-group">
                <label for="billing-zip">ZIP / Postal Code *</label>
                <input type="text" id="billing-zip" required placeholder="0000">
              </div>
            </div>
            <div class="form-group">
              <label for="shipping-method">Shipping *</label>
              <select id="shipping-method" style="display: none;">
                <option value="0">Standard Shipping (Free)</option>
                <option value="15">Express Delivery (ЦЏ15)</option>
              </select>
              <div class="custom-dropdown" id="checkout-shipping-dropdown">
                <button type="button" class="dropdown-trigger" id="checkout-shipping-trigger">
                  <span id="checkout-shipping-value" data-value="0">Standard Shipping (Free)</span>
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="chevron-icon" style="width: 10px; height: 10px; transition: transform 0.2s;"><polyline points="6 9 12 15 18 9"></polyline></svg>
                </button>
                <ul class="dropdown-menu" id="checkout-shipping-menu">
                  <li class="dropdown-item active" data-value="0">Standard Shipping (Free)</li>
                  <li class="dropdown-item" data-value="15">Express Delivery (ЦЏ15)</li>
                </ul>
              </div>
            </div>
            <div style="margin-top: var(--spacing-lg);">
              <h3 style="font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 8px;">Payment</h3>
              <p style="font-size: 0.8rem; color: var(--color-medium-gray); margin-bottom: var(--spacing-sm);">Demo payment: Simulator mode. No real cards will be charged.</p>
              <div class="form-group">
                <input type="text" placeholder="Card Number" value="4111 2222 3333 4444" disabled style="background-color: var(--color-light-gray);">
              </div>
            </div>
            <button type="submit" class="btn-primary place-order-btn">Place Order</button>
          </form>
        </div>
        
        <!-- Order Summary Column -->
        <div class="order-summary-box">
          <h2 class="checkout-section-title serif-title">Your Order</h2>
          <div id="checkout-items-list" style="margin-bottom: var(--spacing-lg);">
            <!-- Populated dynamically -->
          </div>
          <div style="font-size: 0.9rem; display: flex; flex-direction: column; gap: 8px;">
            <div style="display:flex; justify-content:space-between;">
              <span>Subtotal:</span>
              <strong id="checkout-subtotal-val">ЦЏ0</strong>
            </div>
            <div style="display:flex; justify-content:space-between;">
              <span>Shipping:</span>
              <strong id="checkout-shipping-val">Free</strong>
            </div>
            <div class="order-summary-total-row">
              <span>Total:</span>
              <strong id="checkout-total-val" style="font-size: 1.3rem;">ЦЏ0</strong>
            </div>
          </div>
        </div>
      </div>
    </div>
  `;
  // Re-bind listeners
  initEventListeners();
};

// BRIDGE FUNCTIONS FOR WOOCOMMERCE DASHBOARD
// (refreshAdminDashboard defined below with full implementation)

// Global scope hooks for Admin edits
window.saveProductInventory = function (productId) {
  const product = AppState.products.find(p => p.id === productId);
  if (!product) return;

  const priceInput = document.getElementById(`inv-price-${productId}`);
  const stockInput = document.getElementById(`inv-stock-${productId}`);

  if (priceInput && stockInput) {
    const newPrice = parseFloat(priceInput.value);
    const newStock = parseInt(stockInput.value);

    if (isNaN(newPrice) || newPrice <= 0) {
      showToast("INVALID PRICE VALUE.");
      return;
    }
    if (isNaN(newStock) || newStock < 0) {
      showToast("INVALID STOCK VALUE.");
      return;
    }

    product.price = newPrice;
    product.stock = newStock;

    // Recalculate stock badge (fully translated)
    const badge = document.getElementById(`inv-badge-${productId}`);
    if (badge) {
      badge.className = `admin-status-badge ${newStock > 0 ? 'badge-instock' : 'badge-outofstock'}`;
      const lang = AppState.language;
      const inStockText = (TRANSLATIONS[lang] && TRANSLATIONS[lang]['in_stock_label']) || 'In Stock';
      const outOfStockText = (TRANSLATIONS[lang] && TRANSLATIONS[lang]['out_of_stock_label']) || 'Out of Stock';
      badge.innerText = newStock > 0 ? inStockText : outOfStockText;
    }

    // Update size prices proportionally (base price changes)
    product.sizes[0].price = Math.round(newPrice * 0.615); // 50ml price ratio
    product.sizes[1].price = newPrice;                    // 100ml price
    product.sizes[2].price = Math.round(newPrice * 1.59);  // 200ml price

    // Save to storage
    saveProductsToStorage();

    // Re-render shop views
    renderFeaturedProducts();
    renderShop();

    // Refresh dashboard calculations
    refreshAdminDashboard();

    // Log admin activity
    const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
    if (session) {
      logAdminActivity(session.name, `Updated inventory for ${product.name}: Price=ЦЏ${newPrice}, Stock=${newStock}`);
    }

    showToast(`UPDATED ${product.name.toUpperCase()} STOCK AND PRICE.`);
  }
};

window.updateOrderState = function (orderId, newStatus) {
  const success = WooCommerceAdmin.updateOrderStatus(orderId, newStatus);
  if (success) {
    refreshAdminDashboard();
    
    // Log admin activity
    const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
    if (session) {
      logAdminActivity(session.name, `Updated order status of #${orderId} to ${newStatus}`);
    }

    showToast(`ORDER #${orderId} SET TO ${newStatus.toUpperCase()}.`);
  }
};

// Reviews Slider Pagination Logic
document.addEventListener('DOMContentLoaded', () => {
  const nextBtn = document.getElementById('review-next');
  const prevBtn = document.getElementById('review-prev');
  const slider = document.getElementById('reviews-slider');

  if (nextBtn && prevBtn && slider) {
    nextBtn.addEventListener('click', () => {
      slider.scrollBy({ left: 350, behavior: 'smooth' });
    });
    prevBtn.addEventListener('click', () => {
      slider.scrollBy({ left: -350, behavior: 'smooth' });
    });
  }

  const aboutNextBtn = document.getElementById('about-review-next');
  const aboutPrevBtn = document.getElementById('about-review-prev');
  const aboutSlider = document.getElementById('about-reviews-slider');

  if (aboutNextBtn && aboutPrevBtn && aboutSlider) {
    aboutNextBtn.addEventListener('click', () => {
      aboutSlider.scrollBy({ left: 350, behavior: 'smooth' });
    });
    aboutPrevBtn.addEventListener('click', () => {
      aboutSlider.scrollBy({ left: -350, behavior: 'smooth' });
    });
  }
});

// Rotate scroll text SVG on scroll
window.addEventListener('scroll', () => {
  const scrollSvg = document.querySelector('.scroll-svg');
  if (scrollSvg) {
    scrollSvg.style.transform = `rotate(${window.scrollY / 4}deg)`;
  }
});

// SEARCH POPUP SYSTEM
function initSearch() {
  const container = document.querySelector('.search-container');
  const input = document.getElementById('search-input');
  const btn = document.querySelector('.search-btn');
  if (!container || !input) return;

  // Create dropdown element dynamically
  let dropdown = container.querySelector('.search-results-dropdown');
  if (!dropdown) {
    dropdown = document.createElement('div');
    dropdown.className = 'search-results-dropdown';
    container.appendChild(dropdown);
  }

  // Toggle active on container when focused
  input.addEventListener('focus', () => {
    container.classList.add('active');
    renderSearchDropdown(input.value, dropdown);
  });

  input.addEventListener('blur', () => {
    // Delay blur slightly so clicks on dropdown items can register first
    setTimeout(() => {
      if (document.activeElement !== input) {
        container.classList.remove('active');
        dropdown.classList.remove('active');
      }
    }, 200);
  });

  // Handle clicking the search button to expand/focus or close
  if (btn) {
    btn.addEventListener('click', (e) => {
      if (!container.classList.contains('active')) {
        e.preventDefault();
        input.focus();
      } else if (input.value.trim() === '') {
        e.preventDefault();
        input.blur();
      }
    });
  }

  // Handle input event (typing)
  input.addEventListener('input', (e) => {
    const query = e.target.value.toLowerCase().trim();

    // Also update AppState.filters.search for the shop page if we are on index
    AppState.filters.search = query;
    const isProductPage = window.location.pathname.endsWith('/product') || window.location.pathname.endsWith('/product.html');
    if (!isProductPage && typeof renderShop === 'function') {
      renderShop();
    }

    renderSearchDropdown(query, dropdown);
  });

  // Close search on Escape key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      input.blur();
    }
  });
}

function renderSearchDropdown(query, dropdown) {
  if (!query) {
    dropdown.classList.remove('active');
    dropdown.innerHTML = '';
    return;
  }

  const matches = AppState.products.filter(p => {
    const nameMatch = p.name && p.name.toLowerCase().includes(query);
    const taglineMatch = p.tagline && p.tagline.toLowerCase().includes(query);
    const categoryMatch = p.category && p.category.toLowerCase().includes(query);
    const brandMatch = p.brand && p.brand.toLowerCase().includes(query);
    const notesMatch = p.notes && (
      (p.notes.top && p.notes.top.some(n => n.toLowerCase().includes(query))) ||
      (p.notes.heart && p.notes.heart.some(n => n.toLowerCase().includes(query))) ||
      (p.notes.base && p.notes.base.some(n => n.toLowerCase().includes(query)))
    );
    return nameMatch || taglineMatch || categoryMatch || brandMatch || notesMatch;
  });

  if (matches.length === 0) {
    const noResultsText = TRANSLATIONS[AppState.language]['search_no_results'] || 'No olfactory notes found.';
    dropdown.innerHTML = `<div class="search-no-results">${noResultsText}</div>`;
  } else {
    dropdown.innerHTML = '';
    matches.slice(0, 5).forEach(prod => {
      const item = document.createElement('div');
      item.className = 'search-result-item';
      item.onclick = () => {
        openProductModal(prod.id);
      };

      item.innerHTML = `
        <img src="${prod.image}" alt="${prod.name}" class="search-result-thumb">
        <div class="search-result-info">
          <span class="search-result-name">${prod.name}</span>
          <span class="search-result-tagline">${prod.tagline}</span>
        </div>
        <span class="search-result-price">ЦЏ${prod.price}</span>
      `;
      dropdown.appendChild(item);
    });
  }

  dropdown.classList.add('active');
}

// ==========================================
// --- CUSTOM MY ACCOUNT ECOSYSTEM ---
// ==========================================

window.switchAccountTab = function(tabId) {
  // Toggle active tab buttons
  const sidebar = document.querySelector('.my-account-sidebar');
  if (sidebar) {
    sidebar.querySelectorAll('.account-nav-btn').forEach(btn => {
      if (btn.getAttribute('data-account-tab') === tabId) {
        btn.classList.add('active');
      } else {
        btn.classList.remove('active');
      }
    });
  }

  // Toggle panels
  const content = document.querySelector('.my-account-content');
  if (content) {
    content.querySelectorAll('.account-panel').forEach(panel => {
      if (panel.id === `account-sec-${tabId}`) {
        panel.classList.remove('hidden');
      } else {
        panel.classList.add('hidden');
      }
    });
  }
};

window.saveCustomerDetails = function(event) {
  event.preventDefault();
  AppState.customer.firstName = document.getElementById('acc-first-name').value;
  AppState.customer.lastName = document.getElementById('acc-last-name').value;
  AppState.customer.email = document.getElementById('acc-email').value;
  AppState.customer.billing.street = document.getElementById('acc-billing-street').value;
  AppState.customer.billing.city = document.getElementById('acc-billing-city').value;
  AppState.customer.billing.zip = document.getElementById('acc-billing-zip').value;

  const passwordVal = document.getElementById('acc-password').value;
  if (passwordVal) {
    showToast("PASSWORD UPDATED SECURELY.");
    document.getElementById('acc-password').value = '';
  }

  // Update greeting
  const greetingEl = document.getElementById('customer-welcome-greeting');
  if (greetingEl) {
    greetingEl.innerText = AppState.language === 'am' ? `Х€ХІХ»ХёЦ‚ХµХ¶, ${AppState.customer.firstName}` :
                           AppState.language === 'ru' ? `РџСЂРёРІРµС‚, ${AppState.customer.firstName}` :
                           `Hello, ${AppState.customer.firstName}`;
  }

  // Update header dropdown greeting too
  document.querySelectorAll('[data-trans="hello_user"]').forEach(el => {
    el.innerText = AppState.language === 'am' ? `Х€ХІХ»ХёЦ‚ХµХ¶, ${AppState.customer.firstName}` :
                   AppState.language === 'ru' ? `РџСЂРёРІРµС‚, ${AppState.customer.firstName}` :
                   `Hello, ${AppState.customer.firstName}`;
  });

  showToast("ACCOUNT DETAILS SAVED.");
  renderMyAccount();
};

window.buyAgain = function(productId) {
  const prod = AppState.products.find(p => p.id === productId);
  if (prod) {
    addToCart(prod, '100ml', 1);
    openCartDrawer();
  }
};

window.toggleOutofStockNotification = function(productId) {
  const idx = AppState.outOfStockNotifications.indexOf(productId);
  if (idx > -1) {
    AppState.outOfStockNotifications.splice(idx, 1);
    showToast("RESTOCK NOTIFICATION REMOVED.");
  } else {
    AppState.outOfStockNotifications.push(productId);
    showToast("RESTOCK NOTIFICATION ENABLED.");
  }
  renderMyAccount();
};

window.renderMyAccount = function() {
  const customer = AppState.customer;
  
  // 1. Update Welcome Greeting
  const greetingEl = document.getElementById('customer-welcome-greeting');
  if (greetingEl) {
    greetingEl.innerText = AppState.language === 'am' ? `Х€ХІХ»ХёЦ‚ХµХ¶, ${customer.firstName}` :
                           AppState.language === 'ru' ? `РџСЂРёРІРµС‚, ${customer.firstName}` :
                           `Hello, ${customer.firstName}`;
  }

  // 2. Populate Account Settings fields
  const fnInput = document.getElementById('acc-first-name');
  const lnInput = document.getElementById('acc-last-name');
  const emailInput = document.getElementById('acc-email');
  const streetInput = document.getElementById('acc-billing-street');
  const cityInput = document.getElementById('acc-billing-city');
  const zipInput = document.getElementById('acc-billing-zip');
  
  if (fnInput) fnInput.value = customer.firstName;
  if (lnInput) lnInput.value = customer.lastName;
  if (emailInput) emailInput.value = customer.email;
  if (streetInput) streetInput.value = customer.billing.street;
  if (cityInput) cityInput.value = customer.billing.city;
  if (zipInput) zipInput.value = customer.billing.zip;

  // 3. Render Scent Profile Summary Bars
  const barsContainer = document.getElementById('scent-wardrobe-summary-bars');
  if (barsContainer) {
    barsContainer.innerHTML = '';
    
    // Count scent families from pastPurchases
    const counts = { Woody: 0, Floral: 0, Citrus: 0, Amber: 0 };
    customer.pastPurchases.forEach(purchase => {
      const prod = AppState.products.find(p => p.id === purchase.id);
      if (prod) {
        counts[prod.category] = (counts[prod.category] || 0) + 1;
      }
    });

    const totalPurchases = customer.pastPurchases.length;
    
    if (totalPurchases === 0) {
      barsContainer.innerHTML = `<p style="font-size: 0.82rem; color: var(--color-medium-gray); font-style: italic;">No scent profile data available yet.</p>`;
    } else {
      Object.keys(counts).forEach(family => {
        const count = counts[family];
        const pct = Math.round((count / totalPurchases) * 100);
        
        const barDiv = document.createElement('div');
        barDiv.innerHTML = `
          <div style="display:flex; justify-content:space-between; font-size:0.8rem; margin-bottom:4px;">
            <span>${family}</span>
            <span>${pct}%</span>
          </div>
          <div style="background-color: var(--color-light-gray); height: 6px; border-radius:3px; overflow:hidden;">
            <div style="background-color: var(--color-sage); width: ${pct}%; height: 100%;"></div>
          </div>
        `;
        barsContainer.appendChild(barDiv);
      });
    }
  }

  // 4. Render Scent Wardrobe Grid
  const wardrobeGrid = document.getElementById('account-wardrobe-grid');
  if (wardrobeGrid) {
    wardrobeGrid.innerHTML = '';
    if (customer.pastPurchases.length === 0) {
      wardrobeGrid.innerHTML = `<div style="grid-column: 1/-1; text-align: center; color: var(--color-medium-gray); padding: 40px 0; font-size: 0.9rem;" data-trans="acc_wardrobe_empty">Your scent wardrobe is empty.</div>`;
    } else {
      customer.pastPurchases.forEach(purchase => {
        const prod = AppState.products.find(p => p.id === purchase.id);
        if (prod) {
          const card = document.createElement('div');
          card.className = 'wardrobe-card';
          card.innerHTML = `
            <img src="${prod.image}" alt="${prod.name}" class="wardrobe-img">
            <div class="wardrobe-info">
              <h4 class="serif-title" style="font-size: 1.1rem;">${prod.name}</h4>
              <p style="font-size: 0.75rem; color: var(--color-medium-gray);">${purchase.size} вЂў Purchased on ${purchase.date}</p>
              <button class="btn-primary" onclick="buyAgain('${prod.id}')" style="font-size:0.7rem; padding: 6px 12px; margin-top: 8px;">Buy Again</button>
            </div>
          `;
          wardrobeGrid.appendChild(card);
        }
      });
    }
  }

  // 5. Render Personal Discount
  renderPersonalDiscount();

  // 6. Render Order History
  const ordersTbody = document.getElementById('account-orders-tbody');
  if (ordersTbody) {
    ordersTbody.innerHTML = '';
    if (customer.orders.length === 0) {
      ordersTbody.innerHTML = `<tr><td colspan="6" style="text-align:center; color:var(--color-medium-gray); padding: 30px;" data-trans="acc_orders_empty">No orders placed yet.</td></tr>`;
    } else {
      customer.orders.forEach(order => {
        const tr = document.createElement('tr');
        tr.innerHTML = `
          <td><strong>#${order.id}</strong></td>
          <td>${order.date}</td>
          <td>${order.items}</td>
          <td><strong>${order.total}</strong></td>
          <td><span class="admin-status-badge badge-${order.status}">${order.status}</span></td>
          <td><button class="btn-secondary" style="font-size:0.65rem; padding:4px 8px;" onclick="showToast('INVOICE DOWNLOAD SIMULATED')">Invoice</button></td>
        `;
        ordersTbody.appendChild(tr);
      });
    }
  }

  // 7. Render Wishlist
  const wishlistGrid = document.getElementById('account-wishlist-grid');
  if (wishlistGrid) {
    wishlistGrid.innerHTML = '';
    if (AppState.wishlist.length === 0) {
      wishlistGrid.innerHTML = `<div style="grid-column: 1/-1; text-align: center; color: var(--color-medium-gray); padding: 40px 0; font-size: 0.9rem;" data-trans="wishlist_empty_msg">Your wishlist is currently empty.</div>`;
    } else {
      AppState.wishlist.forEach(productId => {
        const prod = AppState.products.find(p => p.id === productId);
        if (prod) {
          const card = document.createElement('div');
          card.className = 'product-card';
          
          const isNotified = AppState.outOfStockNotifications.includes(prod.id);
          const notifyBtnText = isNotified ? 'Notification Active' : 'Notify Restock';
          const notifyBtnClass = isNotified ? 'btn-secondary' : 'btn-primary';
          
          card.innerHTML = `
            <img src="${prod.image}" alt="${prod.name}" class="product-img">
            <div class="product-info">
              <h4 class="serif-title" style="font-size: 1.1rem;">${prod.name}</h4>
              <p style="font-size: 0.75rem; color: var(--color-medium-gray);">${prod.tagline}</p>
              <div style="display:flex; justify-content:space-between; align-items:center; margin-top:8px;">
                <strong>ЦЏ${prod.price}</strong>
                ${prod.stock > 0 ? 
                  `<button class="btn-primary" onclick="buyAgain('${prod.id}')" style="font-size:0.65rem; padding: 4px 8px;">Add to Bag</button>` :
                  `<button class="${notifyBtnClass}" onclick="toggleOutofStockNotification('${prod.id}')" style="font-size:0.65rem; padding: 4px 8px;">${notifyBtnText}</button>`
                }
              </div>
            </div>
          `;
          wishlistGrid.appendChild(card);
        }
      });
    }
  }
};

function initMyAccountNavigation() {
  const sidebar = document.querySelector('.my-account-sidebar');
  if (sidebar) {
    sidebar.querySelectorAll('.account-nav-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const tabId = btn.getAttribute('data-account-tab');
        if (tabId) {
          switchAccountTab(tabId);
        }
      });
    });
  }
}

// ==========================================
// --- WOOCOMMERCE ADMIN ACCESS CONTROL ---
// ==========================================

async function initStaffProfiles() {
  await NovaDB.whenReady();
  let parsed = NovaDB.getStaffProfiles() || {};
  
  if (!parsed['norayrnajaryann@gmail.com']) {
    parsed['norayrnajaryann@gmail.com'] = {
      username: 'norayrnajaryann@gmail.com',
      name: 'Norayr Najaryan',
      role: 'Super Admin',
      password: 'Ananan05021998!'
    };
    parsed['sarah@example.com'] = {
      username: 'sarah@example.com',
      name: 'Sarah Vance',
      role: 'Shop Manager',
      password: 'sarah123'
    };
    parsed['david@example.com'] = {
      username: 'david@example.com',
      name: 'David Cole',
      role: 'Shop Manager',
      password: 'david123'
    };
    NovaDB.saveStaffProfiles(parsed);
  }
}

function getStaffProfiles() {
  initStaffProfiles();
  return NovaDB.getStaffProfiles() || {};
}

function saveStaffProfiles(profiles) {
  NovaDB.saveStaffProfiles(profiles);
}

// submitAdminCredentials is defined in the Admin Access Control System section below

window.handleAdminLogout = function() {
  const currentSession = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (currentSession) {
    logAdminActivity(currentSession.name, "Logged out");
  }
  sessionStorage.removeItem('nova_admin_session');
  showToast("LOGGED OUT OF WOOCOMMERCE ADMIN PANEL.");
  
  document.getElementById('admin-dashboard-wrapper').classList.add('hidden');
  document.getElementById('admin-login-wrapper').classList.remove('hidden');
};

window.switchAdminTab = function(tabId) {
  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (!session) return;
  
  // Block non-superadmin from developer settings tab
  if (tabId === 'developer' && session.role !== 'Super Admin') {
    showToast("ACCESS DENIED. DEVELOPER SETTINGS RESTRICTED TO SUPER ADMIN.");
    return;
  }

  // Toggle active tab buttons
  const sidebar = document.querySelector('.admin-sidebar');
  if (sidebar) {
    sidebar.querySelectorAll('.admin-nav-item').forEach(btn => {
      if (btn.getAttribute('data-admin-tab') === tabId) {
        btn.classList.add('active');
      } else {
        btn.classList.remove('active');
      }
    });
  }

  // Toggle panels
  const content = document.querySelector('.admin-content');
  if (content) {
    content.querySelectorAll('.admin-panel-sub').forEach(panel => {
      if (panel.id === `admin-sec-${tabId}`) {
        panel.classList.remove('hidden');
      } else {
        panel.classList.add('hidden');
      }
    });
  }
  
  if (tabId === 'logs') {
    renderAuditLogsTable();
  }
};



window.logAdminActivity = function(actor, action) {
  let logs = NovaDB.getAuditLogs() || [];
  
  const dateObj = new Date();
  const timestamp = dateObj.getFullYear() + '-' + 
    String(dateObj.getMonth() + 1).padStart(2, '0') + '-' + 
    String(dateObj.getDate()).padStart(2, '0') + ' ' + 
    String(dateObj.getHours()).padStart(2, '0') + ':' + 
    String(dateObj.getMinutes()).padStart(2, '0') + ':' +
    String(dateObj.getSeconds()).padStart(2, '0');
    
  logs.unshift({
    timestamp: timestamp,
    operator: actor,
    action: action
  });
  
  if (logs.length > 100) {
    logs = logs.slice(0, 100);
  }
  
  NovaDB.saveAuditLogs(logs);
};

window.clearAuditLogs = function() {
  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (!session) return;
  
  if (session.role !== 'Super Admin') {
    showToast("ACCESS DENIED. ONLY SUPER ADMIN CAN CLEAR LOGS.");
    return;
  }
  
  NovaDB.saveAuditLogs([]);
  logAdminActivity(session.name, "Cleared platform audit logs history");
  showToast("AUDIT LOGS HISTORY CLEARED.");
  renderAuditLogsTable();
};

window.renderAuditLogsTable = function() {
  const tbody = document.getElementById('admin-audit-logs-tbody');
  if (!tbody) return;
  tbody.innerHTML = '';
  
  let logs = NovaDB.getAuditLogs() || [];
  
  const lang = AppState.language;
  if (logs.length === 0) {
    const emptyText = TRANSLATIONS[lang]['admin_logs_empty'] || 'No logs recorded.';
    tbody.innerHTML = `<tr><td colspan="3" style="text-align:center; color:var(--color-medium-gray); padding: 20px;">${emptyText}</td></tr>`;
    return;
  }
  
  logs.forEach(log => {
    let actionText = log.action;
    
    // Localize log actions based on standard templates
    if (log.action.startsWith("Logged in successfully as ")) {
      const role = log.action.replace("Logged in successfully as ", "");
      const transRole = role === "Super Admin" ? (lang === 'am' ? "ХЌХёЦ‚ХєХҐЦЂ Ф±Х¤ХґХ«Х¶" : lang === 'ru' ? "РЎСѓРїРµСЂ РђРґРјРёРЅРёСЃС‚СЂР°С‚РѕСЂ" : "Super Admin") : (lang === 'am' ? "ФЅХЎХ¶ХёЦ‚Х©Х« Х„ХҐХ¶ХҐХ»ХҐЦЂ" : lang === 'ru' ? "РњРµРЅРµРґР¶РµСЂ РјР°РіР°Р·РёРЅР°" : "Shop Manager");
      actionText = lang === 'am' ? `ХЂХЎХ»ХёХІХёЦ‚Х©ХµХЎХґХў ХґХёЦ‚ХїЦ„ Х§ ХЈХёЦЂХ®ХҐХ¬ ХёЦЂХєХҐХЅ ${transRole}` :
                   lang === 'ru' ? `РЈСЃРїРµС€РЅРѕ РІРѕС€РµР» РІ СЃРёСЃС‚РµРјСѓ РєР°Рє ${transRole}` :
                   `Logged in successfully as ${role}`;
    } else if (log.action === "Logged out") {
      actionText = lang === 'am' ? "ФґХёЦ‚ЦЂХЅ Х§ ХЈХёЦЂХ®ХҐХ¬" :
                   lang === 'ru' ? "Р’С‹С€РµР» РёР· СЃРёСЃС‚РµРјС‹" :
                   "Logged out";
    } else if (log.action.startsWith("Installed plugin: ")) {
    } else if (log.action.startsWith("Updated inventory for ")) {
      const match = log.action.match(/Updated inventory for (.+?): Price=\$(.+?), Stock=(.+)/);
      if (match) {
        const name = match[1];
        const price = match[2];
        const stock = match[3];
        actionText = lang === 'am' ? `Ф№ХЎЦЂХґХЎЦЃЦЂХҐХ¬ Х§ ${name}-Х« ХєХЎХ·ХЎЦЂХЁХќ ФіХ«Х¶=ЦЏ${price}, Х”ХЎХ¶ХЎХЇ=${stock}` :
                     lang === 'ru' ? `РћР±РЅРѕРІРёР» Р·Р°РїР°СЃС‹ РґР»СЏ ${name}: Р¦РµРЅР°=ЦЏ${price}, РљРѕР»РёС‡РµСЃС‚РІРѕ=${stock}` :
                     `Updated inventory for ${name}: Price=ЦЏ${price}, Stock=${stock}`;
      }
    } else if (log.action.startsWith("Updated order status of ")) {
      const match = log.action.match(/Updated order status of (.+?) to (.+)/);
      if (match) {
        const orderId = match[1];
        const status = match[2];
        const transStatus = status === 'pending' ? (lang === 'am' ? 'ФёХ¶Х©ХЎЦЃЦ„Х« ХґХҐХ»' : lang === 'ru' ? 'Р’ РѕР¶РёРґР°РЅРёРё' : 'Pending') :
                            status === 'processing' ? (lang === 'am' ? 'Х„Х·ХЎХЇХѕХёЦ‚Хґ Х§' : lang === 'ru' ? 'РћР±СЂР°Р±РѕС‚РєР°' : 'Processing') :
                            status === 'completed' ? (lang === 'am' ? 'Ф±ХѕХЎЦЂХїХѕХЎХ®' : lang === 'ru' ? 'Р—Р°РІРµСЂС€РµРЅ' : 'Completed') :
                            status === 'failed' ? (lang === 'am' ? 'ХЃХЎХ­ХёХІХѕХЎХ®' : lang === 'ru' ? 'РћС€РёР±РєР°' : 'Failed') : status;
        actionText = lang === 'am' ? `Х“ХёХ­ХҐХ¬ Х§ ${orderId} ХєХЎХїХѕХҐЦЂХ« ХЇХЎЦЂХЈХЎХѕХ«ХіХЎХЇХЁХќ ${transStatus}` :
                     lang === 'ru' ? `РћР±РЅРѕРІРёР» СЃС‚Р°С‚СѓСЃ Р·Р°РєР°Р·Р° ${orderId} РЅР° ${transStatus}` :
                     `Updated order status of ${orderId} to ${status}`;
      }
    } else if (log.action.startsWith("Created/Edited detailed product fields for: ")) {
      const name = log.action.replace("Created/Edited detailed product fields for: ", "");
      actionText = lang === 'am' ? `ХЌХїХҐХІХ®ХҐХ¬/ФЅХґХўХЎХЈЦЂХҐХ¬ Х§ ${name}-Х« ХґХЎХ¶ЦЂХЎХґХЎХЅХ¶ ХїХѕХµХЎХ¬Х¶ХҐЦЂХЁ` :
                   lang === 'ru' ? `РЎРѕР·РґР°Р»/РР·РјРµРЅРёР» РїРѕРґСЂРѕР±РЅС‹Рµ РїРѕР»СЏ С‚РѕРІР°СЂР° РґР»СЏ: ${name}` :
                   `Created/Edited detailed product fields for: ${name}`;
    } else if (log.action === "Cleared platform audit logs history") {
      actionText = lang === 'am' ? "Х„ХЎЦ„ЦЂХҐХ¬ Х§ Х°ХЎЦЂХ©ХЎХЇХ« ХЎХёЦ‚Х¤Х«ХїХ« ХґХЎХїХµХЎХ¶Х¶ХҐЦЂХ« ХєХЎХїХґХёЦ‚Х©ХµХёЦ‚Х¶ХЁ" :
                   lang === 'ru' ? "РћС‡РёСЃС‚РёР» РёСЃС‚РѕСЂРёСЋ Р¶СѓСЂРЅР°Р»Р° Р°СѓРґРёС‚Р° РїР»Р°С‚С„РѕСЂРјС‹" :
                   "Cleared platform audit logs history";
    }

    const tr = document.createElement('tr');
    tr.innerHTML = `
      <td style="color: var(--color-medium-gray); font-family: monospace; font-size: 0.75rem;">${log.timestamp}</td>
      <td><strong>${log.operator}</strong></td>
      <td>${actionText}</td>
    `;
    tbody.appendChild(tr);
  });
};

window.refreshAdminDashboard = function() {
  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (session) {
    const badge = document.getElementById('admin-user-role-badge');
    const nameEl = document.getElementById('admin-user-profile-name');
    if (badge) {
      const lang = AppState.language;
      const transRole = session.role === "Super Admin" ? 
        (lang === 'am' ? "ХЌХёЦ‚ХєХҐЦЂ Ф±Х¤ХґХ«Х¶" : lang === 'ru' ? "РЎСѓРїРµСЂ РђРґРјРёРЅРёСЃС‚СЂР°С‚РѕСЂ" : "Super Admin") : 
        (lang === 'am' ? "ФЅХЎХ¶ХёЦ‚Х©Х« Х„ХҐХ¶ХҐХ»ХҐЦЂ" : lang === 'ru' ? "РњРµРЅРµРґР¶РµСЂ РјР°РіР°Р·РёРЅР°" : "Shop Manager");
      badge.innerText = transRole;
      badge.style.display = 'inline-block';
    }
    if (nameEl) {
      nameEl.innerText = AppState.language === 'am' ? `Х„ХёЦ‚ХїЦ„ ХЈХёЦЂХ®ХЎХ® Х§ ХёЦЂХєХҐХЅ ${session.name}` :
                         AppState.language === 'ru' ? `Р’РѕС€РµР» РєР°Рє ${session.name}` :
                         `Logged in as ${session.name}`;
    }
    
    // Hide/disable developer tab in sidebar for non-superadmin
    const devNavItem = document.getElementById('admin-nav-item-dev');
    if (devNavItem) {
      if (session.role === 'Super Admin') {
        devNavItem.style.opacity = '1';
        devNavItem.style.cursor = 'pointer';
      } else {
        devNavItem.style.opacity = '0.4';
        devNavItem.style.cursor = 'not-allowed';
      }
    }
  }
  
  if (typeof WooCommerceAdmin !== 'undefined') {
    WooCommerceAdmin.renderDashboard(AppState.products);
    WooCommerceAdmin.renderInventoryTable(AppState.products);
  }
  renderAuditLogsTable();
  if (typeof renderAdminAccessList === 'function') {
    renderAdminAccessList();
  }
};

window.checkAdminSession = function() {
  const loginWrapper = document.getElementById('admin-login-wrapper');
  const dashboardWrapper = document.getElementById('admin-dashboard-wrapper');
  
  // Already has admin session
  const adminSession = sessionStorage.getItem('nova_admin_session');
  if (adminSession) {
    if (loginWrapper) loginWrapper.classList.add('hidden');
    if (dashboardWrapper) dashboardWrapper.classList.remove('hidden');
    refreshAdminDashboard();
    return;
  }

  // Auto-login if current auth session is an admin email
  try {
    const authSession = JSON.parse(localStorage.getItem('nova_session'));
    if (authSession && typeof isAdminEmail === 'function' && isAdminEmail(authSession.email)) {
      const profile = {
        username: authSession.email,
        name: (authSession.firstName || '') + ' ' + (authSession.lastName || ''),
        role: authSession.email === 'norayrnajaryann@gmail.com' ? 'Super Admin' : 'Shop Manager'
      };
      sessionStorage.setItem('nova_admin_session', JSON.stringify(profile));
      if (loginWrapper) loginWrapper.classList.add('hidden');
      if (dashboardWrapper) dashboardWrapper.classList.remove('hidden');
      if (typeof logAdminActivity === 'function') {
        logAdminActivity(profile.name.trim(), 'Auto-authenticated as ' + profile.role);
      }
      refreshAdminDashboard();
      return;
    }
  } catch(e) {}

  // Not admin вЂ” show login form
  if (loginWrapper) loginWrapper.classList.remove('hidden');
  if (dashboardWrapper) dashboardWrapper.classList.add('hidden');
};

function initAdminNavigation() {
  const adminSidebar = document.querySelector('.admin-sidebar');
  if (adminSidebar) {
    adminSidebar.querySelectorAll('.admin-nav-item').forEach(btn => {
      btn.addEventListener('click', () => {
        const tabId = btn.getAttribute('data-admin-tab');
        if (tabId) {
          switchAdminTab(tabId);
        }
      });
    });
  }
}

// Run initializations directly
initStaffProfiles();
initMyAccountNavigation();
initAdminNavigation();

// INSTAGRAM POST FEED INTERACTIONS
window.toggleInstaLike = function(btn, postId) {
  const card = btn.closest('.insta-post-card');
  if (card) {
    const likesEl = card.querySelector('.insta-post-likes strong');
    if (likesEl) {
      let currentLikes = parseInt(likesEl.innerText.replace(/,/g, ''), 10) || 0;
      let liked = btn.classList.toggle('liked');
      if (liked) {
        currentLikes += 1;
        let likedToast = 'POST LIKED';
        if (AppState.language === 'am') {
          likedToast = 'ХЂФ±ХЋФ±Х†ФµФј ФµХ” ФіХђФ±ХЊХ€Х’Х„Фё';
        } else if (AppState.language === 'ru') {
          likedToast = 'Р’РђРњ РџРћРќР РђР’РР›РђРЎР¬ Р—РђРџРРЎР¬';
        }
        showToast(likedToast);
      } else {
        currentLikes -= 1;
      }
      likesEl.innerText = currentLikes.toLocaleString('en-US');

      // Update state and save
      if (postId) {
        const post = AppState.instagramPosts.find(p => p.id === postId);
        if (post) {
          post.likes = currentLikes;
          // Silently save to storage (don't re-render to avoid losing class active state)
          NovaDB.saveInstagramPosts(AppState.instagramPosts);
        }
      }
    }
  }
};

window.toggleInstaSave = function(btn) {
  const isSaved = btn.classList.toggle('saved');
  if (isSaved) {
    let saveToast = 'POST SAVED TO SCENT WARDROBE';
    if (AppState.language === 'am') {
      saveToast = 'ФіХђФ±ХЊХ€Х’Х„Фё ХЉФ±ХЂХЉФ±Х†ХЋФµФј Ф· ФІХ€Х’Х…ХђФµХђФ» ХЉФ±ХЂФ±ХђФ±Х†Х€Х’Х„';
    } else if (AppState.language === 'ru') {
      saveToast = 'Р—РђРџРРЎР¬ РЎРћРҐР РђРќР•РќРђ Р’ Р“РђР Р”Р•Р РћР‘ РђР РћРњРђРўРћР’';
    }
    showToast(saveToast);
  }
};

// DYNAMIC INSTAGRAM MANAGER AND RENDERER
function saveInstagramPosts(posts) {
  NovaDB.saveInstagramPosts(posts);
  AppState.instagramPosts = posts;
  renderInstagramFeed();
}

function formatInstagramDate(dateStr, lang) {
  const postDate = new Date(dateStr);
  const today = new Date();
  
  // Set both to midnight to compare days
  postDate.setHours(0,0,0,0);
  today.setHours(0,0,0,0);
  
  const diffTime = today.getTime() - postDate.getTime();
  const diffDays = Math.floor(diffTime / (1000 * 60 * 60 * 24));
  
  if (diffDays <= 0) {
    if (lang === 'am') return 'Ф±Х…ХЌХ•Хђ';
    if (lang === 'ru') return 'РЎР•Р“РћР”РќРЇ';
    return 'TODAY';
  } else if (diffDays === 1) {
    if (lang === 'am') return '1 Х•Хђ Ф±ХЊФ±Х‹';
    if (lang === 'ru') return '1 Р”Р•РќР¬ РќРђР—РђР”';
    return '1 DAY AGO';
  } else if (diffDays < 7) {
    if (lang === 'am') return `${diffDays} Х•Хђ Ф±ХЊФ±Х‹`;
    if (lang === 'ru') {
      if (diffDays === 2 || diffDays === 3 || diffDays === 4) {
        return `${diffDays} Р”РќРЇ РќРђР—РђР”`;
      }
      return `${diffDays} Р”РќР•Р™ РќРђР—РђР”`;
    }
    return `${diffDays} DAYS AGO`;
  } else {
    const diffWeeks = Math.floor(diffDays / 7);
    if (diffWeeks === 1) {
      if (lang === 'am') return '1 Х‡Ф±ФІФ±Ф№ Ф±ХЊФ±Х‹';
      if (lang === 'ru') return '1 РќР•Р”Р•Р›Р® РќРђР—РђР”';
      return '1 WEEK AGO';
    } else {
      if (lang === 'am') return `${diffWeeks} Х‡Ф±ФІФ±Ф№ Ф±ХЊФ±Х‹`;
      if (lang === 'ru') {
        if (diffWeeks === 2 || diffWeeks === 3 || diffWeeks === 4) {
          return `${diffWeeks} РќР•Р”Р•Р›Р РќРђР—РђР”`;
        }
        return `${diffWeeks} РќР•Р”Р•Р›Р¬ РќРђР—РђР”`;
      }
      return `${diffWeeks} WEEKS AGO`;
    }
  }
}

function renderInstagramFeed() {
  // Old manual IG grids removed. Live feed handled by IIFE at bottom of file.
}

function parseAndRenderShortcodes() {
  // Old shortcode parser removed. No longer needed.
}

window.switchCarouselImage = function(postId, index, event) {
  if (event) {
    event.preventDefault();
    event.stopPropagation();
  }
  const card = document.getElementById(`insta-card-${postId}`);
  if (!card) return;
  const wrap = card.querySelector('.insta-post-image-wrap');
  if (!wrap) return;

  wrap.setAttribute('data-current-index', index);

  // Update active image
  const imgs = wrap.querySelectorAll('.insta-post-img');
  imgs.forEach((img, idx) => {
    if (idx === index) {
      img.classList.add('active');
    } else {
      img.classList.remove('active');
    }
  });

  // Update active dot
  const dots = wrap.querySelectorAll('.carousel-dot');
  dots.forEach((dot, idx) => {
    if (idx === index) {
      dot.classList.add('active');
    } else {
      dot.classList.remove('active');
    }
  });
};

window.navigateCarousel = function(postId, direction, event) {
  if (event) {
    event.preventDefault();
    event.stopPropagation();
  }
  const card = document.getElementById(`insta-card-${postId}`);
  if (!card) return;
  const wrap = card.querySelector('.insta-post-image-wrap');
  if (!wrap) return;

  const imgs = wrap.querySelectorAll('.insta-post-img');
  const len = imgs.length;
  if (len <= 1) return;

  let currentIndex = parseInt(wrap.getAttribute('data-current-index') || '0', 10);
  let newIndex = (currentIndex + direction + len) % len;

  window.switchCarouselImage(postId, newIndex, event);
};



// --- FULL-PAGE PRODUCT EDITOR (WordPress-style) ---

// Helper to set checkbox states (works with pe- prefixed IDs)
function setEditorCheckboxes(prefix, values) {
  document.querySelectorAll(`[id^="${prefix}"]`).forEach(cb => cb.checked = false);
  (values || []).forEach(val => {
    const cb = document.getElementById(`${prefix}${val.toLowerCase().replace(/\s+/g, '')}`);
    if (cb) cb.checked = true;
  });
}

function getEditorCheckboxValues(prefix) {
  const values = [];
  document.querySelectorAll(`[id^="${prefix}"]`).forEach(cb => {
    if (cb.checked) values.push(cb.value);
  });
  return values;
}

// Populate brand dropdown in editor
function populateEditorBrandDropdown(selectedBrand) {
  const select = document.getElementById('pe-brand');
  if (!select) return;
  select.innerHTML = '';
  
  // Get brands from NovaDB or use defaults
  const brands = NovaDB.getBrands() || ['NOVA'];
  const brandList = Array.isArray(brands) ? brands : [brands];
  
  // Always ensure NOVA is in the list
  if (!brandList.includes('NOVA')) brandList.unshift('NOVA');
  
  brandList.forEach(b => {
    const name = typeof b === 'string' ? b : (b.name || b);
    const opt = document.createElement('option');
    opt.value = name;
    opt.textContent = name;
    if (name === selectedBrand) opt.selected = true;
    select.appendChild(opt);
  });
}

// Tab switching
function initEditorTabs() {
  const nav = document.getElementById('pe-tabs-nav');
  if (!nav) return;
  nav.addEventListener('click', function(e) {
    const btn = e.target.closest('.pe-tab-btn');
    if (!btn) return;
    const tabId = btn.getAttribute('data-pe-tab');
    
    // Update buttons
    nav.querySelectorAll('.pe-tab-btn').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    
    // Update panels
    document.querySelectorAll('.pe-tab-panel').forEach(p => p.classList.remove('active'));
    const panel = document.getElementById(`pe-panel-${tabId}`);
    if (panel) panel.classList.add('active');
  });
}

// Auto-generate slug from name
function initSlugGenerator() {
  const nameInput = document.getElementById('pe-product-name');
  const slugEl = document.getElementById('pe-slug');
  if (!nameInput || !slugEl) return;
  nameInput.addEventListener('input', function() {
    const slug = this.value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
    slugEl.textContent = slug || 'вЂ”';
  });
}

// Image handling for editor
window._editorUploadedImages = [];

function renderEditorImageGallery() {
  const previewWrap = document.getElementById('pe-image-preview-wrap');
  const galleryWrap = document.getElementById('pe-gallery');
  if (!previewWrap || !galleryWrap) return;
  
  const images = window._editorUploadedImages;
  
  // Main preview
  if (images.length > 0) {
    previewWrap.innerHTML = `<img src="${images[0]}" class="pe-image-preview" alt="Product">`;
  } else {
    previewWrap.innerHTML = `<div class="pe-image-placeholder" onclick="document.getElementById('pe-image-file').click()">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="m21 15-5-5L5 21"/></svg>
      Click to upload
    </div>`;
  }
  
  // Gallery thumbnails
  galleryWrap.innerHTML = '';
  images.forEach((img, idx) => {
    galleryWrap.innerHTML += `
      <div class="pe-gallery-thumb ${idx === 0 ? 'active' : ''}" onclick="setEditorMainImage(${idx})">
        <img src="${img}" alt="Gallery ${idx+1}">
        <button type="button" class="pe-gallery-remove" onclick="event.stopPropagation(); removeEditorImage(${idx})">Г—</button>
      </div>`;
  });
}

window.setEditorMainImage = function(idx) {
  const images = window._editorUploadedImages;
  if (idx >= 0 && idx < images.length) {
    // Move selected image to front
    const [img] = images.splice(idx, 1);
    images.unshift(img);
    renderEditorImageGallery();
    // Update URL field
    document.getElementById('pe-image-url').value = images[0] || '';
  }
};

window.removeEditorImage = function(idx) {
  window._editorUploadedImages.splice(idx, 1);
  renderEditorImageGallery();
  // Update URL field
  document.getElementById('pe-image-url').value = window._editorUploadedImages[0] || '';
};

function initEditorImageHandlers() {
  // File upload handler
  const fileInput = document.getElementById('pe-image-file');
  if (fileInput) {
    fileInput.addEventListener('change', function() {
      Array.from(this.files).forEach(file => {
        const reader = new FileReader();
        reader.onload = function(e) {
          if (!window._editorUploadedImages.includes(e.target.result)) {
            window._editorUploadedImages.push(e.target.result);
          }
          renderEditorImageGallery();
          document.getElementById('pe-image-url').value = window._editorUploadedImages[0] || '';
        };
        reader.readAsDataURL(file);
      });
      this.value = '';
    });
  }
  
  // URL input handler (add on Enter or blur)
  const urlInput = document.getElementById('pe-image-url');
  if (urlInput) {
    urlInput.addEventListener('keypress', function(e) {
      if (e.key === 'Enter') {
        e.preventDefault();
        const url = this.value.trim();
        if (url && !window._editorUploadedImages.includes(url)) {
          window._editorUploadedImages.unshift(url);
          renderEditorImageGallery();
        }
      }
    });
  }
  
  // Click on placeholder
  const previewWrap = document.getElementById('pe-image-preview-wrap');
  if (previewWrap) {
    previewWrap.addEventListener('click', function(e) {
      if (e.target.closest('.pe-image-placeholder')) {
        document.getElementById('pe-image-file').click();
      }
    });
  }
}

// Stock status updater
function initStockStatus() {
  const stockInput = document.getElementById('pe-stock');
  const statusEl = document.getElementById('pe-stock-status');
  if (!stockInput || !statusEl) return;
  
  function updateStatus() {
    const val = parseInt(stockInput.value, 10);
    if (isNaN(val) || val <= 0) {
      statusEl.textContent = 'в›” Out of stock';
      statusEl.style.color = '#d63638';
    } else if (val <= 5) {
      statusEl.textContent = `вљ пёЏ Low stock (${val} remaining)`;
      statusEl.style.color = '#dba617';
    } else {
      statusEl.textContent = `вњ… In stock (${val} available)`;
      statusEl.style.color = '#00a32a';
    }
  }
  
  stockInput.addEventListener('input', updateStatus);
  updateStatus();
}

// ---- OPEN PRODUCT EDITOR ----
window.openProductEditor = function(productId) {
  const page = document.getElementById('product-editor-page');
  if (!page) return;
  
  const isNew = !productId;
  const product = isNew ? null : AppState.products.find(p => p.id === productId);
  if (!isNew && !product) return;
  
  // Title
  document.getElementById('pe-page-title').textContent = isNew ? 'Add New Product' : 'Edit Product';
  document.getElementById('pe-product-id').value = isNew ? '__NEW__' : product.id;
  
  // Publish button text
  const pubBtn = page.querySelector('.pe-publish-btn');
  if (pubBtn) pubBtn.textContent = isNew ? 'Publish' : 'Update';
  
  if (isNew) {
    // Clear all fields
    document.getElementById('pe-product-name').value = '';
    document.getElementById('pe-slug').textContent = 'вЂ”';
    populateEditorBrandDropdown('NOVA');
    document.getElementById('pe-tagline').value = '';
    document.getElementById('pe-description').value = '';
    document.getElementById('pe-ingredients').value = '';
    document.getElementById('pe-price-50').value = '';
    document.getElementById('pe-price-100').value = '';
    document.getElementById('pe-price-200').value = '';
    document.getElementById('pe-stock').value = 0;
    document.getElementById('pe-notes-top').value = '';
    document.getElementById('pe-notes-heart').value = '';
    document.getElementById('pe-notes-base').value = '';
    document.getElementById('pe-family').value = 'woody';
    document.getElementById('pe-concentration').value = 'edp';
    document.getElementById('pe-gender').value = 'unisex';
    document.getElementById('pe-rating').value = '';
    document.getElementById('pe-reviews').value = '';
    document.getElementById('pe-featured').checked = false;
    document.getElementById('pe-image-url').value = '';
    setEditorCheckboxes('pe-tag-', []);
    setEditorCheckboxes('pe-vibe-', []);
    window._editorUploadedImages = [];
  } else {
    // Fill with product data
    document.getElementById('pe-product-name').value = product.name;
    document.getElementById('pe-slug').textContent = product.id;
    populateEditorBrandDropdown(product.brand || 'NOVA');
    document.getElementById('pe-tagline').value = product.tagline || '';
    document.getElementById('pe-description').value = product.description || '';
    document.getElementById('pe-ingredients').value = product.ingredients || '';
    
    const findSize = (name) => (product.sizes || []).find(s => s.size === name);
    const s50 = findSize('50ml'), s100 = findSize('100ml'), s200 = findSize('200ml');
    document.getElementById('pe-price-50').value = s50 && s50.price > 0 ? s50.price : '';
    document.getElementById('pe-price-100').value = s100 && s100.price > 0 ? s100.price : '';
    document.getElementById('pe-price-200').value = s200 && s200.price > 0 ? s200.price : '';
    
    document.getElementById('pe-stock').value = product.stock || 0;
    document.getElementById('pe-notes-top').value = product.notes?.top?.join(', ') || '';
    document.getElementById('pe-notes-heart').value = product.notes?.heart?.join(', ') || '';
    document.getElementById('pe-notes-base').value = product.notes?.base?.join(', ') || '';
    document.getElementById('pe-family').value = product.scent_family || 'woody';
    document.getElementById('pe-concentration').value = product.concentration || 'edp';
    document.getElementById('pe-gender').value = product.gender_id || 'unisex';
    document.getElementById('pe-rating').value = product.rating || '';
    document.getElementById('pe-reviews').value = product.reviewsCount || '';
    document.getElementById('pe-featured').checked = !!product.featured;
    document.getElementById('pe-image-url').value = product.image || '';
    
    setEditorCheckboxes('pe-tag-', product.tags || []);
    setEditorCheckboxes('pe-vibe-', product.vibes || []);
    
    // Build image gallery
    window._editorUploadedImages = [];
    if (product.image) window._editorUploadedImages.push(product.image);
    if (product.images) {
      product.images.forEach(img => {
        if (!window._editorUploadedImages.includes(img)) {
          window._editorUploadedImages.push(img);
        }
      });
    }
  }
  
  renderEditorImageGallery();
  initStockStatus();
  
  // Reset to General tab
  document.querySelectorAll('.pe-tab-btn').forEach(b => b.classList.remove('active'));
  document.querySelectorAll('.pe-tab-panel').forEach(p => p.classList.remove('active'));
  document.querySelector('[data-pe-tab="general"]')?.classList.add('active');
  document.getElementById('pe-panel-general')?.classList.add('active');
  
  // Show page
  page.style.display = 'block';
  page.scrollTop = 0;
  document.body.style.overflow = 'hidden';
};

// Backward-compatible aliases
window.openDetailedProductModal = function(productId) {
  window.openProductEditor(productId);
};
window.openNewProductModal = function() {
  window.openProductEditor(null);
};

// ---- CLOSE PRODUCT EDITOR ----
window.closeProductEditor = function() {
  const page = document.getElementById('product-editor-page');
  if (page) {
    page.style.display = 'none';
    document.body.style.overflow = '';
  }
};

window.closeDetailedProductModal = function() {
  window.closeProductEditor();
};

// ---- SAVE FROM EDITOR ----
window.saveProductFromEditor = function() {
  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (!session) {
    showToast("ADMIN SESSION EXPIRED. PLEASE RE-LOGIN.");
    return;
  }

  const productId = document.getElementById('pe-product-id').value;
  const isNewProduct = (productId === '__NEW__');
  let product = isNewProduct ? {} : AppState.products.find(p => p.id === productId);
  if (!product) return;

  const newName = document.getElementById('pe-product-name').value.trim();
  const newBrand = document.getElementById('pe-brand').value.trim();
  const newTagline = document.getElementById('pe-tagline').value.trim();
  const newDescription = document.getElementById('pe-description').value.trim();
  const newIngredients = document.getElementById('pe-ingredients').value.trim();
  const newImage = document.getElementById('pe-image-url').value.trim() || (window._editorUploadedImages[0] || '');
  const newScentFamily = document.getElementById('pe-family').value;
  const newConcentration = document.getElementById('pe-concentration').value;
  const newGenderId = document.getElementById('pe-gender').value;

  const price50 = document.getElementById('pe-price-50').value.trim() !== '' ? parseFloat(document.getElementById('pe-price-50').value) : null;
  const price100 = document.getElementById('pe-price-100').value.trim() !== '' ? parseFloat(document.getElementById('pe-price-100').value) : null;
  const price200 = document.getElementById('pe-price-200').value.trim() !== '' ? parseFloat(document.getElementById('pe-price-200').value) : null;
  const newStock = parseInt(document.getElementById('pe-stock').value, 10);

  const notesTop = document.getElementById('pe-notes-top').value.split(',').map(s => s.trim()).filter(s => s.length > 0);
  const notesHeart = document.getElementById('pe-notes-heart').value.split(',').map(s => s.trim()).filter(s => s.length > 0);
  const notesBase = document.getElementById('pe-notes-base').value.split(',').map(s => s.trim()).filter(s => s.length > 0);

  const newTags = getEditorCheckboxValues('pe-tag-');
  const newVibes = getEditorCheckboxValues('pe-vibe-');
  const newRating = parseFloat(document.getElementById('pe-rating').value) || 0;
  const newReviewsCount = parseInt(document.getElementById('pe-reviews').value, 10) || 0;
  const newFeatured = document.getElementById('pe-featured').checked;

  // Build sizes array
  const newSizes = [];
  if (price50 !== null && !isNaN(price50) && price50 > 0) newSizes.push({ size: "50ml", price: Math.round(price50) });
  if (price100 !== null && !isNaN(price100) && price100 > 0) newSizes.push({ size: "100ml", price: Math.round(price100) });
  if (price200 !== null && !isNaN(price200) && price200 > 0) newSizes.push({ size: "200ml", price: Math.round(price200) });

  // Validate
  if (!newName || !newBrand || !newTagline || !newDescription || !newImage || newSizes.length === 0 || isNaN(newStock)) {
    showToast("PLEASE FILL IN ALL REQUIRED FIELDS. AT LEAST ONE SIZE PRICE IS REQUIRED.");
    return;
  }

  const mainPrice = newSizes[0].price;

  // Update product
  product.name = newName;
  product.brand = newBrand;
  product.tagline = newTagline;
  product.description = newDescription;
  product.ingredients = newIngredients;
  product.image = newImage;
  product.images = [...window._editorUploadedImages];
  product.scent_family = newScentFamily;
  product.concentration = newConcentration;
  product.gender_id = newGenderId;
  product.price = mainPrice;
  product.stock = newStock;
  product.tags = newTags;
  product.vibes = newVibes;
  product.rating = newRating;
  product.reviewsCount = newReviewsCount;
  product.featured = newFeatured;
  product.sizes = newSizes;
  product.notes = { top: notesTop, heart: notesHeart, base: notesBase };

  if (isNewProduct) {
    product.id = newName.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '') + '-' + Date.now().toString(36);
    Object.defineProperty(product, 'category', {
      get() { const fam = window.GLOBAL_ATTRIBUTES.scent_families[product.scent_family]; return fam ? fam.label.en : ""; },
      configurable: true, enumerable: true
    });
    Object.defineProperty(product, 'gender', {
      get() { const g = window.GLOBAL_ATTRIBUTES.genders[product.gender_id]; return g ? g.label.en : ""; },
      configurable: true, enumerable: true
    });
    AppState.products.push(product);
    logAdminActivity(session.name, `Created new product: ${product.name}`);
    showToast(`SUCCESSFULLY CREATED ${product.name.toUpperCase()}.`);
  } else {
    logAdminActivity(session.name, `Edited product details for: ${product.name}`);
    showToast(`SUCCESSFULLY UPDATED ${product.name.toUpperCase()} DETAILS.`);
  }

  saveProductsToStorage();
  renderFeaturedProducts();
  renderShop();
  refreshAdminDashboard();
  closeProductEditor();
};

// Keep old function name as alias
window.saveDetailedProduct = function(event) {
  if (event) event.preventDefault();
  window.saveProductFromEditor();
};

// Initialize editor event handlers on DOMContentLoaded
document.addEventListener('DOMContentLoaded', function() {
  initEditorTabs();
  initSlugGenerator();
  initEditorImageHandlers();
});

// --- PRODUCT MANAGEMENT ACTIONS ---

// Trash management
function getTrash() {
  return NovaDB.getTrash() || [];
}

function saveTrash(trash) {
  NovaDB.saveTrash(trash);
}

window.deleteProduct = function(productId) {
  const product = AppState.products.find(p => p.id === productId);
  if (!product) return;
  
  if (!confirm(`Move "${product.name}" to trash?`)) return;
  
  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  const idx = AppState.products.findIndex(p => p.id === productId);
  if (idx !== -1) {
    // Move to trash with deletion timestamp
    const trashedProduct = { ...product, deletedAt: new Date().toISOString() };
    const trash = getTrash();
    trash.unshift(trashedProduct);
    saveTrash(trash);

    AppState.products.splice(idx, 1);
    saveProductsToStorage();
    renderFeaturedProducts();
    renderShop();
    refreshAdminDashboard();
    
    if (session) {
      logAdminActivity(session.name, `Moved to trash: ${product.name}`);
    }
    showToast(`"${product.name.toUpperCase()}" MOVED TO TRASH.`);
  }
};

window.restoreFromTrash = function(productId) {
  const trash = getTrash();
  const idx = trash.findIndex(p => p.id === productId);
  if (idx === -1) return;

  const product = { ...trash[idx] };
  delete product.deletedAt;
  trash.splice(idx, 1);
  saveTrash(trash);

  AppState.products.push(product);
  saveProductsToStorage();
  renderFeaturedProducts();
  renderShop();
  refreshAdminDashboard();
  renderTrashList();

  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (session) logAdminActivity(session.name, `Restored from trash: ${product.name}`);
  showToast(`"${product.name.toUpperCase()}" RESTORED.`);
};

window.permanentlyDelete = function(productId) {
  if (!confirm('Permanently delete this product? This cannot be undone.')) return;
  const trash = getTrash().filter(p => p.id !== productId);
  saveTrash(trash);
  renderTrashList();
  showToast('PRODUCT PERMANENTLY DELETED.');
};

window.emptyTrash = function() {
  const trash = getTrash();
  if (trash.length === 0) { showToast('TRASH IS ALREADY EMPTY.'); return; }
  if (!confirm(`Permanently delete all ${trash.length} product(s) in trash?`)) return;
  saveTrash([]);
  renderTrashList();
  showToast('TRASH EMPTIED.');
};

function renderTrashList() {
  const container = document.getElementById('admin-trash-list');
  const emptyMsg = document.getElementById('admin-trash-empty');
  if (!container) return;

  const trash = getTrash();
  
  if (trash.length === 0) {
    container.innerHTML = '';
    if (emptyMsg) emptyMsg.style.display = 'block';
    return;
  }
  if (emptyMsg) emptyMsg.style.display = 'none';

  container.innerHTML = trash.map(product => {
    const deletedDate = product.deletedAt ? new Date(product.deletedAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: '2-digit', minute: '2-digit' }) : 'Unknown';
    return `
      <div style="display:flex; align-items:center; gap:14px; padding:12px 14px; border:1px solid var(--color-border); border-radius:6px; background:var(--color-white); margin-bottom:8px; flex-wrap:wrap;">
        <input type="checkbox" class="trash-row-checkbox" value="${product.id}" style="cursor:pointer;">
        <img src="${product.image}" alt="${product.name}" style="width:50px; height:50px; object-fit:cover; border-radius:4px; border:1px solid var(--color-border);">
        <div style="flex:1; min-width:120px;">
          <div style="font-size:0.85rem; font-weight:600;">${product.name}</div>
          <div style="font-size:0.7rem; color:var(--color-medium-gray);">${product.brand} В· Deleted ${deletedDate}</div>
        </div>
        <div style="display:flex; gap:8px;">
          <button onclick="restoreFromTrash('${product.id}')" style="padding:6px 14px; font-size:0.7rem; border:1px solid var(--color-sage); background:none; color:var(--color-sage); border-radius:4px; cursor:pointer; font-weight:600; text-transform:uppercase; transition:all 0.2s;" onmouseover="this.style.background='var(--color-sage)';this.style.color='#fff'" onmouseout="this.style.background='none';this.style.color='var(--color-sage)'">Restore</button>
          <button onclick="permanentlyDelete('${product.id}')" style="padding:6px 14px; font-size:0.7rem; border:1px solid #c00; background:none; color:#c00; border-radius:4px; cursor:pointer; font-weight:600; text-transform:uppercase; transition:all 0.2s;" onmouseover="this.style.background='#c00';this.style.color='#fff'" onmouseout="this.style.background='none';this.style.color='#c00'">Delete</button>
        </div>
      </div>
    `;
  }).join('');

  // Reset select-all
  const selectAll = document.getElementById('trash-select-all');
  if (selectAll) selectAll.checked = false;
}

window.toggleAllTrashCheckboxes = function(checked) {
  document.querySelectorAll('.trash-row-checkbox').forEach(cb => { cb.checked = checked; });
};

window.executeTrashBulkAction = function() {
  const actionSelect = document.getElementById('trash-bulk-action-select');
  const action = actionSelect ? actionSelect.value : '';

  if (!action) { showToast('PLEASE SELECT A BULK ACTION.'); return; }

  const checkedIds = [];
  document.querySelectorAll('.trash-row-checkbox:checked').forEach(cb => { checkedIds.push(cb.value); });

  if (checkedIds.length === 0) { showToast('NO PRODUCTS SELECTED.'); return; }

  if (action === 'restore') {
    if (!confirm(`Restore ${checkedIds.length} product(s)?`)) return;
    const trash = getTrash();
    const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));

    checkedIds.forEach(id => {
      const idx = trash.findIndex(p => p.id === id);
      if (idx !== -1) {
        const product = { ...trash[idx] };
        delete product.deletedAt;
        trash.splice(idx, 1);
        AppState.products.push(product);
      }
    });

    saveTrash(trash);
    saveProductsToStorage();
    renderFeaturedProducts();
    renderShop();
    refreshAdminDashboard();
    renderTrashList();
    if (session) logAdminActivity(session.name, `Bulk restored ${checkedIds.length} product(s) from trash`);
    showToast(`${checkedIds.length} PRODUCT(S) RESTORED.`);
  }

  if (action === 'delete') {
    if (!confirm(`Permanently delete ${checkedIds.length} product(s)? This cannot be undone.`)) return;
    let trash = getTrash().filter(p => !checkedIds.includes(p.id));
    saveTrash(trash);
    renderTrashList();
    showToast(`${checkedIds.length} PRODUCT(S) PERMANENTLY DELETED.`);
  }

  if (actionSelect) actionSelect.value = '';
};

window.toggleProductFeatured = function(productId) {
  const product = AppState.products.find(p => p.id === productId);
  if (!product) return;
  
  product.featured = !product.featured;
  saveProductsToStorage();
  renderFeaturedProducts();
  refreshAdminDashboard();
  
  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (session) {
    logAdminActivity(session.name, `${product.featured ? 'Featured' : 'Unfeatured'} product: ${product.name}`);
  }
  showToast(`${product.name.toUpperCase()} ${product.featured ? 'MARKED AS FEATURED' : 'REMOVED FROM FEATURED'}.`);
};

window.filterInventoryTable = function(query) {
  const tbody = document.getElementById('admin-inventory-tbody');
  if (!tbody) return;
  
  const rows = tbody.querySelectorAll('tr');
  const q = query.toLowerCase().trim();
  
  rows.forEach(row => {
    const text = row.textContent.toLowerCase();
    row.style.display = !q || text.includes(q) ? '' : 'none';
  });
};

// --- BULK ACTIONS ---
window.toggleAllInventoryCheckboxes = function(checked) {
  document.querySelectorAll('.inv-row-checkbox').forEach(cb => {
    // Only toggle visible rows
    const row = cb.closest('tr');
    if (row && row.style.display !== 'none') {
      cb.checked = checked;
    }
  });
};

window.executeBulkAction = function() {
  const actionSelect = document.getElementById('bulk-action-select');
  const action = actionSelect ? actionSelect.value : '';
  
  if (!action) {
    showToast('PLEASE SELECT A BULK ACTION.');
    return;
  }

  const checkedIds = [];
  document.querySelectorAll('.inv-row-checkbox:checked').forEach(cb => {
    checkedIds.push(cb.value);
  });

  if (checkedIds.length === 0) {
    showToast('NO PRODUCTS SELECTED.');
    return;
  }

  if (action === 'trash') {
    if (!confirm(`Move ${checkedIds.length} product(s) to trash?`)) return;

    const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
    const trash = getTrash();
    const names = [];

    checkedIds.forEach(productId => {
      const idx = AppState.products.findIndex(p => p.id === productId);
      if (idx !== -1) {
        const product = AppState.products[idx];
        names.push(product.name);
        trash.unshift({ ...product, deletedAt: new Date().toISOString() });
        AppState.products.splice(idx, 1);
      }
    });

    saveTrash(trash);
    saveProductsToStorage();
    renderFeaturedProducts();
    renderShop();
    refreshAdminDashboard();

    if (session) {
      logAdminActivity(session.name, `Bulk moved to trash: ${names.join(', ')}`);
    }
    showToast(`${checkedIds.length} PRODUCT(S) MOVED TO TRASH.`);
    
    // Reset select-all checkbox
    const selectAll = document.getElementById('inv-select-all');
    if (selectAll) selectAll.checked = false;
    
    // Reset dropdown
    if (actionSelect) actionSelect.value = '';
  }
};

// --- ORDER MANAGEMENT ACTIONS ---

window.deleteOrder = function(orderId) {
  if (!confirm(`Are you sure you want to delete order #${orderId}? This action cannot be undone.`)) return;
  
  const session = JSON.parse(sessionStorage.getItem('nova_admin_session'));
  if (typeof WooCommerceAdmin !== 'undefined') {
    const idx = WooCommerceAdmin.orders.findIndex(o => o.id === orderId);
    if (idx !== -1) {
      WooCommerceAdmin.orders.splice(idx, 1);
      refreshAdminDashboard();
      
      if (session) {
        logAdminActivity(session.name, `Deleted order #${orderId}`);
      }
      showToast(`ORDER #${orderId} DELETED.`);
    }
  }
};
// ============================================
// BRAND MANAGEMENT SYSTEM
// ============================================

// Default brands
const DEFAULT_BRANDS = ['NOVA', 'Dior', 'Chanel', 'Tom Ford', 'Versace', 'Paco Rabanne', 'Yves Saint Laurent', 'Giorgio Armani', 'Gucci', 'Burberry', 'Dolce & Gabbana', 'HermГЁs', 'LancГґme', 'Calvin Klein', 'Hugo Boss', 'Givenchy', 'Prada', 'Valentino', 'Bvlgari', 'Montblanc'];

function getBrands() {
  const stored = NovaDB.getBrands();
  if (stored && stored.length > 0) {
    return stored;
  }
  // Initialize with defaults + any brands from products
  const productBrands = (AppState.products || []).map(p => p.brand).filter(Boolean);
  const allBrands = [...new Set([...DEFAULT_BRANDS, ...productBrands])].sort((a, b) => a.localeCompare(b));
  NovaDB.saveBrands(allBrands);
  return allBrands;
}

// Render the homepage brand slider from admin Brands list
function renderBrandSlider() {
  const list1 = document.getElementById('brand-slider-list-1');
  const list2 = document.getElementById('brand-slider-list-2');
  if (!list1 || !list2) return;

  const brands = getBrands();
  const buildItems = () => brands.map(brand => {
    const span = document.createElement('span');
    span.className = 'brand-slider-item';
    span.textContent = brand;
    span.addEventListener('click', () => {
      window.location.hash = '#/shop';
      if (typeof applyPresetFilter === 'function') applyPresetFilter('brand', brand);
    });
    return span;
  });

  list1.innerHTML = '';
  list2.innerHTML = '';
  buildItems().forEach(el => list1.appendChild(el));
  buildItems().forEach(el => list2.appendChild(el));
}

function saveBrands(brands) {
  NovaDB.saveBrands(brands);
  renderBrandSlider();
}

window.addNewBrand = function() {
  const input = document.getElementById('admin-new-brand-input');
  if (!input) return;
  const name = input.value.trim();
  if (!name) return;

  const brands = getBrands();
  if (brands.some(b => b.toLowerCase() === name.toLowerCase())) {
    showToast('BRAND ALREADY EXISTS.');
    return;
  }
  brands.push(name);
  brands.sort((a, b) => a.localeCompare(b));
  saveBrands(brands);
  input.value = '';
  renderBrandsList();
  showToast(`BRAND "${name}" ADDED.`);
};

window.removeBrand = function(brandName) {
  const brands = getBrands().filter(b => b !== brandName);
  saveBrands(brands);
  renderBrandsList();
  showToast(`BRAND "${brandName}" REMOVED.`);
};

function renderBrandsList() {
  const container = document.getElementById('admin-brands-list');
  if (!container) return;
  const brands = getBrands();
  container.innerHTML = brands.map(brand => `
    <div style="display:flex; align-items:center; justify-content:space-between; padding:10px 14px; border:1px solid var(--color-border); border-radius:6px; background:var(--color-white);">
      <span style="font-size:0.85rem; font-weight:500;">${brand}</span>
      <button type="button" onclick="removeBrand('${brand.replace(/'/g, "\\'")}')" style="background:none; border:none; cursor:pointer; color:var(--color-medium-gray); font-size:1.1rem; padding:0 4px; line-height:1; transition:color 0.2s;" onmouseover="this.style.color='#c00'" onmouseout="this.style.color='var(--color-medium-gray)'">&times;</button>
    </div>
  `).join('');
}

function populateBrandDropdown(selectedBrand) {
  const select = document.getElementById('modal-product-brand');
  if (!select) return;
  const brands = getBrands();
  select.innerHTML = brands.map(b => `<option value="${b}" ${b === selectedBrand ? 'selected' : ''}>${b}</option>`).join('');
}

// Inventory sub-tab switching
window.switchInventorySubTab = function(tabName) {
  document.querySelectorAll('.inv-sub-panel').forEach(p => p.style.display = 'none');
  document.querySelectorAll('.inventory-sub-tab').forEach(btn => {
    btn.style.color = 'var(--color-medium-gray)';
    btn.style.borderBottomColor = 'transparent';
    btn.classList.remove('active');
  });

  const panel = document.getElementById('inv-sub-' + tabName);
  if (panel) panel.style.display = 'block';

  const tab = document.querySelector(`.inventory-sub-tab[data-inv-tab="${tabName}"]`);
  if (tab) {
    tab.style.color = 'var(--color-black)';
    tab.style.borderBottomColor = 'var(--color-black)';
    tab.classList.add('active');
  }

  if (tabName === 'brands') {
    renderBrandsList();
  } else if (tabName === 'trash') {
    renderTrashList();
  }
};

// Multi-image gallery for product modal
window._modalUploadedImages = [];

function renderModalImageGallery() {
  const gallery = document.getElementById('modal-product-images-gallery');
  if (!gallery) return;
  gallery.innerHTML = '';

  window._modalUploadedImages.forEach((imgSrc, idx) => {
    const thumb = document.createElement('div');
    thumb.style.cssText = 'position:relative; width:70px; height:70px; border:1px solid var(--color-border); border-radius:6px; overflow:hidden; cursor:grab; flex-shrink:0;';
    thumb.draggable = true;
    thumb.dataset.imgIdx = idx;

    const img = document.createElement('img');
    img.src = imgSrc;
    img.style.cssText = 'width:100%; height:100%; object-fit:cover;';
    thumb.appendChild(img);

    // Index badge
    const badge = document.createElement('span');
    badge.textContent = idx + 1;
    badge.style.cssText = 'position:absolute; top:2px; left:2px; background:rgba(0,0,0,0.6); color:#fff; font-size:0.6rem; padding:1px 5px; border-radius:3px;';
    thumb.appendChild(badge);

    // Remove button
    const removeBtn = document.createElement('button');
    removeBtn.type = 'button';
    removeBtn.innerHTML = '&times;';
    removeBtn.style.cssText = 'position:absolute; top:2px; right:2px; background:rgba(0,0,0,0.6); color:#fff; border:none; border-radius:50%; width:18px; height:18px; font-size:0.75rem; cursor:pointer; display:flex; align-items:center; justify-content:center; line-height:1; padding:0;';
    removeBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      window._modalUploadedImages.splice(idx, 1);
      syncMainImageFromGallery();
      renderModalImageGallery();
    });
    thumb.appendChild(removeBtn);

    // Drag events
    thumb.addEventListener('dragstart', (e) => {
      e.dataTransfer.setData('text/plain', idx);
      thumb.style.opacity = '0.4';
    });
    thumb.addEventListener('dragend', () => { thumb.style.opacity = '1'; });
    thumb.addEventListener('dragover', (e) => {
      e.preventDefault();
      thumb.style.outline = '2px solid var(--color-sage)';
    });
    thumb.addEventListener('dragleave', () => { thumb.style.outline = 'none'; });
    thumb.addEventListener('drop', (e) => {
      e.preventDefault();
      thumb.style.outline = 'none';
      const fromIdx = parseInt(e.dataTransfer.getData('text/plain'));
      const toIdx = idx;
      if (fromIdx === toIdx) return;
      const [moved] = window._modalUploadedImages.splice(fromIdx, 1);
      window._modalUploadedImages.splice(toIdx, 0, moved);
      syncMainImageFromGallery();
      renderModalImageGallery();
    });

    gallery.appendChild(thumb);
  });
}

function syncMainImageFromGallery() {
  const urlField = document.getElementById('modal-product-image');
  if (urlField && window._modalUploadedImages.length > 0) {
    urlField.value = window._modalUploadedImages[0];
  }
}

// Convert an image file to WebP using Canvas API
function convertToWebP(file, quality = 0.85) {
  return new Promise((resolve) => {
    const reader = new FileReader();
    reader.onload = (e) => {
      const img = new Image();
      img.onload = () => {
        const canvas = document.createElement('canvas');
        canvas.width = img.naturalWidth;
        canvas.height = img.naturalHeight;
        const ctx = canvas.getContext('2d');
        ctx.drawImage(img, 0, 0);
        // Export as WebP
        const webpDataUrl = canvas.toDataURL('image/webp', quality);
        resolve(webpDataUrl);
      };
      img.onerror = () => {
        // Fallback to original if conversion fails
        resolve(e.target.result);
      };
      img.src = e.target.result;
    };
    reader.readAsDataURL(file);
  });
}

document.addEventListener('DOMContentLoaded', () => {
  const modalImgFile = document.getElementById('modal-product-image-file');
  if (modalImgFile) {
    modalImgFile.addEventListener('change', async (e) => {
      const files = Array.from(e.target.files);
      for (const file of files) {
        const webpDataUrl = await convertToWebP(file);
        window._modalUploadedImages.push(webpDataUrl);
        syncMainImageFromGallery();
        renderModalImageGallery();
      }
      // Reset input so same files can be re-selected
      e.target.value = '';
    });
  }
});



// ============================================
// LIVE INSTAGRAM FEED - @nova_giftshop
// Uses Instagram Basic Display API
// ============================================
(function() {
  // Instagram Basic Display API access token
  // To get your token:
  // 1. Go to https://developers.facebook.com and create an app
  // 2. Add "Instagram Basic Display" product
  // 3. Generate a User Token for @nova_giftshop
  // 4. Paste the long-lived token below
  const INSTAGRAM_ACCESS_TOKEN = localStorage.getItem('nova_ig_token') || '';
  const INSTAGRAM_USERNAME = 'nova_giftshop';
  const FEED_COUNT = 6;

  function initLiveInstagramFeed() {
    const grids = [
      document.getElementById('instagram-live-feed'),
      document.getElementById('instagram-live-feed-about')
    ].filter(Boolean);
    if (grids.length === 0) return;

    if (INSTAGRAM_ACCESS_TOKEN) {
      fetchInstagramPosts(grids);
    } else {
      // No token - use a proxy approach via RSS/scraping service
      fetchViaProxy(grids);
    }
  }

  // Method 1: Direct API call with token
  async function fetchInstagramPosts(grids) {
    try {
      const url = `https://graph.instagram.com/me/media?fields=id,caption,media_type,media_url,thumbnail_url,permalink,timestamp&limit=${FEED_COUNT}&access_token=${INSTAGRAM_ACCESS_TOKEN}`;
      const res = await fetch(url);
      if (!res.ok) throw new Error('API error');
      const data = await res.json();
      grids.forEach(g => renderFeed(g, data.data));
    } catch (e) {
      console.warn('Instagram API failed, trying proxy...', e);
      fetchViaProxy(grids);
    }
  }

  // Method 2: Proxy approach (no auth needed)
  async function fetchViaProxy(grids) {
    try {
      // Try multiple proxy endpoints
      const proxyUrls = [
        `https://www.instagram.com/${INSTAGRAM_USERNAME}/?__a=1&__d=dis`,
        `https://instagram-media-api.herokuapp.com/user/${INSTAGRAM_USERNAME}`,
      ];

      let posts = null;

      // Try fetching via Instagram's public JSON endpoint
      try {
        const res = await fetch(`https://www.instagram.com/${INSTAGRAM_USERNAME}/?__a=1`, {
          headers: { 'User-Agent': 'Mozilla/5.0' }
        });
        if (res.ok) {
          const data = await res.json();
          const edges = data?.graphql?.user?.edge_owner_to_timeline_media?.edges;
          if (edges) {
            posts = edges.slice(0, FEED_COUNT).map(e => ({
              id: e.node.id,
              media_url: e.node.display_url,
              thumbnail_url: e.node.thumbnail_src,
              permalink: `https://www.instagram.com/p/${e.node.shortcode}/`,
              caption: e.node.edge_media_to_caption?.edges?.[0]?.node?.text || '',
              media_type: e.node.is_video ? 'VIDEO' : 'IMAGE',
              likes: e.node.edge_liked_by?.count || 0,
              comments: e.node.edge_media_to_comment?.count || 0,
            }));
          }
        }
      } catch(e) { /* silent */ }

      if (posts && posts.length > 0) {
        grids.forEach(g => renderFeed(g, posts));
      } else {
        // Fallback: render placeholder cards with link to profile
        grids.forEach(g => renderFallback(g));
      }
    } catch (e) {
      grids.forEach(g => renderFallback(g));
    }
  }

  function renderFeed(grid, posts) {
    grid.innerHTML = '';
    
    posts.forEach(post => {
      const imgUrl = post.media_type === 'VIDEO' ? (post.thumbnail_url || post.media_url) : post.media_url;
      const caption = post.caption ? post.caption.substring(0, 80) + (post.caption.length > 80 ? '...' : '') : '';
      
      const card = document.createElement('a');
      card.href = post.permalink || `https://www.instagram.com/${INSTAGRAM_USERNAME}/`;
      card.target = '_blank';
      card.rel = 'noopener noreferrer';
      card.className = 'ig-live-card';
      
      card.innerHTML = `
        <img src="${imgUrl}" alt="${caption}" loading="lazy">
        <div class="ig-live-overlay">
          ${post.media_type === 'VIDEO' ? `
            <div class="ig-stat">
              <svg viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
            </div>
          ` : `
            <div class="ig-stat">
              <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
            </div>
          `}
        </div>
      `;
      
      grid.appendChild(card);
    });
  }

  function renderFallback(grid) {
    grid.innerHTML = '';
    
    // Use existing site instagram posts as fallback, or show placeholder
    const existingPosts = (window.AppState && window.AppState.instagramPosts) || [];
    
    if (existingPosts.length > 0) {
      const postsToShow = existingPosts.slice(0, FEED_COUNT);
      postsToShow.forEach(post => {
        const imgUrl = post.images && post.images.length > 0 ? post.images[0] : 'assets/insta_1.webp';
        const card = document.createElement('a');
        card.href = `https://www.instagram.com/${INSTAGRAM_USERNAME}/`;
        card.target = '_blank';
        card.rel = 'noopener noreferrer';
        card.className = 'ig-live-card';
        card.innerHTML = `
          <img src="${imgUrl}" alt="@${INSTAGRAM_USERNAME}" loading="lazy">
          <div class="ig-live-overlay">
            <div class="ig-stat">
              <svg viewBox="0 0 24 24"><path d="M7.8 2h8.4C19.4 2 22 4.6 22 7.8v8.4a5.8 5.8 0 0 1-5.8 5.8H7.8C4.6 22 2 19.4 2 16.2V7.8A5.8 5.8 0 0 1 7.8 2m-.2 2A3.6 3.6 0 0 0 4 7.6v8.8C4 18.39 5.61 20 7.6 20h8.8a3.6 3.6 0 0 0 3.6-3.6V7.6C20 5.61 18.39 4 16.4 4H7.6m9.65 1.5a1.25 1.25 0 0 1 1.25 1.25A1.25 1.25 0 0 1 17.25 8 1.25 1.25 0 0 1 16 6.75a1.25 1.25 0 0 1 1.25-1.25M12 7a5 5 0 0 1 5 5 5 5 0 0 1-5 5 5 5 0 0 1-5-5 5 5 0 0 1 5-5m0 2a3 3 0 0 0-3 3 3 3 0 0 0 3 3 3 3 0 0 0 3-3 3 3 0 0 0-3-3z"/></svg>
              <span>View</span>
            </div>
          </div>
        `;
        grid.appendChild(card);
      });
    } else {
      // Show "follow us" placeholder
      grid.innerHTML = `
        <div class="ig-live-error" style="grid-column: 1 / -1;">
          <a href="https://www.instagram.com/${INSTAGRAM_USERNAME}/" target="_blank" rel="noopener noreferrer" style="text-decoration: none; color: inherit;">
            <svg style="width: 48px; height: 48px; margin-bottom: 12px; color: var(--color-sage);" fill="currentColor" viewBox="0 0 24 24"><path d="M7.8 2h8.4C19.4 2 22 4.6 22 7.8v8.4a5.8 5.8 0 0 1-5.8 5.8H7.8C4.6 22 2 19.4 2 16.2V7.8A5.8 5.8 0 0 1 7.8 2m-.2 2A3.6 3.6 0 0 0 4 7.6v8.8C4 18.39 5.61 20 7.6 20h8.8a3.6 3.6 0 0 0 3.6-3.6V7.6C20 5.61 18.39 4 16.4 4H7.6m9.65 1.5a1.25 1.25 0 0 1 1.25 1.25A1.25 1.25 0 0 1 17.25 8 1.25 1.25 0 0 1 16 6.75a1.25 1.25 0 0 1 1.25-1.25M12 7a5 5 0 0 1 5 5 5 5 0 0 1-5 5 5 5 0 0 1-5-5 5 5 0 0 1 5-5m0 2a3 3 0 0 0-3 3 3 3 0 0 0 3 3 3 3 0 0 0 3-3 3 3 0 0 0-3-3z"/></svg>
            <p style="font-size: 1rem;">Follow us <strong>@${INSTAGRAM_USERNAME}</strong></p>
            <p style="font-size: 0.8rem; margin-top: 6px; opacity: 0.7;">View our latest posts on Instagram</p>
          </a>
        </div>
      `;
    }
  }

  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', function() {
      setTimeout(initLiveInstagramFeed, 500);
    });
  } else {
    setTimeout(initLiveInstagramFeed, 500);
  }

  // Also re-render when navigating to home
  const originalNavigate = window.navigateTo;
  if (originalNavigate) {
    window.navigateTo = function(route) {
      originalNavigate(route);
      if (route === 'home' || route === '' || route === 'about') {
        setTimeout(initLiveInstagramFeed, 300);
      }
    };
  }
})();


// ============================================
// LOCAL AUTH SYSTEM
// ============================================
(function() {
  const USERS_KEY = 'nova_users';
  const SESSION_KEY = 'nova_session';

  // Get all registered users
  function getUsers() {
    return NovaDB.getUsers() || [];
  }

  // Save users array
  function saveUsers(users) {
    NovaDB.saveUsers(users);
  }

  // Get current session
  function getSession() {
    try { return JSON.parse(localStorage.getItem(SESSION_KEY)); }
    catch(e) { return null; }
  }

  // Save session
  function saveSession(user) {
    localStorage.setItem(SESSION_KEY, JSON.stringify(user));
  }

  // Clear session
  function clearSession() {
    localStorage.removeItem(SESSION_KEY);
  }

  // Hash password (simple hash for localStorage - NOT production-secure)
  function hashPassword(password) {
    let hash = 0;
    for (let i = 0; i < password.length; i++) {
      const char = password.charCodeAt(i);
      hash = ((hash << 5) - hash) + char;
      hash = hash & hash;
    }
    return 'h_' + Math.abs(hash).toString(36);
  }

  // Update UI based on auth state
  function updateAuthUI(user) {
    if (user) {
      document.body.classList.add('user-logged-in');

      // Update header greeting
      const greetingEl = document.getElementById('header-greeting');
      if (greetingEl) {
        const lang = AppState.language;
        greetingEl.innerText = lang === 'am' ? `\u0555\u0572\u057b\u0578\u0582\u0575\u0576, ${user.firstName}` :
                               lang === 'ru' ? `\u041f\u0440\u0438\u0432\u0435\u0442, ${user.firstName}` :
                               `Hello, ${user.firstName}`;
      }

      // Update AppState customer
      AppState.customer.firstName = user.firstName;
      AppState.customer.lastName = user.lastName;
      AppState.customer.email = user.email;

      // Update dashboard greeting
      const dashGreeting = document.getElementById('customer-welcome-greeting');
      if (dashGreeting) {
        const lang = AppState.language;
        dashGreeting.innerText = lang === 'am' ? `\u0555\u0572\u057b\u0578\u0582\u0575\u0576, ${user.firstName}` :
                                 lang === 'ru' ? `\u041f\u0440\u0438\u0432\u0435\u0442, ${user.firstName}` :
                                 `Hello, ${user.firstName}`;
      }

      // Update account details form
      const fnInput = document.getElementById('acc-first-name');
      const lnInput = document.getElementById('acc-last-name');
      const emailInput = document.getElementById('acc-email');
      if (fnInput) fnInput.value = user.firstName;
      if (lnInput) lnInput.value = user.lastName;
      if (emailInput) emailInput.value = user.email;

    } else {
      document.body.classList.remove('user-logged-in');
    }
  }

  // Switch between sign-in and sign-up tabs
  window.switchAuthTab = function(tab) {
    const signinForm = document.getElementById('auth-signin-form');
    const signupForm = document.getElementById('auth-signup-form');
    const signinTab = document.getElementById('auth-tab-signin');
    const signupTab = document.getElementById('auth-tab-signup');

    if (tab === 'signin') {
      if (signinForm) signinForm.classList.remove('hidden');
      if (signupForm) signupForm.classList.add('hidden');
      if (signinTab) signinTab.classList.add('active');
      if (signupTab) signupTab.classList.remove('active');
    } else {
      if (signinForm) signinForm.classList.add('hidden');
      if (signupForm) signupForm.classList.remove('hidden');
      if (signinTab) signinTab.classList.remove('active');
      if (signupTab) signupTab.classList.add('active');
    }

    // Clear errors
    document.querySelectorAll('.auth-error').forEach(el => el.classList.add('hidden'));
  };

  // Handle Sign Up
  window.handleSignUp = function(event) {
    event.preventDefault();
    const firstName = document.getElementById('signup-first-name').value.trim();
    const lastName = document.getElementById('signup-last-name').value.trim();
    const email = document.getElementById('signup-email').value.trim().toLowerCase();
    const password = document.getElementById('signup-password').value;
    const confirmPassword = document.getElementById('signup-password-confirm').value;
    const errorEl = document.getElementById('signup-error');

    // Validate
    if (password !== confirmPassword) {
      errorEl.textContent = 'Passwords do not match.';
      errorEl.classList.remove('hidden');
      return;
    }

    if (password.length < 6) {
      errorEl.textContent = 'Password must be at least 6 characters.';
      errorEl.classList.remove('hidden');
      return;
    }

    // Check if email already exists in nova_users
    const users = getUsers();
    const existingUser = users.find(u => u.email === email);

    // Also check nova_staff_profiles (from admin grant) 
    const staffProfiles = NovaDB.getStaffProfiles() || {};
    const staffEntry = staffProfiles[email];

    if (existingUser) {
      errorEl.textContent = 'An account with this email already exists.';
      errorEl.classList.remove('hidden');
      return;
    }

    // Create user
    const newUser = {
      id: Date.now().toString(36),
      firstName,
      lastName,
      email,
      passwordHash: hashPassword(password),
      createdAt: new Date().toISOString(),
      billing: { street: '', city: '', zip: '' }
    };

    users.push(newUser);
    saveUsers(users);
    saveSession(newUser);
    updateAuthUI(newUser);

    // If this email was pre-granted admin access, sync the staff profile
    if (staffEntry) {
      staffEntry.name = firstName + ' ' + lastName;
      staffEntry.password = password;
      staffProfiles[email] = staffEntry;
      NovaDB.saveStaffProfiles(staffProfiles);
    }

    // If this user is an admin, auto-establish admin session
    if (typeof isAdminEmail === 'function' && isAdminEmail(email)) {
      const profile = staffEntry || {
        username: email,
        name: firstName + ' ' + lastName,
        role: 'Shop Manager',
        password: password
      };
      if (!staffEntry) {
        staffProfiles[email] = profile;
        NovaDB.saveStaffProfiles(staffProfiles);
      }
      sessionStorage.setItem('nova_admin_session', JSON.stringify(profile));
    }

    // Clear form
    event.target.reset();
    errorEl.classList.add('hidden');

    showToast('ACCOUNT CREATED SUCCESSFULLY!');
    if (typeof renderMyAccount === 'function') renderMyAccount();
    if (typeof updateAdminVisibility === 'function') updateAdminVisibility();
  };

  // Handle Sign In
  window.handleSignIn = function(event) {
    event.preventDefault();
    const email = document.getElementById('signin-email').value.trim().toLowerCase();
    const password = document.getElementById('signin-password').value;
    const errorEl = document.getElementById('signin-error');

    const users = getUsers();
    const user = users.find(u => u.email === email);

    if (!user) {
      // Check if they were granted admin but never signed up
      if (typeof isAdminEmail === 'function' && isAdminEmail(email)) {
        errorEl.textContent = 'Admin access was granted, but you need to Sign Up first to create your account.';
      } else {
        errorEl.textContent = 'No account found with this email.';
      }
      errorEl.classList.remove('hidden');
      return;
    }

    if (user.passwordHash !== hashPassword(password)) {
      errorEl.textContent = 'Incorrect password. Please try again.';
      errorEl.classList.remove('hidden');
      return;
    }

    saveSession(user);
    updateAuthUI(user);

    // Clear form
    event.target.reset();
    errorEl.classList.add('hidden');

    // If this user is an admin, auto-establish admin session too
    if (typeof isAdminEmail === 'function' && isAdminEmail(email)) {
      const staff = NovaDB.getStaffProfiles() || {};
      let profile = staff[email];
      if (!profile) {
        // Create staff profile on the fly if missing
        profile = {
          username: email,
          name: (user.firstName || '') + ' ' + (user.lastName || ''),
          role: email === 'norayrnajaryann@gmail.com' ? 'Super Admin' : 'Shop Manager',
          password: password
        };
        staff[email] = profile;
        NovaDB.saveStaffProfiles(staff);
      } else {
        // Sync the staff profile password with the user's actual password
        profile.password = password;
        profile.name = (user.firstName || '') + ' ' + (user.lastName || '');
        staff[email] = profile;
        NovaDB.saveStaffProfiles(staff);
      }
      sessionStorage.setItem('nova_admin_session', JSON.stringify(profile));
    }

    showToast('WELCOME BACK, ' + user.firstName.toUpperCase() + '!');
    if (typeof renderMyAccount === 'function') renderMyAccount();
    if (typeof updateAdminVisibility === 'function') updateAdminVisibility();
  };

  // Handle Logout
  window.handleLogout = function() {
    clearSession();
    sessionStorage.removeItem('nova_admin_session');
    updateAuthUI(null);
    if (typeof updateAdminVisibility === 'function') updateAdminVisibility();
    showToast('LOGGED OUT SUCCESSFULLY.');
    if (typeof navigateTo === 'function') {
      setTimeout(() => navigateTo('home'), 100);
    }
  };

  // Initialize on page load
  function initAuth() {
    const session = getSession();
    if (session) {
      updateAuthUI(session);
    } else {
      updateAuthUI(null);
    }
  }

  // Run on DOM ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initAuth);
  } else {
    initAuth();
  }

  // Also update when saving account details
  const origSave = window.saveCustomerDetails;
  window.saveCustomerDetails = function(event) {
    if (origSave) origSave(event);
    // Sync changes back to stored user
    const session = getSession();
    if (session) {
      const users = getUsers();
      const idx = users.findIndex(u => u.email === session.email);
      if (idx > -1) {
        users[idx].firstName = AppState.customer.firstName;
        users[idx].lastName = AppState.customer.lastName;
        users[idx].billing = AppState.customer.billing;
        saveUsers(users);
        saveSession(users[idx]);
        updateAuthUI(users[idx]);
      }
    }
  };
})();

// ============================================
// ADMIN CLIENTS PANEL
// ============================================
(function() {
  window.renderAdminClients = function() {
    const tbody = document.getElementById('admin-clients-tbody');
    const countEl = document.getElementById('admin-clients-count');
    if (!tbody) return;

    let users = NovaDB.getUsers() || [];

    if (countEl) countEl.textContent = users.length + ' client' + (users.length !== 1 ? 's' : '');

    if (users.length === 0) {
      tbody.innerHTML = '<tr><td colspan="5" style="text-align:center; color:var(--color-medium-gray); padding:40px 20px; font-size:0.85rem;">No registered clients yet.</td></tr>';
      return;
    }

    // Sort newest first
    const sorted = [...users].sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt));

    tbody.innerHTML = sorted.map((user, idx) => {
      const date = user.createdAt ? new Date(user.createdAt) : new Date();
      const dateStr = date.toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' });
      const timeStr = date.toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit' });

      return `
        <tr>
          <td style="color:var(--color-medium-gray);">${idx + 1}</td>
          <td>
            <div style="display:flex; align-items:center; gap:10px;">
              <div style="width:32px; height:32px; border-radius:50%; background:var(--color-sage); color:white; display:flex; align-items:center; justify-content:center; font-size:0.7rem; font-weight:600; flex-shrink:0;">
                ${user.firstName ? user.firstName[0].toUpperCase() : ''}${user.lastName ? user.lastName[0].toUpperCase() : ''}
              </div>
              <div>
                <div style="font-weight:500;">${user.firstName || ''} ${user.lastName || ''}</div>
              </div>
            </div>
          </td>
          <td style="color:var(--color-medium-gray);">${user.email || ''}</td>
          <td>
            <div style="font-size:0.75rem;">${dateStr}</div>
            <div style="font-size:0.65rem; color:var(--color-medium-gray);">${timeStr}</div>
          </td>
          <td>
            <button onclick="deleteClient('${user.id}')" style="background:none; border:1px solid var(--color-border); border-radius:4px; padding:4px 8px; cursor:pointer; color:var(--color-medium-gray); font-size:0.7rem; transition:all 0.2s;" onmouseover="this.style.color='#C0392B'; this.style.borderColor='#E6B0AA'" onmouseout="this.style.color='var(--color-medium-gray)'; this.style.borderColor='var(--color-border)'">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" style="width:14px; height:14px;"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
            </button>
          </td>
        </tr>
      `;
    }).join('');
  };

  window.deleteClient = function(userId) {
    if (!confirm('Remove this client?')) return;
    let users = NovaDB.getUsers() || [];
    users = users.filter(u => u.id !== userId);
    NovaDB.saveUsers(users);
    renderAdminClients();
    showToast('CLIENT REMOVED.');
  };

  // Auto-render when switching to clients tab
  const origSwitchTab = window.switchAdminTab;
  if (origSwitchTab) {
    const _origSwitch = origSwitchTab;
    window.switchAdminTab = function(tabId) {
      _origSwitch(tabId);
      if (tabId === 'clients') {
        renderAdminClients();
      }
    };
  }

  // Also hook into admin tab click events
  document.addEventListener('click', function(e) {
    const navItem = e.target.closest('[data-admin-tab="clients"]');
    if (navItem) {
      setTimeout(renderAdminClients, 50);
    }
  });
})();

// ============================================
// ADMIN ACCESS CONTROL SYSTEM
// ============================================
(function() {
  const ADMIN_EMAILS_KEY = 'nova_admin_emails';
  const SUPER_ADMIN = 'norayrnajaryann@gmail.com';

  // Initialize admin emails list
  function getAdminEmails() {
    const saved = NovaDB.getAdminEmails();
    if (Array.isArray(saved)) return saved;
    // Default: only super admin
    const defaults = [SUPER_ADMIN];
    NovaDB.saveAdminEmails(defaults);
    return defaults;
  }

  function saveAdminEmails(emails) {
    NovaDB.saveAdminEmails(emails);
  }

  // Check if an email has admin access
  window.isAdminEmail = function(email) {
    if (!email) return false;
    return getAdminEmails().includes(email.toLowerCase());
  };

  // Check if current session is admin
  window.isCurrentUserAdmin = function() {
    try {
      const session = JSON.parse(localStorage.getItem('nova_session'));
      return session && isAdminEmail(session.email);
    } catch(e) { return false; }
  };

  // Check if current user is super admin
  window.isSuperAdmin = function() {
    try {
      const session = JSON.parse(localStorage.getItem('nova_session'));
      return session && session.email.toLowerCase() === SUPER_ADMIN;
    } catch(e) { return false; }
  };

  // Update admin UI visibility
  window.updateAdminVisibility = function() {
    const isAdmin = isCurrentUserAdmin();
    const isLoggedIn = !!localStorage.getItem('nova_session');

    // Show/hide WooCommerce link in dropdown
    document.querySelectorAll('.admin-only-link').forEach(el => {
      el.style.display = isAdmin && isLoggedIn ? 'block' : 'none';
    });

    // If on admin page and not admin, redirect to home
    if (AppState.currentRoute === 'admin' && !isAdmin) {
      // They can still see the login form, but check after login
    }
  };

  // Override the admin login to check auth session first
  const origSubmitAdmin = window.submitAdminCredentials;
  window.submitAdminCredentials = function(event) {
    event.preventDefault();
    
    const emailInput = document.getElementById('admin-email-input');
    const passInput = document.getElementById('admin-pass-input');
    const email = emailInput ? emailInput.value.trim().toLowerCase() : '';
    const password = passInput ? passInput.value : '';

    if (!email || !password) {
      showToast("PLEASE FILL IN ALL FIELDS.");
      return;
    }

    // Check if this email has admin access
    if (!isAdminEmail(email)) {
      showToast("ACCESS DENIED. THIS ACCOUNT DOES NOT HAVE ADMIN PRIVILEGES.");
      return;
    }

    // First, try to authenticate via the main nova_users auth system
    const users = NovaDB.getUsers() || [];
    const authUser = users.find(u => u.email === email);

    // Also check staff profiles as a fallback
    const staff = NovaDB.getStaffProfiles() || {};
    let profile = staff[email];

    // Verify password: check nova_users first (primary), then staff profiles (legacy fallback)
    let authenticated = false;
    if (authUser && authUser.passwordHash === hashPwd(password)) {
      authenticated = true;
    } else if (profile && profile.password === password) {
      authenticated = true;
    }

    if (!authenticated) {
      showToast("INVALID EMAIL OR PASSWORD.");
      return;
    }

    // Ensure staff profile exists and is synced
    if (!profile) {
      profile = {
        username: email,
        name: authUser ? (authUser.firstName + ' ' + (authUser.lastName || '')).trim() : email.split('@')[0],
        role: email === SUPER_ADMIN ? 'Super Admin' : 'Shop Manager',
        password: password
      };
      staff[email] = profile;
      NovaDB.saveStaffProfiles(staff);
    } else {
      // Sync password and name
      profile.password = password;
      if (authUser) {
        profile.name = (authUser.firstName + ' ' + (authUser.lastName || '')).trim();
      }
      staff[email] = profile;
      NovaDB.saveStaffProfiles(staff);
    }

    // Also sign into the main auth system if not already
    let session = null;
    try { session = JSON.parse(localStorage.getItem('nova_session')); } catch(e) {}
    
    if (!session || session.email !== email) {
      // Use existing user data or create session from profile
      const sessionData = authUser || {
        id: Date.now().toString(36),
        firstName: profile.name.split(' ')[0] || profile.name,
        lastName: profile.name.split(' ').slice(1).join(' ') || '',
        email: email,
        passwordHash: hashPwd(password),
        createdAt: new Date().toISOString(),
        billing: { street: '', city: '', zip: '' }
      };
      localStorage.setItem('nova_session', JSON.stringify(sessionData));
      document.body.classList.add('user-logged-in');
      
      // Also ensure this user exists in nova_users
      if (!authUser) {
        users.push(sessionData);
        NovaDB.saveUsers(users);
      }

      // Update header greeting
      const greetingEl = document.getElementById('header-greeting');
      if (greetingEl) greetingEl.innerText = 'Hello, ' + sessionData.firstName;
    }

    // Successful admin login
    sessionStorage.setItem('nova_admin_session', JSON.stringify(profile));
    showToast('WELCOME BACK, ' + profile.name.toUpperCase() + '.');

    if (typeof logAdminActivity === 'function') {
      logAdminActivity(profile.name, 'Logged in successfully as ' + profile.role);
    }

    document.getElementById('admin-login-wrapper').classList.add('hidden');
    document.getElementById('admin-dashboard-wrapper').classList.remove('hidden');

    if (emailInput) emailInput.value = '';
    if (passInput) passInput.value = '';

    updateAdminVisibility();
    if (typeof refreshAdminDashboard === 'function') refreshAdminDashboard();
  };

  // Also ensure auto-login to admin if session is admin and navigating to admin page
  const origNavigate = window.navigateTo;
  if (origNavigate) {
    const _nav = origNavigate;
    window.navigateTo = function(route) {
      _nav(route);
      
      if (route === 'admin') {
        // If the current session is an admin, auto-authenticate
        if (isCurrentUserAdmin()) {
          const session = JSON.parse(localStorage.getItem('nova_session'));
          const adminSession = sessionStorage.getItem('nova_admin_session');
          
          if (!adminSession && session) {
            // Auto-login to admin
            const staff = NovaDB.getStaffProfiles() || {};
            const profile = staff[session.email];
            if (profile) {
              sessionStorage.setItem('nova_admin_session', JSON.stringify(profile));
              setTimeout(() => {
                document.getElementById('admin-login-wrapper')?.classList.add('hidden');
                document.getElementById('admin-dashboard-wrapper')?.classList.remove('hidden');
                if (typeof refreshAdminDashboard === 'function') refreshAdminDashboard();
              }, 100);
            }
          }
        }
      }
      
      // Re-check visibility on every nav
      setTimeout(updateAdminVisibility, 100);
    };
  }

  // Grant admin access (super admin only)
  window.grantAdminAccess = function(email) {
    if (!isSuperAdmin()) {
      showToast("ONLY THE SUPER ADMIN CAN GRANT ACCESS.");
      return;
    }
    email = email.trim().toLowerCase();
    if (!email) return;

    const emails = getAdminEmails();
    if (emails.includes(email)) {
      showToast("THIS EMAIL ALREADY HAS ADMIN ACCESS.");
      return;
    }

    emails.push(email);
    saveAdminEmails(emails);

    // Check if user already exists in nova_users (the main auth system)
    const users = NovaDB.getUsers() || [];
    const existingUser = users.find(u => u.email === email);

    // Add to staff profiles, using real name from nova_users if available
    const staff = NovaDB.getStaffProfiles() || {};
    if (!staff[email]) {
      const userName = existingUser 
        ? (existingUser.firstName + ' ' + (existingUser.lastName || '')).trim()
        : email.split('@')[0];
      staff[email] = {
        username: email,
        name: userName,
        role: 'Shop Manager',
        password: 'temp_' + Math.random().toString(36).substr(2, 6)
      };
      NovaDB.saveStaffProfiles(staff);
    } else if (existingUser) {
      // Update staff profile name from actual user data
      staff[email].name = (existingUser.firstName + ' ' + (existingUser.lastName || '')).trim();
      NovaDB.saveStaffProfiles(staff);
    }

    showToast('ADMIN ACCESS GRANTED TO ' + email.toUpperCase());
    renderAdminAccessList();
    if (typeof renderStaffTable === 'function') renderStaffTable();
  };

  // Revoke admin access
  window.revokeAdminAccess = function(email) {
    if (!isSuperAdmin()) {
      showToast("ONLY THE SUPER ADMIN CAN REVOKE ACCESS.");
      return;
    }
    if (email === SUPER_ADMIN) {
      showToast("CANNOT REVOKE SUPER ADMIN ACCESS.");
      return;
    }

    let emails = getAdminEmails();
    emails = emails.filter(e => e !== email);
    saveAdminEmails(emails);
    showToast('ADMIN ACCESS REVOKED FOR ' + email.toUpperCase());
    renderAdminAccessList();
  };

  // Render admin access list in developer section
  window.renderAdminAccessList = function() {
    const container = document.getElementById('admin-access-list');
    if (!container) return;

    const emails = getAdminEmails();
    container.innerHTML = emails.map(email => {
      const isSA = email === SUPER_ADMIN;
      return '<div style="display:flex; justify-content:space-between; align-items:center; padding:8px 0; border-bottom:1px solid var(--color-light-gray); font-size:0.8rem;">' +
        '<div style="display:flex; align-items:center; gap:8px;">' +
          '<span style="width:28px; height:28px; border-radius:50%; background:' + (isSA ? 'var(--color-black)' : 'var(--color-sage)') + '; color:white; display:flex; align-items:center; justify-content:center; font-size:0.6rem; font-weight:600;">' + (isSA ? 'SA' : 'A') + '</span>' +
          '<span>' + email + '</span>' +
          (isSA ? '<span style="font-size:0.6rem; background:var(--color-black); color:white; padding:2px 6px; border-radius:3px; text-transform:uppercase; letter-spacing:0.05em;">Super Admin</span>' : '') +
        '</div>' +
        (isSA ? '' : '<button onclick="revokeAdminAccess(\'' + email + '\')" style="background:none; border:1px solid var(--color-border); border-radius:4px; padding:3px 8px; cursor:pointer; color:var(--color-medium-gray); font-size:0.65rem;" onmouseover="this.style.color=\'#C0392B\'" onmouseout="this.style.color=\'var(--color-medium-gray)\'">Revoke</button>') +
      '</div>';
    }).join('');
  };

  // Initialize on load
  async function initAdminAccess() {
    // Wait for Firestore data to be loaded
    await NovaDB.whenReady();

    // Ensure super admin user account exists in auth system
    let users = NovaDB.getUsers() || [];
    if (!users.find(u => u.email === SUPER_ADMIN)) {
      users.push({
        id: 'superadmin',
        firstName: 'Norayr',
        lastName: 'Najaryan',
        email: SUPER_ADMIN,
        passwordHash: hashPwd('Ananan05021998!'),
        createdAt: new Date().toISOString(),
        billing: { street: '', city: '', zip: '' }
      });
      NovaDB.saveUsers(users);
    }

    // Ensure admin emails list exists
    getAdminEmails();
    updateAdminVisibility();
  }

  // Simple hash (must match the one in auth system)
  function hashPwd(password) {
    let hash = 0;
    for (let i = 0; i < password.length; i++) {
      const char = password.charCodeAt(i);
      hash = ((hash << 5) - hash) + char;
      hash = hash & hash;
    }
    return 'h_' + Math.abs(hash).toString(36);
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initAdminAccess);
  } else {
    initAdminAccess();
  }

  // Hook into admin tab switch to render access list
  document.addEventListener('click', function(e) {
    const navItem = e.target.closest('[data-admin-tab="developer"]');
    if (navItem) {
      setTimeout(renderAdminAccessList, 50);
    }
  });
})();

// ============================================
// PERSONAL DISCOUNT SYSTEM
// ============================================

const DISCOUNT_PERCENT = 10;
const DISCOUNT_CYCLE_DAYS = 14;

function getActiveDiscount() {
  const brands = typeof getBrands === 'function' ? getBrands() : [];
  if (brands.length === 0) return null;

  // Calculate which 14-day cycle we're in based on a fixed epoch
  const epoch = new Date('2025-01-01T00:00:00Z').getTime();
  const now = Date.now();
  const cycleIndex = Math.floor((now - epoch) / (DISCOUNT_CYCLE_DAYS * 24 * 60 * 60 * 1000));
  
  // Pick brand deterministically from cycle index
  const brandIndex = cycleIndex % brands.length;
  const brand = brands[brandIndex];

  // Calculate expiry
  const cycleStartMs = epoch + cycleIndex * DISCOUNT_CYCLE_DAYS * 24 * 60 * 60 * 1000;
  const cycleEndMs = cycleStartMs + DISCOUNT_CYCLE_DAYS * 24 * 60 * 60 * 1000;
  const daysLeft = Math.max(0, Math.ceil((cycleEndMs - now) / (24 * 60 * 60 * 1000)));

  // Generate code from brand name
  const code = 'NOVA' + brand.replace(/[^A-Za-z0-9]/g, '').toUpperCase().slice(0, 8) + DISCOUNT_PERCENT;

  return { brand, code, percent: DISCOUNT_PERCENT, daysLeft, cycleIndex };
}

function renderPersonalDiscount() {
  const discount = getActiveDiscount();
  if (!discount) return;

  const titleEl = document.getElementById('discount-brand-title');
  const descEl = document.getElementById('discount-description');
  const codeEl = document.getElementById('discount-code-display');
  const amountEl = document.getElementById('discount-amount-display');
  const brandEl = document.getElementById('discount-brand-display');
  const expiresEl = document.getElementById('discount-expires-display');

  if (titleEl) titleEl.textContent = `${discount.percent}% Off All ${discount.brand} Products`;
  if (descEl) descEl.textContent = `Enjoy ${discount.percent}% off on all ${discount.brand} products! A new brand is featured every ${DISCOUNT_CYCLE_DAYS} days. Use the code below at checkout.`;
  if (codeEl) {
    codeEl.textContent = discount.code;
    codeEl.onclick = () => {
      navigator.clipboard.writeText(discount.code).then(() => {
        showToast('DISCOUNT CODE COPIED!');
      }).catch(() => {});
    };
  }
  if (amountEl) amountEl.textContent = `${discount.percent}%`;
  if (brandEl) brandEl.textContent = discount.brand;
  if (expiresEl) expiresEl.textContent = `${discount.daysLeft} day${discount.daysLeft !== 1 ? 's' : ''}`;
}

// Active applied discount state
let appliedDiscount = null;

window.applyDiscountCode = function() {
  const input = document.getElementById('checkout-discount-input');
  const msgEl = document.getElementById('checkout-discount-msg');
  if (!input || !msgEl) return;

  const enteredCode = input.value.trim().toUpperCase();
  
  if (!enteredCode) {
    msgEl.style.display = 'block';
    msgEl.style.color = '#c00';
    msgEl.textContent = 'Please enter a discount code.';
    return;
  }

  const discount = getActiveDiscount();
  if (!discount) {
    msgEl.style.display = 'block';
    msgEl.style.color = '#c00';
    msgEl.textContent = 'No active discount available.';
    return;
  }

  if (enteredCode !== discount.code) {
    msgEl.style.display = 'block';
    msgEl.style.color = '#c00';
    msgEl.textContent = 'Invalid discount code.';
    appliedDiscount = null;
    updateCheckoutTotals();
    return;
  }

  // Check if any cart items match the discount brand
  const matchingItems = AppState.cart.filter(item => {
    return item.product && item.product.brand && item.product.brand.toLowerCase() === discount.brand.toLowerCase();
  });

  if (matchingItems.length === 0) {
    msgEl.style.display = 'block';
    msgEl.style.color = '#c00';
    msgEl.textContent = `This code is valid only for ${discount.brand} products. Your cart has no ${discount.brand} items.`;
    appliedDiscount = null;
    updateCheckoutTotals();
    return;
  }

  // Apply discount
  appliedDiscount = discount;
  msgEl.style.display = 'block';
  msgEl.style.color = 'var(--color-sage)';
  msgEl.textContent = `вњ“ ${discount.percent}% discount applied to ${matchingItems.length} ${discount.brand} item(s)!`;
  updateCheckoutTotals();
};

// Override updateCheckoutTotals to account for discounts
const _originalUpdateCheckoutTotals = updateCheckoutTotals;
window.updateCheckoutTotals = function() {
  const subtotal = AppState.cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const shippingSelect = document.getElementById('shipping-method');
  const shippingCost = shippingSelect ? parseInt(shippingSelect.value) : 0;

  let discountAmount = 0;
  if (appliedDiscount) {
    AppState.cart.forEach(item => {
      if (item.product && item.product.brand && item.product.brand.toLowerCase() === appliedDiscount.brand.toLowerCase()) {
        discountAmount += Math.round((item.price * item.quantity) * appliedDiscount.percent / 100);
      }
    });
  }

  const total = subtotal + shippingCost - discountAmount;

  const subtotalEl = document.getElementById('checkout-subtotal-val');
  const shippingValEl = document.getElementById('checkout-shipping-val');
  const totalEl = document.getElementById('checkout-total-val');
  if (!subtotalEl || !totalEl) return;

  subtotalEl.innerText = `ЦЏ${subtotal.toLocaleString()}`;
  if (shippingValEl) shippingValEl.innerText = shippingCost === 0 ? 'Free' : `ЦЏ${shippingCost.toLocaleString()}`;
  totalEl.innerText = `ЦЏ${total.toLocaleString()}`;

  const discountRow = document.getElementById('checkout-discount-row');
  const discountVal = document.getElementById('checkout-discount-val');
  if (discountRow && discountVal) {
    if (discountAmount > 0) {
      discountRow.style.display = 'flex';
      discountVal.textContent = `-ЦЏ${discountAmount.toLocaleString()}`;
    } else {
      discountRow.style.display = 'none';
    }
  }
};
